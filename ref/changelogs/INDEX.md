# Changelog Index

> **Scope**: functional changes, including new features, behavior changes, APIs, and dependency upgrades.
> Debug, performance, and security reviews live in [`ref/reviews/`](../reviews/INDEX.md); this split starts at CHANGELOG_16.

| File | Summary (<=80 chars) |
|------|------|
| [CHANGELOG_1.md](CHANGELOG_1.md) | Project initialization M1-M9 (Electron+React+TS+Tailwind translucent window / Adapter+Diff dual plug-in / SDK+Hook dual channel / DB v3) + application icon |
| [CHANGELOG_2.md](CHANGELOG_2.md) | Tool adaptation AskUserQuestion + ExitPlanMode: independent type + independent IPC + dedicated UI (option button / approve plan + markdown feedback) |
| [CHANGELOG_3.md](CHANGELOG_3.md) | Dialogue bubble + Markdown rendering (user message + MD/TXT switch from global cascade to single independent) + AskRow solid color submission + frosted glass background color deepening + header count alignment |
| [CHANGELOG_4.md](CHANGELOG_4.md) | Notification/prompt sound set: remove window splash screen + sound.ts asynchronous fallback bug + SoundPicker customization + Windows PowerShell MediaPlayer + anti-overlay + real appName |
| [CHANGELOG_5.md](CHANGELOG_5.md) | README Three rounds of synchronization: the first message is required / settings env / Permission embedded / resume / 466→264 reconstruction |
| [CHANGELOG_6.md](CHANGELOG_6.md) | Permission interaction evolution: permissionMode persisted to DB + bypassPermissions support + four deadlock fallbacks (pending rebuild / warning / timeout abort / chip) + slash interception + cancelled no longer misreported |
| [CHANGELOG_7.md](CHANGELOG_7.md) | Command line `agent-deck new`: reuse single-instance + macOS shell wrapper + default prompt "Hello" |
| [CHANGELOG_8.md](CHANGELOG_8.md) | SessionList/Detail behavior fixes: stickySelected no flicker + archivedAt filtering + cwd defaults to home + PendingTab consolidation (extracted pending-rows + batch buttons) |
| [CHANGELOG_9.md](CHANGELOG_9.md) | 5-step packaging/installation process: mac.icon + extraResources bin + ad-hoc codesign + pkill old process (chunk hash mismatch → monaco source code exposed) |
| [CHANGELOG_10.md](CHANGELOG_10.md) | Monaco / Diff red screen fix: window.onerror synchronization + unhandledrejection async double whitelist + ActivityFeed select-text can be copied |
| [CHANGELOG_11.md](CHANGELOG_11.md) | Main process stability: safeSend isDestroyed + pin root cause upgrade (`::before mix-blend-mode` root cause + invalidate 100ms + setBackgroundThrottling(false)) |
| [CHANGELOG_12.md](CHANGELOG_12.md) | Permissions tab Three layers → Four layers (user / user-local / project / local) + HistoryPanel The whole row can be clicked + Responded / Canceled by SDK to display |
| [CHANGELOG_13.md](CHANGELOG_13.md) | Comprehensive optimization batch (haiku model + 12 items + 10 items): Bearer authentication / Summarizer timeout / write amplification / env whitelist / historical expiration cleanup / dead code cleanup |
| [CHANGELOG_14.md](CHANGELOG_14.md) | Adapter expansion: bundled CLAUDE.md+skill injection / Codex CLI adapter / MCP image tools / vision text-to-image and image-to-image / independent thinking detection / pin one-shot refresh / codex spawn ENOTDIR fix |
| [CHANGELOG_15.md](CHANGELOG_15.md) | SDK runtime mainline: 0.1.x PATH cannot find node, use process.execPath to correct / upgrade 0.2.118, fix Task tool session dead / 0.2.x native binary spawn ENOTDIR + summarizer, three bugs |
| [CHANGELOG_16.md](CHANGELOG_16.md) | Introducing reviews/ dual-track mechanism + CLAUDE.md simplified alignment + feedback upgrade plus Agent stepping on pitfalls, double confrontation, three-state ruling + REVIEW_1 eight repairs implemented |
| [CHANGELOG_17.md](CHANGELOG_17.md) | Universal CLAUDE.md skeleton optimization (section sequence rearrangement/redundant merging/template slimming) + new CHANGELOG/REVIEW template (extracted according to the actual paradigm of this project) |
| [CHANGELOG_18.md](CHANGELOG_18.md) | REVIEW_2 Twenty fixes + 2 bug reports reported by users + Expiration mechanism of reviewed files + REVIEW_X template plus frontmatter + CLAUDE.md injection toggle + codex batch splitting and running convention |
| [CHANGELOG_19.md](CHANGELOG_19.md) | ActivityFeed 695 lines split into 8 files + initialization race fix + 6-candidate adversarial evaluation (only candidate 4 implemented; the other 5 documented with trigger conditions) |
| [CHANGELOG_20.md](CHANGELOG_20.md) | Double confrontation architecture review Phase 0: N1 payload truncation (256KB + shrink large field) + N2 delete session synchronization switch SDK live Query (adapter capabilities plus canCloseSession) |
| [CHANGELOG_21.md](CHANGELOG_21.md) | Double confrontation architecture review Phase 1-3: SettingsDialog 720→338 rows progressive split + introduction of vitest (14 single tests) + ingest split into 5 segments + SettingsSet split into 8 functions + N6 transaction protection + N8 token branch + notify event-router + summarizer Error diagnosis is aggregated into the settings panel |
| [CHANGELOG_22.md](CHANGELOG_22.md) | Double confrontation architecture review Phase 4: N4 migrations single-track (vite ?raw inline) + N5 FTS5 history search (trigram + external content + trigger synchronization) + Opus real-running SQL to capture alias MATCH fatal broken + EXISTS→IN+DISTINCT 200× acceleration + UPDATE WHEN defense + case_sensitive=1 to maintain the original behavior + verify-fts5.sh 12 integrated verifications |
| [CHANGELOG_23.md](CHANGELOG_23.md) | REVIEW_4 Fixed implementation (4 HIGH + 17 MED + 2 LOW, across 19 files): H1 delete session race condition (intentionallyClosed + blacklist double insurance) + H2 SettingsSet runtime rollback + H3 byte budget changed to utf-8 safe cut + KNOWN_LARGE recursion + H4 RECENT_LIMIT and listEvents Alignment + IPC boundary check helper + double finished shielding + event-router try/catch + ClaudeMd save returns the actual written disk content + NumberInput integer + ActivityRow memo + thinking/message collapse |
| [CHANGELOG_24.md](CHANGELOG_24.md) | REVIEW_5 Fixed implementation: Resume two active paths to eliminate duplicate sessions (H4 sdk-bridge entrance pre-occupation claim + fallback reuse OLD_ID) + H1 manager dedupOrClaim double insurance (cwd hits even if existing is claimed) + payload-truncate single field 8KB→64KB (fix "still with [truncated] after expansion" marker") |
| [CHANGELOG_25.md](CHANGELOG_25.md) | In-application session "discontinued and automatically resumed": ComposerSdk deletes the recovery button, sendMessage throws not found and goes directly to SDK resume; App.tsx history detail and sessions Map get the latest (repair closed→active, resurrection cannot be seen) + U2 candidate count=3 Go to dual confrontation upgrade, create a new "session recovery/disconnection UX (resume priority)" section and enter CLAUDE.md |
| [CHANGELOG_26.md](CHANGELOG_26.md) | Disconnection and self-healing sink to sdk-bridge.recoverAndSend (Plan B, double confrontation recommended): single flight lock + 30s UX placeholder message + restore cwd/permissionMode from sessionRepo + sdk-bridge.test.ts 4 case coverage; renderer deletes string matching, agrees to change the subject and sinks to owner layer |
| [CHANGELOG_27.md](CHANGELOG_27.md) | CLI streaming + resume implicit fork: first realId ≠ opts.resume → renameSdkSession(OLD_ID, NEW_ID) in sdk-bridge.consume to migrate the DB record + sub-table as a whole; App.tsx historySession follows session-renamed to cut ID; REVIEW_6 double confrontation + minimum recurrence script is the ironclad root cause CLI native binary, the documentation does not match the actual measurement; new sdk-bridge.test.ts 2 case (44/44) |
| [CHANGELOG_28.md](CHANGELOG_28.md) | The second kind of fork boundary check (jsonl is not present): recoverAndSend pre-checks whether ~/.claude/projects/<encoded-cwd>/<sid>.jsonl exists → If not, use createSession without resume + manual renameSdkSession(OLD_ID, newRealId) afterwards; CLI history is lost but application layer events DB is retained + The sessionId switching link is consistent; vitest adds 1 case (45/45); CLAUDE.md fork boundary is expanded into two categories |
| [CHANGELOG_29.md](CHANGELOG_29.md) | HistoryPanel peripheral fine-tuning (after-the-fact): automatically switch live view after rename + internal/external labels + monitoring rename/upsert automatic reload + recoverAndSend archived automatic unarchive |
| [CHANGELOG_30.md](CHANGELOG_30.md) | REVIEW_7 landing fix (1 HIGH + 4 MED + 4 LOW + 1 new vitest case): sdk-bridge post-fallback using createSession return value + renameSdkSession cohesion claim transfer + HistoryPanel listener stale closure + renderer renameSession defensive + App.tsx updater side effect picking + label tooltip + ipc apply Unified source of lists + precise annotations (46/46) |
| [CHANGELOG_31.md](CHANGELOG_31.md) | Add a "transparent when pinned" switch to the settings panel: AppSettings.transparentWhenPinned (the default is true to maintain the original behavior). If it is turned off, the macOS under-window real glass will still be retained when pinned; the change will take effect immediately. |
| [CHANGELOG_32.md](CHANGELOG_32.md) | Fix the hookStatus red box in the settings panel (user scope does not require cwd, main checks scope-aware) + the pin and non-pin colors are inconsistent after turning off the transparent glass (CSS is changed to data-transparent, App counts pinned && transparentWhenPinned) |
| [CHANGELOG_33.md](CHANGELOG_33.md) | ExitPlanMode 4-level target permission + bypass cold cut: respondExitPlanMode followed by `{decision:'approve',targetMode}` goes hot / `{decision:'approve-bypass'}` goes cold; new `restartWithPermissionMode` destroys the old SDK child process + uses `allowDangerouslySkipPermissions:true` Restart (reuse the full set of recoverAndSend guardrails); SessionDetail drop-down cut bypass synchronizes cold cut; PendingTab section header shared selector + ExitPlanRow 4 files; easily fix the zombie row hidden danger of closeSession without emit cancel |
| [CHANGELOG_34.md](CHANGELOG_34.md) | ExitPlanMode approved bypass no longer pops up the red text + no more cli orphan sessions: InternalSession plus expectedClose flag (double insurance before approve-bypass resolver + closeSession interrupt before), the catch block identifies SDK by-product errors (typical [ede_diagnostic]) that the application actively closes down to warn without popping up the red text; renameSdkSession puts fromId Added to the recentlyDeleted 60s blacklist, the late hook event that flew asynchronously after the OLD CLI child process SIGTERM was discarded at the ingest entry. |
| [CHANGELOG_35.md](CHANGELOG_35.md) | Agent Teams access M1 (infrastructure + creation portal): v006 plus sessions.team_name + AdapterCapabilities.canJoinTeam + AppSettings.agentTeamsEnabled (default OFF) + sdk-bridge per-query env injection CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 (summarizer natural isolation) + NewSessionDialog/SessionCard/SettingsDialog three UI closures; resume path with two lines of defense throw; M2/M3 follow-up |
| [CHANGELOG_36.md](CHANGELOG_36.md) | agent-deck-plugin injects agents/ for the first time (0 application code changes, SDK protocol native support): extract reviewer-claude (Opus 4.7) + reviewer-codex (sonnet wrapper, including complete codex CLI template + large scope split batch running) two independent subagents, the main agent starts with message 2 Task calls for confrontation; new rebuttal round (only unique to one party HIGH Trigger, spawn the other reviewer to focus on single point rebuttal); deep-code-review skill Step 2 entrusts a new agent + adds Step 2.5 rebuttal round; two CLAUDE.md decision-making confrontation sections are rewritten and decoupled simultaneously (~/.claude/CLAUDE.md uses a naked name and does not bind agent-deck, and resources uses agent-deck:* full name) |
| [CHANGELOG_37.md](CHANGELOG_37.md) | M1+: After NewSessionDialog fills in teamName, the prompt input box automatically backfills the editable template (including 3 teammate placeholders + shared task list coordination and closing); use lastInjectedTemplateRef to record the last backfill, and the user will respect it if it changes it and will not overwrite it; clear teamName / switch adapter / turn off toggle without returning prompt (one-time boot, non-two-way binding) |
| [CHANGELOG_38.md](CHANGELOG_38.md) | The ActivityFeed Task tool specifically renders: describe.ts plus Task case one-line summary; tool-row.tsx plus Task branch display 🤖 + subagent_type purple chip + fold/expand prompt full text button (imitation diff folding mode); a large number of Task calls in deep-code-review / Agent Teams scenarios are upgraded from the "🔧 Task" icon to "🤖 Task → agent-deck:reviewer-claude [expand prompt]" high information density |
| [CHANGELOG_39.md](CHANGELOG_39.md) | Agent Teams M2: team-fs.ts read-only access ~/.claude/teams + tasks (realpath protection against unauthorized access) + team-watcher.ts chokidar reference count + 60s grace; 4 IPC channels + TeamDataChanged event; App.tsx plus view='teams' tab + TeamHub.tsx (list + 5s polling) + TeamDetail.tsx (4 sections: summary / members / in-app session / shared task list markdown) + subscribeTeam fs watch real-time refresh; in-process cleanup upstream bug to give UI prompts |
| [CHANGELOG_40.md](CHANGELOG_40.md) | Agent Teams M3: Adapted from v2.1.32+ three new hook events (TaskCreated / TaskCompleted / TeammateIdle) - hook-installer + hook-routes + translate tolerant field extraction (schema evolution details + raw for reference) + SessionManager ingest plus team-* return early to avoid being swallowed by sdkOwned dedup + event-repo.findTeamEvents JOIN query; TeamDetail adds hook event stream section (icon + timeline) + "Give teammate instructions" entrance (application packaging `Tell teammate <name>: <text>` stuffed lead.sendMessage) + force-cleanup button (cover Claude in-process cleanup upstream bugs, rm -rf two directories); do not do CLI version gating (old version CLI silently ignores new ones) hook, zero conflict) |
| [CHANGELOG_41.md](CHANGELOG_41.md) | Sandbox three-level access ('off'/'workspace-write'/'strict', default off) + canUseTool top SandboxNetworkAccess auto-deny + structured message → model 100% fallback dangerouslyDisableSandbox only 1 popup UX closing; delete spike env gate and change settings driver; SettingsDialog 11 section folded + localStorage Persistence + sandbox drop-down box + amber color "only takes effect for the next new session" hint; add sandbox-config.ts + 22 cases vitest; add sandbox mode section to resources/claude-config/CLAUDE.md; align with codex for main entrance OS isolation |
| [CHANGELOG_42.md](CHANGELOG_42.md) | SDK Task Manager foundation (5 in-process MCP tools task_create/list/get/update/delete + tasks table + zod schema): v007 migration plus tasks table (including team_name TEXT and sessions.team_name have the same semantics, team deletion does not cascade delete tasks); task-repo.ts imitates summary-repo synchronization SQL + cascade BFS to prevent self-looping + safeJsonArray fault tolerance; tools.ts uses loadSdk asynchronous loading (avoiding Vite ESM translation traps) + snake_case args ↔ camelCase repo mapping + isError does not interrupt the agent loop; server.ts getTaskMcpServer() singleton lazy construction; 23 cases vitest (Node 20.18 binding compatible, 22+ automatic skip gatekeeping, copy search-predicate.test.ts convention); this issue does not include sdk-bridge / does not automatically inject team_name / does not push, list follow-up |
| [CHANGELOG_43.md](CHANGELOG_43.md) | SDK Task Manager access runtime (gap 1+2+3): sdk-bridge.ts query() options hang mcpServers + allowedTools (controlled by settings.enableTaskManager, default OFF) + getTasksMcpServerForSession(teamName) per-session instance (delete CHANGELOG_42 singleton) + buildTaskTools(repo, teamName) closure injection (task_create/update/delete write lock task.teamName === closure, task_list/get read-only allowed across teams) + tools.ts handler written emit task-changed → eventBus → IPC IpcEvent.TaskChanged push renderer (Layer A+B, Layer C UI does not do) + preload onTaskChanged + SettingsDialog experimental function section plus toggle + amber "The next new session will take effect" prompt; added tools.test.ts 24 cases pure mock verification closure + emit + write permission lock; README settings section synchronization |
| [CHANGELOG_44.md](CHANGELOG_44.md) | deep-code-review skill Change teammate main process + subagent fallback: SKILL.md rewrite §Step 2 (lead natural language spawns two teammates instead of Task subagent) + §Step 2.5 (sendMessage the other teammate refutes, reuses R_N context) + §Step 5 (sendMessage reuses the same pair of teammates, does not respawn — teammate mode token Save + Refute accurate core gain landing point) + §Step 6 (cleanup teammate + force-cleanup upstream bug cover) + Added §Fallback subagent (CLI<v2.1.32/agentTeamsEnabled OFF/non-SDK session returns to the original Task subagent, zero-cost compatibility); reviewer-{claude,codex}.md body changes to dual-mode (frontmatter is reserved for subagent) For registration, body plus §Use shape recognition A/B + teammate mode multi-round context reuse prompt); reviewer-codex plus wrapper-level skip splicing (external codex process stateless, wrapper teammate session remembers the last round of codex output, the new round appends the last round of finding summary to the new codex prompt skip field, allowing stateless codex to indirectly enjoy the benefits of teammate context persistence); use double confrontation (Explore Opus 4.7 + External codex gpt-5.5 xhigh concurrency) Initial judgment ❌ → User challenge F2 misreading (canJoinTeam is codex adapter capability, not reviewer-codex wrapper restriction) + Missing multiple rounds of rebuttal context persistence gain → On-site grep verification → Translation ✅; resume limitation is Anthropic’s official experimental feature constraint, not agent-deck self-added; subsequent addition of reviewer-codex emphasizes codex CLI **Must have `run_in_background: true`** (Synchronous blocking will lock concurrency confrontation) + Three-file simplification -22% (777 → 602 lines, deduplication "Use morphological recognition A/B merge table" "Reviewer-codex sentence self-check section deleted" "SKILL.md anti-pattern table merge" "Example trigger 4→2", behavioral constraints are fully retained); add "single vs multiple rounds" diversion guide + three-level back-up chain (teammate skill → subagent plugin agents → manual concurrency) at the top of the "Decision Confrontation" section of resources/claude-config/CLAUDE.md; remove the word `agent-deck` in the resources/claude-config/CLAUDE.md Decision Confrontation section, and use the "naked name + environment replacement" pattern (with ~/.claude/CLAUDE.md (unified wording); synchronize adding the diversion section to the top of the decision-making confrontation section of ~/.claude/CLAUDE.md |
| [CHANGELOG_45.md](CHANGELOG_45.md) | Inbox Watcher (Part A) — Teammate permission approval channel is opened: in-process backend inserts teammate’s permission_request text into the lead inbox file and waits for approval. There is no monitoring before application → teammate adjusts Bash and waits. Added inbox-protocol.ts (slugify/lock/permission_response schema aligned with SDK CLI binary implementation) + inbox-watcher.ts (chokidar + 60s grace + per-team requestId deduplication) + main bootstrap automatic subscription by active team + IPC TeamRespondPermission write permission_response to teammate inbox + TeamPermissionRow UI reuse PendingTab Category 4 section + cross-renderer TeamPermissionResolved synchronization event + 28 cases vitest; Part D (teammate cannot see `mcp__tasks__*`) Still waiting for 5 minutes to manually spike to confirm whether in-process teammate will load MCP from .mcp.json before investing in implementation |
| [CHANGELOG_46.md](CHANGELOG_46.md) | Pure engineering solution for Team name consistency: fs SSOT, application DB reverse synchronization. Added team-coordinator.ts (sync single outlet + chokidar root watcher to monitor ~/.claude/teams/*/config.json) + hook-routes PreToolUse block CLI builtin tool TeamCreate/TeamDelete/Teammate/SendMessage pump team_name (earliest channel) + three team hooks (TaskCreated/TaskCompleted/TeammateIdle) payload Reverse synchronization (reinforcement) + fs add (reinforcement); delete the NewSessionDialog teamName input box (team is freely built by lead); change task-mcp closure to lazy factory; release sdk-bridge resume + teamName throw and change it to warn; withdraw all prompt-engineering routes (system prompt injection / SKILL.md first ls check) - no longer make LLM obedient; 31 cases vitest; HOOK_EVENTS 28 full columns have no create class hook, and PreToolUse is the real earliest channel. |
| [CHANGELOG_47.md](CHANGELOG_47.md) | Activity stream tool row display enhancement + toolName dual-channel leakage bug fix: Create new tool-icons.ts to centrally map emoji (Empirical jq 7-day frequency driver whitelist + collision avoidance ✅/📝/📋/🤖); describe.ts adds Skill case to extract `{skill, args}`; tool-row.tsx ToolStartRow / ToolEndRow / ExitPlanMode / Task are all connected to toolIcon, ToolEndRow adds icon symmetry; SessionCard changes toolIcon + summarizeToolInput simultaneously and adds Skill case; sdk-bridge.ts emit tool-use-end checks toolName from toolUseNames map, codex-cli/translate.ts three places emit (Bash/mcp_tool_call/WebSearch) Directly spell toolName to align with start; in the plan stage, use double heterogeneous confrontation (Explore subagent + codex CLI gpt-5.5 xhigh) + three-state ruling, and the confrontation will find the same bug in the Codex channel + the same hard coding in SessionCard 🔧 Both are included. Patch 1: Add Agent/TeamCreate/SendMessage/TaskOutput/TaskStop describe case + Agent and merge it into the Task specialized branch; ActivityFeed build toolUseId→startEvent map transparently transmits ToolEndRow, allowing Skill to complete the replacement of the skill name / Bash to complete the replacement of the cmd summary / old events, the missing toolName is also restored (the premise is the same as the window) |
| [CHANGELOG_48.md](CHANGELOG_48.md) | fs watcher symlink path mismatch fix (chokidar returns realpath under macOS fsevents, strictly `!==` comparison with raw symlink path is always true → inbox-watcher / team-coordinator / team-watcher three fs channels are all silently invalid; user ~/.claude → .claude-default symlink trigger; modification = subscribe entry realpathSync Cache comparison baseline ~30 lines, see REVIEW_16 for details) + Agent Teams permission approval UX three-piece set: ① systemPrompt injection ## Agent Teams section hard constraint lead does not SendMessage persuade teammate to give up / do not return permission_response (events 43839-43843 double verification takes effect) ② TeamPermissionCancelled channel (idle_notification detection → emit team-permission-cancelled → store delete pending + activity-feed marked gray ⊘ has been canceled by teammate) ③ lead session detail activity-feed also renders TeamPermissionRow + PendingTab This row can be clicked to jump lead session detail (refer to the same session header onOpenSession, button stopPropagation); the events table automatically verifies the full link (inject fake permission_request out row 43837 + inject idle_notification out row 43846 reason teammate-idle) |
| [CHANGELOG_49.md](CHANGELOG_49.md) | deep-code-review SKILL.md Add §Milestone tracking subsection (5 milestone adjustment mcp__tasks__task_create / task_update: Round start / Round closing / rebuttal round start / rebuttal round judgment / closing verification) + Step 2/3/5/6 Add task rhythm reference + key constraints 8 + 2 anti-patterns; do not destroy SendMessage The main path, enableTaskManager OFF skips the entire section; the TeamDetail hook event flow timeline is completed from "Only TeammateIdle" to TaskCreated/Completed; known risks: teammate also gets the task tool. Theoretically, it may be randomly built. This issue does not prevent it (reviewer agents/*.md does not ban task_*). It will be repaired after running. |
| [CHANGELOG_50.md](CHANGELOG_50.md) | Large file splitting S1+S2+S3 three atomic commits: shared/types.ts (777→13 barrel + 8 domain module) / ipc.ts (997→ipc/ 10 sub-files per-domain register) / sdk-bridge.ts formatAskAnswers pure function (1995→1972 class (zero byte changes); explicitly protect sdk-bridge class / session/manager / codex sdk-bridge / 4 UI components unchanged; zero behavior changes typecheck+build+54 vitest all passed |
| [CHANGELOG_51.md](CHANGELOG_51.md) | The second round of large file splitting P1+P2+P3+P4 four atomic commits: pending-rows/index.tsx (728→22 barrel + 4 row + helper) / SessionDetail.tsx (618→322 directory + 5 sub-component) / TeamDetail.tsx (587→257 directory + 6 sub-component) / session/manager.ts (548→486, 4 pure helpers normalizeCwd/nextActivityState/extractCwd/deriveTitle, class byte zero change); explicit protection sdk-bridge class / session/manager class / codex sdk-bridge / store / translate does not change; 4 import sites have zero changes, typecheck+build+54 vitest all passed |
| [CHANGELOG_52.md](CHANGELOG_52.md) | The third round of large file splitting 13 atomic commit: claude sdk-bridge (1972→839 facade + 7 sub-module / 5 sub-class) / codex sdk-bridge (559→253 facade + 4 sub-module + ThreadLoop) / session-store (534→473 pump 8 type-guards) / manager.test (561 Split 3 files + shared setup) + audio resources (waiting/done.m4a built-in + extraResources path modification) + CLAUDE.md added "single file ≤ 500 lines trial demolition" convention; radical demolition class implementation (Plan agent 5 finding all repairs, absorbing F1 thunk + F2 SHARED Map key design); REVIEW_20 double confrontation 0 HIGH/MED/LOW + 1 INFO comment stale, typecheck+build+76 vitest all passed |
| [CHANGELOG_53.md](CHANGELOG_53.md) | deep-code-review skill boot repair: original SKILL.md §Step 2 Use "natural language command" + template to indicate spawn teammate, `TeamCreate` tool name does not appear / does not say that a team must be created first / is not listed `Agent(subagent_type, name, team_name, prompt)` four fields are indispensable → lead degradation application §Fallback template = pure subagent Path → reviewer-codex Bash is defaulted to the settings.json permission policy in the SDK upstream deny → PendingTab is never triggered; db events dual-session tool call comparison hard evidence root cause. The revision only changes the document (plugin injection path is open): SKILL.md §Step 2 Remove "TeamCreate + Agent × 2 four fields complete" two-step process + red text mark + self-test + §Fallback red text warning at the top; reviewer-codex/-claude.md adds "Bash permission path difference" section (A/B two-form trend + response to the first failure); do not apply the code (the path is not broken) / do not move CLAUDE.md (changed by user injectAgentDeckClaudeMd OFF will not take effect)/do not change settings.json allow list (only needed for subagent, and zsh:* large decentralization benefits < risk) |
| [CHANGELOG_54.md](CHANGELOG_54.md) | Small-step reinforcement of the sandbox mechanism A-1+A-2+B-4: A-1 SANDBOX_EXCLUDED_COMMANDS extension docker/watchman/orb/lima/colima/make/xcodebuild (conservative version, without node/npx/brew - direct spawn JS equals universal backdoor/brew writing /usr/local is too wide) + A-2 buildSensitiveDenyReadPaths expands ~/.zsh_history / ~/.bash_history / ~/Library/Keychains / ~/Library/Cookies; B-4 Codex is connected to the sandbox three files (settings.codexSandbox: workspace-write/read-only/danger-full-access, the default workspace-write zero behavior change, reuse the codex SDK native union without mapping) to complete REVIEW_14 The other half of the "dual backend sandbox symmetry" target: AppSettings add fields + ipc/_helpers add parseCodexSandboxMode + ipc/settings add applyCodexSandboxMode into APPLY_FNS + AgentAdapter interface add setCodexSandboxMode? + CodexCliAdapter init hook + setter forwarding + CodexSdkBridge added currentSandboxMode field (transmitted when startThread) + SettingsDialog experimental function section adds a three-level drop-down box; switching files will only take effect next time a new session is created (spawn-time lock, the same mode as claudeCodeSandbox); A-3 (default 'off' → 'workspace-write') user decision-making is not yet switched, only expanding the list; 22 sandbox-config case is still all green (no new single test file is introduced) |
| [CHANGELOG_55.md](CHANGELOG_55.md) | Cross-platform compatibility Windows support infrastructure (REVIEW_21 implementation): Added src/main/platform.ts abstraction IS_DARWIN/IS_WIN/IS_LINUX + encodeClaudeProjectDir; 5 hardcoded '/' changes to path.sep (recoverer.ts jsonl path / team-watcher chokidar dispatch / team-coordinator unlink+sync / manager-helpers deriveTitle / electron.vite.config alias); add resources/bin/agent-deck.cmd Win wrapper (three sections of .exe fallback + auto-complement new --cwd); add win/nsis/linux block to package.json + dist:mac/dist:win/dist:linux split + scripts/gen-icon-ico.mjs use png-to-ico Generate icon.ico (279 KB submitted to git); notify/sound.ts Win uses PowerShell [console]::beep to replace stdout BEL + visual.ts dock.bounce comment by-design no-op; add manager-helpers.test.ts 15 cases; 180 vitest all pass the mac terminal with zero fallback; Win terminal E2E remains CI runner |
| [CHANGELOG_56.md](CHANGELOG_56.md) | TeamDetail Structured tasks UI (mcp__tasks__* write real-time display, new IPC TaskListByTeam + onTaskChanged subscription + McpTasksSection 5 status chip) + A3 mcp tools.ts with ingest team-task-created/completed AgentEvent Let the event stream also display mcp operations (same kind as CLI hook, same view, unchanged SQL/Type/TeamEventRow) + Teammate permission auto-approve three levels (off/read-only/follow-lead, default read-only): pump READ_ONLY_TOOLS+EDIT_TOOLS to shared/constants to let lead canUseTool share the whitelist with inbox-watcher + create a new auto-approve.ts decision (read-only hit release / follow-lead and lead) permissionMode: bypass → all, acceptEdits → add EDIT_TOOLS) + lookupLeadPermissionMode three-level fallback (fs SSOT config.json.leadSessionId priority → DB SDK candidate → null downgrade) + inbox-watcher.processInboxFile access nested try/catch (append fail → rollback dedup + emit UI to prevent deadlock; after append ok emit fail does not roll back to prevent double response; reviewer double confrontation 4 HIGH/MED modifications) + SettingsDialog plus select line + takes effect immediately when running (unlike sandbox, which has to wait for the next new session); 37 + 9 task-tools case passed; 189 vitest passed all |
| [CHANGELOG_57.md](CHANGELOG_57.md) | Setting panel copywriting unification and platform separation (preload `process.platform` static field + IS_DARWIN/IS_WIN/IS_LINUX renderer util; 4 places Mac/Win mixed description only displays the corresponding version by platform: transparent window vibrancy / Claude sandbox Seatbelt / sensitive directory path example / shortcut key format; Section title and field label Unify Chinese, and mark English terms in parentheses when necessary; StatusBadge `closed`→`Close`/`Sleeping`→`Sleep` + SummaryView `Summarizer`→`Intermittent Summary` Global terminology closing) + Header Added "📚 Asset Library" button Independent Dialog (three Tabs: Skills / Agents / Application convention, concentrated display agent-deck plugin built-in + User ~/.claude/{agents,skills}/ Customize two types of assets, including view full content read-only + user-defined new/edit/delete/Finder reveal, add 6 new Assets* IPC channel + main bundled-assets.ts startup cache + user-assets.ts atomic write) + Settings panel PluginAssetsSection / ClaudeMdSection Description slimming (delete a large section of technology list and change it to one sentence "See the asset library for details") + SettingsDialog 532→200 Line split 9 sections sub-files (settings/sections/{Hook,Notify,Lifecycle,Summary,Window,HookServer,ExternalTools,ClaudeMd,PluginAssets,Experimental}Section.tsx); the unified table of terms is written conventions-tally count=1; typecheck+build all pass |
| [CHANGELOG_58.md](CHANGELOG_58.md) | CLAUDE.md The entire editor has been moved to the AssetsLibraryDialog "Application Convention" tab: the settings panel ClaudeMdSection only needs to inject toggle + jump asset library link (toggle = setting semantics / editor = content semantics, separated and clean) + asset library three tab "Application Convention" has been changed from the preview + "Edit in Settings" loop to directly drop ClaudeMdEditor (save/undo/restore default/dirty interception and migrate the entire set, add closeInFlightRef + guardedClose + guardedSwitchTab) + ClaudeMd / PluginAssets section jump asset library button copy is unified as "View in the asset library ↗" (the original PluginAssets "View built-in assets ↗" and ClaudeMd "View in the asset library ↗" coexist to make users confused) + Delete the AssetsLibraryDialog.onOpenSettings loop; SettingsDialog 254 → 186 lines; typecheck all passed |
| [CHANGELOG_59.md](CHANGELOG_59.md) | NewSessionDialog Delete the "Model" drop-down + CLI `--model` flag + IPC `raw.model` + Each adapter / sdk-bridge input parameters + SDK `query({ model })` Transparent transmission of five layers and delete it all at once (let the Claude / Codex CLI child process read it by itself ~/.claude/settings.json / ~/.codex/config.toml default model). Codex adds "permission mode (sandbox)" per-session drop-down (the three levels directly map the codex SDK's native SandboxMode: workspace-write/read-only/danger-full-access, override the settings.codexSandbox global value, and only create new paths for transparent transmission). CreateSessionOptions Delete the model and add codexSandbox and IPC adapters. Use parseCodexSandboxMode to reuse the ready-made whitelist and codex adapter+sdk-bridge to complete the transparent transmission chain; do not expose approvalPolicy (codex has no canUseTool equivalent callback); typecheck all passes |
| [CHANGELOG_60.md](CHANGELOG_60.md) | ComposerSdk + NewSessionDialog input box "paste/drag and drop/upload button" three-piece set to send pictures. Claude SDK uses base64 image content block; codex SDK only connects to local_image path, and the main process base64 is placed in `<userData>/image-uploads/<uuid>.<ext>` and fed downstream. Added attachment shared type + paths.ts + image-uploads module (write/load strict realpath+sep+TOCTOU / reapStaleUploads 14 days mtime guarantee) + IPC `UploadedImageLoad` channel + AdapterSendMessage three parameter envelope + AdapterCreateSession opts.attachments + pumping _image-constants.ts. Adapter layer: claude `pendingUserMessages` thunk to lazy materialize non-resident 30MB×N base64 (HIGH-2) + recoverer thunk three-parameter signature transparent transmission attachments The second waiter does not lose the image (HIGH-1) + codex `pendingMessages: Input[]` Go to SDK native + closeSession residual attachments fire-and-forget unlink. Renderer: useImageAttachments hook (fullBase64 uses useRef but does not enter React state, thumbnail 200px canvas-resize) + useImageBlob extracts the cache+abort state machine + UploadedImageThumb component fails with a gray bottom + MessageBubble renders the attached picture. mime tightens 4 types (png/jpeg/gif/webp) to align with the Claude SDK Base64ImageSource limit; single image 20MB / total image 30MB / 100KB text upper limit does not change; typecheck+build all pass |
| [CHANGELOG_61.md](CHANGELOG_61.md) | R1.A stage codex adapter capability alignment with Claude (plan v3 implementation, 6 commits + 5 spike verifications): A1 codex item.updated incremental open + UI in-place upsert by toolUseId + cross-adapter tool-use-end red status display; A2a sandbox persistence (DB v008 + sessionRepo.codexSandbox + resumeThread pass-through) + A2b cold switch (canRestartWithCodexSandbox capability + bridge.restartWithCodexSandbox + IPC channel); A3 Summarizer cross-adapter dispatch + codex SDK oneshot runner (read-only sandbox + low effort + cachedCodex lazy creation); A4a ~/.codex/config.toml mcp_servers marker block + atomic write + 13 tests (manual serialization without TOML parser); A9 agent-deck CLI `--adapter codex/claude` alias + `--codex-sandbox` flag. Spike checks all pass: sandbox pass-through works / 5 concurrent codex sessions are moderate / AGENTS.md has no 32KiB ceiling / mcp-sdk HTTP transport integration ~80 LOC / tool handler caller must force input schema injection. Remaining A4b UI / A5 (dep B'4) / A8 unit test expansion / A2c UI dropdown follow-up |
| [CHANGELOG_62.md](CHANGELOG_62.md) | R1.D stage codex configuration ecological alignment Claude (plan v3 implementation, 4 commit): D1 (+ D5 decision) AGENTS.md injected into the Agent Deck segment (HTML comment marker package + atomic write + self-healing + user segment retention); D2 skills synchronized to ~/.codex/skills/agent-deck/ (mtime comparison avoids each write + delete rules to maintain mirroring + agent-deck/ namespace to avoid name collision); D4 Settings UI "Codex Injection" section double toggle (parallel to ClaudeMd / PluginAssets); D6 packaging check does not need to be changed (extraResources resources/claude-config already contains path diversion and is ready); D5 decision-making is merged into D1 JSDoc without separate documentation. Dual synchronization after bootstrap step 7.0 app ready. Remaining D3 (reviewer-* codex skill content creation) / D7 (codex-runtime config dep B'4) stay follow-up |
| [CHANGELOG_63.md](CHANGELOG_63.md) | R2 stage Agent Deck MCP server (plan v3 implemented, 8 commit + reviewer dual confrontation ADR): self-running in-process MCP allows claude / codex / third-party MCP client to orchestrate other coding agent sessions across adapters through 5 tools (spawn / send / wait_reply / list / shutdown). Three transports coexist (in-process / HTTP / stdio) + 4 anti-recursion rules (depth / spawn-rate / fan-out / cwd cycle whole chain backtracking + per-caller race protection) + 47 single tests. Merge R1.A5 (codex automatically injects mcp_servers.agent-deck via SDK config) + R1.D7 (does not write disk ~/.codex/config.toml, env var bearer_token_env_var references). Added v009_mcp_spawn_chain.sql + sessionRepo upsert/rename to expand spawned_by/spawn_depth + conveniently fix v008 codex_sandbox missing latent bug + sessionManager.close (lifecycle=closed instead of hard-delete to prevent ON DELETE CASCADE from losing events). Settings UI 8 new fields + token automatically generated. reviewer-claude + reviewer-codex double confrontation 4 HIGH full revision: spawn schema plus team_name / wait_reply baseline_ts + backfill / sessionRepo rename column expansion / shutdown change lifecycle=closed |
| [CHANGELOG_64.md](CHANGELOG_64.md) | deep-code-review skill process reconstruction + global/application-level CLAUDE.md adds "complex plan: worktree isolation + cross-session hand off" convention (reviewer-claude + reviewer-codex double confrontation review closing: 1 HIGH rebuttal 1 / 6 MED / 2 LOW / 5 SUGGEST-SIMPLIFY full fix). SKILL.md disassembles §Step 2 "Building a team (pre-infrastructure, orthogonal to the Round concept)" + §Step 3 "Initial spawn", description 1073→698 bytes, §Step 2 self-test merged into the red field, task_create timing unified, anti-pattern table merged cross-ref. CLAUDE.md adds hand off section explicit trigger (expected to span ≥ 2 sessions/destructive hope to roll back the entire piece to two core signals) + 4-step process (EnterWorktree/plan file falls into main repo absolute path/relay posture/cleanup goes ExitWorktree(keep) + Bash git worktree remove, bypasses ExitWorktree(remove) cross-session errorCode 4) + with changelog/tasks/ExitPlanMode/Decision Confrontation Relationship Clarification (the two semantic distinctions between the word "plan") |
| [CHANGELOG_65.md](CHANGELOG_65.md) | R3 Universal Team Backend hard cut (plan v3 PR-A 5 commit + PR-B with PR ~3000 LOC churn): team abstract decoupling Claude Code Agent Teams in-process backend, cross-adapter (claude/codex/aider/generic-pty) session delivers cross-adapter messages through DB envelope + universal-message-watcher. Create three new v010 tables (agent_deck_teams + members + messages) + v011 tasks.team_id + repo + watcher (hybrid event + poll + retry/backoff + per-team rate limit + per-target backpressure + crash recovery). adapter interface extension canCollaborate + receiveTeammateMessage + notifyTeammateEvent; MCP send_message changed to messageRepo + team_id selector; spawn_session changed to ensureByName + addMember; task-manager teamNameProvider → teamIdProvider; sessionManager.delete added leaveTeam pre-check + 0-lead automatic archive; wait-reply-coordinator added session-upserted lifecycle=closed listening. **Old backend deleted**: inbox-watcher (452) + team-coordinator (313) + inbox-protocol (306) + auto-approve (117) + team-watcher (152) + old tests (570) + team-fs 296 LOC + hook-routes 3 team route + sdk-bridge env injection + translate 5 old fn + canJoinTeam capability + TeamPermission* type + agentTeamsEnabled / autoApproveTeammateMode settings field (REMOVED_KEYS clear history) + 10 old IPC channel + 4 old event-bus event + 50 renderer consumption points. UI rewrite: TeamHub/TeamDetail/NewSessionDialog/PendingTab/session-store/activity-feed/event-type-guards/session-selectors/App.tsx/ExperimentalSection. CLI adds --team / --member across adapter spawn. SKILL + reviewer-claude + reviewer-codex agent body is completely rewritten into the mcp__agent_deck__* paradigm (since_ts race buffer recommendation + failure matrix). E12 old team data one-time export + start dialog. typecheck + 233 vitest passed all |
| [CHANGELOG_66.md](CHANGELOG_66.md) | R4 Generic-PTY adapter implementation + aider PTY access (plan v3 §F 5 tasks + F-bonus, 6 atomic commit): Use node-pty to wrap any stdin/stdout-only CLI (aider/continue/self-managed wrapper), double-layer spawn-helper permissions; F1 shared types + zod + 2 preset; F2 GenericPtyBridge per-session lifecycle (spawn/sendMessage/interrupt/close/shutdownAll) + v012 migration generic_pty_config + sessionRepo upsert/rename 17 column alignment + IPC zod parse + asarUnpack; F3 inline stripAnsi (copied ansi-regex@6 MIT does not introduce dependencies) + PtyOutputBuffer ring + IdleDetector emit waiting-for-user (promptSuffixRegex secondary verification); F4 chokidar PtyFileWatcher emit file-changed (fs-event kind, do not read content) + DEFAULT_IGNORED 14 items + skipHomedirWatch + close must await; F-bonus aider/generic-pty are both enabled canCollaborate=true + receiveTeammateMessage = bridge.sendMessage to make universal team backend truly support cross-adapter Delivery (aider shares GenericPtyBridge class, each own instance + injects 'aider' preset as fallback); F5 NewSessionDialog plus GenericPtyConfigForm subcomponent (preset drop-down + field editing + real-time zod parse anti-dirty + command preview); full typecheck + build + 319 vitest passed |
| [CHANGELOG_67.md](CHANGELOG_67.md) | REVIEW_24 Implementation fix (11 ✅ finding, 3 atomic commit): HIGH-1 PtyOutputBuffer single chunk ≥ capacity entire buffer reset to zero (aider --no-stream 5-15KB chunk + `> ` prompt must hit) + HIGH-2 bridge cap (byteLength 100_000) ↔ messageRepo cap (length 102_400) Inconsistent CJK delivery failure (repair R4 part / R3 old adapter to stay follow-up) + MED Codex 1+2 closeSession order inversion SIGTERM unreachable + killTimer leak (first SIGTERM → check sessions.has() and then set killTimer → fileWatcher.close fire-and-forget) + MED Claude4 sendMessage still writes PTY after closeSession, waste of PTY watcher retry quota (intentionallyClosed check throw "is closing") + MED Claude3 spawn-helper asar path replacement naked String.replace change regex anchor path segment (same as sdk-runtime.ts:87) + MED Claude5 chmod boolean solo → promise solo elimination race window + MED Codex 3 promptSuffixRegex ReDoS length limit 200 + MED Codex 6 parseGenericPtyConfigJson weak cast → genericPtyConfigSchema.safeParse secondary verification + MED Claude6 + Codex 7 GenericPtyConfigForm key={agentId} force remount + LOW Codex 5 fix-pty-permissions access TOCTOU delete access direct chmod capture ENOENT + LOW Codex 9 args parse The comment "wrapped in quotation marks" is changed to "does not support quotation marks" field label synchronization. reviewer-codex wrapper three times in a row 503 / reconnect / silent timeout failed, bypassing the wrapper until Bash split 2 batches of background run through (heterogeneous retention - codex CLI is still gpt-5.5); 6 regression case gatekeeping; typecheck + 325 vitest all passed |
| [CHANGELOG_68.md](CHANGELOG_68.md) | R3.E12 One-time "hard cut is about to happen" pop-up window + LegacyTeamExportSection is offline as a whole (PR-B is online + R4 has been implemented, and the backup window has expired). **Only delete but not add**: 3 whole files (LegacyTeamExportSection / legacy-teams ipc / accidentally discovered team-fs orphan file) + 7 partial files (App.tsx startup useEffect / SettingsDialog import & rendering / preload two facade / ipc-channels two channel / ipc bootstrap import & call / settings.ts field definition & default value) + REMOVED_KEYS Add `r3LegacyExportNoticeAcked` to self-clear the historical persistence value (the first log `[settings] removed legacy field "r3LegacyExportNoticeAcked"` appears after the upgrade). typecheck + dist + wrapper ping all green; same session REVIEW_25 repair package chunk empty shell bootstrap error follow-up cleanup |
| [CHANGELOG_69.md](CHANGELOG_69.md) | Settings panel information architecture reorganization: 13 sections tiled → 4 theme groupings (Session/Alerts and Appearance/Integration and Runtime Environment/Cross-Tool Collaboration MCP); 5 asset injection toggle moved from the three sections of SettingsDialog to the top of the three tabs of AssetsLibraryDialog (realizing the single source of truth for "Asset Edit + Injection Switch"); delete 3 old sections File (ClaudeMd/Plugin/CodexInjection) + remove settings ↔ asset library cross-dialog jump prop; create a new InjectionToggleBar + extract ContentViewerModal to control AssetsLibraryDialog single file ≤500; TeamHub "no team" is empty and deleted CHANGELOG_68 has been offline "old Claude Code Agent Teams data" compatible copy; LifecycleSection connects to HookSection defaultOpen High priority; README "Settings" section is rewritten in groups of 4. 0 main/preload/shared changes, 5 settings key all retained |
| [CHANGELOG_70.md](CHANGELOG_70.md) | After repairing the package, bootstrap mount MCP HTTP transport failed (FST_ERR_INSTANCE_ALREADY_LISTENING): cdb01ae R2/B'4 introduced the 6.5 section by mistake after hookServer.start(), and addRoute was rejected after fastify listen. The amendment moves the 6.5 section to 5.5 PRE_LISTEN (before hookServer.start()) + HookServer.registerRoute and adds application layer invariant to fix the contract at the boundary. See REVIEW_27 for details |
| [CHANGELOG_71.md](CHANGELOG_71.md) | REVIEW_28 Implementation: Remove spawn-guards.ts §6.2 cwd realpath entire chain traceback (interception of deep-code-review SKILL legal use case) + rate token deduction timing is rearranged to fan-out to prevent starvation (reviewer-codex MED-1) + list_sessions plus spawned_by_filter + new get_session tool (5→6 tool; EXTERNAL_CALLER_ALLOWED Record / document synchronization across src/docs/resources/README ≥15) + ADR §6.2 / §3.4 / §3.5 / §10 V11/V20 obsolete + V22-V24 newly added + SKILL Step 0 Delete list_sessions Self-test + Replace with failure get_session/spawned_by_filter |
| [CHANGELOG_72.md](CHANGELOG_72.md) | Decision-making confrontation section goes to subagent to unify dual Bash CLI (~/.claude + resources + SKILL.md synchronization, delete §Fallback section) + large image automatic compression client-side (base64 super 4.8MB go JPEG re-encoding + downscale sequence) + Bug 3 bypass short circuit repair (InternalSession.permissionMode in-memory cache + canUseTool Short circuit before default path; 6 single test) |
| [CHANGELOG_73.md](CHANGELOG_73.md) | Clean up experiments/ expired archives (R1 hand off + plan v3 spike report, the conclusion has already been implemented in R1-R4) + docs/agent-deck-mcp-protocol.md:7 Delete the "association" field SPIKE_REPORT.md reference (to avoid broken links at the top of ADR); docs/ overall retention (8 src comments + README/5 changelog Referenced as active protocol specification) |
| [CHANGELOG_74.md](CHANGELOG_74.md) | claudeCodeSandbox per-session coverage + runtime switching (three-piece symmetrical with codex): DB v013 + IPC + adapter capability literal image + NewSessionDialog 4-file drop-down "follow settings / off / workspace-write / strict" + ComposerSdk runtime cold cutting (switch to 'off' pop-up confirm, with codex `danger-full-access` reverse isomorphism); by the way, the `RestartController` sub-module contains restartWithPermissionMode + restartWithClaudeCodeSandbox two semantically isomorphic methods (sdk-bridge/index.ts 866 → 816 line net -50 towards the ≤500 guardrail, the remaining splits are written into the "immovable file protection list") |
| [CHANGELOG_75.md](CHANGELOG_75.md) | Cmd+Alt+T Transparency global shortcut key (plan deep-review-flow-fix Phase T1): toggle "Settings → Window → Transparent when pinned" switch, immediately switch to CSS frosted-frame + macOS vibrancy in pin state; and `Cmd+Alt+P` (pin) mirror pattern (main globalShortcut + IpcEvent.TransparentToggled + preload onTransparentToggled + renderer App.tsx listens for setSettings persistence). Choose `Cmd+Alt+T` instead of plan D9's default `Cmd+Shift+T` (the latter is snatched by the browser's "reopening and closing tabs", and the OS-level globalShortcut will really conflict), which is consistent with the naming of `Cmd+Alt+P`; README adds a new "Keyboard Shortcuts" section |
| [CHANGELOG_76.md](CHANGELOG_76.md) | spawn_session schema, add agent_name, automatically inject plugin agent body (D1) + projectSession, reverse check universal team backend, fix "lead teamName: null asymmetry" (D3) (plan deep-review-flow-fix Phase B). tools.ts SPAWN_SESSION_SCHEMA Add `agent_name: regex([a-zA-Z0-9._-]+).optional()` + handler adjust `getBundledAssetContent('agent', name)` and spell body to prompt header, not found and immediately err (anti-silence fallback fails); projectSession is changed to "Adjust first findActiveMembershipsBySession + agentDeckTeamRepo.get reverse check → fallback to sessions.team_name". SKILL.md synchronizes the descriptions of "Step 1 prompt to inject two forms" and "Step 0.6 Option B". tools.test.ts added 5 new tests (D1 three + D3 two), and all 54 vitests passed. D2 inherit_caller_permissions / D4 wait_reply distinguish finished/waiting-for-user and leave follow-up (detailed CHANGELOG remarks) |
| [CHANGELOG_77.md](CHANGELOG_77.md) | SessionList press spawnedBy tree folding + lead/teammate badge (plan deep-review-flow-fix Phase C): real-time panel active/dormant root + children indent display in each group (thin blue edge indicator on the left); SessionCard plus teamRole prop (lead blue border / 👑 lead light blue chip; teammate ↳ teammate lighter blue chip), and existing 🛡 Purple teamName chip style distinction (purple=team name / blue=character in team). The orphan teammate (owner has been closed/archived/cross-group) press D8 to not bind it, and the tile is root without badge. The click behavior remains unchanged (onSelect → store.selectSession, continue to be used in detail / ComposerSdk process); change renderer to HMR, no need to restart dev |
| [CHANGELOG_78.md](CHANGELOG_78.md) | Team cohesion fix, 6 phases (plan team-cohesion-fix-20260513): A drops sessions.team_name for universal team backend + SessionRecord.teams[] / SessionCard +N badge; B updates send/reply/wait_reply with messages.reply_to_message_id, B5 spawn placeholder, and B6 wait-reply-coordinator removal; B7 injects wire format `[from X][msg <id>]` so teammates can reply_message and syncs 4 docs (SKILL.md / reviewer-*.md / application CLAUDE.md); C rewrites TeamDetail 6-section team work panel (8 files <=200 lines + new IPC AgentDeckTeamGetFull); D adds PendingTab teammate chip + role badge; F adds D6 close auto leaveTeam + D7 TeamLifecycleScheduler 5min/30min grace + IPC ShutdownAllTeammates + Header close/archive buttons. |
| [CHANGELOG_79.md](CHANGELOG_79.md) | deep-code-review SKILL + two copies of CLAUDE.md Large-scale document weight loss (REVIEW_30 implemented): 5 core doc 1910→728 lines (62% thinner, SKILL.md 282→92 / reviewer-*.md micro/application CLAUDE.md 679→56 SSOT split / user CLAUDE.md 624→272) + migrate 11 files to ~/.claude/{templates,SOPs}/ (6 markdown templates + 2 reviewer bash templates + 3 SOP scripts/notes); discard the application CLAUDE.md ↔ user CLAUDE.md manual synchronization convention (the machine cannot execute and has drifted), change user SSOT + app Δ; conventions-tally from .claude/ Isolated to top-level conventions/ independent directory (tally.md + INDEX.md, same level as changelog/ reviews/ plans/), count=3 Upgrade **New** `conventions/<X>-<topic>.md` instead of writing the project CLAUDE.md (leave CLAUDE.md static) |
| [CHANGELOG_80.md](CHANGELOG_80.md) | bug fix: lead archive → team auto-archive linkage + REVIEW_32 R1 fix 9 items (plan deep-review-and-split-20260513 Phase 1): original bug chain countActiveLeads SQL JOIN sessions + manager.archive/unarchive changed to async and added _archiveTeamsIfOrphaned/_unarchiveTeamsForRevivedLead Two helpers (lazy import mode); REVIEW_32 R1 Heterogeneous confrontation between both sides mining 16 finding, this session picks 8+1 repair: HIGH 1 markDelivered SQL accepts 'pending' (spawn placeholder is no longer re-delivered by watcher) / HIGH 2 findSharedActiveTeams JOIN archived_at (archived team no longer misjudges common active)/ HIGH 3 wait_reply nudge self-loop filtering (reply direction verification)/ HIGH 4 spawn_session return add agentName+displayName (user added)/ HIGH 5 spawn default inheritance lead permission/sandbox + claude_code_sandbox schema (user added + reviewer-codex evidence)/ HIGH 6 setRole otherLeads JOIN sessions (consistent with countActiveLeads) / MED 7 v016 archive_reason column + 5 union source + unarchive linkage only resurrects 'last-lead-archived' / HIGH 9 caller_session_id changed optional (user added, 7 schema + deriveCaller helper) |
| [CHANGELOG_81.md](CHANGELOG_81.md) | Split tools.ts 1060 → tools/ 11 files (plan deep-review-and-split-20260513 Phase 2 Step 2.1): facade index.ts (139) + schemas.ts (236) + helpers.ts (154) + handlers/{spawn,send,reply,wait,list,get,shutdown}.ts (39-281). The external caller import path remains unchanged (`from './tools'` automatically resolves to tools/index.ts). Fix REVIEW_32 §Follow-up 2 MEDs: MED-1 fan-out race (setSpawnLink mentions closing the race window in try) / MED-3 send teamId cross-contamination (reply_to_message_id reverse check original.teamId and resolved teamId consistency check). The remaining MED is left in H3 and will be repaired nearby when the scheduler/watcher is dismantled. typecheck double-ended pass. |
| [CHANGELOG_82.md](CHANGELOG_82.md) | Split agent-deck-team-repo.ts 658 → agent-deck-team-repo/ 5 files (plan deep-review-and-split-20260513 Phase 2 Step 2.2): facade index.ts (130) + types.ts (139) + team-crud.ts (211) + member-crud.ts (181) + member-query.ts (166). Dependency DAG: member-query → team-crud → member-crud. Facade re-export error class + input/option type keeps external caller import path unchanged (`from '@main/store/agent-deck-team-repo'` automatically resolves). Only physical split with zero business behavior changes (19 method signatures / SQL / API surface 100% consistent). typecheck double-ended pass. |
| [CHANGELOG_83.md](CHANGELOG_83.md) | Split session-repo.ts 590 → session-repo/ 7 files (plan deep-review-and-split-20260513 Phase 2 Step 2.3 / Tier 1 closing): facade index.ts (38) + types.ts (92) + core-crud.ts (198) + archive.ts (21) + lifecycle.ts (131) + rename.ts (122) + spawn-chain.ts (91). Archive.ts is extracted separately to echo the principle of "lifecycle and archived_at are orthogonal". `delete` reserved-word workaround go `_delete` → `delete` spread rename. Only physical split with zero business behavior changes (27 method/SQL/API surface 100% consistent). The three major files of Tier 1 (tools/team-repo/session-repo) have been completely dismantled, and the max sub-file LOC is 281. typecheck double-ended pass. |
| [CHANGELOG_84.md](CHANGELOG_84.md) | Split pty-bridge.ts 506 → pty-bridge/ 5 files (plan deep-review-and-split-20260513 Phase 3 Step 3.1 / Tier 2 start): facade index.ts (274 including createSession body) + pty-session-state.ts (91 type/constant) + spawn-helper.ts (49 chmod function + promise solo state stay class) + lifecycle.ts (104 closeSession + shutdownAll) + message-io.ts (150 sendMessage + interrupt + onData/onExit listener factory). The external caller import path remains unchanged (3 places `from './pty-bridge'`/`'../generic-pty/pty-bridge'` are automatically resolved). Only physical split with zero business behavior changes (9 method signatures / SIGTERM-grace-SIGKILL timing / fileWatcher.close fire-and-forget 100% consistent). typecheck double-ended passed + 31 pty-bridge + 7 adapter shared passed all single tests. |
| [CHANGELOG_85.md](CHANGELOG_85.md) | Slimming sdk-bridge/index.ts 816 → 495 ≤ 500 (plan deep-review-and-split-20260513 Phase 3 Step 3.2 / Tier 2 closing): Pump 6 new sub-modules (pending-cancellation 125 / mcp-server-init 85 / query-options-builder 151 / send-validation 65 / session-finalize 68 / sandbox-resolve 35) + types.ts plus makeInternalSession factory + 13 stale import cleanup + class field/ctor/thin wrapper jsdoc slimming down (~110 lines jsdoc slimming down). 3 high-density pumping (mcp assembly / query options / closeSession cleanup) + 3 low-density pumping (sandbox resolve / internal factory / finalize chain) + sendMessage validation pumping. Only physical split with zero business behavior changes (method signature / emit sequence / SIGTERM timing / sub-module ctx pattern 100% consistent). typecheck double-ended pass + 28 single test all passed. |
| [CHANGELOG_86.md](CHANGELOG_86.md) | Split manager.ts 734 → 5 sibling files (plan deep-review-and-split-20260513 Phase 4 Step 4.3 / Tier 3 closing): 3 atomic commit — Step 4.3.1 enrich (55) / Step 4.3.2 team-coordinator (158, including dup elimination leaveTeamsAndAutoArchive Merge _leaveAllActiveTeams + delete segment 1 Use satisfies map to distinguish reason) / Step 4.3.3 ingest-pipeline (224, IngestContext facade replaces the original implements design - Object.freeze 5 closures to make the pipeline cast `(ctx as any).sdkOwned` unreachable + 5 helper methods remain private). manager.ts 439 LOC (margin 12%); go deep-code-review SKILL R1 heterogeneous confrontation (reviewer-claude + reviewer-codex 22 finding, HIGH-B1 facade reverse ruling and adoption); 29 session it + full 324 vitest all passed; 4 H5 follow-up (markDormant dead code / mock missing .get .unarchive / characterization test / #sdkOwned true private upgrade). |
| [CHANGELOG_87.md](CHANGELOG_87.md) | J bug fix (lead detail repeated display reply) + check_reply mcp tool (plan mcp-bug-and-feature-batch-20260513 Phase 1): 2 atomic commit. J Fix - `universal-message-watcher.deliver` short-circuit skips `adapter.receiveTeammateMessage` for reply (replyToMessageId != null) to prevent lead detail dual display (wait_reply tool_result + SDK echo user message dual) + prevent lead Claude from treating reply as user input and running empty agent loop. B check_reply - a non-blocking paired version of wait_reply (the lead call immediately returns `{ reply \\| null, timedOut: false }` without hanging on the listener / cannot afford nudge / does not block), and is coordinated with J (after the reply is no longer injected to the sender SDK, the sender uses wait_reply to block / check_reply poll takes the initiative). The helper isLegitReply / replyProj extracts helpers.ts to be shared by wait.ts / check.ts. 47 tests passed (3 watcher J + 44 tools including 3 new check_reply). |
| [CHANGELOG_88.md](CHANGELOG_88.md) | Main end backlog cleanup C/E/G/H 4 items (plan mcp-bug-and-feature-batch-20260513 Phase 2): 4 atomic commit. C MED-D7 — TeamEventDispatcher.start() pre-fills lastArchivedAt cache to prevent the first archive transition from being swallowed (lead archive linkage + emit team-updated when the team is first seen, prev=undefined until return → active member cannot receive team-archived event). E LOW — team-lifecycle-scheduler.scan() while-loop pagination prevents long-running instances team > 200 from missing scans. G MED-A7 - makeAgentDeckTeamRepoMock completes the 18 method (use AgentDeckTeamRepo strong type to cover the whole situation, and in the future, the real repo will add method to force prompt during compilation). H HIGH-B2 — New manager-team-coordinator.test.ts 5 it covers leaveTeamsAndAutoArchive closed/deleted/0-lead/multi-membership/orphan boundaries (H4 dup was eliminated only by code diff). typecheck double-ended passed + all 34 session tests + 5 watcher + 5 coordinator tests passed. D/F backlog confirmed non-genuine bug removed. |
| [CHANGELOG_89.md](CHANGELOG_89.md) | SessionManager `#sdkOwned` Upgrade ECMAScript true private + public hasSdkClaim API (plan mcp-bug-and-feature-batch-20260513 Phase 3 / CHANGELOG_86 §H5 follow-up #4 closed): 1 atomic commit. `private sdkOwned: Set<string>` → `#sdkOwned` (runtime forces inaccessibility, cast `(sessionManager as any).sdkOwned === undefined`) + new public method `hasSdkClaim(sessionId: string): boolean` (same origin as IngestContext.hasSdkClaim); ingestCtx facade hasSdkClaim changes the delegate public API; deletes the obsolete "cannot upgrade true private" jsdoc warning in manager.ts/manager-ingest-pipeline.ts; tests reflection `as { sdkOwned }` changes sessionManager.hasSdkClaim() API (manager-public-api.test.ts:134-136). typecheck double-ended passed + all vitest 23 files 344 it passed (including sdk-bridge.test.ts 7 it verified SDK fallback / fork detection path without regression). The decision is made by plan § Decision 6. The user decides to "change directly" and goes through full vitest verification without confrontation. |
| [CHANGELOG_90.md](CHANGELOG_90.md) | N bug fix — Automatically unarchive user "continue chat" archived sessions (plan mcp-bug-and-feature-batch-20260513 Phase 1.5 / user H2 new report critical UX bug): 1 atomic commit. The root cause is that the IPC `AdapterSendMessage` handler does not check `archivedAt`, and archived_at is maintained after the archived session is "resumed" by the user from the SessionDetail history. The revised law strictly distinguishes between the two paths of "user active sendMessage = explicit signal → unarchive" and "event stream passive arrival → unarchived (maintaining the orthogonal convention of manager.ts:152-156)". The new public API `sessionManager.unarchiveOnUserSend(sid)` encapsulates archived guard + reuses `unarchive()`. It already has archive_at = null + emit + team unarchive linkage; the IPC handler calls a line `await sessionManager.unarchiveOnUserSend(sidParsed)`. 3 New single test covers dormant + archived / not archived noop / not-found noop. typecheck + full vitest 23 files 347 it passed. |
| [CHANGELOG_91.md](CHANGELOG_91.md) | archive_plan mcp tool implementation + document synchronization (plan mcp-bug-and-feature-batch-20260513 Phase 4a / K1 hand-off automation): 2 atomic commits. **Step 1-5** (commit `81a15d8`) archive_plan tool implementation — types.ts/schemas.ts/tools/index.ts plus 9 tool registration; archive-plan-impl.ts ~330 LOC DEFAULT_DEPS inject mode 14-step process (rev-parse main repo / preflight worktree clean + cwd not in worktree + plan status ≠ completed + non-detached HEAD / ff merge / frontmatter update + mv plan to `<main>/plans/<id>.md` / INDEX append (jump if there is no creation / plan_id already exists) / git add + commit / worktree remove + branch -D); archive-plan.ts thin wrapper handler; 11 it single test cover happy path + 5 preflight failed branches + 3 path fallback. **Step 6-9** (this CHANGELOG) document synchronization - `~/.claude/CLAUDE.md §Step 4 cleanup` split 3 sub-section (recommended mcp tool automation + fallback manual Bash + abort special cases without leaving the tool); `resources/claude-config/CLAUDE.md` 7 tool → 9 tool list + newly added §check_reply non-blocking poll + §plan hand-off automation (archive_plan full ts template + 14-step process overview). typecheck double-ended + full vitest 24 file 358 it all passed (base 347 + 11 new archive-plan it). decision: plan §Decision 3+7 The user decides on the mcp tool path without confrontation. |
| [CHANGELOG_92.md](CHANGELOG_92.md) | start_next_session mcp tool implementation + document synchronization (plan mcp-bug-and-feature-batch-20260513 Phase 4b / K2 hand-off automation): 1 atomic commit. **Implementation** — types.ts 9→10 tool adds `startNextSession` + EXTERNAL_CALLER_ALLOWED false (same as spawn / archive_plan to prevent SDK fork bomb); schemas.ts START_NEXT_SESSION_SCHEMA 8 fields (plan_id required + phase_label / cwd / adapter / team_name / permission_mode / plan_file_path / caller_session_id optional); start-next-session-impl.ts ~190 LOC deps inject 5-step business (plan file path analysis caller cwd reverse check main-repo > fallback ~/.claude / parseFrontmatter / verification worktree_path / verification status === 'in_progress' completed/abandoned respective hint / construction cold start prompt including phase_label suffix); start-next-session.ts ~95 LOC handler entry (deny external + adjust impl + assemble SpawnSessionArgs cwd=worktree_path/team_name=plan_id/adapter=claude-code default + adjust spawnSessionHandler transparently transmit the same ctx + JSON.parse spawn field + package K2 metadata + transparently transmit spawn field, spawn isError Direct transparent transmission without nested packaging); tools/index.ts 9→10 tool registration. 22 it single test (impl happy path 6 + failed branch 8 + base_branch transparent transmission 2 / handler deny external 1 + happy 4). **Document synchronization** — `~/.claude/CLAUDE.md §Step 3 relay posture` dismantling §Option A User manual cold start prompt + §Option B K2 mcp tool automatically starts a new session dual posture; `resources/claude-config/CLAUDE.md` 9 tool → 10 tool + Add §plan hand-off automation: start_next_session section (complete ts call template + Business process + "New session system prompt must contain user CLAUDE.md" campus police). typecheck double-ended + full vitest 25 files 380 it passed all. |
| [CHANGELOG_93.md](CHANGELOG_93.md) | K3 hand-off UI button + LLM history summary + modal preview (plan mcp-bug-and-feature-batch-20260513 Phase 4c): 3 atomic commits (main implementation / renderer implementation / this CHANGELOG). **main side** — summarizer.ts Add `summariseSessionForHandOff(cwd, events)` ~115 LOC Copy summariseViaLlm SDK query template (loadSdk + permissionMode:'plan' + settingSources:[]) Change model `process.env.ANTHROPIC_DEFAULT_SONNET_MODEL \|\| ANTHROPIC_MODEL \|\| 'sonnet'` Level 3 fallback + K3-specific 4-section structured prompt ([Target] [Done] [Next Step] [Related Files]) + 60s timeout + 4000 words result max; formatEventsForPrompt plus export shared; ipc-channels.ts plus `SessionHandOffSummarize: 'session:hand-off-summarize'` + `SessionHandOffSpawn: 'session:hand-off-spawn'` Dual channel; add 2 handlers to ipc/sessions.ts (dual-stage split to allow modal preview / restart the session after editing: Stage 1 pulls 200 events + LLM summary return, Stage 2 uses finalPrompt adapter.createSession to start a new session and continues to use the original cwd / agent / permissionMode + automatic sessionManager.archive original session warn-only No blocking + emit session-focus-request to let the App automatically switch to detail); hand-off.test.ts 5 it (happy path / empty events / SDK empty result / timeout placeholder / ANTHROPIC_DEFAULT_SONNET_MODEL env override). **renderer side** — preload adds handOffSummarize / handOffSpawn 2 facade method; HandOffPreviewDialog.tsx creates new ~140 LOC modal (summarizing / spawning / error three-state + retry summary button + busy disabled anti-interruption + disposed flag anti-unmount setState); SessionDetail header adds button group: only displayed when isSdk 📤 hand-off button (CLI session starts a new session without prompt, the injection is of little significance to avoid ambiguity). **User open problem decision-making**: Q1 automatic archiving / Q2 200 events / Q3 sonnet 4.6 take local settings.json / Q4 modal preview + editing form. **Known pitfall** — modal busy disabled close Anti-interruption + archive warn-only No blocking newSid + emit focus-request Reuse cli.ts `agent-deck new` The same model automatically switches the focus channel. dev smoke postpones Phase 6 H6 to smoke together with other phases. typecheck double-ended + full vitest 26 files 385 it passed all. |
| [CHANGELOG_94.md](CHANGELOG_94.md) | Phase 5 — A cross-session UI rendering differentiation + L SessionCard enhancement + M transparency/top decoupling (plan mcp-bug-and-feature-batch-20260513 Phase 5): 3 atomic commits (A `cdb2f87` + L `cdb46d1` + M this commit). **A** (Step 5.1+5.2) - `src/shared/wire-prefix.ts` new parser + 9 it single test symmetrical with the main side `buildWireBody` form (regex `[^\]]+` anti-greed); `message-row.tsx` only user role parse → cyan chip "↩ X" hover title complete adapter+msgId + body-only render; `agent-deck-message-repo.ts` add `listBySession(sid)` SQL `from OR to` ORDER BY DESC + 1 it; new IPC `AgentDeckMessageListBySession` + handler + preload facade; create new `SessionDetail/MessagesPanel.tsx` ~120 LOC (refer to TeamDetail/MessagesSection style + distinction Add → / ↩ mark to sender/receiver + reply chain chip + onAgentDeckMessageChanged 200ms throttling and repulsing); Add `'messages'` "cross-session" button to SessionDetail Tab type. **L** (Step 5.3+5.4) - summarizeToolInput 5 case enhancements (TodoWrite `[N/M done] · activeForm` progress / WebSearch query / WebFetch url / Task+Agent subagent_type · description); describeLiveActivity returns string[] up to 3 rows + deduplication consecutive peers + cycle recent.slice(0, 12); SessionCard render multi-line truncate (i=0 main color i≥1 secondary color visual layering) + useMemo cache anti-scroll recalculation. **M** (Step 5.6) — settings field `transparentWhenPinned` → `windowTransparent` rename + REMOVED_KEYS one-time migration (old value → new field does not lose preferences); window.ts setVibrancy no longer relies on alwaysOnTopCurrent (deleted dead field) + `setTransparentWhenPinned` → `setWindowTransparent`; ipc/settings.ts applyTransparentWhenPinned → applyWindowTransparent; main/index.ts bootstrap + Cmd+Alt+T shortcut handler synchronous name change; App.tsx state + FloatingFrame transparent prop no longer `&& pinned` decoupling alwaysOnTop; WindowSection.tsx label "Window Transparent" + copywriting express decoupling. typecheck double-ended + full vitest 27 files 394 it + 56 skipped all passed. **Known pitfall** — listBySession does not remove some indexes (thousands of rows OK, 100k+ in the future, plus double indexes + UNION ALL) / migration must be before REMOVED_KEYS delete / wire prefix chip only user role / Step 5.5 dev smoke postpones Phase 6 H6. |
| [CHANGELOG_95.md](CHANGELOG_95.md) | UX Fine-tune six items + review and fix 9 items - hand-off explicit triggering + detail header style unified + relay button moved to the lower right of Composer + ActivityFeed tool description completion + deep-code-review SKILL changed to user-driven check_reply poll + nudge poll two messageId at the same time to solve the wire prefix protocol conflict (the user sends six feedbacks in a row and implements it in one go, and then uses a single decision to fight against double Bash review × Opus 4.7 + gpt-5.5 Heterogeneous confrontation → 9 items ✅ Real problems all repaired): HandOffPreviewDialog mount no longer automatically adjusts sonnet to change CTA explicit trigger + **review modification requestSeqRef + capturedSid double guardrail replacement disposedRef** (prevent sessionId switching in-flight IPC from polluting new sessions) + idle derivation plus `&& !spawning` + textarea cond adds `\|\| spawning` (to prevent CTA flashing during submit + textarea disappears); TeamDetail Header "← Return" moves from the left to the right emoji-only square button (mirror with SessionDetail); SessionDetail header `📤` is removed in its entirety + ComposerSdk adds `onHandOff?` optional prop bottom button group Insert "📤 relay"; activity-feed/describe.ts `describeToolInput` Supplement TodoWrite/WebSearch/WebFetch three cases + **review revision TodoWrite null element type guard gatekeeping**; deep-code-review SKILL Step 2/4/5 Change wait_reply to user-driven check_reply poll (**review revision remove ScheduleWakeup (limited to /loop) Scope) Change user-driven**: lead check_reply is triggered by user message at the beginning of each turn, UI real-time activities allow users to self-judge the progress) + **review revision nudge must poll two messageIds at the same time** (watcher automatically injects `[msg <nudgeId>]` into nudge wire prefix → teammate according to discipline reply nudgeId instead of originalId → lead single poll original can never be obtained, codex Actual measurement and irrefutable evidence) + 7→10 tool digital correction. typecheck double-ended pass. tally U3/U4/U5/U6 + P32 (disposedRef anti-pattern) to be accumulated. |
| [CHANGELOG_96.md](CHANGELOG_96.md) | session rename / delete hits FK constraint closure (v017 schema CASCADE + rename explicit migration team_members/messages/spawned_by, plan linked-swimming-platypus): User reported SDK flow interruption "⚠ FOREIGN KEY constraint failed" screenshot + DB data fully verified root cause (lead fork when 4 active teams) rename triggers DELETE OLD in sessionRepo.rename and crashes RESTRICT FK rolls back the entire transaction → consume catch + finally emit red letter + finalizeSessionStart emit session-start/user message all goes to NEW name and becomes cwd=empty orphan row) + Incidentally, sessionManager.delete also crashes with the same FK link (leaveTeam writes left_at without deleting the row → DELETE FROM sessions still crashes RESTRICT, the UI "delete" button can never delete a session with active team_members). **v017 migration** recreate `agent_deck_team_members` table session_id FK RESTRICT → ON DELETE CASCADE (SQLite 12-step: CREATE _new + INSERT FROM old + DROP old + RENAME _new + Rebuild 3 indexes) + REGISTER migrations/index.ts. **rename.ts** Add 3 UPDATEs immediately after the existing events/file_changes/summaries UPDATE in the transaction: (a) `agent_deck_team_members.session_id` OLD→NEW allows NEW to continue the OLD lead/teammate role (CASCADE automatic deletion will cause NEW to lose membership team auto-archive violates rename semantics; explicit migration + defensive PK conflict DELETE-then-UPDATE); (b) `agent_deck_messages.from/to_session_id` (FK is not forced but the watcher checks sessionRepo.get(toSessionId) and delivers otherwise markFailed false negative); (c) `sessions.spawned_by` self-reference (v009 ON DELETE SET NULL does not hit FK but preserves spawn chain integrity). Add `renameWithDb(db, fromId, toId)` test seam (production `rename(fromId, toId)` is changed to thinner wrapper, sessionRepo facade `spread { rename }` remains unchanged). **Test** — agent-deck-repos.test.ts import v012-v017 + makeMemoryDb array synchronization + import renameWithDb; rewrite the RESTRICT test as CASCADE (delete sessions do not throw + COUNT=0 verification cascade); add 3 rename migrations (team_members continues lead role/messages double field/spawned_by self-reference). **Annotation update** — v010 adds marker "v017 update: CASCADE corrects v010 design conflict"; rename.ts top jsdoc rewrites the entire section and deletes the expired contract "caller leavesTeam(OLD)+addMember(NEW)" (the actual 6 callers are not implemented) + adds dual-track instructions; manager.ts delete + manager-team-coordinator.ts leaveTeamsAndAutoArchive jsdoc delete `@warning FK RESTRICT` section + change `@note await UX correctness instead of FK detour` (first leaveTeam writes left_at + emit member-changed + 0-lead auto-archive, then sessionRepo.delete does CASCADE physical clearing, reversing the order will cause CASCADE to delete the row and then leaveTeam cannot find the active row → do not emit → UI does not refresh + archive linkage runs empty). typecheck double-ended + dev actual v17 migration runs through (`[db] migrated to v17 (agent_deck_team_members_cascade)` + sqlite3 verification schema CASCADE + PRAGMA user_version=17). Decision: User Q1 decides on schema CASCADE without confrontation (single point of design + solid evidence from actual measurement + 2 Explore agent surveys). Q2 The user manually writes SQL cleanup historical orphan data (plan §Step 5). tally P33 candidate +1: "DB FK ON DELETE RESTRICT + application layer leave only UPDATE left_at without row deletion anti-pattern (pre-check form is essentially invalid)". |
| [CHANGELOG_97.md](CHANGELOG_97.md) | K2 relay baton semantic transformation (default does not add team + default archive caller): user actual test K2 plan relay hit "the original session is labeled lead / the new session is labeled teammate" UX noise + DB empirical evidence (plan team `desk-assistant-20260512` There is only 1 K2 automatic placeholder message since its creation, lead/teammate There has never been a real dialogue between them; the control group is really reviewer team `deep-review-32` / `phase4-manager-split-review` (4-8 messages each) → design semantic mismatch (plan relay is essentially a baton one-way handover, not dispatch to send people to work). **A. start-next-session.ts:96** spawn args `team_name: args.team_name ?? args.plan_id` → Conditional expansion `...(args.team_name !== undefined ? { team_name } : {})`, default is not passed to spawn → spawn handler short-circuit ensureByName/addMember/placeholder full set, caller/new session None of them are tagged lead/teammate. **B. start-next-session.ts:126** After successful spawn, add `archiveSession(caller.callerSessionId)` block (baton semantics = original session exits, new session takes over independently), failure warn-only does not block K2 ok return (same as K3 hand-off mode), EXTERNAL_CALLER_SENTINEL double insurance defense. `StartNextSessionHandlerDeps` adds `archiveSession?` test seam (the same inject mode as `spawnSession` seam). When spawn fails/impl fails, archive will not be adjusted (baton has not yet taken action). **schemas.ts** team_name `.describe()` rewrites the explicit statement "Default: not set (CHANGELOG_97 baton semantic) ... pass explicitly only if you specifically want lead/teammate communication". **index.ts** K2 tool annotation rewrites the entire section and adds the "Baton semantic" section + returns the field annotation default null (teamId/teamName/spawnPromptMessageId). **Two copies of CLAUDE.md** Synchronization: user CLAUDE.md L188-194 + application CLAUDE.md L88-102, delete "join plan-id team" + add "do not add any team" + "automatically archive caller session" section, add a default null annotation to the return field comment, and remove "lead monitoring teammate feedback" in the applicable scenario section (caller has been archived and is no longer supervised under baton semantics) + Added "caller can still view pre-relay conversations in the history panel after archiving". **Test** — happy path Change mockSpawn return to all null + add `mockArchive` test seam + `archiveCalls` record call sid + assert `spawnArgs.team_name).toBeUndefined()` + `mockArchive).toHaveBeenCalledTimes(1)`; new it "archive fails warn-only does not block K2 ok return" Use warnSpy Verify; spawn fails / impl fails it add `mockArchive).not.toHaveBeenCalled()`; explicit team_name override it add mockArchive still archives caller when validating explicit team_name (baton semantics are orthogonal to whether team communication relationship is enabled). Decision: A+B merge without confrontation (the user has chosen Plan C + solid empirical evidence + single point of design). **Known pitfalls** — caller archive chain A→B→C→D. The step-by-step archiving is the expected baton chain; K3 hand-off is archive+without team mode. This PR does not move; `team_name` still archives the caller when overriding explicitly (orthogonal design); the caller can still view the pre-relay dialogue in the history panel; EXTERNAL_CALLER_SENTINEL defends redundancy but retains it (anti-future external caller Deletion due to accidental deletion of the path); not deleting historical plan team data is up to the user. |
| [CHANGELOG_98.md](CHANGELOG_98.md) | mcp tool R1 deep review 25 finding + R2 feedback 9 items + R3 closure + B14 dormant counter-intuitive stepping on pitfalls (plan `mcp-handoff-fix-and-skill-timer-20260514`): 4 atomic commits. **A** (`438a613`) src fixes 7 items: H5 caller cwd reverse check / H2 wait_reply nudge deadlock repair double check nudgeMessageIds / H1 mcp__agent-deck__ hyphen 41 replacement + server pattern / M7 abandoned rejection / A5 archived tri-state field / M1 shutdown_session data retention + 90 mcp test All passed. **B** (`0cc4f79`) 13 major document changes: H3 docs/agent-deck-mcp-protocol.md 729→30 lines stub / H4 cross-session firefighting shared-team front-end callout / H6+D4 user CLAUDE.md §Step 2.5 hand off trigger section / M4+D5 SKILL.md timer fallback (CronCreate non-blocking cover) + strictly prohibit blocking timer boundary clarification / M3 Opus 4.7 default thinking / M5 wire format id invariant anchoring / M6 NO MSG ANCHOR complete (send_message + list_sessions reverse check + SDK assistant output three-layer cover) / M8 reviewer template mktemp $TMPDIR / B12 shutdown does not delete the data list team_member soft-exit + spawn_link / 8 LOW INFO Handy. **B14** (`58a5db9`) dormant ≠ lost mental model, counter-intuitively stepped on the pit and settled in 3 places md: incorrect inference when spawning R2 reviewer dormant=fresh → user reminder SDK resume → verify recoverer.ts:103-220 dormant + send_message → createSession({resume}) restore conversation history → mental model is implicitly retained through conversation history ✅. Supplement app CLAUDE.md / SKILL.md / reviewer-{claude,codex}.md FRESH SESSION self-inspection terms. **E + F** (`4d48ef0`) R2 Feedback 1 HIGH + 4 MED + 3 LOW Closing: E1 HIGH K2 baton spawn_depth misjudgment repair (spawn-guards + spawn.ts plus opts.batonMode double modification + start-next-session explicitly pass batonMode:true + setSpawnLink write lateral parentDepth + 4 case test Override batonMode jump depth / fan-out still enforce / rate still enforce / batonMode=false default - reviewer-codex warning "double changes must be synchronized" otherwise depth 4/5/... cumulative pollution common spawn) / F1 MED A5 archived caller row missing probe repair + 1 new case / F2 MED supplement A5 tri-state + A2 nudge double check 4 case including reply to nudgeId core reverse scenario / F3 MED ~/.claude/CLAUDE.md §Step 2.5 Trigger signal vs precondition grouping / F4 LOW 4 Change README + wait.ts comment + tools/index nudgeMessageIds + schemas.ts:296. **R3 closing** reviewer-claude ✅ "All R2 finding fixes are symptomatic + 0 new bugs + can be combined (R3 closing)" (including the measured 8-phase K2 baton chain + fallback chain, each path + 6 focus all passed). typecheck + build + 98 mcp test all passed (R2→R3 +8 case). tally P34 "mcp baton spawn_depth misjudgment" + U7 "dormant ≠ lost mental model" candidate +1. |
| [CHANGELOG_99.md](CHANGELOG_99.md) | cwd failure cure: K2 default mainRepo + dual mode + archive caller + recoverer heuristic fallback + tool rename hand_off_session(plan `cwd-resilience-fix-20260514`): 8 atomic commit. **Resolve the user report of "session message hits Path does not exist and detours error chain"** (K2 baton starts session cwd=worktree, sessionRepo.cwd becomes invalid after worktree is deleted by archive_plan). **A** (`bdbaace`) Phase A K2 default cwd = mainRepo + tool rename `start_next_session` → `hand_off_session` (user feedback aligns with existing hand-off terminology): 3 files git mv + all export name synchronization + EXTERNAL_CALLER_ALLOWED key + impl plus mainRepo calculation + handler double layer fallback + schemas description + 28 tests pass. **A′** (`1b21f6c`) Phase A′ dual-mode transformation (user feedback "hand-off mcp should be more universal"): plan_id becomes optional + add prompt field + impl dual-mode distribution (plan-driven / generic) + handler default cwd is different by mode + ok return add mode/ignoredFields + 8 new generic test → 36 tests pass. **B** (`aa6575a`) Phase B archive_plan default archive caller (K2 baton same semantics): 4 new archive caller test → 17 tests pass. **C** (`aadd3d2`) Phase C recoverer cwd heuristic fallback (old K2 session + corner): cwd precheck + findFallbackCwd heuristic (`/.claude/worktrees/` mode + parent walk no more than home) + cwdFellBack force jsonl missing downstream + 4 new fallback case + cwdExistsOverride test seam → 11 tests pass. **D** (`db7c893`) Document synchronization at Phase D 4 (app CLAUDE.md / SKILL.md / docs/protocol.md / user CLAUDE.md): rename + dual mode + cwd resilience + archive caller. **E3** (`b213d61`) R1 dual heterogeneous confrontation reviewer 5 MED + 4 LOW fully repaired (reviewer-claude 0H/1M/2L/2I + reviewer-codex 1H/4M/2L/1I, consensus + unilateral exclusive, three-state ruling): MED-1 sessionRepo.get asymmetric dual handler try/catch + MED-2 archived auto-unarchive order prefix + MED-3 findFallbackCwd regex allow subdirectories + MED-4 generic callerCwd existsSync precheck + MED-5 archive caller recheck ground truth + LOW-2 home boundary isAncestorOrEqual + LOW-7 plan comment misalignment + LOW-8 cwd fallback test rename assertion + LOW-9 spawn-guards stale comment → 254 tests pass. **E4** R2 double reviewer **0 finding compatibility (R3 closing)**: 9 fixes, all symptomatic + 0 new bugs introduced. **G2** Phase G2 3 documents plus CHANGELOG_99 transition period warning (J fix is ​​found to block reply_message dispatch design flaws across the board, new plan `mcp-tool-simplify-20260514` completely deletes reply_message + wait_reply + check_reply + J fix unified send_message + adapter dispatch mental model, 10→7 tool). typecheck + build + 259 test all passed. |
| [CHANGELOG_100.md](CHANGELOG_100.md) | mcp protocol major simplification 10→7 tool + wire format dual anchor point + document de-redundancy (plan `mcp-tool-simplify-20260514`): delete `reply_message` / `wait_reply` / `check_reply` three tool + universal-message-watcher J fix (reply short circuit) + wait-reply nudge mechanism. Unify `send_message` as the only entry for sending messages, and use `send_message + reply_to_message_id` for all replies. The same dispatch as ordinary send_message is automatically injected into the receiver SDK conversation flow (**The mental model changes from "lead actively wait_reply / check_reply pulls reply" to "lead receives the reply as a normal user message"**). Wire format is upgraded to three-segment double anchor point `[from <name> @ <adapter>][msg <id>][sid <senderSessionId>]`, spawn handler injects lead context block (Lead session_id / Team id / displayName + send_message usage), double-layer redundancy to prevent protocol drift. Renderer side message-row.tsx adds hand-off context disclosure (folded by default) + chip to display senderSessionId 8-char hash. R2 dual heterogeneous confrontation reviewer mined 6 MED / 5 LOW / 3 INFO finding in total, and fixed 3 three-state rulings must-fix: codex MED-1 displayName unescaped `]` destroyed wire prefix (new `sanitizeWireFieldName` helper + spawn/buildWireBody double sanitize)/codex MED-2 spawn ensureByName advanced after createSession failed and left active empty team (teamCreatedNow flag + hardDelete cleanup after verifying again)/Both parties’ consensus tools.test.ts adds cross-team `reply_to_message_id` reject test coverage (3 cases). The additional documents were greatly removed from redundancy and all 5 categories were cut based on user feedback (CHANGELOG/Phase/K2/start_next_session historical codename + old protocol comparison narrative + obsolete mode statement + future assumptions + duplicate content), 4 agent-deck documents + user CLAUDE.md, a total of 29 lines were reduced. Typecheck is all green + 103/107 test pass (4 fail is a known issue of Electron binding and has nothing to do with this phase). |
| [CHANGELOG_101.md](CHANGELOG_101.md) | REVIEW_33 9 HIGH fixes (plan `review-33-high-fix-20260513`): 8 atomic commit + merge main. **H1** archive_plan verify+checkout base_branch before ff-merge (the old implementation is on the caller's current HEAD ff merges the wrong branch)/ **H2** reject abandoned + unknown status (revised at the same time as main `438a613` Phase A4 R1 deep review, converges the main wording when merging)/ **H4** team-lifecycle-scheduler.scan() two-stage collection first and then batching archive (before repairing, iterate while archive changes archived_at to non-NULL → shrink list active → offset jump error → measured 500 all ghosts and miss 200 scans) / **H6** sessions.ts handOffSpawn transparent transmission codexSandbox / claudeCodeSandbox (before repairing, user hand-off implicit downgrade after cutting the sandbox) / **H7** HandOffPreviewDialog ref guard + main dedupHandOff inflight Map double gate (multiple sonnet IPC + multiple SDK sub-processes after double-clicking before repair) / **H8** delete settings.ts:209-212 + _helpers.ts:147 parseAutoApproveTeammateMode orphan IPC validation (fields after CHANGELOG_56 have been moved to settingsStore for direct reading, and the IPC path remains 33 lines of dead code) / **H9** archive-plan-impl post-ff-merge 9 try/catch plus `[post-ff-merge:<phase>]` prefix (before repair, ff fails after caller gets generic error, does not know main HEAD has moved → simple retry hits "branch already merged")/ **H10** archive_plan + hand-off-session, plus worktreePath exists pre-check (after repair before worktree has been deleted) cwd invalidates spawn and starts SDK ENOENT). **H3 Obsolete**: main commit `7639b23` deletes wait_reply/check_reply/reply_message three tools, the target file wait.ts does not exist. merge commit `ef11087` resolves the conflict between archive-plan-impl.ts + archive-plan.test.ts where both sides have changed (H2 wording converges to main, H1 verify+checkout and main changes are orthogonal and automatically retained). typecheck double-ended passed + a full set of vitest 465/465 passed (including 23 H1+H2+H9+H10 archive-plan cases + 6 H4 scheduler cases + 14 H6+H7 sessions cases + 2 H10 hand-off-session cases). **Known**: To run vitest on worktree, you need to first link the main warehouse electron binding (path.txt + dist symlink); hand-off-session.test.ts makeDeps fallback modification makes the `.claude/worktrees/` path exist by default, and `state.missingWorktree=true` is explicitly controlled. |
| [CHANGELOG_102.md](CHANGELOG_102.md) | REVIEW_35 deep-review-and-refactor-20260514 plan: 12 file hotspot synthesis, 3 rounds of heterogeneous confrontation (4 batch × 3 files, 8 reviewer concurrency, across R1/Wave 2/R2 three rounds), a total of 49+ findings, 3 commits, and 36 real problem fixes (9 HIGH + 20 MED + 7 LOW). Wave 1 (cd1af8c) fixes backpressure deadlock + dispatcher cache leak + summarizer codex timeout + N+1 lookup + multiple emit leaks + dead code deletion, etc. 15 real problems; Wave 2 (d2c9e68) fixes useImageAttachments 30MB race + ComposerSdk attachments capability gating + IdleDetector dispose flag + busy double send race + main bootstrap Isolation, etc. 12 real problems; R2 (4a85f68) dug 5 new HIGH (including 2 fix-to-fix derived races + 1 incomplete fix): dispatcher cache half solution (spawn_session/CLI still leaks emit → change dispatcher defense plan) / summarizer in-flight rename FK race / fullBase64Ref orphan leak (React 18 batching Closure trap) / IPC capability last-line defense / bootstrap fatal dialog. typecheck 0 errors + 467/526 vitest passed. Follow-up 18 MED + 25+ LOW/INFO covering architectural changes (starvation guard fairness / pty listener timing / chokidar reuse / multi-file 500 LOC split / test infrastructure completion). |
| [CHANGELOG_103.md](CHANGELOG_103.md) | REVIEW_35 follow-up priority 1+2 implementation (plan `review-35-followup-p1-p2-20260514`): 1 commit 9 files +390/-36 = 5 real problem fixes + 4 test blind area completion. **P1 real problem**: C-M5 pty sendMessage emit/write sequence + try/catch catch-up / C-M1 pty-bridge listener register after spawn + earlyDataBuffer/earlyExit cache (codex measured misses=6/20 seconds to exit) / rF R2-2 SIGTERM/SIGKILL kill() throw throw catch-up (clear fileWatcher + emit session-end + sessions.delete) / rH R2-M3 useImageAttachments mountedRef + generationRef to prevent unmount/clear race / rH R2-M4 HookServer EADDRINUSE fail-loud (dialog + app.exit(1) does not allow half-start). **P2 test completion**: A1 R2 backpressure stateful test 5 case replacement regex grep (upgrade technical debt) / A2 R2 findActiveMembershipIn 4 case + listActiveMembers JOIN 1 case / rE R2 #2 IdleDetector dispose flag 2 case / rE R2 #3 TaskOutput READ_ONLY 2 case. typecheck 0 errors + 475/539 vitest passed (+8 new additions). **Follow-up leaves independent plan**: A3 starvation guard fair queuing (architectural changes) + priority 3 split guardrail (4 files ≤500 LOC, 1 file 1 plan) + priority 4 cosmetic (GIF dataUrl / double read / COMPRESS card UI, etc.). |
| [CHANGELOG_104.md](CHANGELOG_104.md) | summarizer.ts 625 LOC split 3 sub-files (plan `summarizer-split-20260514`, CHANGELOG_103 priority 3 split guardrail item 2 implemented): **Pure physical split** Zero business behavior changes. `summarizer/{event-formatter.ts:110, llm-runners.ts:243, index.ts:287}` Total 640 LOC, max 287 ≤ 500 ✅. Gear 1 (module-level pure function) - event-formatter contains formatEventsForPrompt + summarizeToolInput + truncate + localStatsFallback / llm-runners contains summariseViaLlm + summarizeSessionForHandOff two independent oneshot helper / index contains Summarizer class body + summarizer singleton + re-export facade (external 4 callers `from '@main/session/summarizer'` zero changes, TS module resolution automatically fallback to index.ts). All REVIEW_35 comments (HIGH-B1 / MED-B1 / MED-B2 / R2 HIGH-B1 / LOW-B3, a total of 5 reinforcements) will be migrated unchanged. typecheck double-ended 0 wrong + hand-off.test.ts 5/5 all passed. |
| [CHANGELOG_105.md](CHANGELOG_105.md) | Whole warehouse ≤ 500 LOC Guardrail batch closing (plan `universal-message-watcher-split-20260514`, CHANGELOG_103 follow-up priority 3 split guardrails are all completed): Scan the entire warehouse 11 over-standard files, 11 independent commit closing = **8 files physically split** (3 src + 5 Test) + **1 bug fix** (TeamHub memberCount=0 pre-existing: listAgentDeckTeams IPC handler does not return the members field, handler adds N+1 listActiveMembers + type signature upgrade, TeamHub’s existing logic has zero changes and can be directly worked + lastEventAt is also considered correct) + **2 added to the "immovable file protection list"** (sdk-bridge/index.ts 511 LOC only exceeds 11 lines + CHANGELOG_52+85 have been deeply split in 4 rounds; mcp tools.test.ts 1227 LOC but vitest vi.mock file-level hoisting restrictions + ~425 LOC setup cannot be shared across files → any sub-test ≥ 500). 8 true split single files LOC all ≤ 500 ✅, maximum file: universal-message-watcher/index.ts 346 (split 4) / preload/api/misc.ts 154 (split 5) / ComposerSdk.tsx 422 (extract ErrorBanner+SelectRow generic) / test single file maximum hand-off-session.handler-cwd-generic.test.ts 475. Test split into two modes: deps inject (archive-plan/hand-off-session) pumps _setup.ts shared, vi.mock mode (sdk-bridge/pty-bridge/agent-deck-repos/task-manager-tools) per sub-test independent hoisted vi.mock + shareable pure helper (class/factory/mutable state) pumps _setup.ts. typecheck double-ended 0 error + 198/198 vitest verification passed (agent-deck-repos 38 probe-skipped binding ABI mismatch has nothing to do with splitting). **Known pitfalls**: worktree lacks electron binary and reports an error when running vitest (same as CHANGELOG_101/104); preload composite project only adds api/**/*.ts without adding index.ts (d.ts type-only import delayed parsing); vitest vi.mock file-level hoisting is the root cause of mcp tools.test.ts protection list; _setup.ts automatically skips test starting with an underscore glob. |
| [CHANGELOG_106.md](CHANGELOG_106.md) | baton closure + cwd invalidation UX double repair (two things packaged with the same session): **A. baton teammate shutdown** (archive_plan / hand_off_session default shutdown caller=lead's team other active teammates to avoid orphans, the new keep_teammates schema field allows skipping + ok return.teammatesShutdown three-state field; new helper `shutdown-teammates-on-baton.ts` 106 LOC three deps inject seam serial close + failure tolerance; **timing** first helper and then archive caller cannot be reversed, otherwise archiveTeamsIfOrphaned first put team auto-archive, helper listActiveMembers JOIN sessions archived_at IS NULL and caller cannot be seen). **B. jsonl missing emit repair** (user reported "The historical context was lost after session resume" "Do you have no historical session information, here is" the root cause - recoverer.ts jsonl missing fallback path only console.warn does not emit UI prompt, inconsistent with the symmetric cwdFellBack=true path implementation; add the same emit info message to tell the user "CLI internal conversation history has been lost, Claude this newly started CLI I don’t know the previous situation. If you want to continue the previous topic, please tell it the background again in the next message." The copywriting distinguishes "**has** been lost" vs. the cwdFellBack path "**will** be lost". The difference is one word so that the test filter can be accurately anchored). Added 17 cases + 6 original case injection noopShutdown to prevent stderr collision and DB not init noise; typecheck 0 errors + 148/148 vitest all passed. **Known pitfalls**: When the caller explicitly passes team_name to let the new session take over the lead, it should also pass keep_teammates: true, otherwise the original reviewer will be shut down and no one will talk to the new session; the helper timing reversal will make the semantics confusing; the test seam must be actively injected, otherwise the real helper will hit the DB by default after integration. **Method 2 "fallback automatic LLM summary injection" creates a new independent plan** (user decision). |
| [CHANGELOG_107.md](CHANGELOG_107.md) | CHANGELOG_106 "Method 2" is implemented (independent plan `llm-handoff-summary-fallback-20260514`): let jsonl missing / cwdFellBack=true two fallback paths automatically adjust LLM (sonnet) summary application DB events history before fresh CLI → prepend to the first prompt and let Claude Know the previous situation, allowing users to feel "can continue the conversation" (replacing CHANGELOG_106's "Please fill in the background" manually). **9 step / 6 commit** Sequence 1→2→5→3→4→6+7 (5 mentioned before 3 to avoid occupying true and changed later): Step 1 (`c2c564d`) recoverer plus SummariseFnThunk type + ctor 6th parameter + facade `protected summariseForHandOff` double-layer wrapper / Step 2 (`7962a11`) pump helper `recoverer-helpers.ts` ~190 LOC, 5 types of failReason (settings-off / no-events / summary-empty / over-length / thunk-throw), never throw errors, five equal sign block splicing, no direct emit to decouple the caller / Step 5 (`bd9863b`) `settings.autoSummariseOnFallback: boolean` field full stack (schema + DEFAULT + UI Toggle Contains sonnet billing warning in ExperimentalSection) - do not move ipc/settings.ts Same as enableSound and other pure boolean / Step 3 (`c8e123d`) jsonl missing path adjust helper before createThunk, `result.used=true` emit "LLM summary has been injected, Claude should be able to continue" / `false` emit original CHANGELOG_106 Copywriting + vi.mock event-repo + settings-store (complete Step 7 noopSummarise injection at the same time) / Step 4 (`3f4c8a3`) cwdFellBack outer emit Delete the word "will be lost" and keep cwd switch fact + inner Add cwdFellBack sub-branch according to result.used Copywriting branch / Step 6+7 (`00adbd0`) Add 4 New integration case (jsonl summary success / settings off / thunk throw / cwdFellBack summary success) + TestBridge summariseThrow field. **Core invariants**: Summary failed/DB has no history/Summary is too long/settings off → return CHANGELOG_106 The emit path does not block the main path/is not persisted and is recalculated every time fallback/only the fallback path triggers normal resume. The path does not adjust the summary. typecheck 0 errors + sdk-bridge entire directory 37/37 passed. **Known pitfalls**: mockImplementation hits settingsStore.get. Generic signature requires `as never` cast / cwd input parameter. When cwdFellBack=true, pass rec.cwd instead of effectiveCwd so that the summary retains the semantics of "which worktree it originally was" / outer. The word "will be lost" must be moved to inner, otherwise it will be inconsistent and misleading. |
| [CHANGELOG_108.md](CHANGELOG_108.md) | REVIEW_36 R1+R2 sandbox + resume + hand-off real effectiveness + optimized space depth review × fix closure (user actively "whether the sandbox configuration resume / hand-off really takes effect"): Heterogeneous confrontation (1 reviewer-claude full scope + 2 reviewer-codex split batch, codex first hit sandbox EPERM failed → split batch + prompt Hard constraint retry) Mining R1 6 real problems + R2 5 real problems (including 1 R2 HIGH-A **fix-to-fix bug** hand_off_session(team_name=x) baton turns off new spawn session). **R1 fix**: 3 HIGH independently proposed by both/multiple parties (HIGH-1 recoverer fallback claudeCodeSandbox **silent downgrade safety critical** / HIGH-2 hand_off_session schema missing sandbox field / HIGH-3 external worktree allowWrite not covered) + 3 LOW (spawn single reverse check / annotation update / delete dead code). **R2 fix**: R2 HIGH-A baton excludeSessionIds + R2 HIGH-B cwd fallback write permission emit warn transparency + R2 MED-A restart transparent transmission permissionMode + R2 MED-B restart solo mark in advance set + R2 MED-C external worktree mainRepo plan file sandbox extraAllowWrite transparent transmission chain architectural reconstruction (buildSandboxOptions / createSession / spawn / hand-off / schema five-layer transparent transmission + plan-driven external worktree automatically adds mainRepo). **User mid-stage feedback**: user CLAUDE.md §Step 4 ff-merge target branch should be base_branch instead of directly main → synchronously repair the document + archive_plan tool schema default value (caller > frontmatter > main priority). **INFO follow-up**: HIGH-3 comment + schema cwd describe + codex restartWithCodexSandbox rename defense (anti-SDK upgrade silently fail). A total of 13 files + 8 new regression cases + a full set of 503 tests / typecheck passed both ends. |
| [CHANGELOG_109.md](CHANGELOG_109.md) | Plan `model-wiring-and-handoff-20260514`: There is no closed loop point at link 3 of the closed-loop model. **A**: spawn_session solves the agent body frontmatter `model` field and transparently transmits it to SDK + persistence sessionRepo (v018 migration), so that reviewer-claude.md `model: opus` and so on can really take effect (dead fields before modification); the codex SDK does not accept per-thread model override, only persistence allows the UI to be displayed + console.warn prompts the user to change toml. **B**: settings add `summaryModel` / `handOffModel` two fields + SummarySection UI adds input box (draft / focus / blur mode), priority `settings explicit value > ANTHROPIC_DEFAULT_*_MODEL env > ANTHROPIC_MODEL > alias , empty = continue to use the existing env link (zero migration cost). **C**: Added `adapters/codex-cli/handoff-runner.ts` `summariseCodexSessionForHandOff` (4-section template + reasoningEffort='medium' + 60s timeout), `ipc/sessions.ts:70` is distributed by session.agentId - codex session is finally issued by codex itself (borrowed from claude before repair) SDK + sonnet are inconsistent). A total of 14 files were modified + 3 new files + 1 new regression case + a full set of 504 tests / typecheck passed both ends. Details [`plans/model-wiring-and-handoff-20260514.md`](../plans/model-wiring-and-handoff-20260514.md). |
| [CHANGELOG_110.md](CHANGELOG_110.md) | REVIEW_37 R1+R2+R3 macro-reconstruction opportunity three rounds of heterogeneous confrontation × P1+P2+P3 third gear landing (plan deep-review-and-refactor-r37-20260515). reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 xhigh teammate Three rounds of full scope main process reconstruction opportunity scanning Total mining 8 ✅ HIGH (R1 7 + R2 1) + 10 ✅ MED (R1 8 + R2 2) + 6 LOW/INFO Real problem + 3 R2 ❌ pre-existing not returned R37 (codex/claude If the architecture is symmetrical, a special plan will be reserved) + 3 calibration skipped (the plan decision will not be implemented + R3, both parties can independently verify that the reasons are tenable). **Phase 1 P1 trivial high ROI** (4 commits): D withMcpGuard + preload subscribe / G codex-instance-pool / B recoverer message builder / Phase 2 trivial (omitUndefined / comment fragment / placeholder copywriting). **Phase 2 P2 medium transformation** (6 commits): F shared mock factory (5 categories) / H 4 LLM oneshot runner pumping helper (race/clean/prompt/SDK 1 SSOT) / I summariseEvents dispatch decentralized adapter / E codex sdk-bridge demolition input-pack/session-finalize/restart-controller (543→409 over 500 guardrail) / M runBatonCleanup helper (archive_plan/hand_off_session shared ~80 LOC) /N message-delivery-state SSOT + SQL backoff fragment derived. **R2 fix** (2 commit): R2 HIGH-1 spawn handler batonRole opts + hand-off-session transparent transmission 'lead' (fix hand_off_session(team_name=X) baton 0-lead auto-archive) + R2 MED-1 codex handoff slice unlimited length + R2 MED-2 codex timeout race scope package SDK init. **Phase 4 P3 scattered closure** (4 commit): C cwd resolver / C recoverer.emitFallbackMessage / L 7 result type satisfies (blocked 3 real type drifts) / J shared / jsdoc contract vs policy boundary. **3 calibration skipped**: Step 4.2 single-flight helper (only 2 places for true single flight + sub-class state ownership transformation high risk) / Step 4.4 preload misc split (misc.ts 154 LOC untouched 500 LOC guardrail + invoke<T>() Step 1.1 rejected) / Step 4.6 IPC errorMode wrapper (throw vs result two categories have been reasonably divided + handler return type self-expression + transformation without bugs). **R3 closing**: reviewer-claude (sid `cb9e3722`) 0 HIGH/0 MED + 2 INFO non-blocking / reviewer-codex (sid `f366fbde`) 0 finding + command verification passed → both parties are consistent ✅ Can be combined. A total of 16 commits / zero behavior changes / typecheck double-ended + vitest 524/524 all passed. Details [REVIEW_37.md](../reviews/REVIEW_37.md) + [`plans/deep-review-and-refactor-r37-20260515.md`](../plans/deep-review-and-refactor-r37-20260515.md). |
| [CHANGELOG_111.md](CHANGELOG_111.md) | Plan `worktree-stale-base-bug-20260515`: Claude Code CLI v2.1.112 EnterWorktree builtin **stale base bug** R1 heterogeneous confrontation × **document-only** closed (details [REVIEW_38.md](../reviews/REVIEW_38.md)). R37 archive_plan immediately hand-offs after closing. Actual test hits bug in preparation phase: adjust `EnterWorktree(name: "X")` to innovate worktree HEAD = `origin/<default-branch>` instead of local HEAD (actual test worktree HEAD = f253794 is twenty commits behind local main 91c4568). Lead independent grep + minified bundle decompilation, 100% empirical positioning root cause (cli.js byte ≈ 11794070, default to `git worktree add --no-track -B <branch> <path> origin/<default-branch>`; CLI tool description explicit promise `based on HEAD` and serious inconsistency with the implementation = contract vs implementation contradiction bug) spawn R1 heterogeneous confrontation review (reviewer-claude Opus 4.7 focus code + minified bundle verification + reviewer-codex gpt-5.5 xhigh focus git plumbing + man page + reflog evidence) Both parties agree ✅ 6 HIGH (including reflog `branch: Created from origin/main` to strengthen the evidence) + 3 ✅ MED/LOW + 1 ❌ (fromHead hidden parameters are unreliable) + 1 ❓ (bare/submodule not verified). **Lead three-state ruling document-only** (plan § design decision 5 threshold "fix complex stay independent plan"; hand_off_session / archive_plan are not in the EnterWorktree call chain; application has no hook point wrap CLI builtin; non-evidence conflicts do not go through the rebuttal round). **Workaround**: (b) Bash `git worktree add -b ... <path>` The implicit HEAD main path has been confirmed by the plan worktree itself + (a) Self-check + reset to get the bottom of it; user CLAUDE.md §Step 1 typed warning is implemented synchronously. **Upstream**: Anthropics/claude-code GitHub issue should be reported as a bug (see REVIEW_38 for reproduction recipe + suggested fix + template). **Long-term fix proposal**: Add `enter_worktree_safe(plan_id)` mcp tool to leave independent plan anchor points. **No code changes** (no need to run typecheck / vitest). |
| [CHANGELOG_112.md](CHANGELOG_112.md) | hand_off_session does not write baton spawn-link + UI ↳ teammate badge bug plan 1 fix (plan `hand-off-mcp-teammate-bug-20260515`). Users actually tested "'hand off mcp' will still hang up as teammate" + added "**There is no team logo, but there is a hierarchical relationship on the real-time session page**". Heterogeneous confrontation R1+R1.5+R2 double reviewer same pair cross-round reuse: **root cause** spawn.ts:262 setSpawnLink still writes spawnedBy=callerSid + SessionList Phase C (CHANGELOG_77) in the batonMode=true path. Render according to spawnedBy tree grouping ↳ teammate badge, **completely irrelevant to team membership**. **HIGH time window race**: The real window comes from spawn subsequent child upsert(typical recordCreatedPermissionMode → manager notifyTeamMembershipChanged emit session-upserted) before the runBatonCleanup archive caller completes — fires even if the archive is 100% successful. **R1+R1.5 The two parties exchange positions in the rebuttal round** (claude pushes 1 → change 5 / codex pushes 5 → change 1) mutually irreconcilable, the user decides to follow plan 1. **fix**: spawn.ts:260-263 Add `&& !opts?.batonMode` jump setSpawnLink shunt + ok return spawnDepth fallback synchronization + 2 new gatekeeping test case (batonMode=true jump + default gatekeeping) + R37 R2 HIGH-1 Add 1 line of assertion to the existing test (LOW-1/INFO-1 both sides are independent ✅ Gatekeeping "batonMode + explicit team_name" combination) + hand-off-session.ts:306 stale comment cleaning (INFO-4 codex unilateral). **HIGH independent follow-up**: R37 caller `024289d4` is still active according to actual measurement and is independent root cause (archive failure warn-only / old archive_plan / unarchiveOnUserSend), which is orthogonal to this fix. The user decides to build a separate plan `archive-failure-ux-upthrow-20260515` to follow up. Typecheck double-ended + tools.test.ts 46/46 + spawn-guards.test.ts 12/12 passed all. Details [REVIEW_39.md](../reviews/REVIEW_39.md). |
| [CHANGELOG_113.md](CHANGELOG_113.md) | REVIEW_40 R1+R2+R3 codex/claude adapter architectural symmetry depth review × Phase 2+3 fix implemented (plan codex-claude-adapter-symmetry-20260515,REVIEW_37 R2 ❌ pre-existing triggers this plan). reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 xhigh teammate three rounds of focused single-topic review 13 files + 6 sub-focus (sandbox field naming / restart method signature / resume defense / event emit timing / ensureXxx pool mode / SDK lifecycle boundary). **Key reversal**: R37 R2 ❌ pre-existing HIGH-2/HIGH-3 "naming/signature inconsistency" claim **not true** (reviewer-claude grep 36/38 + 32 empirical rules have been unified, enum value inconsistency is an inherent difference in the SDK); only MED-3 "codex resume defense and claude asymmetry" is a real problem. R1 mined a total of 9 real questions (2 HIGH + 6 MED + 1 LOW), including 1 rebuttal round. Reviewer-codex's unilateral HIGH was partially supported by reviewer-claude and was downgraded to MED. **Phase 2 P2 R1 fix** (6 commit / in ascending order of risk): MED-C jsdoc / MED-B delete codex currentSandboxMode three-layer image (R37 P1 G same model precedent) / HIGH-A single-flight + MED-A emit upserted (literal image claude REVIEW_36 R2 MED-B) / HIGH-B create new codex SessionRecoverer ~280 LOC + MED-E jsonl pre-check + LOW-A cwdExists / MED-D thread-loop case 3 + resume await thread.started(future-proof) / MED-F extraAllowWrite jsdoc reflect reality(fictional claim deleted). **Phase 3 R2 fix** (1 commit): R2-1 sessions cleanup (reviewer-codex HIGH unilateral + lead empirical c9c94d7 introduction) + R2-2 cwdFellBack does not force fallback + emit text correction (reviewer-claude MED-G unilateral, fix codex jsonl independent of cwd self-contradictory) + R2-3 30s timeout supplement emit info(reviewer-claude LOW-B). **Phase 4 polish is skipped** (R37 R3 INFO #1/#2 trigger condition is not met). **Phase 5 R3 fix** (1 commit): R3-1 late earlyErr cleanup (reviewer-codex unilateral R2-1 vulnerability, short-circuit cleanup after 30s timeout). **3 follow-up to stay independent plan**: cross-adapter parity (recoverer waiter Promise<string>)/ double rename owner cleanup (idempotent non-blocking)/ codex sdk-bridge unit tests (across HIGH-A/B/MED-D/E). A total of 8 commits / zero change in behavior / typecheck double-ended + vitest 524/524 passed / completed in the same session without hand-off trigger (R37 lesson avoidance successful). Details [REVIEW_40.md](../reviews/REVIEW_40.md) + [`plans/codex-claude-adapter-symmetry-20260515.md`](../plans/codex-claude-adapter-symmetry-20260515.md). |
| [CHANGELOG_114.md](CHANGELOG_114.md) | `hand_off_session` mcp tool adds `archive_caller` field to allow caller optional opt-out archiving (plan `hand-off-mcp-archive-opt-20260515`). Before the repair, hand-off forced the archive caller (baton's default semantics of one-way handover), which was inflexible; the new `archive_caller: false` allows the caller to start a new session and do things in parallel while still being active - typical use cases: lead starts multiple follow-up hand-offs and continues to coordinate the progress/debug tool to actually test a plan, but the caller still needs to observe. The schema defaults to `true` to maintain backward compatibility and is named symmetrically with the `keep_teammates` field (the default action of baton can be explicitly opt-out). **Change 4 files**: schemas.ts(HAND_OFF_SESSION_SCHEMA added field + HandOffSessionResult.archived jsdoc extended 'skipped' multi-source)/ hand-off-session.ts(transparent args.archive_caller !== false to runBatonCleanup)/baton-cleanup.ts(RunBatonCleanupInput added archiveCaller?: boolean optional ensures zero changes to the archive_plan caller + phase 2 entry short road sign archived='skipped' zero side effects)/ test 5 case (baton-cleanup plus case 11+12 / hand-off-session.handler-deny-happy new archive_caller opt-out describe plus 3 cases including orthogonal two fields are enabled at the same time). trivial schema plus + handler transparent transmission + helper branch, no need to go through multiple rounds of confrontation review. typecheck double-ended + vitest full set 39 files / 531 tests 0 regression. |
| [CHANGELOG_115.md](CHANGELOG_115.md) | Plan `codex-sdk-bridge-tests-20260515`: codex sdk-bridge single test suite + LOW double rename owner cleanup (REVIEW_40 R2 reviewer-claude INFO-T horizontal technical debt + reviewer-codex LOW smooth cleanup). **P1**: Extract codex `__tests__/sdk-bridge/_setup.ts` (mirroring claude TestBridge extend pattern, 3 protected method override: createSession / cwdExists / codexResumeJsonlExists) + `sdk-bridge.recovery.test.ts` 15 case gatekeeping HIGH-B (recoverAndSend) + MED-E (jsonl pre-check) + LOW-A (cwd heuristic fallback) + R2-2 (cwdFellBack preserves conversation history) + solo / placeholder 5s dedup / MAX_LENGTH / archived → unarchive / codexSandbox+model transparent transmission (HIGH-1 equivalent). **P2**: `sdk-bridge.consume-fork.test.ts` 9 case gatekeeping ThreadLoop case 1/2/**3 (MED-D core fix)** / intentionallyClosed catch silent exit / RestartController HIGH-A solo / MED-A emit `session-upserted` prefix + rollback dual path / boundary. **P3**: Delete `restart-controller.ts:113-128` post-rename defense block + unused sessionManager import (thread-loop case 3 has owner rename three-piece set, restart-controller is adjusted here to be idempotent no-op but console.warn is typed one more time to mislead log readers, delete it to focus SSOT). A total of 3 commits / zero behavior changes / typecheck double-ended + codex/__tests__/ 48/48 all passed (24 translate + 15 recovery + 9 consume-fork). **Uncovered follow-up**: R2-1 sessions cleanup (resume earlyErrCb path) + R3-1 late earlyErr cleanup (after 30s timeout) - need to createSession + fake codex SDK + control thread.runStreamed to throw an error, and the workload of testing infra is large. **Power cutoff**: vi.mock 6 entry modules (codex-binary/image-uploads/paths/settings-store/mcp-injector/instance-pool) bypass the 'failed to install' of the electron module in the vitest node environment. Details [`plans/codex-sdk-bridge-tests-20260515.md`](../plans/codex-sdk-bridge-tests-20260515.md). |
| [CHANGELOG_116.md](CHANGELOG_116.md) | CHANGELOG_115 §End of Phase 2 R2-1 + R3-1 follow-up gatekeeping completion — `sdk-bridge/index.ts:337-388` createSession resume path earlyErrCb wrapper two repair points (commit `6e0eb37` R2-1 + commit `726af8d` R3-1) Upgrade from "no unit test gatekeeping / only bugs found in actual production testing" to "3 case unit test end-to-end gatekeeping". **Key differences in testing infra**: earlyErrCb wrapper must actually run createSession inside the true createSession closure (the shortcut of _setup.ts TestCodexBridge override createSession cannot be used), so this file is directly used naked with `new CodexSdkBridge` + `vi.mock('@main/adapters/codex-cli/sdk-loader', () => ({ loadCodexSdk: vi.fn() }))` injects the fake `Codex` class, whose `resumeThread/startThread` returns `ControlledThread` (test-only `vi.fn` returns pending Promise, and externally controls the fulfill timing through `rejectStreamed / resolveStreamed`). **3 case**: (1) R2-1 path 1 — runStreamed throws an error immediately → cleanup + reject + finished:error once (also verify that 30s warn does not hit by mistake + R3-1 late err does not touch by mistake) / (2) R3-1 path 2 — `vi.useFakeTimers()` pushes for 30s to trigger fallback resolve → then reject runStreamed simulates late earlyErr → cleanup + emit late err msg + finished:error once + createPromise does not throw / (3) R2-1 combined with recoverer - sendMessage after cleanup and go to `recoverer.recoverAndSend` Self-healing (sessions miss + sdkClaim release → emit placeholder + sessionRepo.get) end-to-end verification. **fake codex SDK does not extract _setup.ts** (only used by this follow-up to avoid contamination of P1/P2) + **bare loadCodexSdk mock does not reuse `makeBareSdkLoaderMock()`** (factory return `loadSdk` is the claude side export name, codex side `loadCodexSdk` is written inline to avoid mismatch). **No confrontation review**: Pure test infrastructure plus + fix code itself has been reviewed by REVIEW_40 three states, and the correctness of the behavior is verified by vitest practice (51/51 passed). typecheck double-ended 0 errors + codex/__tests__/ 51/51 all passed (24 translate + 15 recovery + 9 consume-fork + **3 early-err-cleanup**). |
| [CHANGELOG_117.md](CHANGELOG_117.md) | cross-adapter-parity-20260515 plan closing (REVIEW_40 R1 reviewer-codex MED-F + R2 MED parity limit dual follow-up independent plan) - `sessions.extra_allow_write` persistent full link + recoverer waiter Promise<string> signature. Phase A 6 commit:migration v019 + session-repo Row/crud/rename column expansion 17→19 (fixing v018 model missing latent bug in rename.ts) + SessionRecord field + claude session-finalize/createSession/recoverer full-link persistence spawn → recover + codex end same style (parity-symmetric DB persistence / SDK runtime does not consume it / same semantics as model) + adapters/types.ts jsdoc reflects reality + 3 regression case. Phase B 1 commit: claude/codex recoverer.recoverAndSend signature change Promise<string>(return final session id)+ 1 waiter regression case lock fallback rename after the waiter takes newRealId without collision not found. **Phase C heterogeneous confrontation review fix** (details REVIEW_41): reviewer-claude sandbox lock cwd failed (6 items *unverified* all ❌ not included) + reviewer-codex gpt-5.5 xhigh 4 finding all lead grep on-site verification → 3 ✅ MED real problem (MED-1 codex CodexCliAdapterImpl.createSession is not connected at all) extraAllowWrite field is broken / MED-2 claude/codex recoverer resume path implicit fork case waiter is still not found / MED-3 claude restart-controller cold restart path without extraAllowWrite)+ 1 ❓ LOW (follow-up within the trust boundary). MED-1/2/3 fix all 1 commit implemented + 1 forkOnResumeOverride seam regression case. Total 7 commits / 16 files / +693/-54 LOC / typecheck double-ended + vitest 531/595 all passed (64 skipped is better-sqlite3 ABI environment problem has nothing to do with this plan). **Number Note**: During the closure of this plan, main has been preempted by hand-off-mcp-archive-opt(114) + codex-sdk-bridge-tests(115) + sdk-bridge-tests follow-up(116), rebase and then rename 114→117. Details [REVIEW_41.md](../reviews/REVIEW_41.md) + [`plans/cross-adapter-parity-20260515.md`](../plans/cross-adapter-parity-20260515.md). |
| [CHANGELOG_118.md](CHANGELOG_118.md) | archive-failure-ux-upthrow-20260515 plan closing (REVIEW_39 R1 HIGH-3 R37 caller is still active independent orthogonal root cause follow-up) - caller archive failed UX upthrow channel, triggered from three places: mcp baton-cleanup(archive_plan / hand_off_session) + K3 SessionHandOffSpawn emit `caller-archive-failed` event → main bootstrap listener bridges macOS system notifications + IPC throw renderer. **Before revision**: The archive='failed' field is transparently transmitted to mcp tool ok return string. The field has no consumer (grep verification renderer / IPC handler / event-bus are all 0), and the user cannot sense it. **Phase 2 implementation commit 8b9a96e**: event-bus.ts EventMap plus 'caller-archive-failed' event(sessionId / toolName / reason / reasonKind)+ ipc-channels.ts IpcEvent.CallerArchiveFailed + baton-cleanup.ts two-way emit(row-missing probe short circuit / archive-throw catch)+ sessions-hand-off-helper.ts extract archiveSourceSessionWithEmit pure function helper (avoid sessions.test.ts pulling Electron import chain) + sessions.ts K3 adjustment helper + EventMap satisfies compile-time gatekeeping + main/index.ts bootstrap listener notifyUser + safeSend + gatekeeping test 8 case. **R2 heterogeneous confrontation fix commit ccd6d93**: HIGH-1 Consensus of both parties listener throw crashes through emit caller (node:events actual test ironclad evidence)→ top try/catch + change error comment / MED-1 codex K3 row-missing impossible conclusion error (setArchived no-op empirical evidence) → archiveSourceSessionWithEmit add getSession probe + reasonKind union / MED-1 claude SessionHandOffSpawn IPC internal name exposed → TOOL_DISPLAY_NAME mapping table + INFO easily repaired case 9/10 emit not gatekeeper. **R3 fix commit 67365e7**: reviewer-codex MED-1 listener notifyUser/safeSend is the same as the try block serial (notifyUser throws an error and swallows safeSend) → split the dual channel independent try/catch. **Leave follow-up plan**: K3 helper probe-then-archive TOCTOU race (R3 mutual consensus is not introduced in R2, the same model as mcp baton-cleanup, scope ultrabook plan remains archive-toctou-fix-<date>) / LOW probe-throw reasonKind / 4 INFO polish + P2 enhancement (renderer toast UI + retry button). 3 commits / 8 files / +432/-60 LOC / typecheck double-ended + vitest 531/595 passed / `heterogeneous_dual_completed: true`. Details [REVIEW_42.md](../reviews/REVIEW_42.md) + [`plans/archive-failure-ux-upthrow-20260515.md`](../plans/archive-failure-ux-upthrow-20260515.md). |
| [CHANGELOG_119.md](CHANGELOG_119.md) | archive-toctou-fix-20260515 plan closing (REVIEW_42 § Known follow-up MED race window + LOW probe-throw + INFO TOOL_DISPLAY_NAME union three data layer coupling one-time closing) - K3/baton archive helper TOCTOU race window fix(setArchived `.changes === 1` throw `SessionRowMissingError` SSOT revision A, R1 consensus of both parties) + reasonKind 'probe-throw' value-added push full link (LOW probe-throw bug fix, there is no difference between DB exception retry and row true) + EventMap toolName + TOOL_DISPLAY_NAME `Record<CallerArchiveFailedToolName, string>` union narrow force complete coverage (add new emit Trigger point forgets to add mapping tsc compile time fail) + IPC SessionArchive/SessionUnarchive idempotent silent return true (the user's active operation of row is no longer equivalent, harmless and does not disturb) + adapters.ts AdapterSendMessage unarchiveOnUserSend into try/catch block shared attachments cleanup (after revising method A, unarchive throw must be cleaned up to avoid attachments leak). **Phase 2 implementation commit 3c27b98**: archive.ts new SessionRowMissingError class + setArchived throw / event-bus.ts EventMap union narrow / baton-cleanup.ts + sessions-hand-off-helper.ts probe try/catch demolition + archive instanceof distinction (R1 reviewer-codex MED-1 revision - before revision, catch-all mistakenly attributes setter no-op 'archive-throw' misleading UI retry) / ipc/sessions.ts SessionArchive/Unarchive idempotent silent + ipc/adapters.ts cleanup share / main/index.ts TOOL_DISPLAY_NAME narrow + listener three-file copy / gatekeeper test 11 files (8 prod + 3 test including archive.test.ts 7 case + baton-cleanup.test.ts Case 7 changes probe-throw + case 8b and adds SessionRowMissingError + sessions.test.ts 3 case). **R2 heterogeneous confrontation fix commit ddea608**: MED-1 reviewer-claude dead code cleanup (main/index.ts isRetryable + void workaround YAGNI violation → delete two paragraphs) / LOW-1 reviewer-codex shared/ipc-channels.ts comment union synchronization 'probe-throw' / LOW-2 reviewer-codex acceptable defer(mock setArchived throw collision vi.mock The hoisting trap cannot be cleanly repaired (P2 plan reorganization). **Leave follow-up**: LOW mock behavior alignment (P2 test infrastructure upgrade) + LOW IPC handler integration test (both sides defer) + LOW renderer bare await + recoverer error.message UX (P2 toast plan) + INFO instanceof dual discrimination retention (engineering aesthetics). Total 2 commits / 13 files / +467/-66 LOC / typecheck double-ended + vitest 577/641 all passed (64 skipped is a better-sqlite3 ABI environment issue) / `heterogeneous_dual_completed: true`. Details [REVIEW_43.md](../reviews/REVIEW_43.md) + [`plans/archive-toctou-fix-20260515.md`](../plans/archive-toctou-fix-20260515.md). |
| [CHANGELOG_120.md](CHANGELOG_120.md) | adapter-architecture-design-20260515 plan closure (REVIEW_40 P2 leaves architectural plan 3 design questions: P4 BaseAdapter / cross-adapter sandbox inheritance / scheduler naming) - **Only produces design decisions without moving src/code**, output `docs/adapter-architecture-rfc-20260515.md` + Chapter 3 follow-up decision record (no independent stub plan will be built, and if it is actually triggered in the future, a new implementation plan will be created to reference the RFC chapter). R1 dual external CLI (reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 xhigh) heterogeneous confrontation 9 finding (3 HIGH + 6 MED), R1.5 rebuttal Chapter 2 codex HIGH "Option D default relaxation" rebuttal established → rewrite Option D as string enum + Chapter 1 D l D2 typed registry binding Both sides independently proposed + Chapter 3 Scheduler scope definition (exclusion Summarizer/Watcher) + dual-type period settings convention. R2 revision +322/-98 After the user has completed all three chapters ✅ accept. **Chapter 1 sign-off**: Option D2 (split decision union + typed registry overload + buildCreateSessionOptions builder helper + exhaustive switch), trigger condition = parity-plan Phase A+B closing + coordinate extraAllowWrite field position. **Chapter 2 sign-off**: Option D rewrite (`inherit_sandbox: 'restrictions-only'` only safe direction mapping / `allow_unrestricted_mapping: true` escape hatch) + Option E rewrite (`warnings: string[]` field + emit message double-sent, not only console.warn is invisible) + add hand-off-session.ts:281-303 Literal mirroring logic, trigger condition = user reports cross-adapter spawn sandbox mismatch bug. **Chapter 3 sign-off**: Scheduler scope definition + dual-type period settings convention (lifecycle hot update threshold / periodic task unified setIntervalMs setter), no independent plan is required. **R1.5 key reversal**: reviewer-claude R1 found factual errors in F3 (the reviewer-* reference is application CLAUDE.md, not user CLAUDE.md, and the cross-reference in the same system prompt does not constitute a hard dependency). The lead made a mistake between supplementary citation and hard dependency in R1, which was corrected by independent refutation by both parties in R1.5. Total 4 commits / 1 file docs / +389/-111 LOC / typecheck does not need to be run (no code changes) / `heterogeneous_dual_completed: true`. Details [`plans/adapter-architecture-design-20260515.md`](../plans/adapter-architecture-design-20260515.md) (including `adapter-architecture-rfc-20260515` decision record). |
| [CHANGELOG_121.md](CHANGELOG_121.md) | `p4-baseadapter-d2-implement-20260515` plan closure (RFC §1 Chapter 1 sign-off Option D2 implementation, one of the three trigger conditions of CHANGELOG_120 architectural design plan chapter three is fulfilled) - `CreateSessionOptions` split 4 union arm + 4 interface(`ClaudeCreateOpts/CodexCreateOpts/PtyCreateOpts × 2 shared`) + `agentId` field discriminant union + `buildCreateSessionOptions` builder helper exhaustive switch + 4 adapter typed export. The caller side TS compile time prevents field miscommunication (codexSandbox to claude / permissionMode to codex, etc.) + directly import typed instance to expose adapter-exclusive methods (claudeCodeAdapter.restartWithClaudeCodeSandbox, etc.). **3 rounds of heterogeneous confrontation review × fix closing**: R1 reviewer-claude Y / reviewer-codex N (MED gatekeeping only one side) → R1 fix `8a15a6f` Fix F2 cosmetic + F3 4 SSOT gatekeeping (AssertSameKeys two-way trick) → R2 reviewer-claude Y + 1 LOW comment orphan / reviewer-codex N + 1 MED isAgentId handwritten whitelist is not SSOT → R2 fix `ad221de` fix F4 pump AGENT_IDS as const SSOT driver isAgentId + add gatekeeping (5) + F5 fix comment table → R3 both sides 0 true finding closing Y. **5 TS compile-time strong gatekeeping** (types union ↔ builder switch ↔ ByAdapter entry ↔ AdapterIdMap entry ↔ AGENT_IDS list) + 3 runtime boundary process checks (register / MCP zod / cli enum). **6 caller migration** (RFC §1.5 omitted sessions-hand-off-helper.ts 6 helper and added it together): spawn / cli×2 / ipc/adapters / sessions-hand-off-helper. **Phase 3 skipped** (Option C delegateOrThrow RFC self-assessed income is small + adapter lifecycle behavior is inconsistent and requires an independent plan). Total 4 commits (Phase 1+2 ebefdfb 14 files + R1 fix 8a15a6f 3 files + R2 fix ad221de 1 file + Phase 5 closing)/ ~+880/-254 LOC (slightly higher than RFC §1.5 estimate +165/-140, mainly in new single test + builder annotation + gatekeeping reinforcement)/ typecheck double-ended 0 Wrong + vitest 596 all passed + 18 type-narrow tests (6 @ts-expect-error all trigger) + reverse leakage change actual test (temporary leakage of any one of AdapterIdMap.aider / ByAdapter.aider / AGENT_IDS.aider → TS error)/ `heterogeneous_dual_completed: true`. Details [`plans/p4-baseadapter-d2-implement-20260515.md`](../plans/p4-baseadapter-d2-implement-20260515.md) + Parent RFC `adapter-architecture-rfc-20260515` Chapter 1 (see [`plans/adapter-architecture-design-20260515.md`](../plans/adapter-architecture-design-20260515.md)). |
| [CHANGELOG_122.md](CHANGELOG_122.md) | `archive-plan-content-overwritten-fix-20260515` plan closure (`p4-baseadapter-d2-implement-20260515` plan closure archive actual test collision archive_plan body overwrite bug + commit `30467f6` manual fix post-plan systematic root cause)—— archive_plan tool step 9-10 When writing an archive file, use the stub planContent.body read in step 6 (before ff-merge) to overwrite the caller coming in from ff-merge. Finish the commit ([x] step checklist / reason for skipping / correction of known pitfalls, etc.) to roll back bug fixes. Modification method A: split read twice: step 6 pre-check → step 8 ff-merge → step 8b re-read and get freshFm + freshContent → step 9-10-11 use fresh data to write the archive + step 8c add freshFm.status === 'in_progress' re-check (to prevent caller worktree branch drift status silent archive abandoned is completed, violating user CLAUDE.md §Step 4 Abort the contract + return REVIEW_33 H2 abandoned defense line) + INDEX.md summary cut freshFm.description (same root cause step 11 missing repair) + cleanup hint complete decision tree (reset --hard ORIG_HEAD recommended + revert limited abandoned path only, continue must reset can not use revert to prevent git topology bifurcation). **4 rounds of heterogeneous confrontation review × fix closing**: R1 independent of both sides 1 HIGH (INDEX summary stale) + rebuttal round codex unique to each side 1 HIGH (status carry-forward missing)✅ Required → R1 fix `39e7b14` + 2 case → R2 codex 1 MED (cleanup hint range multi-commit) + claude 2 INFO doc nit → R2 fix `f377a11` → R3 codex 1 MED (option (2) closed to prevent error editing main hits dirty working tree) + codex 1 LOW (line 19 docstring abandoned contradicts implementation) + claude 2 INFO doc drift → R3 fix `f974d2c` → R4 both sides ack ✅ can be combined + codex 1 LOW polish (revert+continue git topology fork — revert limited abandoned) → R4 polish `fa0f0ec`. **4 rounds of cluster convergence**: fm/freshFm SSOT cutting contract (pre-ff-merge uses fm preflight + ff-merge target; post-ff-merge uses freshFm to write) grep actual measurement 0 stale residue, docstring + inline comments + implementation of three-layer code alignment + invariant to remind future contributors. Total 7 commits / 2 files / +328/-23 LOC / archive-plan tests **42/42 pass** (impl-core 12 + impl-r33 15 + handler 9 + ff-merge-body **6** = old 4 + new 2) + full set of vitest **602/602 pass + 64 skip + 0 fail** + typecheck double-ended clean / `heterogeneous_dual_completed: true`. Details [REVIEW_44.md](../reviews/REVIEW_44.md) + [`plans/archive-plan-content-overwritten-fix-20260515.md`](../plans/archive-plan-content-overwritten-fix-20260515.md). |
| [CHANGELOG_123.md](CHANGELOG_123.md) | `archive-plan-tool-ux-followup-20260515` plan closing (REVIEW_44 leaves 4 followups with the same theme and closed together, all of them belong to the archive_plan tool UX improvement category)—— **(a)** fallback chain plus `<main-repo>/plans/<id>.md` intermediate file (post project stub Actual convention);**(b)** schema plus optional `changelog_id` (string + csv, space allowed) + INDEX smart update (row-level matching anchored at the beginning of the row + canonical rewrite 4 columns + α/β fallback does not force clearing of existing ones) + return shape upgrade `plansIndexAppended: boolean → plansIndexAction: 'created'\\|'appended'\\|'updated'\\|'unchanged'` + `warnings: string[]` field;**(c)** INDEX 4 columns canonical header `\\| file \\| status \\| association changelog \\| summary \\|` + escapeTableCell escape `\\\\|` + newline + formatChangelogCell csv → markdown link splicing;**(d)** 7 phase-specific phaseHint replaces the old generic GENERIC + gives specific manual recovery decision trees (rev-parse-HEAD / mkdir-plans-dir / write-archived-plan / sync-plans-INDEX / unlink-original-plan / git-add / git-commit). **3 rounds of heterogeneous confrontation review × fix closure**: R1 consensus of both parties 0 HIGH + 5 true MED + 1 polish + 1 rebuttal (claude MED-1 'unchanged' git-commit deadlock — `status: in_progress → completed` must generate archivedPath frontmatter diff, deadlock unreachable, rebuttal established ✅); R1 fix 6 items including `upgradeIndexHeader` helper Automatically smooth migration of the old 2-column INDEX header → 4 columns (idempotent + conservative detect to prevent accidental changes) + `POST_FF_MERGE_RETRY_INVARIANT_PREFIX` so that the phaseHint override path will not be lost "Cannot retry archive_plan as a whole" invariant + HIGH-1 plan_file_path stem gatekeeping (to prevent step 12 silent unlink caller files) + HIGH-2 silent override warn(plans/<id>.md dual memory overwrite warning); R2 both sides ack 0 HIGH/MED + consensus LOW polish 2 items combined (escape-aware invariant gatekeeping + multi-table INDEX single table invariant comment). Total 8 file changes / +410/-61 LOC / archive-plan tests **92/92 pass** (original 42 + new 50 case gatekeeper 11 fixes + R1/R2) + full set of vitest **652/652 pass + 64 skip + 0 fail** + typecheck double-ended clean / `heterogeneous_dual_completed: true`. Details [`plans/archive-plan-tool-ux-followup-20260515.md`](../plans/archive-plan-tool-ux-followup-20260515.md). |
| [CHANGELOG_124.md](CHANGELOG_124.md) | Global shortcut keys plus "one-click to maximize/one-click to return to default" window size switching + 4 rounds of heterogeneous confrontation review × fix closure - consistent with the `Cmd+Alt+P` (top)/`Cmd+Alt+T` (transparent) series, new `Cmd+Alt+=` one-click to maximize the screen workArea (minus 40px margin)/ `Cmd+Alt+-` One click to return to the default 520×680; two keys to toggle each, press again to restore the last "custom" size (shared `preferredSize` memory field). `FloatingWindow.toggleMaximize/toggleDefault` new method + `applyTargetSize/rememberIfCustom/isNear/centerInDisplay/clampPositionInDisplay` 5 helper + `preferredSize`/`lastToggleAt`/`emitCompactChanged` 3 fields + 5 constants. **After the user completes the modification, the deep-code-review SKILL is actively triggered, and the heterogeneous confrontation runs for 4 rounds × fix closure** (reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 high reuses the same pair across R1-R4): R1 HIGH 1 (setSize does not change the position off-screen codex on-site arithmetic verification default x=900 + maxW=1400 right boundary 2300 > 1440 off-screen 860px, repair method setBounds + centerInDisplay) + 4 MED (compact UI out of sync / preferredSize clamp deadlock / R1 LOW-1 minHeight=COMPACT_HEIGHT regression / preferredSize cross-screen pollution) + 4 LOW + 5 INFO full → 7 repair + 2 defer + 1 follow-up plan. **Three-layer short-circuit design** rememberIfCustom: (1) curSize === lastNormalSize cross-screen without pollution + (2) Date.now() - lastToggleAt < 300ms macOS animate intermediate frame without pollution + (3) !atMax && !atDefault self-jump without pollution. **fallback top double-layer protection** Simple isNear + clamp and then isNear to go alt fallback. BrowserWindow constructor adds minWidth/minHeight bottom line + toggleCompact bidirectional setMinimumSize + create() 4 fields at the end reset to prevent Cmd+W to close the window dock activate to rebuild and carry the old state. Added IpcEvent.CompactToggled end-to-end channel to synchronize renderer when toggle returns to compact to avoid button label inversion. Typecheck passed all three rounds / `heterogeneous_dual_completed: true`. Details [REVIEW_45.md](../reviews/REVIEW_45.md). |
| [CHANGELOG_125.md](CHANGELOG_125.md) | `codex-handoff-team-alignment-20260518` plan closure (5 phase + 22 commit + 71 file / 6809+/167-) - codex-cli adapter fully accesses hand_off / archive_plan / team mcp orchestration, resolves 4 HIGH blocks (HIGH-1 caller_session_id transport injection / HIGH-2 cold-start protocol codex is not recognized / HIGH-4 Wire format protocol codex is not visible / HIGH-C archive_plan cwd_release_marker 4-state shunt) + EnterWorktree CLI v2.1.112 stale base bug solution + bundled assets multi-root routing. **P1**: sessions.cwd_release_marker column + double mcp tool (enter_worktree / exit_worktree) + archive-plan-impl 4-state shunt change (cwd valid/invalid × marker null/match/mismatch) + rename.ts 20 column extension + sessionManager.markClosed/close hook linkage clearing marker. **P2**: per-session bearer token map (mcp-session-token-map.ts SSOT) + HookServer.checkMcpAuth three-state shunt + transport-http extra.authInfo + codex bridge per-session Codex instance + sessionManager.renameSdkSession integrated 4-key atomic migration (sessions Map / sdkOwned / token map / codexBySession Map). **P3**: getAgentDeckPluginPath remove adapter-aware + bundled-assets multi-root scan + AssetMeta.adapter field (breaking change 6-caller cascade) + spawn handler agent_name according to args.adapter routing + agents-md-installer source cut to codex-config/CODEX_AGENTS.md + options-builder reviewer-* 4 fields unsafe default spread enforce. **P4**: 4 codex perspective content assets (CODEX_AGENTS.md 178 lines + reviewer-codex.md 132 lines + reviewer-claude.md 190 lines + claude-config/CLAUDE.md plus §enter/exit_worktree section) + spike4 mini-spike claude -p bypassPermissions nested sandbox PASS + options-builder additionalDirectories plus /tmp. **P5 ends 6-way heterogeneous confrontation review × fix**: 3 batch × claude+codex 2 ways each (including 1 way compliance lead self-running external codex CLI) = 6 reviewers, ruling out 3 ✅ HIGH (H1 archive_plan 4-state shunt change / H2 codex bridge resume earlyErrCb missed codexBySession + mcpSessionTokenMap Both parties are independent / H3 reviewer-claude wrapper `claude -C` parameter does not exist) + 21 MED + 4 downgrade MED + multiple LOW/INFO, full inline fix (4 commit P5 batch A/B/C + settings default value raised mcpSpawnRatePerMinute 10→20 / mcpMaxFanOutPerParent 5→10). Smoke test partially passed: spawn link + options-builder default spread + per-session Codex envOverride full ✅; codex SDK runtime macOS `failed to initialize in-process app-server client: Operation not permitted` env block did not reach mcp tool actual test, run fresh `pnpm dev` in an independent environment to verify and follow-up. **Invariant 5/6/7**: marker 4 state shunt / options-builder enforce point / mcpSessionTokenMap.rename via sessionManager.renameSdkSession. 26 commits / typecheck + build 0 errors + 730/801 vitest 0 fail / `heterogeneous_dual_completed: true`. Details [`plans/codex-handoff-team-alignment-20260518.md`](../plans/codex-handoff-team-alignment-20260518.md). |
| [CHANGELOG_126.md](CHANGELOG_126.md) | Plan `codex-handoff-team-alignment-20260518` P6 Process Improvement Closure (CHANGELOG_125 P5 Closure Companion) - Precipitate the problems exposed by the writing process of this plan v1→v4.1 into process assets. **A** user CLAUDE.md §Complex plan section upgrade v2 flow chart (Step 0 RFC → Step 0.5 spike → Step 1 Plan → Step 1.5 Deep-Review → Step 2 EnterWorktree user confirm → 2.5/3/4 the same style) + §Trigger condition v2 widens bullet 3 across adapter/schema/process boundary + bullet 1 sub-condition plus spike. **B** Apply CLAUDE.md cross-ref minimum action + enter_worktree / archive_plan / hand_off_session to call the signature to synchronize the actual schema (hand_off_session complements the archive_caller field). **C** 4 reviewer body adds §Sandbox restriction description section (claude-config × 2 + codex-config × 2) to cover the SDK actual sandbox boundaries (READ loose / WRITE strict / macOS Seatbelt OS layer) + caller responsibility shunt (take SKILL auto cp / bypass SKILL and handle it yourself). **D** `deep-code-review` SKILL transformation (typed scope kind code/plan/mixed + 3 sets of prompt templates + auto sandbox cp + manifest + kind='mixed' cost and failure) + physical git mv to `deep-review/` + old directory reconstruction and 6 months of deprecation stub. **E** `.gitignore` Add `.deep-review-cache/` entry to prevent cache entry into git. **meta-review double confrontation (reviewer-claude + reviewer-codex) total 5 HIGH + 13 MED + 3 LOW full inline fix** (5 HIGH including SKILL slash name vs physical name inconsistent / cache file concurrency race / kind='mixed' design contradictory / claude-config sandbox description over-restrict actual measurement inconsistent / .gitignore missing entry; 1 ❌ false positive reviewer-claude mistakenly believed that the spike4 file did not exist). Details [`plans/codex-handoff-team-alignment-20260518.md`](../plans/codex-handoff-team-alignment-20260518.md) §P6 Chapter. |
| [CHANGELOG_127.md](CHANGELOG_127.md) | Prompt assets according to user CLAUDE.md § Prompt word asset maintenance 6 hard constraints cleaning + `deep-code-review` SKILL deprecation stub physical deletion (git rm entire directory) - 8 asset normalization (user CLAUDE.md + app CLAUDE.md + 4 reviewer body claude-config × 2 + codex-config × 2 + deep-review/SKILL.md + codex-config CODEX_AGENTS.md M1 fix): delete plan phase historical annotation (P3/P5/P6.x revision + spike3/spike4 number + spike4 connection section) + delete deprecation time window prediction language ("6-month deprecation stub" / "P6.7 name change") + Change "suggested/recommended/strongly recommended/usually" to executable constraint action + delete placeholder nonsense section (claude-config CLAUDE.md § application environment RFC/spike difference) + merge redundant Bash duplicate blocks (user CLAUDE.md Step 2 body + stale base bug callout (b)). **Single decision against review** (dual Bash heterogeneous CLI concurrency: reviewer-claude Opus 4.7 xhigh + reviewer-codex gpt-5.5 xhigh): 0 HIGH + 1 ✅ M1 fix (codex-config CODEX_AGENTS.md: 28 old SKILL name drift, both parties independently proposed + grep actual test hit, stub physical deletion protocol layer points to non-existent SKILL) + 1 ❌ M3 rebuttal (reviewer fresh session / NO MSG ANCHOR template string embedded "suggestion lead" is advisory message, template content ≠ regulation compliance) + M2/INFO multiple follow-up cleanup (top-level README / docs/ / conventions/ / .gitignore old name reference + claude-config CLAUDE.md pre-existing residue). The changes have not destroyed key guardrails (Sandbox/Wire format/NO MSG ANCHOR/Fresh session/Worktree path self-check/send_message reply chain/heterogeneous duality disciplines are all retained). `heterogeneous_dual_completed: true`. Details [REVIEW_46.md](../reviews/REVIEW_46.md). |
| [CHANGELOG_128.md](CHANGELOG_128.md) | `deep-review-batch-a1-b-fixes-20260519` plan closing — deep-code-review SKILL 6 batch accumulated **6 HIGH + 11 MED** (A1 batch claude-code SDK bridge + B batch agent-deck-mcp tool handlers) centralized closing, using "complex plan process v2" (RFC two rounds + spike + plan-review double confrontation + Phase 1/2/3 fix + R3 verify). **Phase 1 P0 Security/Data Loss 3 HIGH**(commit `91cb584`):B-HIGH-1 (C) Two-layer spoofing gatekeeping (helpers.ts stdio invariant assertion + transport-stdio.ts force sentinel + transport-http.ts fallbackToGlobal force sentinel)+ B-HIGH-3 base_branch `refs/heads/<name>` Strict namespace verification (reject SHA/tag/detached) + B-HIGH-4 mainRepo dirty fail-fast precheck (prevent git commit -m from swallowing staged files and mixing them into the archive). **Phase 2 P0 function broken 3 HIGH**(commit `f260fd8`):A1-HIGH-1 createSession `realId === tempKey` guard complete failure semantics (let catch complete cleanup + rethrow, block false session leak) + A1-HIGH-2 setTimeout fallback symmetrically cut sessions Map(delete tempKey + set fallbackId, prevent sendMessage(fallbackId) miss triggers recoverer to start the second SDK CLI sub-process) + B-HIGH-2 hand_off batonMode conditionalization (`args.archive_caller !== false`,archive_caller=false degrades normal spawn and uses depth/fan-out double guardrails). **Phase 3 11 MED + test completion**(commit `c64ba31`):A1-MED ×6(setPermissionMode rollback / REVIEWER_AGENT_NAMES SSOT + isReviewerAgentName guard / recoverer findFallbackCwd caller link NOTE / translatePostToolUse 4 places file-changed emit toolCallId transparent transmission / file-changed emit Delay until user.tool_result+completed (pendingFileChangeIntents Map + pushFileChangeIntent/consumePendingFileChangeIntent helpers) / restart-controller while(inflight) loop re-check to prevent multi-waiter race)+ B-MED ×5(cwd 4 state marker split c-1/c-2/c-3 sub-file / exit_worktree partial-success markerCleared symmetric + return error / new plan-path-helpers.ts extract resolvePlanFilePath 3 files fallback helper / archive_plan filesToAdd add planRelative to archive commit containing source D+archived A / hand_off plan_file_path stem verification) + 5 new test files (translate-post-tool-use-toolcallid 5 cases + file-change-intent-delay 6 cases + set-permission-mode-rollback 3 cases + plan-path-helpers 8 cases + plan-path-helpers helper module) + archive-plan/_setup.ts makeDeps Add transparent mainRepo status mock Fix the mock queue offset regression introduced in Phase 1. **R3 verify**: spawn 4 reviewer fresh review (caller session is not in the original R1/R2 team, lose mental model), 0 fix introduce regression + out 5 HIGH + 9 MED follow-up (all are unrecognized deep bugs of the same type + test completion debt — consume vs setTimeout fallback 30s concurrent mutate race both parties are independent + createSession throw path does not interrupt SDK query ghost session + translateSdkMessage synchronization SDK status mode only writes to DB but does not write to internal cache. Security boundary misalignment + transport-http-extra-auth.test.ts inline contract stale + plan §Phase 1+2 promises 7 P0 regression test files (zero creation). **Feedback from user R3**: hand_off baton should shutdown dormant teammate + team should not be passed along with the old lead (default keep_teammates:false's shutdownTeammatesOnBaton helper does not take effect on dormant teammate). Total 3 commit + 24 file changed + 920+/73- LOC + typecheck/826 test/build all passed + `heterogeneous_dual_completed: true`. Details [REVIEW_47.md](../reviews/REVIEW_47.md) + archive plan [`plans/deep-review-batch-a1-b-fixes-20260519.md`](../plans/deep-review-batch-a1-b-fixes-20260519.md). |
| [CHANGELOG_129.md](CHANGELOG_129.md) | `deep-review-batch-a1-b-followup-r3-20260519` plan closing - issued by the last round of R3 verify **5 HIGH + 9 true MED + 2 user feedback (F1/F2)** full follow-up + R3 fresh reviewer dig deep **4 true HIGH + 6 true MED + 2 LOW/INFO**, 22 commit chain implemented (Phase 1-6 total 15 + R3 fix 7 atomic). **Phase 1-6 (commit `034efea`-`e24e335`)**: Test debt engineering completion (lambda export real production code + MockSdkQuery stateful three-state machine) + Race modification (C) double insurance (fallback fire / createSession throw dual path fire-and-forget interrupt + idempotency `interruptFired` flag + consume L221 first-id (B) guard temporary `incomingId` does not write realId + sid three-speed chain) + H3 internal cache synchronization + archive_plan precheck refinement (NUL parser + R/C double path + repo-relative + commit pathspec) + F1 baton + escape hatch `shutdown_baton_teammates` mcp tool + miscellaneous closure comment refinement. **Phase R3 fresh reviewer fix (commit `313410f`-`b08359e`)**: 4 reviewer pair heterogeneous confrontation (reviewer-claude full + reviewer-codex Batch A/B/C+D split due to 32 files xhigh hit 6m4s budget limit) issued 4 true HIGH full land — H1 restart-controller race (claude MED-1 + codex A HIGH-1 merge upgrade: two restart methods symmetric listener + transfer recovering Map entry from OLD → NEW) + H2 archive-plan `runGit.trim()` breaks porcelain `-z` NUL output (codex B HIGH-1: runGit adds `opts.raw` option) + H3 base_branch rev suffix bypasses (codex C+D HIGH-1: Add `git check-ref-format --branch` first-order reject `main~1` / `main^{commit}`) + H4 git status is missing `--untracked-files=all` (codex C+D unverified upgrade: untracked critical file expansion full path). **6 True MED full land** — M3 setPermissionMode chain serialization instead of Phase 2.7 seq counter (codex A HIGH-2 downgrade: prevent double failure dirty cache) + M2 stdio override lambda export (codex C+D MED-1: test true import tied to production) + M5 exit-worktree markerCleared transparent transmission (codex B MED-2: err helper plus optional extras parameter) + M6 dormant SQL invariant in-memory DB case (codex C+D MED-2: defense in depth) + M1 writeTools matrix complement `shutdown_baton_teammates` (reviewer-claude LOW upgrade) + M4 commit message plus mainRepo unrelated dirty footnote (codex B MED-1: git log persistent archive time status). **2 True LOW/INFO trivial clears easily** — L1 application CLAUDE.md `archive_caller=false` degenerates normal spawn document revision + I1 two fire-and-forget interrupts add `.catch` to prevent unhandled rejection. typecheck / 32 file 416 test pass + 5 skip (binding ABI by-design) / build all passed. Details [REVIEW_48.md](../reviews/REVIEW_48.md) + archive plan [`plans/deep-review-batch-a1-b-followup-r3-20260519.md`](../plans/deep-review-batch-a1-b-followup-r3-20260519.md). |
| [CHANGELOG_130.md](CHANGELOG_130.md) | `reviewer-codex-cross-adapter-20260519` plan closure - deep-review SKILL heterogeneous dual reviewer arrangement in the bidirectional lead **Phase 0 cross-adapter dispatch BLOCKER 5 commit fix chain**:fix v1 stateless old path(`c67ddde`)→ fix v2 transport-http per-request fresh transport(`835aa7c`, go to mcp-sdk 1.29 `simpleStatelessStreamableHttp.js` standard pattern)→ phantom dep `@modelcontextprotocol/sdk` Go to direct dep(`1f70582`,Node ESM strictly resolve without pnpm hoist fallback)→ 8 tool spec-compliant ToolAnnotations(`eb65878`,readOnly/destructive/idempotent/openWorld 4 fields)→ fix v3 spawn_session/hand_off_session `openWorldHint:true → false`(`923468e`,lock ow:true is the codex CLI mcp_tool_call_approval gate cancel trigger). **Step 0.5 Bidirectional cross-adapter pair PASS**: claude lead × codex reviewer-codex direction A + codex lead × claude reviewer-claude direction B + same-adapter regression × 2 (claude × claude in-process / codex × codex HTTP) double PASS. **Phase 1** SKILL cross-adapter spawn orchestration + fail-safe nativeization (`8cb9ec4`). **Phase 2** Delete 2 copies of wrapper agent body + cleanup wrapper code (`da5c0eb`, net -450 lines, delete `resolve-bundled-claude.ts` entire file + `options-builder.ts` reviewer-claude wrapper envOverrideExtra injection branch + rewrite `teammate-spawn-defaults.test.ts`). **Phase 3** SKILL build-time auto cp SSOT(`5b727e0`,`scripts/sync-codex-skills.mjs` Node fs cpSync + `predev`/`prebuild` hook + `.gitignore` plus entry). **Phase 4** Asset panel dual-adapter UI dedup(`48141ec`)— `dedupBundledByName` group helper (same as kind+name across adapters and merged into a single group, claude-code takes priority / codex-cli is sorted later)+ `AssetCard` signature `asset → assets[]` plus dual-adapter chip double subscript + `ContentViewerModal` schema `asset → assets[]+currentAdapter` + `[claude]/[codex]/[user]` tab switch + `onTabSwitch` callback go seq guard fetch + pump `AssetCard.tsx` (line 132) main file returns 420 ≤500 threshold. **Phase 5** End-to-end regression (Step 5.1-5.4 spawn pair / dormant wake-up, Step 5.3 codex CLI interactive delegate user / Step 5.5 Asset panel UI visual delegate user). **Phase 6** sweep application CLAUDE.md / CODEX_AGENTS.md 2 missing wrapper description cleanup. typecheck / 71 file 876 test pass + 76 skip 0 fail / build all pass + `heterogeneous_dual_completed: true`. Details [`plans/reviewer-codex-cross-adapter-20260519.md`](../plans/reviewer-codex-cross-adapter-20260519.md) + spike-reports/(spike1+2/3/4). |
| [CHANGELOG_131.md](CHANGELOG_131.md) | `remove-aider-generic-pty-adapters-20260520` plan closing - delete aider + generic-pty two adapters (whole directory + all references) + ComposerSdk slash intercept + node-pty/chokidar native dep + GenericPtyConfig TS type delete the entire piece, leaving 2 adapters (`claude-code` + `codex-cli`). There is no historical data measured by users, N3 has one-way irreversible forced deletion, and no graceful fallback. **24 commits chain** P0-P9 serial (base `84a6910` → archive `0dc2fd9`): **P0** ComposerSdk slash interception deletion → **P1 (9)** delete adapter implementation layer + GenericPtyConfig type whole piece deletion (SSOT chain enforce 5 compilation periods + 3 runtime boundary fully closed loops)→ **P2 (4)** Delete UI → **P3 (5)** Delete IPC / MCP schema + comment cleaning → **P4 (7)** Delete shared types + jsdoc residual cleaning → **P5 (2)** Delete test/mock(spawn-agent-name-routing delete TC7+TC7b + tools.test.ts vi.spyOn local mock)→ **P6 (1)** Uninstall native dep node-pty + chokidar + scripts/fix-pty-permissions.mjs delete + asarUnpack 2 globs delete, unlock by the way install-app-deps hit distutils environmental blocker → **P7** DB schema 0 hit self-test → **P8 (2)** document scan + CHANGELOG_131 → **P9 (3)** deep-review 3 round heterogeneous duality reviewer pair. **P9.3 ⚑ pnpm test runs all ahead of schedule unblock**: 64/64 file passed \| 762/838 test passed (76 skipped are all better-sqlite3 binding ABI v130 vs v137 gatekeeping skip, in line with the lessons of CHANGELOG_42). **P9 R1 fix `a8a4685`**: MED Both parties are independent → escalate and delete the `SessionRecord.genericPtyConfig` field (plan focus #7 0 alive caller trigger) + 4 LOW comments remain; **R2 fix `c66b4e5`**: reviewer-codex One party MED + lead spot-check verify → hand-off `SessionHandOffSummarize` Stage 1 plus `if (!adapter?.createSession) throw` early gate (mirror with Stage 2 line 156) anti-aging SQLite row fallback paid Claude oneshot waste LLM quota; **R3** Both sides ✅ can be combined + 0 finding final ruling. typecheck PASS/build PASS/test PASS. **Follow-up F1**(LOW): hand_off_session adopt_teammates feature (leave independent plan); **Follow-up F2**(MED): The default value of the anti-recursion threshold is increased (this plan will be implemented in the follow-up CHANGELOG_132). Details [`plans/remove-aider-generic-pty-adapters-20260520.md`](../plans/remove-aider-generic-pty-adapters-20260520.md). |
| [CHANGELOG_132.md](CHANGELOG_132.md) | Plan `remove-aider-generic-pty-adapters-20260520` Follow-up F2 implementation - Anti-recursion threshold default value is raised for the last mile. `shared/types/settings.ts` defaults is 20/10 (CHANGELOG_125 P5 plan codex-handoff-team-alignment-20260518 upgraded) + jsdoc description is also aligned with "default 20/min / default 10", but `spawn-guards.ts:80-81` runtime fallback is still `?? 5` / `?? 10` (old value) + user persisted store old value 5/10 not migrated (the user screenshot setting panel shows 5/10 = persisted instead of default). Fix: ① `spawn-guards.ts:80-81` fallback `?? 5`/`?? 10` → `?? 10`/`?? 20` aligned with settings-defaults; ② `settings-store.ts:51` Add one-time migration (transparentWhenPinned same mode): persisted value is exactly equal to the old default(5/10)→ Upgrade to new default(10/20); user explicitly selects non-default value (7/15, etc.)→keep. Trade-off: rare false positive (the user actively selects the same value as the old default and is migrated). However, in the deep-review multi-batch scenario, the old default has been tested and the migration is actually a friendly action. The semantics are consistent with the "default 10/20" in the settings panel jsdoc. typecheck GREEN / spawn-guards.test 12/12 pass (testfile explicitly set settingsState, fallback is not triggered, no regression). Details [`plans/remove-aider-generic-pty-adapters-20260520.md`](../plans/remove-aider-generic-pty-adapters-20260520.md) §Follow-up Section F2. |
| [CHANGELOG_133.md](CHANGELOG_133.md) | Plan `add-claude-cli-path-override-and-bump-sdks-20260520` closed - add `claudeCliPath` setting item to fill in historical asymmetry (codex already has codexCliPath, claude has no corresponding entry) + bump the built-in SDK to the latest stable version. **4 commits chain**(`99a9373`-`d6d72d3`): **Phase 1**(`99a9373`,5 files ~32 LOC base)settings.ts +field +DEFAULT null / ipc/settings.ts +applyClaudeCliPath no-op hook (D6: claude SDK does not support instance pool and does not require invalidate, with codexCliPath apply chain symmetry)/ sdk-bridge:253 + claude-runner:55 inline priority chain `(claudeCliPath && trim) \\|\\| getPathToClaudeCodeExecutable()`(mirror codex codex-instance-pool:46 + codex-cli/sdk-bridge:240 inline pattern)/ ExternalToolsSection.tsx +Claude picker;**Test mock 0 LOC** (D8 priority chain short-circuit semantics automatically picks up 5 existing mocks). **Phase 2**(`b855fe1`)bump @anthropic-ai/claude-agent-sdk 0.2.118 → 0.3.144 (major numbers up but d.ts +390 lines full additive, removed 4 unstable_v2_* experimental export agent-deck 0 grep hits). **Phase 3**(`38483f1`)bump @openai/codex-sdk 0.120.0 → 0.131.0 (11 minor spanning but d.ts +2 lines additive only `reasoning_output_tokens`, vendored binary path 5 platform is exactly the same). **Phase 4.1 build GREEN**(`pnpm build` 187+8+449 modules, 4.2-4.3 user actual test deferred); **Phase 4.4 / 4.5 partial implicit smoke** (this session is started by hand_off baton + running deep-review SKILL dual reviewer pair, the complete 5 major smoke is still waiting for user). **Phase 5 deep-review SKILL kind='code' R1+R2** Double reviewer ✅ Compatible — rebuttal 1 true MED(codex unilateral / asarUnpack glob in pnpm layout compatibility, history .app `/Applications/Agent Deck.app/...node_modules/@anthropic-ai/claude-agent-sdk-darwin-arm64/version: 0.2.118` Empirical electron-builder pnpm-aware hoist true takes effect, reviewer-codex R2 accepts withdrawal); fix 1 true MED (claude unilateral/plan §current progress stale)+ 1 true LOW `d6d72d3` controls.tsx tooltip tool-independent (codex unilateral/ExecutablePicker reuse codex designed hardcoded Copywriting);3 LOW accepted as backlog/Phase 4. **typecheck GREEN / 762 test pass + 76 skip 0 fail / build GREEN** (per phase ⚑ checkpoint). **Follow-up F1**(LOW backlog,plan §D9):`@anthropic-ai/sdk` peer dep `>=0.93.0` warn stay `pnpm add` follow-up;**F2**(LOW,plan §D7): existsSync guardrail (mirror codex is not currently available);**F3**(LOW,plan §D8 §1.7): priority chain unit test (~80 LOC, codex equivalent, no special test); **F4** (deferred): Phase 4.1-4.3 user complete e2e smoke. Details [`plans/add-claude-cli-path-override-and-bump-sdks-20260520.md`](../plans/add-claude-cli-path-override-and-bump-sdks-20260520.md) + spike-reports/(spike1+2+3). |
| [CHANGELOG_134.md](CHANGELOG_134.md) | Plan `add-claude-cli-path-override-and-bump-sdks-20260520` Follow-up F1+F2+F3 Implementation — **F1**(`dab3101`):`pnpm add @anthropic-ai/sdk@^0.96.0` Direct dep eliminate claude-agent-sdk 0.3.144 peer warn(0.81 → 0.96 across 15 minor stable);**F2+F3**(`d6b7df6`): pump helper `resolveClaudeBinary()`(`src/main/adapters/claude-code/resolve-claude-binary.ts`) package priority chain + existsSync guardrail + console.warn on missing path + bundled fallback,2 caller(`sdk-bridge/index.ts:253` + `claude-runner.ts:55`) change 1 line of helper call; create `__tests__/resolve-claude-binary.test.ts` 6 case to cover priority chain 6 boundaries (null/empty/all blank/does not exist/exists/including leading and trailing blank trim). **Two design decisions deviation** (plan §D5 + §D7, user explicitly opted in): §D5 "inline without helper" → use helper to make F3 single test clean; §D7 "without existsSync" → add existsSync guardrail + warn to prevent user from filling in the wrong path and hitting ENOENT. Result: claude / codex priority chain is no longer literally aligned (claude's one-way enhance, codex's same enhance is reserved for future follow-up). **verify**: typecheck 0 errors / test **768 pass**(+6 F3 new) \| 76 skip 0 fail / build GREEN. **Residual F4**: user complete e2e smoke `pnpm dist` + install + 4.2-4.3 interaction (this follow-up commit caller runs dist). Details [`plans/add-claude-cli-path-override-and-bump-sdks-20260520.md`](../plans/add-claude-cli-path-override-and-bump-sdks-20260520.md) §Design decision D5+D7+D8+D9. |
| [CHANGELOG_135.md](CHANGELOG_135.md) | `hand-off-session-adopt-teammates-20260520` plan closure — `hand_off_session` adopt semantics + spawn-link bug double repair. **bug fix**: Phase 1 root cause investigation and elimination P2 (K2 mcp leaked gatekeeping) + empirical P3 (REVIEW_39 fix 14 historical data left on the day); Phase 2 extracted `shouldWriteSpawnLink(opts)` SSOT helper to strengthen N2.a invariant (spawn.ts three inline changes to use helper). **feature add**: Add `adopt_teammates: boolean` to allow the new session to take over the caller and the same team as lead. The original teammate shares the active team with the new session and can continue send_message communication; supporting N5 ≥ 1 lead hard constraint + firstTeam fatal abort + zero dual-lead window is implemented through swapLead transaction atomic. **schema simplification**: delete `keep_teammates` field (grep == 0 hard gate 1) + zod strict double-layer naming (SHAPE for tool registration / `*_ARGS_SCHEMA = z.object(SHAPE).strict()` for parse verification) + N2.c mutual exclusion invariant (adopt_teammates × team_name cannot be transmitted simultaneously) zod refine reject + handler Defensive hard constraints defense in depth — Phase 7 reviewer-codex Round 1 HIGH Revise the law to prevent production SHAPE-registration path does not run strict leak gatekeeping). **8 invariant** (N1 zero dual-lead window / N2.a default does not write spawn-link / N2.b adopt path independent prompt assembly does not reuse spawn buildLeadContextBlock + does not write placeholder / N2.c mutual exclusion / N3 closed teammate does not silently ignore / N4 schema dual layer / N5 ≥1 lead + firstTeam fatal abort / N8 event emit synchronization). **plan deep-review Round 1-8** ✅ closed by consensus of both parties (0 true HIGH 0 true MED + Round 8, clean up LOW/INFO smoothly and enter v9 final). **Implementation 9 commit chain**(`825d258`-`b486590`) Phase 1-7 full closure: Phase 4 double helper (`lead-context-block.ts` spawn path SSOT refactor + new independent `adopted-teams-context-block.ts` adopt path does not contain wire prefix / placeholderId / "return to lead" command) + Phase 5 swapLead transaction `db.transaction(callback)` package caller demote + newSid INSERT same as transaction three case shunt + Phase 6 handler within phase 1.5 swapLead loop + firstTeam fatal abort try/catch + lifecycle precheck + N8 emit. **Phase 7 deep-review SKILL kind='code' 3 round**(R1 commit `6a43c19` codex 4 finding repair / R2 commit `4ca89e5` codex 3 finding repair / R3 commit `b486590` codex LOW + claude INFO-5 polish smooth closing)— reviewer-codex catch reviewer-claude leak deeper residue (schema strict did not take over the production mcp tool path deep design hole / archived team/teammate ghost membership filter / teamsTotal mathematical consistency / archived teammate still appears cold-start prompt second round residual), both parties R3 consensus ✅ closed. **Test coverage**:Phase 6 baseline 803 → Phase 7 final 807 tests passed(+4 gatekeeping case T4.3d / T6.A1 / T6.A2 / T6.A3)+ 83 skipped(SQLite binding ABI by-design)+ T4.1-T4.7 + T5.1-T5.7 + T6.1-T6.X4 + T6.A1-T6.A3 Complete coverage of 8 N invariants. **Leave follow-up note for non-blocking**: claude INFO-3 (adopt-local filter can be SQL-ified in the future) + INFO-4 (other ok-path case plus emit spy) + INFO-6 (promptGetSessionFn vs phase 1.5 getSessionFn repeated statement) + INFO-7 (archivedTeamIds including team-not-found corner case) - full future-improvement No blocking. `heterogeneous_dual_completed: true`. Details [`plans/hand-off-session-adopt-teammates-20260520.md`](../plans/hand-off-session-adopt-teammates-20260520.md) + spike-reports/(spike1+2). |
| [CHANGELOG_136.md](CHANGELOG_136.md) | `reverse-rename-sid-stability-20260520` plan closure — sessions.id stabilizes the reverse rename design. **Core idea**: `sessions.id` = applicationSid never changes (application layer events / mcp token / wire prefix `[sid]` stable key); a new `sessions.cli_session_id` column is added to carry the SDK / CLI thread sid (allowed to change). bridge InternalSession metadata is dual-stage (applicationSid + cliSessionId two fields); spawn main path ctor=tempKey first realId and then switch to realId to freeze, resume / fallback path ctor=opts.resume remains unchanged throughout. **Reverse rename path modification at 6**: claude jsonl-missing fallback + codex jsonl-missing fallback (resumeMode='fresh-cli-reuse-app' explicit mode + applicationSid reuse) + claude streaming fork detect + codex thread-loop case 3 fork detect (go `sessionManager.updateCliSessionId(applicationSid, realId)` Replace the old `renameSdkSession(OLD, NEW)` cascade rename) + claude restartWithPermissionMode + restartWithClaudeCodeSandbox (RestartCreateOpts plus resumeCliSid dual-track input parameters + delete fork rename block + return currentSid). **SSOT interface split**: applicationSid (sessions.id dimension, events.session_id / mcp token / wire prefix [sid] / sessions Map key are all used for this) + cliSessionId (SDK / CLI thread sid dimension, SDK options.resume + jsonl preflight stat path is used for this) + effectiveResumeCliSid (S1 R6 upgrade bridge internal resolver Three-branch guard) + resumeMode explicit mode field. **InternalSession field naming upgrade**: realSessionId → cliSessionId + Added applicationSid two-stage life cycle. **5 Invariants** (N1 sessions.id never changes / N2 cli_session_id column carries SDK thread sid and allows changes / N3 wire prefix [sid] 100% write sessions.id / N4 mcp token map key=sessions.id dimension is always stable / N5 late hook event blacklist 60s + ingest 4 state shunt). **plan Round 1-8 eight rounds of deep-review** Consensus of both parties ✅ Closing (0 true HIGH 0 true MED + 3 LOW non-blocking implementation-time tweak) A total of ~40+ fixes, detailed plan §Design decisions D1-D8. **Implementation 6 sub-commit chain**(`579f934`-`a33b9b6`): A-1 schema migration v021 + sessionRepo cli_session_id helper / A-2 ingest 4 state split + blacklist double write / A-3 identity bridge split S1+S2+S3+S4+S4b atomic / A-4 S5+S6+S6.5+S8+S9 atomic (createSession return + fork detect + jsonl-missing fallback rewrite + finalizeSessionStart double input parameter) / A-5 test matrix closing + S3 ctor sessions Map key correction + stream-processor.ts S3 three-branch completion / Step C restart-controller inversion (claude × 2 + codex × 1 three-method symmetry). **verify**: typecheck 0 errors / **811 test pass** (+11 new, replacing OLD rename assertions) \| 83 skip 0 fail / build GREEN. Details [`plans/reverse-rename-sid-stability-20260520.md`](../plans/reverse-rename-sid-stability-20260520.md) + spike-reports/(spike1+2+3+4). |
| [CHANGELOG_137.md](CHANGELOG_137.md) | Asset library "Application Convention" tab plus codex perspective editor - the back-end SSOT has been divided into two copies: `claude-config/CLAUDE.md` + `codex-config/CODEX_AGENTS.md` (CHANGELOG_125 P5 plan §D5 all are maintained independently), but the front-end UI only exposed the claude side user copy editor, codex Users can only modify source files + rebuild. Complete dual UI:`agents-md-installer.ts` Add 4 public functions (getActive / getBuiltin / saveUser + immediately synchronize ~/.codex/AGENTS.md section / resetUser) + 3 IPC handler (CodexAgentsMdGet/Save/Reset) + preload api 3 method + create new CodexAgentsMdEditor.tsx(130 OK, mirror ClaudeMdEditor) + AssetsLibraryDialog "Application Convention" tab plus adapter switcher (Claude / Codex choose one, newly added SubTabBtn component) + sub-tab switch dirty secondary confirmation interception. typecheck 0 errors / 811 test pass \| 83 skip 0 fail / build GREEN. |
| [CHANGELOG_138.md](CHANGELOG_138.md) | Image attachment "If you just paste a picture, it will report that it exceeds 30MB" false positive fix: `useImageAttachments` admittedThisRound flag race(setState updater asynchronous enqueue)→ Use attachmentsRef synchronization budget currentTotal instead |
| [CHANGELOG_139.md](CHANGELOG_139.md) | codex spawn main path applicationSid missing cut repair (dual reviewer independent HIGH-1 ironclad evidence): thread-loop.ts case 1 add 1 line `internal.applicationSid = ev.thread_id;` + spawn-chain.ts setSpawnLink add changes=0 warn |
| [CHANGELOG_140.md](CHANGELOG_140.md) | `codex-stream-error-classify-20260521` plan closure — codex CLI 5 built-in reconnection intermediate state fixes. The codex CLI transparently transmits the reconnection intermediate state through `ThreadErrorEvent` (SDK d.ts claims to be `unrecoverable` but the actual behavior of the CLI is recoverable), and the application layer translate.ts mistakenly interprets `finished:error` as the ending, causing the UI state machine to become confused. Fix: use `classifyStreamErrorEvent` + `extractRetryProgress` double helper, fatal priority → transient whitelist (6 entries) → heuristic word-boundary `\b(retry\\|retrying\\|...)\b` → conservative fatal decision tree. **REVIEW HIGH-1** (Both parties are independent + grep codex binary ≥ 13 pieces of ironclad evidence): The old `retr` root swallows `Error retrieving credentials` / `failed to retrieve local addr` / `exec-server transport disconnected` etc. fatal → change word-boundary + fatal whitelist expansion to 12 items; **HIGH-2** (claude unilateral on-site verification): `exceeded retry limit, last status:` Missed capture → fatal regex added `exceeded\s+retr` + whitelist added; **HIGH-3** (both parties are independent): `extractRetryProgress` No anchor error capture date `2026/05` → added `(?:Reconnecting\.\.\.\\|attempt\\|retry)\s+\d+/\d+` Anchor point; **MED-1** (codex unilateral): heuristic hit plus `console.warn` diagnostic signal (plan §D1 design requirement); **MED-2/MED-3**: test +6 cases (C8-C12 true fatal regression / C13 date prefix is not accidentally caught / C4 console.warn triggers verification). **plan §Known pitfalls** backlog: turn stuck defensive watchdog stay future (MED-1-claude *not verified*). typecheck GREEN / **36 test pass** (from 30 +6 new) / 0 fail. Details [`plans/codex-stream-error-classify-20260521.md`](../plans/codex-stream-error-classify-20260521.md). |
| [CHANGELOG_141.md](CHANGELOG_141.md) | `assets-codex-user-and-ui-unify-20260521` plan closing - asset library codex side user custom completion + three-tab full sub-tab switching paradigm unification. **New support**: codex user skill falls into `~/.codex/skills/<name>/SKILL.md` (spike4 + OpenAI document demonstrates that codex CLI is automatically loaded); **user agent codex side is not supported** (plan §D3, OpenAI document clearly states "Codex CLI has skills concept only"), IPC + main double-layer hard rejection codex+agent combination; `AssetMeta.adapter` The type is tightened `'claude-code' \\| 'codex-cli' \\| null` → `'claude-code' \\| 'codex-cli'` (null is deleted, user assets also have adapter). **UI Unification**: Skills/Agents/application convention three-tab full sub-tab switch (dual CHANGELOG_137 application convention sub-tab), extract the public component `AdapterSubTab.tsx`; delete `dedupBundledByName` / `NonEmptyAssetGroup` / `AdapterBadge` three-piece double-click combination; Agents Codex sub-tab user section shows "not supported" banner; AssetEditor adds adapter prop + header chip + codex skill and deletes confirm hint; ContentViewerModal changes single adapter mode and deletes dual-adapter tab switcher. **deep-review × 2 rounds 21 fix**: reviewer-claude × 2 spawn(R1 14 fix + R2 mid-delivery collision no-shared-team fresh respawn sequel R2 equivalent full review 7 fix)+ reviewer-codex × 2 spawn(R1 14 fix + R1.5 follow-up 3 fix + R2 4 fix); 21 fix contains 2 HIGH(cold start) base_commit / changelog number dead copy name) + multiple MED (D6 old text / Codex agent banner spawn_session misleading / Step 4.5 reviewer-claude codex root does not exist / IPC test blind area / sub-tab dirty interception / codex skill delete toast trigger condition / scan partial snapshot fallback, etc.) + LOW/INFO, etc. **Phase 2 test completion + Phase 4.4-4.6 user actual test** user decision-making has been postponed, the transformation subject has passed typecheck / asset-related vitest 4/4 / pnpm build three gates; reviewer pair R2 explicitly said 0 HIGH 0 true MED plan ✅ Can be combined. Details [`plans/assets-codex-user-and-ui-unify-20260521.md`](../plans/assets-codex-user-and-ui-unify-20260521.md). |
| [CHANGELOG_142.md](CHANGELOG_142.md) | prompt Asset streamlining (8 files, 14 lines removed - user CLAUDE.md 494 / claude-config/CLAUDE.md 185 / Delete claude-config/README history section / reviewer-{claude,codex} streamlining / SKILL.md SSOT / Double README, delete changelog reference number) + fight against review fix Closed loop (REVIEW_51 double Bash and launch reviewer-claude Opus 4.7 xhigh + reviewer-codex gpt-5.5 xhigh): 2 HIGH (H1 reviewer-claude.md sandbox section double error - denyRead only lists 3 items and misses 10 items + write macOS TCC into SDK denyRead list / H2 README delete "inbox-only team UI from within CLI" Invisible" current user behavior anchor) + 1 MED (M3 SKILL spot-check misses "Code Snippet" required items) + 1 *Unverified* (U1 README "Recommended for leading sessions / feels stiff / does not affect" weak assertion violates § Constraint 3) All fixes are implemented. `heterogeneous_dual_completed: true`. SDK flow interruption error (ExitPlanMode bypass hits "No conversation found" — restart-controller.ts:182/331 leaks jsonl preflight and recoverer.ts:378 asymmetry) and is fixed in an independent plan. Details [REVIEW_51.md](../reviews/REVIEW_51.md). |
| [CHANGELOG_143.md](CHANGELOG_143.md) | `restart-controller-jsonl-precheck-20260521` plan closure - fix ExitPlanMode bypass / cut sandbox file collision "No conversation found with session ID: <sid>" error (SDK stream interruption error left at the end of CHANGELOG_142, this plan closed loop). **Root cause**: `restart-controller.ts:182/331` The two cold restart paths directly adjust `createSession({resume: currentSid})` without doing jsonl pre-check, which is asymmetrical with the `recoverer.ts:378 recoverAndSend` path. **Fixation**: Extract `jsonl-fallback.ts` helper(~290 LOC + `maybeJsonlFallback(ctx, opts)` + 4 interface including `JsonlFallbackOpts` discriminated union recover/restart) so that `restartWithPermissionMode` / `restartWithClaudeCodeSandbox` / `recoverer.recoverAndSend` three paths are shared jsonl-missing fallback - preflight (`cliSessionId ?? sessionId` dimension, §D3) + `prependHistorySummary` continued history summary + `createSession with resumeMode='fresh-cli-reuse-app'` reuse applicationSid (do not create new sessions row, do not collide with unique index, § invariant 9) + emit fallback info message (press emitContext × cwdFellBack × summary used/failed Three-axis matrix 6 copywriting §D4)+ emit role='user' message (including attachments transparent transmission). `index.ts` createSession finalize chain plus `resumeMode` guard (fresh fallback path completely skips finalizeSessionStart). `recoverer-messages.ts` adds 2 restart path-specific builders(`buildRestartJsonlMissingSummary[Used\\|Skipped]Text`). `recoverer.ts:378-491` inline fallback implementation (~113 LOC) pumped into helper (net -100 LOC + shared). **plan-review 7 rounds of closing 0 HIGH 0 true MED after implementation**;**11 invariants** (behavior is symmetrical / fresh-cli-reuse-app / applicationSid remains unchanged throughout / fail-safe / DB rollback / handoffPrompt is not empty / test coverage / existing resume path does not degrade / fallback does not adjust finalizeSessionStart + Step 3a.5 guard fully effective / fallback opts Hard constraint omit resumeCliSid / helper side effect ownership contract emit order createSession first emit then). **24 new tests** (`jsonl-fallback.test.ts` 20 contains 6 copy matrix + attachments three sub-case T9a/b/c + OR short circuit T10a/b + `restart-controller-jsonl-precheck.test.ts` 4 caller integrating two restart methods symmetrically). typecheck 0 errors / claude-code adapter 14 files 122/122 pass (including `sdk-bridge.recovery.test.ts` 20/20 non-degradation verification) / session manager-ingest 1 fail yes pre-existing has nothing to do with this plan. Details [`plans/restart-controller-jsonl-precheck-20260521.md`](../plans/restart-controller-jsonl-precheck-20260521.md). |
| [CHANGELOG_144.md](CHANGELOG_144.md) | `task-mcp-owner-session-id-rewrite-20260521` plan closure - Change the `mcp__tasks__*` tool data model from "team_id closure + global task" to "owner_session_id required + sessions reverse join" to eliminate the global task accumulation problem (REVIEW_49 root cause). **v023 schema**: DROP TABLE + CREATE TABLE new schema (`owner_session_id NOT NULL REFERENCES sessions(id) ON DELETE CASCADE` + 3 index, drop `team_name/team_id` column). **Core transformation**: repo layer `reassignOwner(oldSid, newSid)` single SQL N row adoption; tools layer `sessionIdProvider` replaces teamIdProvider + `getVisibleOwnerSessionIds` reverse join(JOIN sessions + agent_deck_team_members + double-layer archived team filter)+ same-team Write permission verification (`isCallerAuthorizedToWrite`); hand_off_session plus reassignTaskOwner after calling spawn / after adopt / archive caller before all tasks owned by caller; ipc/teams.ts two reverse join transformation + archived team filter; double prompt asset synchronization (claude-config + codex-config same paragraph §task section + §hand_off taskReassignment field description). **deep-review × 3 rounds × fix loop convergence 9 finding** (F1 reassign and archive_caller decoupled, both parties are independent ✅ HIGH equivalent / F2 task_list archived team filter codex unilateral on-site verification / F3 task reassign failed to expose ok return both parties are independent / F4 task_list total/hasMore paging claude unilateral / F5 reassignOwner does not refresh updated_at claude unilateral / F6 delete cleanup bare JSON.parse package try/catch codex LOW / F7 ShutdownAllTeammates asymmetric documented design choices / F8 codex-config synchronization codex both sides / F9 ipc/teams.ts archived team filter codex one side aligned with F2 rules); R3 double reviewer 0 finding closure. typecheck 0 errors / build GREEN / vitest 841 passed (including the previous round ✅ + 6 new tests) 1 pre-existing baseline fail has nothing to do with this plan. Details [`plans/task-mcp-owner-session-id-rewrite-20260521.md`](../plans/task-mcp-owner-session-id-rewrite-20260521.md). |
| [CHANGELOG_145.md](CHANGELOG_145.md) | `handoff-render-and-image-batch-20260521` plan closing — 3 phase UX defects + 2 round heterogeneous confrontation review. **Phase 3** claude-code adapter `createSession` misses the transmission of attachments → does not display the thumbnails (`session-finalize.ts` adds attachments field + emit spread + bridge transparent transmission). **Phase 4** Picture enlargement view (new `ImageLightbox` `fixed inset-0 z-50` + Esc keydown + parent component condition mount + unicode `✕` close, do not introduce lucide / radix-ui; pump `image-blob-cache.ts` `sharedImageBlobCache` let thumb + lightbox share cache; `UploadedImageThumb` Add optional `onClick` + message-row and add lightboxPath state). **Phase 2** hand-off cold-start prompt special rendering (new `HandOffMetadata` 5 fields schema + 10 steps cross-adapter plumbing:types/builder/MCP schemas/spawn handler/hand-off-session handler/dual adapter facade/claude finalize/codex sdk-bridge resume/codex thread-loop function signature + 2 places first-user-message emit; UI parses two markers spawn warning/late error) without metadata to prevent misjudgment of baton chain recognition. **R1 double reviewer** (claude Opus 4.7 + codex gpt-5.5 heterogeneous confrontation) 5 places finding full fix (1 true LOW generic mode phaseLabel contract consistency + 2 *unverified* LOW SSOT extraction constant / ImageLightbox delete prop + 2 INFO smooth sendMessage invariant annotation / spy extended handOff + 3 plumbing test); **R2 verify** 0 new HIGH/MED/LOW Both parties ✅ confirm. typecheck GREEN / build GREEN / 4 test files 90 tests all passed (including 5 new handOff plumbing assertions) / user 3 phase passed the actual test. |
| [CHANGELOG_146.md](CHANGELOG_146.md) | `task-mcp-merge-into-agent-deck-mcp-20260521` plan closing — task mcp 5 tool is physically merged into agent-deck-mcp namespace (`mcp__tasks__task_*` → `mcp__agent-deck__task_*` **breaking**) to let codex SDK The child process automatically gets the task tool through the existing HTTP transport (the pre-repair codex side `ensureCodex` did not inject `mcp_servers.tasks` which is a bug) + delete `enableTaskManager` independent toggle smart migration 4-case to protect old users (legacy true carry / legacy false default / explicit value skip / fresh install no-op). Added 6 files handler/helpers + deleted `src/main/task-manager/` entire directory + 39 new tests (task-crud 17 / task-events 6 / task-external-caller 13 / settings-store 4) + spoofing added 5 task tool DENY tests. 3 rounds of plan deep-review (33 finding) + 3 rounds of mixed impl deep-review (12 finding fix + 1 rebuttal ADR archived boundary + 2 plan § known follow-up). typecheck GREEN/vitest 861/862 (+39 tests, 1 pre-existing fail irrelevant). Details [`plans/task-mcp-merge-into-agent-deck-mcp-20260521.md`](../plans/task-mcp-merge-into-agent-deck-mcp-20260521.md). |
| [CHANGELOG_147.md](CHANGELOG_147.md) | Real-time / historical panel empty layout plus `h-full` + flex centering, alignment pending / team vision |
| [CHANGELOG_148.md](CHANGELOG_148.md) | `archive_plan` + `task_delete` in agent-deck-mcp 15 tool ok return field unification camelCase (eliminate historical legacy snake_case inconsistency - `archived_path/commit_hash/.../plans_index_action/spike_reports_archived{src_path,dst_path}` + `task_id/deleted_ids` (a total of 10 fields), the handler deletes the snake_case conversion layer and directly transparently transmits impl camelCase, synchronizing both ends of CLAUDE.md. The args field (snake_case convention) is unchanged. **MCP protocol breaking**. |
| [CHANGELOG_149.md](CHANGELOG_149.md) | `task-team-id-restore-20260525` plan closure (v024 task table restoration `team_id NULLABLE`) - REVIEW_53 fix (CHANGELOG_146) v023 derived team scope After the implementation, the user "evaluated mcp format consistency" additionally found lead multi-team task cross-team streaming + hand_off ownership cross-team Boundary missing 2 RFCs not covering the root cause. Revise the law and change the team from derived back to stored (owner_session_id NOT NULL is still covered, GC is not resurrected, and global tasks have accumulated pain points in the v007/v011 era). RFC 2 rounds of 7 decision points D1-D7 (D8 R2 HIGH-1 ripple plus) + 7 phases A-G + Step 1.5 deep-review 7 rounds of fix loop convergence (R5 0 HIGH design stable / R7 0 true MED closure). **MCP protocol breaking**: (1) `tasks.team_id` field recovery + `EXTERNAL_CALLER_ALLOWED.task_get` flip false (D8) + `task_get` team-scoped read instead of v023 cross-team readable (in-process lead cross-team look teammate task / external mcp client check task based on known id two types of use cases overturned); (2) `hand_off_session.team_task_policy` three-state enum optional ('clear-team' default / 'preserve-team' plus policyWarning + unadoptedTeamIds soft warning / 'skip' true delete caller team task + safeEmit per-id + DB throw fallback); (3) `task_list.team_id_filter` three-state literal (undefined / string uuid / 'null-personal') + visibleScope OR mode SQL; (4) `task_create.team_id` optional + handler verification caller active member. Total 9 commits / typecheck + build 0 errors / vitest 1038 / 900 PASS / 137 SQLite ABI skipped. |
| [CHANGELOG_150.md](CHANGELOG_150.md) | `SessionList.tsx` `renderTreeGroup` Change the recursive cap `MAX_VISUAL_DEPTH=2` (3-layer ml-3 indentation limit), fix the bug that the whole layer of L3 disappears (A→B→C three-layer spawn chain, C neither enters roots nor can get root.children → does not render at all); L4+ is tiled in L3 Peers still retain the teammate badge. typecheck + build ✅. |
| [CHANGELOG_151.md](CHANGELOG_151.md) | `handoff-no-spawn-guards-20260526` plan closure — hand-off is completely independent of spawn-guards / never write spawn-link (regardless of archive_caller / adopt_teammates value). **Deliberately overturning REVIEW_46/47** When the "archive_caller=false degenerate normal spawn" law was revised, power-user was responsible for it (the user's original words "are all equal" + "no spawn session-related checks are performed"). **MCP protocol breaking**: `opts.batonMode` rename `opts.handOffMode` + semantic upgrade (skip all three spawn-guards + never write spawn-link; original only jump depth). RFC 3 rounds + Step 1.5 deep-review 3 rounds (R1 12 + R2 6 + R3 4 finding full inline revision). Code 4 files + assets in 5 places (2 copies of prompt asset pairs) + test 6 files (2 cases deleted + whole file reversed 1 + rename 4 + 3 cases added). typecheck + build ✅, vitest 504/505 (1 pre-existing baseline fail irrelevant). |
| [CHANGELOG_152.md](CHANGELOG_152.md) | `session-list-handoff-role-badge-20260526` plan closure - SessionList is not displayed after `hand_off_session({adopt_teammates:true})` 👑 lead / ↳ teammate badge + teammate visual indentation loss fix (CHANGELOG_151 hand-off never write spawn-link side effect). **Revision**: Dual source fallback — universal team backend (DB SSOT) priority + spawn-link degradation. Use `src/renderer/lib/derive-team-role.ts` shared util to let SessionList + PendingTab share "any lead first" (HIGH-1) + `src/renderer/components/session-list-tree.ts` dual phase `computeChildrenByOwner` (Phase 1 spawn-link primary conditional inclusion + Phase 2 universal team inclusion fallback) + `isPureSpawnChain` helper (HIGH-2 blocks `archive_caller:false` after adopt caller stale spawnedBy wrong label lead);SessionList/SessionCard/PendingTab connects shared util,SessionList double phase to make newSid + teammate visual indentation level return after hand_off. **Step 1.5 deep-review × 3 rounds** (reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 heterogeneous confrontation) 34 finding all ✅ accepted (1 ❌ rejected codex INFO-1 unknown role jsdoc does not expand scope): R1 13/R2 13/R3 8, upgrade to 6 HIGH level (D4 reverse plus universal team inclusion fallback / D1 pump shared util / HIGH-A D2 Phase 1 add universal team conditional check blocking archive_caller:false stale spawnedBy / HIGH-B isPureSpawnChain independent single test / HIGH-α shift hand_off non-existent display_name field / HIGH-β scenario 4 expect visual rewrite). 21 single test pass(11 deriveTeamRole + 10 session-list-tree)/ typecheck (tsconfig.node + tsconfig.web) GREEN. The actual testing of the dev UI is postponed until visual verification after the new version of the .app is released. Details [`plans/session-list-handoff-role-badge-20260526.md`](../plans/session-list-handoff-role-badge-20260526.md). |
| [CHANGELOG_153.md](CHANGELOG_153.md) | `ref-layout-full-migration-20260526` plan closing - the agent-deck project is fully migrated from singular `plans/ changelog/ reviews/ conventions/` to unified `ref/` layout (`ref/plans/ ref/changelogs/(single → complex) ref/reviews/ ref/conventions/` + `ref/flows/ ref/architecture/` plantUML placeholder). impl hard cut `ref/plans/` without leaving fallback; synchronize 7 src/ + 7 test + 2 tool def + codex duality + project root CLAUDE.md + cross-ref link 63 places + .gitignore negation nesting + user CLAUDE.md §complex plan + §new project engineering foundation 397 lines moved to application packaging CLAUDE.md (self-contained). **MCP protocol breaking**:fallback middleware from `<main-repo>/plans/` → `<main-repo>/ref/plans/`. Phase F deep-review R1+R2 14 in total, fully repaired. Total 16 commit / typecheck + vitest 931 pass / 0 true residue. |
| [CHANGELOG_154.md](CHANGELOG_154.md) | `build-dir-migration-20260526` plan exit - all build products of the agent-deck project are unified under the `build/` root exit (electron-vite outDir 3 entry → `build/{main,preload,renderer}` + electron-builder output → `build/dist`) to align application packaging CLAUDE.md §src/build canonical standard (ref-layout-full-migration CHANGELOG_153 retro). impl hard cut `build/` without leaving `out/ release/` fallback; synchronization 3 config(electron.vite.config.ts / package.json / .gitignore)+ 4 doc/src(src/main/window.ts:22 comments + README.md:107 + CLAUDE.md:210/214 + ref/conventions/tally.md:79 P26 entry). **MCP protocol has no breaking** (changing the build artifact placement point does not affect the tool interface). deep-review × 3 rounds of fix loop convergence 22 finding (R1 12+3 / R2 6+3 / R3 5+2): 0 refutation + 0 new HIGH boundary conditions, code + config + src comments are completely correct in nature, finding is all plan narrative trace drift / cosmetic. Phase F full set of verification: typecheck ✅ + build ✅ (3 bundles in build/{main, preload, renderer}) + vitest ⚠ partial (665 passed / 69 pre-existing electron binary fail related to this plan 0) + dist ✅ (DMG 254.9 MB build/dist/mac-arm64) + grep 0 residual ✅;F.6 dog-fooding(spike5 .app Reinstall 6 steps + spike2 pnpm dev) and leave user to self-verify (lead is strictly prohibited from running pkill to avoid SDK child process cascade kill). Details [`plans/build-dir-migration-20260526.md`](../plans/build-dir-migration-20260526.md). |
| [CHANGELOG_155.md](CHANGELOG_155.md) | `prompt-asset-review-optimize-20260527` plan closing - prompt word asset SSOT boundary finishing implementation 4 requirements: application CLAUDE.md self-contained does not depend on user / user CLAUDE.md does not bind agent-deck / priority chain explicit declaration SDK preset security > user > this file (spike1 empirical SDK 0.3.144 No prepend (semantic revision)/Simplified information density (project root CLAUDE.md 248→194 lines -22%). 3 rounds of decision-making confrontation review×4 revision round (v1→v2→v3→v4→v5)+ 1 round final review fix loop:v1 out 2 HIGH+6 MED→v2;v3 add scope project root→v4;v4 review 0 HIGH+3 MED→v5;final review reviewer-codex catch codex-config 2 true MED(hand_off_session signature leak team_task_policy + abandoned manual process self-ref null pointer)→ surgical fix. Changes 5 SSOT assets (claude/codex-config CLAUDE.md+CODEX_AGENTS.md+README+2 SKILL) + 1 project root CLAUDE.md (-22%) + 3 codex-config skills mirror (sync generated) + user CLAUDE.md 0 changes (D3 decision). **MCP protocol no breaking** (only prompt asset literal + cross-ref / self-ref reorganization). Details [`plans/prompt-asset-review-optimize-20260527.md`](../plans/prompt-asset-review-optimize-20260527.md). |
| [CHANGELOG_156.md](CHANGELOG_156.md) | Application packaging CLAUDE.md §src/build standard directory structure section relaxes the build product root exit from "`build/`" to "`build/` or `dist/` choose one (unified within the project)"; §.gitignore required entries `build/` + `dist/` double defensive addition. Cross-project execution at the same time personal/next 6 engineering projects (agent-deck-image-mcp / angry-birds / vocabmaster / desk-assistant / dev-config-hub / daily-brief) align new conventions + 2 non-engineering projects (algorithm-learning / literary-appreciation) plus non-engineering identification CLAUDE.md Explicitly declare not to be universal § New project engineering foundation. All projects verify exit 0. |
| [CHANGELOG_157.md](CHANGELOG_157.md) | ad-hoc prompt-asset deep review (`agent-deck:deep-review` SKILL × 4 round double heterogeneous confrontation + rebuttal round + three-state ruling, no plan / no worktree). **Key dead-letter fix**: user follow-up questioned the judgment of "codex SDK does not accept per-thread model override" in the code comment → Empirical evidence `@openai/codex-sdk@0.131.0+` `ThreadOptions.model` already supports per-thread override + application `sdk-bridge/index.ts:483/496` startThread/resumeThread active **not** spread model field (one line of dead-letter), after the modification, the codex frontmatter `model` really takes effect. **New settings field**: `codexSummaryModel` + `codexHandOffModel` benchmark claude haiku/sonnet priority chain (default empty = fallback `~/.codex/config.toml`, zero behavior change). **MCP `hand_off_session.cwd` describe callout**: Added `HAND_OFF_SESSION_CWD_CONTRACT` callout containing 4 paragraphs of SSOT references, and the field describe is reduced from ~400 words to ~200 words. **Prompt-asset revision ~25 places**: `reviewer-claude.sh.tmpl` deleted `conventions/` narrative + `CODEX_AGENTS.md:224` half-width → full-width Phase + wording executable + cwd not persistent across turns double-ended alignment + `sync-codex-skills.mjs` added `SKIP_SKILLS=['flow-arch-plantuml']` (the SKILL contains claude-only `AskUserQuestion`/`Read`) + 12 out-of-date jsdoc/comment sweeps across files + R4 "same model, same style" wording 4 repairs. typecheck GREEN / grep 0 true residue. **Follow-up backlog**: codex model regression test / `SummarySection.tsx` UI new field / settings trim verification / transparent configuration exclusion / shortcut key description section. |
| [CHANGELOG_158.md](CHANGELOG_158.md) | Settings UI closed 3 items follow-up (undertaken from CHANGELOG_157 deep review + user follow-up): **LOW-1** `SummarySection.tsx` Add codex side `codexSummaryModel` + `codexHandOffModel` ModelInput + claude side hint change cross-adapter expression; ModelInput.commit Already `.trim()` makes **LOW-R2-1 automatically eliminated**. **Transparent configuration exclusion** (user request): `WindowSection.tsx` deletes `windowTransparent` toggle + long hint section, field/runtime/global shortcut key Cmd+Alt+T does not move; `IS_DARWIN` import deletes easily. **New `KeyboardShortcutsSection.tsx`** (user request): centralized list of 4 global shortcut keys (Cmd/Ctrl+Alt+P top / T transparent / = maximize / - back to default), platform diversion mod string, table layout + jsdoc annotation SSOT in `src/main/index.ts §10-10.6`. `SettingsDialog.tsx` After connecting the new section to the "Alerts and Appearance" group WindowSection. typecheck GREEN. The remaining **MED-2 codex model regression test** independent commit. |
| [CHANGELOG_159.md](CHANGELOG_159.md) | CHANGELOG_157 R2 reviewer-codex MED-2 follow-up — codex frontmatter `model` true regression test (6 unit test all pass). Create new `src/main/adapters/codex-cli/__tests__/codex-model-passthrough.test.ts` mock `getCodexInstance` returns fake Codex (startThread captures ThreadOptions), verifies that `runCodexOneshot.opts.model` is passed through to ThreadOptions.model + boundary case (undefined / '' / all spaces trim+skip / before and after spaces trim clean value) + Other ThreadOptions fields (sandboxMode / approvalPolicy / skipGitRepoCheck / modelReasoningEffort) are not affected by model spread. **CHANGELOG_157+158+159 Three commits jointly close deep review R2/R3 follow-up backlog 5 items** (LOW-1 + LOW-R2-1 + MED-2 + transparent configuration exclusion + shortcut key section). |
| [CHANGELOG_160.md](CHANGELOG_160.md) | Settings UI reconstruction - three-tab switching + AgentDeckMcpSection simplification + deletion of CodexMcpServersSection reference + mcpStdioEnabled default ON. **Three tab** (general 9 section 4 group / claude code 1 section + placeholder / codex cli 0 section + placeholder): activeTab state per-open reset 'general',`<nav role="tablist">` The three button active state is highlighted. **AgentDeckMcpSection Simplification**: Delete the two Toggle + hint blocks of HTTP / stdio and replace them with 3 lines of transport table (in-process / HTTP / stdio, each containing endpoint + auth requirements); transport sub-switch fields `mcpHttpEnabled` / `mcpStdioEnabled` are persisted - the UI is not exposed, the user wants to fine-tune and edit `settings.json`. **Delete CodexMcpServersSection reference** (User: "Delete codex mcp regardless of mcp"): Only delete SettingsDialog import + usage, `.tsx` file is retained (`settings.codexMcpServers` field + `~/.codex/config.toml` marker section synchronization logic is all retained). **Default value**: `mcpStdioEnabled: false → true` aligns with "all enabled by default". Claude tab contains HookSection + Claude MCP user-direct-edit description placeholder; Codex tab contains ~/.codex/config.toml direct editing description + cross-ref general tab's codexSummaryModel / codexHandOffModel. typecheck GREEN. |
| [CHANGELOG_161.md](CHANGELOG_161.md) | `prancy-forging-penguin` plan closing — Settings panel hand-off / summary model provider + reasoning can be configured. **Field reconstruction**: 4 fields (claude/codex × summary/handoff) → 6 fields (summary/handoff × provider+model+reasoning), REMOVED_KEYS clear orphan `codexSummaryModel` / `codexHandOffModel`. **Routing changes**: summary / IPC handoff dispatch is changed to `settings.summaryProvider` / `handOffProvider` to select adapter (no longer press session.agentId - claude session may also be summarized by codex SDK, and vice versa, user is responsible). **handOff default changed haiku**:fallback alias from `sonnet` → `haiku` to align with summary, env chain ANTHROPIC_DEFAULT_SONNET_MODEL → ANTHROPIC_DEFAULT_HAIKU_MODEL. **codex reasoning can be configured**: `modelReasoningEffort` changed from hardcoded(summary='low' / handoff='medium') to `settings.summaryReasoning` / `handOffReasoning`, UI 4 levels dropdown (minimal/low/medium/high), codex-runner type expanded to 4 levels. **UI redo**: `SummarySection` 4 ModelInput → 2 row (Provider+Model+Reasoning three controls side by side), Reasoning disabled when Provider=claude. **Delete autoSummariseOnFallback toggle**:`ExperimentalSection` Delete toggle UI + Toggle import, the field retains the default true and cannot be configured (settings.json can still be modified manually when cost is sensitive). **Key boundary**: IPC handoff handler must distinguish summaryAdapter (provider-driven) vs sessionAdapter (session.agentId-driven), the latter is used in Stage 2 createSession. typecheck GREEN / hand-off.test.ts 6/6 pass (2 fail, all passed after repair). |
| [CHANGELOG_162.md](CHANGELOG_162.md) | Hand-off default changes back to sonnet (overturning the haiku decision of CHANGELOG_161) + SummarySection `ModelRow` layout rearrangement repair extrusion. **runner fallback alias**:`summariseSessionForHandOff` Priority chain `settings.handOffModel ＞ ANTHROPIC_DEFAULT_HAIKU_MODEL ＞ ANTHROPIC_MODEL ＞ 'haiku'` Change back to `... ＞ ANTHROPIC_DEFAULT_SONNET_MODEL ＞ ... ＞ 'sonnet'`. Reasons: 4-section structured briefing is sensitive to structure accuracy/context compression quality, sonnet is significantly more stable; summary short tag-line is fault-tolerant, high-volume and large-scale, and haiku saves costs. The default inconsistency between the two sides is intentional. **ModelRow rearrangement**: SettingsDialog container 340px - p-4 = 308px, single-line label(w-32) + provider + input(flex-1) + reasoning(w-24) + 3 gap. Press the input to 0 width (user screenshot feedback "Model box is too small"). Change to a 3-line layout: label on a single line → provider+model input(flex-1)+reasoning(w-20) full width on a single line → hint on a single line. The input now takes ~140px, which is enough to display `claude-sonnet-4-6` / `claude-opus-4-7-thinking-max`. **hand-off row copywriting synchronization**: hint / placeholder is changed from haiku back to sonnet, summary row remains haiku unchanged. test synchronizes SONNET env / 'sonnet' alias / 'claude-sonnet-4-6' mock values. typecheck GREEN/6/6 pass. |
| [CHANGELOG_163.md](CHANGELOG_163.md) | UI copywriting streamlined + popularized review × fix(`agent-deck:deep-review` SKILL Round 1 heterogeneous confrontation + three-state ruling): delete SDK / DB / IPC across 30 files (settings sections / main dialog / TeamDetail 9 files / SessionDetail / pending-rows / assets / activity-feed / miscellaneous) Internal terms exposed (`owner_session_id` / `caller_session_id` / `bypassPermissions` / `allowDangerouslySkipPermissions` / `sandbox=danger-full-access` / `leftAt = now` / `archived_at` / `lifecycle` field value / `kind` field / `send_message` function name / `mcp__agent-deck__spawn_session` full namespace path / `Seatbelt` / `bubblewrap` / `marker / build-time installer`, etc.); unified Chinese and English terminology (`lead` → "person in charge" / `teammate` → "collaborator" / `member` → "member" / `pending / delivered / canceled` → "to be sent / delivered / canceled" / `lifecycle` Three-level translation); Added `lifecycleLabel` / `roleLabel` / Chinese `relativeTime` / `statusBadge` centralized helpers; 3 dangerous pop-up windows (completely asking no questions / Codex fully open / Claude closed sandbox) comprehensive user-oriented rewrite; sandbox files are aligned across 3 places (ExperimentalSection / SandboxSelects / NewSessionDialog) SSOT; cancel toast The three articles of copywriting are templated and converged; `⚠` half-width → `⚠️` unified with full-width. Consensus 8 categories HIGH (independently proposed by both parties) + unique to each party 9 HIGH (grep verify) + 8 MED + 12+ LOW/INFO All implemented; 0 refutation / 0 ❌. `pnpm typecheck` has 0 errors in total, and the component props type contract remains unchanged. |
| [CHANGELOG_164.md](CHANGELOG_164.md) | UI copywriting Round 2 Heterogeneous confrontation review × fix (undertaken from CHANGELOG_163, SKILL multi-round digging): **Fix the 4 types of punctuation inconsistencies introduced by R1 fix** (N4 markdown literals are not rendered in Electron native dialog / N7 half-width question marks in 7 places / N8 half-width failed in 6 places / N15 ⚠ Half-width 2 + **N2 Claude literal hardcoded 8 removals** (not allowed for codex sessions - pending protocols are shared across adapters) + **N3 External CLI internal terminology 9 unified** (users don’t know what CLI is) + **N9 SettingsDialog tab case inconsistent** (`claude code` → `Claude Code`)+ **N10 PermissionsView Internal field name**(`cwd` / `defaultMode` / `allow/deny/ask` / `User Local`, etc. Chinese)+ **N11 PendingTab R1 term**(lead/teammate / SDK channel / batch approval select)+ **N1/N17 a11y aria-label 7 Chinese culture + IconButton Add aria-label** (Screen readers read symbols instead of action names) + **N13 diff/renderers/* Developer word removal** (NEW/before/after/Monaco/file_changes, etc.) + **N14 SummarySection + Diagnostic internal field removal + Add next action suggestions** + **N16 "Only effective for new sessions" 5 variants unified** + **H8 sandbox dangerous position label fully rewritten + tooltip**(SelectRow options plus `title?` field, the difference between the 3 files can be seen clearly from the label: `⚠️ Don’t ask again (still in the system sandbox)` / `⚠️ Fully open (can change any file / network / start the process)` / `⚠️ Close (no system sandbox)`) + **LOW semi-full-width bracket correction** (Chinese context `(...)` → `(...)` about 15 places). **SKILL learning point**: When R2 spawns, re-spawn the reviewer that has been shut down in R1. You cannot use "Round N+1 continuation" framing - the reviewer-codex reports FRESH SESSION rejection. Change it to "Round 1 with prior fix context" (the skip field is reserved as the pre-context) to accept the new framing. There are ~80 changes in ~22 files in total, `pnpm typecheck` has 0 errors in total. |
| [CHANGELOG_165.md](CHANGELOG_165.md) | personal task no longer ingests `team-task-*` event (behavior contract change): Fix personal task completion/creation in SessionDetail ActivityFeed + TeamDetail EventsSection to spew out "team-task-*" noise (kind name with team does not match personal semantics - v024 After making personal the first-class default, caller runs its own todo and still sprays a team event which is noise), handler added `teamId` / `team_id` guard the source skip ingest(eventBus.emit('task-changed') will not be affected to ensure the real-time performance of the UI, and the historical events table row will not be deleted (a plan); **ActivityFeed SimpleRow rendering completion** describe.ts Add 3 cases to let team-task-created / team-task-completed / team-teammate-idle display description + teamName + teammateName (original default only displays `e.kind` string handler carefully filled payload, no one renders it); **dedupOrClaim early revision** remove `event.source === 'hook'` restriction (CHANGELOG_40 comment "SDK channel does not emit these kinds" and CHANGELOG_56 §A has drifted) + jsdoc rewrite explicit hook/sdk Both sources go early and return without relying on the bottom 5 sections. Test 5 contract reverse (personal ingest → personal skip) + 2 HTTP fixture changes to team task (pure certification D7 transport guard, personal has been swallowed by the new guard and cannot be pure certification). `pnpm typecheck` 0 error. |
| [CHANGELOG_166.md](CHANGELOG_166.md) | UI copywriting review Round 3-5 convergence (taking over CHANGELOG_163/164, SKILL true convergence): **R3 extracted 3 i18n helpers**(`agentIdLabel` / `eventKindLabel` / `fileKindLabel`)+ 12 raw enum / id leak replacements; **R4 repaired 4 Fix ** (ImageThumb alt dead code / SandboxSelects 7 OS·starter process residual / message-row hand-off badge spawn wrong label relay repair - `HAND_OFF_SPAWN_HEADER` is lead context marker not hand_off, change "Context·Dispatch" / EventsSection JSON.stringify fallback change kind user summary);** R5 fix 2 fixes LOW** (message-row disclosure summary is aligned with R4 badge / EventsSection session-end case reuses `translateSessionEndReason` helper to avoid dual maintenance). **SKILL.md Step 5+6 invariant revision**: The user questioned "why the session is shut down every time" in the R5 stage and went straight to the root cause - Step 6 "end" literally means the end of each round, and the "until the end" at the end of Step 5 does not clearly define the anchor point of the end, resulting in a false shutdown in each round that violates the "reuse of the same pair of teammates" invariant (R2 collision reviewer-codex FRESH SESSION rejection is the symptom). Change the end of Step 5 and add "Do not proceed to Step 6 until the § closing judgment is satisfied; never shutdown during iteration" + Change the title of Step 6 to "**final** closing (executed only after the § closing judgment is satisfied)" + Make the pre-constraint explicit at the beginning - upgrade the reviewer reuse invariant from a hidden callout to an explicit hard constraint. SSOT `resources/claude-config/agent-deck-plugin/skills/deep-review/SKILL.md` + sync codex mirror. R5 personally verified the revised behavior: do not shut down the R4 reviewer pair, directly reuse the same send_message to send R5 prompt, reviewer cross-wheel mental model persistence. R1+R2+R3+R4+R5 has a total of ~234 user-visible copywriting simplification + popularization, ~35 renderer files, and the helper extracts clean contracts (agentIdLabel / eventKindLabel / fileKindLabel / lifecycleLabel / roleLabel / translateSessionEndReason). **Convergence standard achieved**: Both reviewers are "combinable" + 0 HIGH/MED + LOW full fix. `pnpm typecheck` full 0 error. |
| [CHANGELOG_167.md](CHANGELOG_167.md) | settings panel flex single-row layout extrusion batch repair (CHANGELOG_161+162 same as pattern follow-up):**(1) ExperimentalSection two sandboxes select** — Codex longest option `"⚠️ completely open (can change any file/network/run any command)"` 27 words burst 308px container handle span Squeeze it into a vertical single character width, same as Claude Code fragile;**(2) AgentDeckMcpSection Token "Copy" button** — input `w-full` Explode the container and squeeze the button into a vertical single character width. The common root cause is that the default `min-width: auto` for sub-elements in a flex container, when a long input / select is stretched, flex cannot shrink → squeeze the short brother. Modification method alignment SummarySection ModelRow follow-up pattern: two sandbox divs are changed to 2 row layout (label `<div>` + select `w-full`), Codex `danger-full-access` option label is shortened to `"⚠️ Fully open (unrestricted)"` + detailed description is put in `title` attribute + hint to supplement three files bullet alignment Claude Code; MCP Token input add `min-w-0 flex-1` + copy button plus `shrink-0`. `pnpm typecheck` 0 error, renderer HMR automatically pushed. |
| [CHANGELOG_168.md](CHANGELOG_168.md) | Prompt word asset user home directory dependencies are moved to the project self-closing loop: `~/.claude/{SOPs,templates}/` 11 assets are moved to `resources/{SOPs,templates}/`(file-size-guardrail.md / file-level-review-expiry.sh / codex-cli-stuck-lessons.md + 8 markdown templates + reviewer-{claude,codex}.sh.tmpl);**§Decision-making confrontation section inline to application CLAUDE.md**(63 lines, user CLAUDE.md SSOT is completely moved over, completely self-contained without cross-ref callout in line with the original intention of "not mentioning external resources");**18+ All active asset references are removed**Project relative path (project root CLAUDE.md ×2 + application CLAUDE.md ×6 + CODEX_AGENTS.md ×1 + SKILL.md ×6 + 2 reviewer agent body + README + 7 embedded references to assets); add 2 extraResources to package.json to allow .app to be packaged with SOPs/templates; delete 10 user-level redundancies and retain 3 self-references of user CLAUDE.md (codex-cli-stuck-lessons.md/ reviewer-{claude,codex}.sh.tmpl (dual copies are maintained independently). Historical archives (ref/plans / ref/changelogs / ref/reviews / ref/conventions) 19+ historical snapshot references are completely unchanged (the immovable contract is written); SDK runtime credentials/jsonl path/metadata configuration level description/`~/.codex` codex's own configuration dependencies are not counted as references. user 5 rounds of AskUserQuestion decision-making, no move §Decision-making confrontation. |
| [CHANGELOG_169.md](CHANGELOG_169.md) | Deep-Review batch A fix(mcp tools handler):**2 HIGH + 8 MED = 10 required finding** One-time closure. **F1 split** (reviewer-claude+codex consensus HIGH):archive-plan-impl.ts 1488→~1170 archive-plan/precheck-helpers.ts(mainRepo dirty + base_branch verification)+ index-sync-helpers.ts(escapeTableCell + formatChangelogCell + syncPlansIndex + upgradeIndexHeader), re-export to ensure that the 4 test files import path have zero changes; schemas.ts 1215 + hand-off-session.ts 1249 adopt the "really can't be dismantled" protection list (70% are tool description SSOT / handler closure 10+ variable extraction sub-module, which will reduce readability). **F9** Extract _shared/default-impl-deps.ts so that 4 impl(archive-plan / hand-off / enter-worktree / exit-worktree) can share runGit/readFile/writeFile/unlink/mkdir/mvDir/exists/realpath/cwd/homedir 10 fs default helpers (behavior single point maintenance). **Function fix**: **F2 [HIGH]** archive_plan plan_id↔worktree_path frontmatter cross-check (reviewer-codex refutes promotion HIGH, silent corruption, both plans die together) + optional branch naming soft warn;**F3 [MED]** hand-off worktree-missing finalCwd limits mainRepo subtree (comments vs code drift,/tmp silent release → ENOENT);**F4 [MED]** baton-cleanup archive_caller=false skip phase 1 marked skipped='archive-caller-false-keep'(align schema copywriting commitment caller can still see reviewer reply implies teammates alive);**F5 [MED]** preserve-team safety use findActiveMembershipIn repo actual measurement to replace trust spawnData.teamId(spawn addMember fails only warn not set null, task has been transferred but write permission reject); **F6 [MED]** archive_plan INDEX RMW solo flight lock (reviewer-claude HIGH→reviewer-codex refutation drop MED, IIFE package async + .then(_,_) swallow rejection in Map + finally delete reference recoverer pattern); **F7 [MED]** spike-reports rmdir failure no longer swallow push warnings (matching deps.rmdir injection mock controllable); **F8 [MED]** srcSpikeDir==dstSpikeDir guard reuses the same path.resolve mode in step 12 (plan does not accidentally return spikeReportsArchived non-null when in ref/plans/); **F10 [MED]** marker release is advanced to step 13 after commit (when step 14 fails, the marker is cleared, the caller runs away and the plan does not hit the stale marker reject). test updates 6 files to cover contract changes + branch name convention. `pnpm typecheck` + `pnpm exec vitest run` (80 files / 968 tests pass / 158 skip / 0 fail / 0 error) + `pnpm build` all passed. |
| [CHANGELOG_170.md](CHANGELOG_170.md) | Deep-Review Batch B fix (sdk-bridge double-ended): **1 HIGH + 3 MED = 4 src required** One-time closure. **F1 [MED]** Double-ended recoverer single-flight lock covers the entire archived unarchive segment (reviewer-codex R1): Move the unarchive + placeholder message dedup from outside IIFE to inside IIFE, and the recovery.set synchronization registration prevents archived session concurrent sendMessage and double createSession from destroying invariants. **F2 [MED]** codex createSession top-level try/catch + 4 resources best-effort cleanup(reviewer-codex R1): codexBySession.delete + mcpSessionTokenMap.release + sessions.delete + (when resume) releaseSdkClaim, and claude createSession L31-165 try/catch closing template form a cross-adapter parity Shut up. cleanup all idempotent, thread-loop earlyErrCb safe for repeated calls. **F3 [HIGH]** claude createSession catch block double key cleanup sessions Map (reviewer-claude R2 cross-adapter parity counter-evidence codex L799): catch sessions.delete(tempKey) changed to sessions.delete(internal.applicationSid); sessions.delete(tempKey); repair plan reverse-rename-sid-stability §A.4-pre §S2 introduced sessions.set Cut applicationSid but catch is not synchronized regression — resume path applicationSid=opts.resume≠tempKey old delete no-op → opts.resume entry leak → subsequent sendMessage hits stale internal skip recoverer main path → silently stuck. Three-branch coverage (resume / spawn without first realId / spawn first realId cut but throw). **F4 [MED]** codex translate.ts ErrorItem LOADER_WARNING_PATTERNS keyword filter (lead found on site user screenshot):LOADER_WARNING_PATTERNS=['Ignoring malformed','failed to deserialize'] Hit console.warn does not pollute the UI, miss maintain the original emit error:true red bubble. The pre-repair user spawn reviewer-codex saw 15 codex CLI .codex/agents/*.toml schema errors and loader noise red bubbles. **Verification**: typecheck 0 error + 16 files 170 tests pass 0 fail. Zero regression (all belong to the filling category and do not change the main path behavior). **follow-up**: codex jsonl-missing fallback Missing LLM summary prepend (source annotation plan already exists) + test to fill in 2 gaps + other loader pattern extensions (waiting for user submissions). |
| [CHANGELOG_171.md](CHANGELOG_171.md) | Deep-Review batch B R4 split refactor (sdk-bridge double-ended single file size guardrail + cross-adapter parity): R1-R3 leaks cover single file size guardrail dimension, R4 supplements split-feasibility special review. **A [INFO]** claude sdk-bridge/index.ts adds §guard list jsdoc 60 LOC to list 7 reasons for not moving files (file-size-guardrail SOP §file 3, aligned with hand-off-session.ts L8-22 template): 12+ helpers extracted / closure ~10 spans / canUseTool 6 deps two-way coupling / Three-state strong coupling / 4 protected wrappers test seam / plan reverse-rename line number reference / catch block mild candidate P1 does not enter the file 2 threshold. Reason: reviewer-claude R4 file 3 proposition + lead on-site reinforcement argument "Moving out the entire helper section is still ~500+ LOC in actual testing. Just changing files does not reduce the real complexity", reviewer-codex R4 file 1 proposition R5 actively accepts the overthrow. **B [MED]** codex sdk-bridge/index.ts 1010→874 LOC (-13.5%) pump 3 helper (reviewer-claude R4 fine-grained is better than reviewer-codex R4 entire segment move out createCodexSession solution): thread-options-builder.ts buildCodexThreadOptions (49 LOC pure builder replaces resumeThread/startThread dual branch spread)+ create-session-rollback.ts runCreateSessionRollback(96 LOC 4 resources best-effort idempotent cleanup, aligned with closeSession template)+ resume-path-await.ts awaitResumedThreadStart(191 LOC Promise three-state state machine 30s timeout+onFirstId+earlyErrCb 4 resources cleanup). **D [MED]** codex sdk-bridge/recoverer.ts 617→597 LOC mirror claude pump 2 helper(cross-adapter parity maintenance drift cost driver):codex-recoverer-messages.ts 3 builder(cwdMissingError/cwdFallbackInfo/jsonlMissingNoSummary mirror claude recoverer-messages.ts)+ codex-jsonl-fallback.ts maybeCodexJsonlFallback (142 LOC mirror claude jsonl-fallback.ts same Ctx/Opts/Result interface form, simplified version without LLM summary prepend). **Verification**: typecheck 0 error + 16 files 170 tests pass 0 fail + 5 helper Literally equivalent to the original inline (R5 detailed comparison verifies zero behavior change). **Heterogeneous complementarity R5 learning points**: Reviewer actively accepts overthrowing one's own R4 stance is a benign phenomenon - the purpose of heterogeneous confrontation is to converge true knowledge rather than stick to one's stance, and be persuaded by measured data rather than adhere to dogma. **follow-up**:C claude recoverer.ts runRecoverySingleFlight(minus ~10% borderline)+ A mild candidate catch block runCreateSessionRollback(< 5% below threshold)+ F5 codex LLM summary prepend independent plan + R4 §C/§D 4 helper fine-grained solution volume management follow-up. |
| [CHANGELOG_172.md](CHANGELOG_172.md) | Deep-Review Batch C R1+R2+R3 three-round closing (7 files 4140 LOC): **6 required fix + 2 regression test** one-time closing. **R1 5 fix (commit d5549c6)**: MED-A window.ts stale this.win reference after BrowserWindow is destroyed (codex verification ironclad evidence) create() registration once('closed') + showOnce plus isDestroyed protection; MED-B index.ts two fatal app.exit(1) bypass closeDb (codex Electron document ironclad evidence) best-effort Run closeDb to ensure SQLite WAL checkpoint;LOW-α agent-deck-message-repo retryAfterFail final retry do not write attempt_count (codex verification ironclad evidence) final retry single UPDATE at the same time change status + attempt_count;LOW-1 window.ts flash() re-entry pollution baseline opacity (claude verification) timer mentioned instance state + second entry first clear + reset;LOW-β task-repo subject LIKE wildcard does not escape (codex verification ironclad evidence) escape % _ \\ + ESCAPE '\\' repairs the search semantic offset. **R2 LOW (commit 5d389cf)**:window.ts generation guard three-layer defense (codex catch reviewer-claude FIX-2 small supplement leak "uninstall winA + create winB again" two-step pollution surface - old 1.5s fallback / flash setInterval cb / 'closed' listener read mutable this.win across close+recreate operation new winB):create() entry const capturedWin captures this generation + 'closed' listener and adds `this.win !== capturedWin` generation guard + synchronous clearing 4 resources (stopInvalidateLoop / flashTimer / fallbackShowTimer / this.win=null) + 1.5s setTimeout handle storage instance state + flash setInterval cb same generation guard + close() double insurance synchronous clearing. **R2 INFO 2 test (commit 5d389cf)**:agent-deck-message-repo.test.ts retryAfterFail final case plus attemptCount===3 + statusReason containing 'attempt=3' lock R1 LOW-α contract; task-repo.test.ts added subjectKeyword LIKE wildcard literal match 4 case lock R1 LOW-β contract. **5 downgrades INFO not fixed**: MED-1 kickRepaintAfterPin BY DESIGN / MED-2 agent-event none debounce by design / MED-3 listBySession annotation plain text trade-off / LOW-2 cleanupBlocksRefs filter monotonicity confirmed non-bug / LOW-3 LOW-4 excessive defense. **Dimension 5 ≤500 LOC 7 files are all exceeded** This commit will not split the registration protection list + the candidate independent plan will be split in batches (to avoid fix + split mixed 1 commit blame radius difficult to pull). **Heterogeneous duality value textbook case**: reviewer-claude R2 FIX-2's small supplementary leakage point was caught by reviewer-codex R2 - the homologous double Claude will leak this R2 LOW at the same time, and the heterogeneous duality value is once again proved. **SKILL Learning Points**: R2 reviewers complement each other, R1’s own leakage value; unique to each party, LOW + lead on-site verification + dual reviewers, the same risk support does not need to go through the rebuttal round; R3 strictly only looks at the R2 fix-to-fix scope and quick PASS. `pnpm typecheck` 0 error; SQLite test binding ABI skips by default (CLAUDE.md §Packaging trap list and gatekeeping, user.app will run automatically when the binding is repaired). |
| [CHANGELOG_173.md](CHANGELOG_173.md) | Prompt word asset review R1+R2 closing (9 copies prompt asset 1923 LOC): **17 required fix** (R1 13 + R2 4) one-time closing. kind='mixed' dual mode parallel reviewer-claude `dace5ba1` Opus 4.7 + reviewer-codex `019e6e9e` gpt-5.5 xhigh across adapter native pair. Guidelines user CLAUDE.md § Tip words Asset maintenance 5 hard constraints (information density/current facts/executability/scope and failure cover/example restraint). **Rebuttal round 2 HIGH ❌→MED**:C-HIGH-1 .gitignore missing entry not abort → MED UX improvement explicit confirm `ack_cache_unignored`;C-HIGH-2 mixed reviewer fail continue without blocking → MED fallback priority chain callout ①retry→②compliance cover →③downgrade unilaterally. **R1 13 fix (commit b41cdce)**: claude/codex mirroring at both ends SSOT drifts in 4 places (NO MSG ANCHOR recommendation → please / clear-team recommendation + compatibility → the widest range of applicability / shared-team recommendation → two-way conditions → action / trivial) + current facts do not write history in 3 places (deprecated → archived / deleted P5 Round/REVIEW_46/47/original name batonMode/followup traces of historical revisions) + 1 weak assertion execution (reviewer-codex frontmatter "self-confirmation" changed to fallback reference) + 1 clear boundary (reviewer-claude.md Fresh session "CLI implicit fork" double meaning split) + 3 callout improvements (SKILL §65 ack_cache_unignored / SKILL §99+§207 fallback priority chain / CLAUDE.md codex side plantUML method) + design intent inline comment 2 (weak assertion list reviewer body independent injection description / baseline § priority adapter difference description). **R2 4 fix (commit efd0702)**: R2-MED-1 deprecated R1 fix Edit tool race is not true land (infer 33497 token oversized and use system-reminder fallback copy) + flow-arch SKILL missed modification force grep verify;R2-MED-2 SKILL §Scope schema add `ack_cache_unignored?: boolean` Field definition (fix non-executable vulnerability); R2-MED-3 codex side plantUML method rewrite remove -tpng rendering + add strict prohibition -tpng/-tsvg + change § do not render SSOT manual editing .puml + optional -syntax check (fix protocol contradiction); R2-LOW-1 CLAUDE.md:127 "If the environment provides... it is possible" change the fact sentence "Agent Deck" The compliance branches of the application environment are detailed in Section §...". **Heterogeneous duality value textbook case**: reviewer-codex "Edit tool race to capture fs status quo + git show commit diff + skip field three-way cross-validation" unique weapon to catch the silent bug of R1 fix Edit tool race is not true land - reviewer-claude R2 time point judgment deprecated "Legal state semantic non-violation" misses the ironclad evidence, homologous double Claude will leak this R2-MED-1 at the same time. **SKILL learning points**: prompt asset review is stricter than code review (prompt is a protocol directly enforced by LLM); the Edit tool race must grep verify the real land before committing; R1 fix introduces new content and must be reviewed again; lead adopts the more stringent side when adjudicating conflicts; kind='mixed' dual mode parallelism is suitable for prompt asset, a dual-identity asset such as "convention code = design document". The codex SKILL mirror is synchronized by sync-codex-skills.mjs to synchronize the local working tree (.gitignore:18 excludes git,SSOT = claude SKILL). `heterogeneous_dual_completed: true` |
| [CHANGELOG_174.md](CHANGELOG_174.md) | plan deep-project-review-comprehensive-20260528 Phase 2 (B-dimensional prompt word asset streamlining) complete archive — 5 copies of prompt asset streamlining (4 commit +3 LOC, information density increased rather than LOC reduced) + 2 rounds of deep-review SKILL kind='mixed' Heterogeneous confrontation R1+R2 Total 5 finding 4 actionable fix 1 INFO Reserved 0 HIGH/MED Compatible by consensus of both parties (commit 5347c14 + plan §D9 wording precision) + tally.md P35/P36 precipitation candidate (P35 claude/codex mirror dual grep actual measurement / P36 plan row-level reference grep+wc+ls actual measurement ironclad evidence) |
| [CHANGELOG_175.md](CHANGELOG_175.md) | plan deep-project-review-comprehensive-20260528 Phase 3+4+5+6 complete archive (C/A/D three-dimensional + experience accumulation) — Phase 3 C-dimensional 17 photos plantUML popular rewrite (8 architecture + 9 flows + INDEX summary ≤ 80 words in vernacular, R1-R4 4 rounds of heterogeneous confrontation 0 HIGH/MED closing); Phase 4 A dimension 13 large file facade pattern split (11 step 9821→12552 LOC +27.8% readability tax, full facade ≤ 500 LOC meets D1 guardrail, 200+ caller import path 0 changes/tests 0 changes/0 runtime circular dep, Step 4.12 R1+R2 consensus of both parties 0 HIGH/MED);Phase 5 D-dimensional architecture rationality review (REVIEW_63.md spike1 §A1 Candidate 7 HIGH + 1 MED All ✅ 100% Phase 4 has been implemented = Step 5.2 Refactoring 0 work, 3 LOW + 5 INFO + 7 Critical file monitoring is all retained follow-up plan, R1+R2 close commit 8925fb5+31b29eb); Phase 6 general engineering experience has been deposited into resources/claude-config/CLAUDE.md +33 LOC (newly added §Large file splitting practical experience + §Multiple rounds of Deep-Review closing experience + §Single file size guardrail patch facade own LOC monitoring), Step 6.2 project-specific experience evaluation = no-op (the candidate list already has 0 candidates SSOT + tally.md count≥3) |
| [CHANGELOG_176.md](CHANGELOG_176.md) | plan sdk-spawn-shell-path-20260529 Complete archive (X1 solution to fix macOS .app launchd minimal PATH) - Main process startup Phase 0.5 Use execFileSync $SHELL -ilc + sentinel marker to surround PATH output + sentinel bisection memo Measured user real terminal PATH, union into process.env.PATH to let all The SDK child process running pnpm/cargo/brew and other user terminal CLI will no longer encounter command not found. RFC 5 rounds + spike1 6 hypothesis full empirical + Step 1.5 plan deep-review 3 round 16 finding fix + Step 3.6 code deep-review 2 round 6 finding fix. Reviewer-codex's three on-site measurements are irrefutable: tcsh/csh -ilc fails to overturn spike's default list of fully compatible assertions / Node spawnSync defaults to no stderr and requires shell:true to return status 127 / zsh .zlogout contaminates last-line and promotes sentinel marker design. 3 commit implementation (helper + bootstrap + 18 tests) + 1 R1 fix commit. tally.md P37 candidate precipitation (Electron application spawn sub-process cross-OS common pitfalls 8 preventive measures). Follow-up: sentinel nonce hardening / electron-log disk placement / tcsh/csh/fish/nu spike2, etc. |
| [CHANGELOG_177.md](CHANGELOG_177.md) | plan mcp-tool-camelcase-migration-20260529 Complete archive (32 fields snake_case → camelCase **breaking change**) — Agent Deck MCP 15 tool 32 unique Input parameter field names are unified changed to camelCase and outgoing parameters are aligned. Mainstream in JS/TS circle. 3 commit implementation: schemas+handlers+index/refine single green commit (R1 fix MED-E merge to avoid intermediate typecheck-fail commit) + test fixture synchronization + 7 documentation mcp tool input field description change camelCase + codex mirror synchronization. enum value (`'claude-code'` / `'workspace-write'` / `'clear-team'`, etc. 21) fully reserved / plan workflow frontmatter snake_case (archive_plan is parsed by snake_case key hardcode) / SQLite column / mcp tool function name wire-level identifier Four types of legal reservations. Step 0 RFC 2 rounds + Step 1.5 plan deep-review R1-R5 Total 49 fix + Step 3 implementation 11 invariants × test case comparison table full audit passed (pnpm typecheck 0 / pnpm build OK / pnpm vitest 81 files / 991 tests passed exit 0). tally.md U7 candidate precipitation (mcp tool input parameters and output parameters enum three orbit naming differences, count=1 to start). Follow-up: mcp tool runtime e2e manual verification (requires user to restart the application) + Step 3.6 code deep-review skip (mechanical migration zero business logic, plan deep-review is sufficient). **breaking change**: Any mcp__agent-deck__* tool input parameters must be called camelCase after the application is started. The old snake_case will report schema reject immediately. |
| [CHANGELOG_178.md](CHANGELOG_178.md) | plan runtime-logging-electron-log-20260529 Full archive (electron-log v5 introduced + console.* migrate to scoped logger + LogsSection UI at 354) — Introduced [electron-log v5](https://github.com/megahertz/electron-log) to let macOS .app double-click to start the scene console.* The output is actually moved to `~/Library/Logs/Agent Deck/main-YYYY-MM-DD.log` (split by day + 14-day cleanup), plan §Invariant 1 Fundamental goal. 19 commits Implementation: Phase 1 Infrastructure 6 commits (vitest-setup three-piece global mock electron + electron-log/main + electron-store + electron-log/renderer / main + renderer logger.ts dual process / entry import / AppSettings.logLevel 6 files can be changed after pulling down to take effect / logger.test.ts 8 assert + logger-guard.test.ts 4 assert) + Phase 2 7 commits Step 3.3 6 batches + Step 3.4 (78 files / 231 places console. README cross-platform path document) + Phase 4 main merge into worktree (mcp-tool-camelcase breaking change 4 conflict resolution = worktree logger.X + main camelCase). Verification: typecheck PASS + build PASS + vitest 0 fail / 1003 pass / 159 skip maintenance + grep CI 0 console. Behavioral four sets of integration tests step into the trap and patterned - CJS package mock / vi.unmock+vi.doMock sequence setupFiles invalid after locking / import.meta.env.MODE renderer replacement exception / rg -t tsx not recognized). Follow-up: GUI verify (Step 3.0.5/3.7) + Step 3.2.6 preload console.error (depends on spike (d) selected solution) + Step 3.8 deep-review |
| [CHANGELOG_179.md](CHANGELOG_179.md) | plan runtime-logging-electron-log-20260529 §Step 3.2.6 follow-up: preload fatal error download (scheme 2+4 combination) — preload `contextBridge.exposeInMainWorld('api', api)` failed / preload script itself fatal error that failed to load can now be downloaded to the production .app double-click startup scene `~/Library/Logs/Agent Deck/main-YYYY-MM-DD.log` Closed loop plan §Invariant 1 The last corner. When planning archive, press §Step 3.2.6 spec "Depends on spike (d)" to skip to follow-up across the board. The actual solution 2 (typed IPC channel + ipcRenderer.send/ipcMain.on fire-and-forget) + solution 4 (webContents.on('preload-error')) combination does not rely on dev verify and can be implemented stably (full Electron standard API does not rely on electron-log internal). 4 files +55/-4 LOC: shared/ipc-channels.ts Add PreloadFatalError channel + preload/index.ts catch Change ipcRenderer.send + main/ipc/logs.ts Add ipcMain.on listener + main/window/lifecycle.ts Add webContents.on('preload-error'). Both fall into 'preload-fatal' scope complementary (preload-error blocks loading failure / PreloadFatalError internal throw after successful loading). pnpm install + typecheck + vitest 0 fail / 1003 pass + build + logger-check all pass. **Option 1 + 3 is not enabled** (Option 1 relies on undocumented internal API; Solution 3 and D5 console capture take over double the risk of disk failure). |
| [CHANGELOG_180.md](CHANGELOG_180.md) | plan issue-tracker-mcp-20260529 Complete archive (agent execution issue tracking mechanism mcp tool + UI Issues tab + GC scheduler complete set) — 13 new files + 14 modified files ≈ +4200 LOC: v026 SQL migration (issues + issue_appendices dual tables + 6 partial index + FK SET NULL/CASCADE) / issue-repo.ts 486 LOC + 38 tests / mcp tool report_issue + append_issue_context (15→17 tool, write only but not read § invariant 1) + 40 tests / event chain F5 / IPC handler 6 + preload facade + createIssueResolutionSession 11 boundary hardening + in-flight Promise dedupe + 32 tests / settings GC threshold (resolvedRetentionDays=90 + softDeletedRetentionDays=7) + applyIssueGcThresholds into APPLY_FNS hot update / IssueLifecycleScheduler 6h tick including item-by-item snapshot before delete + sourceSessionId nailed + 11 tests / UI Issues tab (IssuesPanel double column list+detail, IssueDetail main field can be changed + appendices read-only, ResolveInNewSessionDialog D8 prompt template, issues-store zustand component self-subscription onIssueChanged) / Settings GC UI hanging LifecycleSection (change minimum decision). **Phase 4 main merge into worktree** (runtime-logging-electron-log 4 conflict resolution = worktree issue-tracker + main electron-log scoped logger fully integrated, IPC handler plus logger import + applyLogLevel) + Number changed from 178 to 180 Collision avoidance main merged CHANGELOG_178+179. Step 0 RFC 3 wheel alignment 20 design decision + Step 0.5 spike (a) 6/6 static demonstration D14 selected path b established + Step 1.5 plan deep-review R1-R5 total 30+ finding fix + Step 3 10 substep group 6 commit + Step 3.10 typecheck/vitest(83 pass + 38 skipped binding ABI)/build all passed + Step 3.10.3 GUI verification left to user to run. **§9 invariants are fully contracted**: agent only writes without checking / issue independent life cycle SET NULL / append source-bound / logsRef is a positioning pointer, not a log body / status only UI advancement / soft delete + GC dual track / write path deny external / not strongly coupled with the runtime-logging plan, graceful degradation / append action table is not broken description 1-2000 char invariant. Follow-up: GUI integrated verification (user runs pnpm dev) + "Start a new session to solve" the SessionDetail associated panel after spawning (plan §D4 decision B does not hang SessionDetail; users need to open a separate plan in the future). |
| [CHANGELOG_181.md](CHANGELOG_181.md) | Phase C (plan deep-review-and-asset-polish-20260530): tool/field description optimization of agent-deck-mcp 17 tool (to prevent LLM from passing wrong parameters/intention deviation, pure description-only does not change the schema logic). Decision-making versus external claude(1H+7M+8L)+codex(5H+5M+2L) Each independent review schemas.ts/index.ts + lead three-state ruling to remove duplicates. **drift fix**: spawn callerSessionId "required" → "SDK does not pass transport automatic injection" / 5 callerSessionId references deleted reply/wait tool → existing write tool / task status enum list + emphasis active non-in_progress / task_create teamId "null=personal" → "omit (create reject null)" / hand_off teamName obsolete (caller is no longer remain lead)+adoptTeammates mutual exclusion / hand_off planId-prompt required contradiction unity / task_create activeForm alignment builtin ongoing semantics / archive_plan cwd precheck process.cwd()→caller session cwd+marker. **Missing complement**: spawn adapter/cwd/prompt/teamName zero describe / enter_worktree "do not change cwd+CREATE semantics" / list_sessions return complement teams[] / task_update array overall replacement semantics / logsRef date required. **Information density**: strip 8 field describe internal number prefix + tool count 15→17. typecheck clear + full 1089 passed. follow-up: hand_off/archive long desc slimming + activeForm code level dual-usage (assignee vs UI in progress) needs to be decided separately. |
| [CHANGELOG_182.md](CHANGELOG_182.md) | Phase D Plan C (plan deep-review-and-asset-polish-20260530): Delete sync-codex-skills + claude/codex skills and independent SSOT at both ends. skills-installer source directory claude-config→codex-config (fix codex runtime always installs claude version flow-arch bad process HIGH bug) + codex skills into git + create new codex flow-arch SKILL (shell/apply_patch + turn boundary) + CODEX_AGENTS add plantUML section + README/CLAUDE document alignment. typecheck clear + full 1089 passed. |
| [CHANGELOG_183.md](CHANGELOG_183.md) | Phase E core (plan deep-review-and-asset-polish-20260530): prompt word asset layering + §Issue reporting chapter. **E1**: claude-config/CLAUDE.md + CODEX_AGENTS.md + reviewer-claude.md Delete 10 agent-deck source code file:line references (recoverer.ts / schemas.ts / hand-off-session.ts / types.ts / sandbox-config.ts; retain all capability descriptions) + REVIEW_38 Retrospective generalization + R37 Internal numbering cleanup (assets injected into all project SDK sessions, must not contain internal details of this codebase). **E2**: Two files plus §Issue reporting (report_issue/append_issue_context + source-bound/resolved/soft delete rejected append + agent only writes without checking; independent SSOT at both ends). **E3**: Review codex flow-arch references for consistency. **E5**: reviewer body 5 constraint self-test passed. **Decision Confrontation** (claude+codex dual external CLI + three-state ruling) Revise Q1 §Issue reporting accuracy (return IssueRecord primary key `id` not `issueId` otherwise append fails/misses soft deletion reject/logsRef date is required). Pure .md without TS delta. Follow-up: codex contract section α (CHANGELOG_184) + F5 internal name residue cleanup. |
| [CHANGELOG_184.md](CHANGELOG_184.md) | Phase E continued (plan deep-review-and-asset-polish-20260530): codex contract section α (CHANGELOG_183 Q3 decision versus dual reviewer unanimous decision). CODEX_AGENTS.md supplements the §Decision Confrontation section (codex style is not copied: double shell external CLI / shell &+wait concurrency / read-only flag template / no timeout command body + §refutation wheel + three-state judgment + §Finding output contract + §reviewer failure cover), insert the mirror claude-config sequence between §Application environment specific capabilities and §Core process must go plantUML. Repair reason: codex session only loads CODEX_AGENTS.md and does not load claude-config → ordinary codex session decision-making against zero coverage + codex SKILL/reviewer 4 cross-ref broken links. 4 cross-refs (SKILL:120/197/215 + reviewer-codex:80) were changed to point to the same file baseline of CODEX_AGENTS.md. Pure .md without TS delta; user spot-check codex shell concurrency/timeout wording recommended. Not included: §Complex plan workflow §Step4 §Stop cross-ref (big sections will not be copied) + F5 internal name cleaning. |
| [CHANGELOG_185.md](CHANGELOG_185.md) | Phase F (plan deep-review-and-asset-polish-20260530): Architecture/process plantUML diagram. **F1 issue-tracker 4 sheets** (flow + append 4 gatekeeper decision tree + architecture cross-process + status×deleted_at dual-track state machine; align existing topic convention) **F2 runtime-logging 2 sheets** (dual-process electron-log architecture + console takeover/drop/fatal sequence) **F3 review**: agent-deck-mcp-architecture update (issue plus 2 tool 15→17 / 4→5 data table / add issue sub-picture reference + issueRepo term; consistent with the picture edge label), the rest of the picture is irrelevant to issue/logging (topic backdrop count / console.warn behavior description is retained) **F4** Two INDEX +3 lines. `plantuml -syntax` 7 files with all 0 errors; only generate .puml SSOT without rendering. user scope confirmed by AskUserQuestion. |
| [CHANGELOG_186.md](CHANGELOG_186.md) | plan sdk-upgrade-thinking-fix-20260530: Upgrade @anthropic-ai/claude-agent-sdk `^0.3.144`→`^0.3.158` to deal with the problem that the outer antml wrapper called by the tool in the app is damaged into garbled characters (`court`) → not executed / output as plain text by the assistant. **DB forensics corrects the old "damaged call does not drop into the DB" error conclusion**: the damaged call DOES drops into the DB (`kind='message'` role='assistant', 11 centralized sessions 07427f50; `id=329524` is the root cause of the unsuccessful upgrade Edit = package.json keeps stopping at `^0.3.144`). Spike comparison: standalone `query()` 0.3.144 and 0.3.158 **Both have 0 recurrences** (7+1 run), the bug only appears in real app long sessions → No regression in upgrade but cannot prove fix in spike. typecheck/build passes. **Remaining**: The running app needs to be restarted to load 0.3.158; the fix has not been proven; it is assumed to be positively correlated with the context length/number of MCP tools/system prompt volume (not verified). If it still recurs, differentiate between the model side and the CLI side. |
| [CHANGELOG_187.md](CHANGELOG_187.md) | Phase E F5 follow-up (plan deep-review-and-asset-polish-20260530): Prompt word asset residual codebase internal name cleaning (claude-config/CLAUDE.md + CODEX_AGENTS.md is injected into all project SDK sessions, and this codebase internal symbol must not be included). According to "Run Concept Preservation vs. Pure Implementation Symbol Deletion/Generalization", both ends are consistent in cleaning 20+ places: class.method (sessionRepo.delete / sessionManager.close / runBatonCleanup / reassignOwner / applyHandOffSkipPolicy) / internal var (mcpSessionTokenMap / options-builder / HookServer / extra.authInfo / historyRetentionDays Remove the class prefix and keep the concept) / DB column name table name (spawned_by / spawn_depth / cwd_release_marker / archived_at / team_member / left_at / spawn_link) / internal number (N5 / N2.c / D1 ADR) / IPC handler (TaskListByTeam / TeamShutdownAllTeammates). **Doc bug #1**: § Decision-making against claude using "--permission-mode plan" conflicts with the actual default+disallow ExitPlanMode of reviewer-claude.sh.tmpl (plan-mode swallows finding, REVIEW_52/54) → Change default. **Doc bug #2**: CODEX_AGENTS 3 cross-ref claude-config §Step4 §Abort manual archiving (codex cannot be read) → inline §Manual archiving fallback section (completed 5 steps + abandoned 3 steps, follow the §cold-start protocol inline precedent) + change to internal sections. Workflow 5-dimensional confrontation + three-state ruling 34 finding→16 survived/18 refuted, 3 adopted (malformed spawn-by field name/shutdown_baton step conditionalization/double lifecycle wording), rejected cross-file asymmetry (both ends are explicitly not forcibly aligned) + inline→pointer (bug #2 will be reintroduced). Pure .md without TS delta. |
| [CHANGELOG_188.md](CHANGELOG_188.md) | deep-review-multi-area-20260530 Code part: Area 5 @openai/codex-sdk `^0.131`→`^0.135` (type surface zero damage, HIGH repair vendored binary `vendor/<triple>/codex/codex`→`bin/codex` layout change causes the packaged version codex to become invalid + bundled rg helper PATH injection issue 8c116860) + Area 2 Issue "New session resolution" dialog box (Bug#1 fixes claude/codex sandbox drop-down + Bug#2 status does not refresh first version fix: store-updatedAt subscription effect + draft guard + UX source/resolution session can be clicked to jump + pump sandbox-options.ts). Concurrent sessions edit .md assets in the same tree → Only pure code changes are covered in this round. |
| [CHANGELOG_189.md](CHANGELOG_189.md) | Issue Tracker experience and protocol improvements (user 4 points + kind convergence): ① Details status pull-down does not refresh - continued 188 Bug#2 residual gap, `onResolved` leaks `setEditing` is short-circuited by the store-updatedAt guard, fills in a line and ends symmetrically ② Active/resolved two mutually exclusive tabs (hidden by default resolved, reuse filters.statuses Bottom layer) ③Kind recommended value 5→2 (only follow-up + app-bug, soft enumeration fallback remains unchanged) ④**Protocol change**: Added `update_issue_status` mcp tool (17→18), authorization relaxed to `source OR resolution session`, optional note reuse appendContext to leave traces, worn out "agent never changes status / only writes and does not check" iron law; closed loop repair ResolveInNewSessionDialog prompt fills in the issueId. SSOT synchronization issue.ts/ipc-channels/report-issue comments + two prompt words ("not reported" strengthens "directly fix the report that can be repaired on the spot" + new §update_issue_status section). issue-tools.test +11 (49 passed), agent-deck-mcp 556 passed. Follow-up: 3 puml go flow-arch SKILL + decision-making versus review. |
| [CHANGELOG_190.md](CHANGELOG_190.md) | Full project deep review plan `deep-review-project-20260531` closed (A-H 25 sub-batches / REVIEW_71-95). Multi-session + worktree isolation + multi-round heterogeneous confrontation (reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5) + three-state adjudication, covering MCP handlers / sdk-bridge (claude + codex) / session-lifecycle-summarizer / store repos / renderer full stack. **Batch H (renderer layer, the subject of this session)**: H1 issue component (1 HIGH error state ambiguity destroys the form + dead code + 3 initial IPC fetch missing catch full screen fatal, REVIEW_93) + H2 App/session-store (MED setPendingRequestsAll snapshot replacement live pending → SDK deadlock / renameSession M4 lost fromId Subtable, REVIEW_94) + H3 SessionDetail (cancelToasts timer binding error dep + cross-session residual / ComposerSdk closed session dual source fallback display looser / listFileChanges no catch, REVIEW_95) = 1 HIGH + 8 MED + 14 LOW + 5 INFO = 28 fix + 33 regression test. Cumulative A-H ≈ 80+ fix, 1269 passed / typecheck dual configuration green. Common themes: same millisecond tie-breaker (SQL×4+renderer×1)/asynchronous boundary IPC reject no catch/full replacement and merge semantics are incomplete/a11y+security copywriting. Batch I (window/reviewed REVIEW_45/61 + ipc/images CHANGELOG_47 TOCTOU mature + issue subject REVIEW_70 just reviewed) user confirmed descope. Follow-up 15 items (new #15 same-ms updatedAt repo monotonic revision) to allow users to make decisions. |
| [CHANGELOG_191.md](CHANGELOG_191.md) | Settings panel "Session→Log" operation area: "Show in Finder" is changed to **In-app Monaco read-only view modal** (Click "View Log" to directly access the main-YYYY-MM-DD.log text of the current day, without external jumps). B1 ipc-channels `LogsReadToday` replaces `LogsShowCurrentInFinder`; B2 main handler changes to read files (refuseSymlink lstat protection + 2MB tail cap read-only tail + finally closeSync); B3 preload `logsReadToday` typed facade; B4 new LogViewerModal.tsx (Monaco lazy import read-only, fixed inset-0 z-[60] escape SettingsDialog overflow, self-management load/refresh, empty state + truncated banner, seqRef anti-race state); B5 LogsSection wiring (button label/onClick + rendering modal). Fix B4 legacy `renderOverviewRuler`→`overviewRulerLanes:0` typecheck error. README § Log Add description of operation buttons. typecheck+build double green; restart dev for manual testing and wait for user confirmation. Inherit plan runtime-logging-electron-log-20260529 §D9 experience improvement. |
| [CHANGELOG_192.md](CHANGELOG_192.md) | Delete the "Decision-Making Confrontation" chapter + create a new **simple-review SKILL** replacement + return the prompt word asset maintenance chapter (the prompt word asset reconstruction subject requested by the user's three-in-one request has the same origin as 191; after the original session channel is broken, hand-off only leaves out the main body of the log subtask, and this session restores the original session context from SQLite). A1 rewrites the deep-review SKILL at both ends (inline three-state ruling + Finding contract, changes to pure spawn after failure, and renames the relationship section); A2 **Creates a new simple-review SKILL at both ends** (single heterogeneous confrontation, spawn reviewer-claude+reviewer-codex full_review→three-state ruling, UI observable, lighter than deep-review); A3 repairs the reviewer body dangling pointer; A4 deletes user/claude/codex Three "Decision Confrontation" entire sections + 6 internal dangling repoints; A5 user CLAUDE.md deletes the "Prompt Word Asset Maintenance" section, and adds this section on the claude/codex application packaging side (codex perspective adaptation); A6 clears peripheral dangling (SOPs/templates/project root CLAUDE.md/README/ref-conventions + 3 src/*.ts comment-only repoints); A7 deletes reviewer-*.sh.tmpl×2 + codex-cli-stuck-lessons.md (in-project + user level) + obsolete convention 01-*. Pure .md + annotation reconstruction without runtime logic changes; grep self-check has no live dangling, simple-review SKILL frontmatter is legal (plugins are automatically discovered by directory), typecheck+build is double green (same as the 191 working tree). |
| [CHANGELOG_193.md](CHANGELOG_193.md) | Prompt word asset slimming (undertaking 192 same topics): CLAUDE.md/CODEX_AGENTS.md + agent-deck-mcp 8 issue/plan/worktree tool description + REPORT/APPEND/UPDATE_ISSUE schema `.describe()` at both ends, net reduction of ~156 lines (deleted internal §D Numbering/implementation details, codex recoverer paragraph and claude symmetry, callerSessionId unified "Auto-injected", 4 SKILL TL;DR blockquote folded inline), retain all calling contracts and strong constraints. team `asset-polish-review` Heterogeneous confrontation (reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5): 0 HIGH, **1 MED Independent hits on both sides + lead reading source code verification ✅**——slimming mistakenly merged the deleted recovery path in the append_issue_context description to update_issue_status (actually both handlers reject soft deletion, and the agent cannot self-restart), has been demolished resolved/deleted two paths + make up for update_issue_status "rejects soft-deleted". 2 INFO: Misreading of adapter default value (pre-existing→issue a46ef1a8 follow-up) + §Slightly soft-deleted after reporting (can be left unchanged). typecheck double green + issue-tools 51/51. Pure copywriting with no runtime logic changes. |
| [CHANGELOG_194.md](CHANGELOG_194.md) | **teamless DM: Lift the shared-team restriction on send_message** (users "can send any messages between sessions"). MCP `send_message` downgrades teamless DM (`team_id=NULL`) when caller/target does not have a shared active team and does not explicitly pass teamId - still enters the messages table + injects receiver SDK conversation, but does not enter TeamDetail; when there is a shared team, byte-identical zero changes. plan `teamless-dm-20260601` (RFC+spike+deep-review full process). **Core**: v027 migration relaxes `agent_deck_messages.team_id` NOT NULL, spike empirical self-reference FK (reply_to_message_id) v017-style naive reconstruction will **silently null drop reply chain** (`DROP old` triggers `_new`'s ON DELETE SET NULL, foreign_key_check instead PASS) → change rename-old-first. teamId full link `string\|null` (including event-bus inline type + preload callback); current-limiting bucket teamless uses `from:<sid>` per-sender; send.ts three superpositions (explicit teamId priority verification to prevent silent degradation / teamless pre-archived reject / teamless reply from-to pair-scope to prevent arbitrary messageId chaining); watcher dispatchClaimed 4 items team Gate package `teamId!==null` guard retains the session gate. deep-review heterogeneous confrontation (Opus 4.7 + gpt-5.5) 2 rounds of 0 HIGH/0 MED closure, all 6 plan flaws fixed. 1533 passed (0 regression) + typecheck/build green + v027 true sqlite migration test (reply chain retains assertion). |
| [CHANGELOG_195.md](CHANGELOG_195.md) | resume/fallback injects DB real historical messages (summary + recent N original conversations). In addition to the LLM summary segment when jsonl-missing fallback is started from fresh CLI/thread, **additional injection** DB events table has the latest N original role/text conversations (original messages are more reliable than summary) + **first connection of codex side history injection** (unlock REVIEW_60 F5). Both ends use the same adapter and have nothing to do with the shared layer `injectResumeHistory` (spelling 1 piece of structured text = spike4 codex source code is the only correct solution). Step1 Create a new `session/resume-history/` (maxLength/agentName/recentMessagesCount/maxEventIdFn parameterized decoupling + dual data sources listEventsFn full feed summary / listMessagesFn message-only raw segment + budget splicing + D6 five-state degradation chain never throws an error); Step2 event-repo `listRecentMessages` + `maxEventId` (beforeIdInclusive `<=` off-by-one excludes the current message); Step3 `summariseSessionForHandOff` and add agentName='Claude'\\|'Agent'; Step4 Add `resumeRecentMessagesCount`(default 30) + delete `autoSummariseOnFallback` (REMOVED_KEYS unconditional injection); Step5 claude access + delete recoverer-helpers.ts; Step6 codex access (+3 ctor thunk + 3 protected wrapper + double emit builder); Step7 test (inject-history 16 + event-repo-recent-messages 11 + claude/codex regression). Undertake plan resume-inject-raw-messages-20260601 (RFC three rounds + 4 spike + Deep-Review R1-R4). Full set of 113 files/1541 test full green + typecheck/build double green. |
| [CHANGELOG_196.md](CHANGELOG_196.md) | Fixed the issue where the "Today's Log" modal in "Settings → Log → View Log" was cut into one line by the settings panel (user screenshot). The root cause is the `fixed` containing block trap - the ancestor chain `backdrop-filter` (SettingsDialog `backdrop-blur-sm` + FloatingFrame `.frosted-frame`) downgrades the `fixed inset-0` containing block of LogViewerModal from the viewport to the ancestor, and then is cut by the `w-[340px] overflow-y-auto` card. Use `createPortal` to render to `document.body` instead, breaking away from the entire backdrop-filter ancestor chain (users demand "separate pop-up rendering"). Synchronously correct the misjudgment of the root cause in old annotations + tally P40 precipitation. typecheck double green, renderer-only HMR automatically pushed. |
| [CHANGELOG_197.md](CHANGELOG_197.md) | **Model Token statistics: Header Top3 token/s + data Tab** (user two requirements + addition). ① The middle part of the top column displays the "output token/s" of the top 3 models that output token today (60s sliding window, narrow width and two-level degradation hidden) ② Added "data" tab (sixth): daily token table for each model (input/output/cacheRead/cacheCreation) + today's summary + full model real-time token/s area ③ The real-time area of ​​the data page has the same origin as the header. plan `model-token-stats-and-dashboard-20260602` (two rounds of RFC + spike + 3 rounds of deep-review). **The collection base was previously zero**: At the collection point, put assistant message(claude, BetaMessage comes with id/model/usage, per-step increment)+turn.completed(codex); max-merge deduplication (same as message_id rare discrepancy, whichever is the highest, DB `ON CONFLICT(message_id) WHERE message_id IS NOT NULL DO UPDATE max`); ingest Early return bypass does not pollute the main event flow; model normalization SSOT (`model-normalize.ts` variant merges friendly name); codex effective model reads the top level of config.toml (section-aware row-level parsing does not use TOML parser); responsiveness introduces ResizeObserver hook from scratch; no fees are collected. v028 `token_usage` table (session_id removes hard FK and eliminates claude’s new spawn race condition). deep-review R2 captures the partial-index conflict introduced by lead R1 fix and the missing WHERE predicate (sqlite3 true table reproduction parse error). Added 49 tests, full **1736 full green** (Electron-as-node ABI 130, 0 skip) + typecheck/build double green + dev measured migration v28 true execution + 3 query/max-merge/normalized true DB verification. |
| [CHANGELOG_198.md](CHANGELOG_198.md) | **codex recover/restart restore reviewer spawn-time networkAccessEnabled + additionalDirectories** (deep-review-project Batch 3 / REVIEW_101 reviewer-codex MED-2 follow-up). `networkAccessEnabled:true` + `additionalDirectories:[~/.claude,~/.codex,/tmp]` injected by options-builder when reviewer-codex spawns neither persistence nor fallback → app restarts /dev hot reload/main crash and recover rebuilds thread lost → reviewer loses web search + cross-directory read plan/config + /tmp. Modify the method to persist two fields (v029) + recover/restart read back transparent transmission, mirror extraAllowWrite (v019) parity mode. **The key difference between extraAllowWrite and extraAllowWrite: codex runtime is really consumed** (via buildCodexThreadOptions → startThread/resumeThread), so persist does not warn. Pit: better-sqlite3 rejects raw boolean bind (DB has no boolean column precedent) → write boolean → 0/1 manual transfer, rowToRecord int → bool 3 state (NULL/0/1); `?? undefined` retains explicit false / persist `!== undefined` guard. rename two-branch defensive parity (non-spawn help - persist ordering is guaranteed). restart Symmetric completion (user confirmation). plan `codex-recover-network-dirs-parity-20260602` (worktree + Plan agent cross-validation - catch boolean-bind pit + correct rename qualitative). claude adapter zero changes. Added 16 new tests (v029 migration 9 + recovery transparent transmission 4 + rename 3), full **1752 full green** + typecheck double green. |
| [CHANGELOG_199.md](CHANGELOG_199.md) | PendingTab tightens the semantics of batch rows: the batch button only acts on PermissionRequest, ExitPlanMode / AskUserQuestion, like ask, must be processed one by one in the row; the `select` drop-down of the header row (originally only used for batch plan switching, missing `bypassPermissions` "don't ask again") the entire row is deleted, and the permission mode selection is completely made by a single row `ExitPlanRow` (File 4 contains "⚠️Don't ask again")/`PermissionRow` has built-in select processing. Only 1 file `src/renderer/components/PendingTab.tsx` has been changed, `batchableCount` has been changed to `permissions.length` + deleted `batchTargetMode` whole group + deleted `for (exitPlanModes)` sub-loop + changed `batchTooltip` copy + top doc comment alignment. There are 0 typecheck errors this time (the baseline 2 `ResolveInNewSessionDialog.tsx` errors have nothing to do with this change), renderer-only HMR is pushed automatically. |
| [CHANGELOG_200.md](CHANGELOG_200.md) | **pending tab resume visibility + new session option memory** (User 2026-06-02 reported double repair, single plan closed). BUG 1: Add `onSessionUpserted` monitoring to ActivityFeed - force re-pulling `listAdapterPending` when switching resume / reconnect / lifecycle to synchronize the in-process waiting state into the store (the user explicitly refuses to add a banner at the top of the details page and keeps the 5 tabs unchanged). BUG 2: Build a top-level `let` store in the `useLastSessionDefaults` module (persistent across mounts/not persistent across restarts) + bucketing across adapters (claude-code / codex-cli do not cross each other) + NewSessionDialog + ResolveInNewSessionDialog sharing. plan `pending-tab-resume-and-new-session-default-20260602`, typecheck double green. |
| [CHANGELOG_201.md](CHANGELOG_201.md) | **agent_deck_messages retention GC + listBySession index/query rewrite** (REVIEW_100 follow-up/issue 7dcb0676). The table only increases but does not decrease (from/to no FK + team almost no physical deletion + teamless team_id=NULL no cascade) + listBySession `WHERE from=? OR to=?` Full table SCAN (teamless DM amplifies). v030 Pure plus 3 index (immovable table structure to avoid v027 self-reference FK reconstruction trap): (from/to,sent_at DESC) double index + partial (sent_at) WHERE status IN terminal GC index. listBySession OR→UNION ALL double index rewrite to eliminate the entire table SCAN + second branch from<>to guard to prevent rename collision self-row duplication (byte-identical N2/N3). Create new gc.ts (INDEXED BY mandatory hint to prevent ANALYZE mis-selection status_last_attempt + status literal inline + defense guard) + MessageLifecycleScheduler (6h tick + 30s catch-up + hitLimit&&deletedCount>0 gatekeeping) + messageRetentionDays (default 30, 0=off) + agent-deck-message-purged event. Only delete terminal+expired, reply_to FK SET NULL to keep the reply line. Heterogeneous confrontation deep-review (plan R1/R2 + mixed after implementation): codex HIGH-1/HIGH-2 + INDEXED BY. Double reviewer sqlite3 was found during the implementation period. Actual measurement. Added 38 new tests, total 1786 full green + typecheck double green. |
| [CHANGELOG_202.md](CHANGELOG_202.md) | useImageAttachments Three image boundaries committed test (REVIEW_111 follow-up issue a28d008f closed). ① makeThumbnail img.onerror returns the original image fullDataUrl ② readAndMaybeCompress large image Path3 canvas re-encoding downshift (first 3 files oversize + 4th file hit / full oversize reject / getContext returns null short circuit) ③ gif exceeds the threshold reject. Reuse REVIEW_111 jsdom+renderHook+FakeFileReader/QueuedImage/canvas mock infrastructure + extend 3 helpers (setMockBigBase64 / setMockImageFail / setMockCompressLengths). Zero changes to production (+289/-19 tested). The lead on-site corrects the original lead assertion that "exactly 1 Image is enqueued for each file" - Path3 triggers 2 Images to be enqueued (`loadImageFromDataUrl` + makeThumbnail in readAndMaybeCompress, Promise.all shares fullDataUrl in parallel). 3 mutation Full evidence can prevent regression. typecheck double green + 1519 passed (+15). |
| [CHANGELOG_203.md](CHANGELOG_203.md) | Fixed two bugs (issue eb0e5a9c + added). ① vitest (Electron-as-node Node 20.18.3) runs the image hook test report html-encoding-sniffer ERR_REQUIRE_ESM - jsdom@28+ replaces the entire encoding stack with ESM-only @exodus/bytes, and the old Node CJS require ESM explodes. Two tests `@vitest-environment` jsdom→happy-dom (pure JS DOM without ESM-only transitive dependency), delete jsdom devDep. Excluded --experimental-require-module (Electron patch readFileSync infinite recursion RangeError) / downgraded jsdom (whack-a-mole). Attached is the addition of `pnpm.onlyBuiltDependencies:[electron,esbuild]` to repair pnpm v10 clean install build-script gate (better-sqlite3 is intentionally not included, use electron-builder rebuild). ② Switch to the issue page status without refreshing - the onIssueChanged subscription is originally in the IssuesPanel component, and the unmount leak event is removed. Move up to App resident `useIssuesBridge` (same as useEventBridge). Added use-issues-bridge.test.tsx (5 test mutation examples). 131 files/1814 passed/0 errors + typecheck/build green. |
| [CHANGELOG_204.md](CHANGELOG_204.md) | HeaderTokenRates centered layout (1 line of CSS plus justify-center) + start tok/s constant 0 troubleshooting (layout diff has nothing to do with this phenomenon, the rate calculation logic is not moved; the troubleshooting path is listed at the end of CHANGELOG, and the conclusion is pending in CHANGELOG_205). |
| [CHANGELOG_205.md](CHANGELOG_205.md) | Log noise reduction + webFrameMain disposed silencing (plan log-noise-and-disposed-20260603). ① codex CLI loader warning warn→debug (264 lines of disk cleared in 5 days, REVIEW_80 'Ignoring malformed' anchor filter does not move). ② Electron framework webContents.send destroys the race error and silences it (fix E three rounds of review evidence: R1 fixes the layer logger.ts file transport hook → R2 fixes the object and installs it to log.hooks + keep-path and returns msg → R3 complements the end-to-end electron-log/node real packet test to prevent no-op secondary recurrence). makeSafeSend draws _deps.ts + 7 case single test + logger hook 9 case single test + logger-end-to-end 3 case end-to-end + translate 40 + safeSend 7 = 72 All tests passed typecheck double green. |
| [CHANGELOG_206.md](CHANGELOG_206.md) | header real-time tok/s (streaming estimation·end calibration) + token_usage GC wiring + DataPanel copywriting (plan tok-rate-realtime-streaming-20260603). D1 data source = text estimation (CJK/1.7 + ASCII/4), Anthropic SSE content_block_delta only carries text, message_delta is pressed by CLI into a single turn (spike3 long output 4704tok/42s is still single); D2 estimates ephemeral using eventBus 'token-rate-tick' and will never fall out of the library; D4 is cured by the way. CHANGELOG_204 tok/s is always 0 (DB output≈0 has no data during generation, it is estimated to be solved from the root); D5 codex has no stream_event and naturally degrades in a 60s window; D6 GC retention 365d defaults to 0=off (token_usage is the only historical source of daily dashboard, looser than session/message 30d); D8 rendering set = fresh-live ∪ poll union + live-first ranking comparator tuple `(hasFreshLive desc, liveTps desc, pollOutput desc)` (HIGH-1 live-only bucket is no longer empty, narrow head does not squeeze out the streaming bucket); D9 turn end instant calibration = onTokenUsageChanged debounce 500ms + requestSeq latest-guard old response late does not overwrite new results. Changes 14 files (2 new live-token-rate.ts + token-usage-accounting.ts + token-usage-lifecycle-scheduler.ts + lib/live-rate.ts + CHANGELOG_205/206 + settings/UI + 7 changes), 2 commit `3228372` (Phase 1-5) / `8961b77` (Phase 6-7). sdk-message-translate 529→408 (token-usage-accounting) + facade import unchanged (invariant 4/5). plan design double confrontation R1+R2 closing (reviewer-claude + reviewer-codex heterogeneous); this batch of users indicated to skip the implementation period confrontation. typecheck is green, the branch has been pushed to origin, and the dev test is waiting for the user to start. |
| [CHANGELOG_207.md](CHANGELOG_207.md) | Settings default value changed to 30d/30min/1h/MCP default enabled; root level AGENTS.md added; MCP 18 tool copywriting duality. |
| [CHANGELOG_208.md](CHANGELOG_208.md) | simple-review / deep-review upgrades to five levels of severity CRITICAL/HIGH/MEDIUM/LOW/INFO; CRITICAL/HIGH must be refuted; only 0 CRITICAL/HIGH is allowed to be merged; reviewer / review document template is synchronized with the plan review gate. |
| [CHANGELOG_209.md](CHANGELOG_209.md) | Codex review workflow is adapted to the turn-based SDK: the turn ends after dispatching the reviewer / rebuttal round / Round 2, and the next round of teammate reply is injected to continue the ruling; the same turn sleep/poll waiting is prohibited. |
| [CHANGELOG_210.md](CHANGELOG_210.md) | The default sandbox of Claude Code is changed to Workspace Write; the default for new session permissions is "Don't ask again"; the old default is off for one-time migration. |
| [CHANGELOG_211.md](CHANGELOG_211.md) | The TeamDetail member area has a new Add Member entry, which can add existing active sessions to existing teams. |
| [CHANGELOG_212.md](CHANGELOG_212.md) | Header/DataPanel is ranked according to the current tok/s; Claude `stream_event` text increment estimation generates tok/s, which is pushed to the renderer through display-only `token-rate-tick`, and turn is not calibrated with `token_usage` for precise 60s window; Codex keeps `turn.completed` statistics. |
| [CHANGELOG_213.md](CHANGELOG_213.md) | CLI wrapper `agent-deck new` injects `--permission-mode bypassPermissions` by default, which is aligned with Dialog new behavior; users can still explicitly pass other gear overrides. |
| [CHANGELOG_214.md](CHANGELOG_214.md) | Fixed the issue where `bypassPermissions` was overwritten by `'default'` in the SDK `system.init` frame, causing `canUseTool` to be short-circuited and invalid, and the DB was written as `'default'`. |
| [CHANGELOG_215.md](CHANGELOG_215.md) | Fix the second-instance argv Chromium reordering problem: `requestSingleInstanceLock({ argv })` + handler reads `additionalData.argv` first, and completely fixes the bug where all CLI paths `--permission-mode`/`--cwd`/`--prompt` are lost. |
| [CHANGELOG_216.md](CHANGELOG_216.md) | Real-time estimation of tok/s during Codex side generation: `item.updated{agent_message/reasoning}` Difference accumulation + EMA smoothing, access to the existing `token-rate-tick` channel, Header Top3 displays the Codex rate synchronously. |
| [CHANGELOG_217.md](CHANGELOG_217.md) | Codex tok/s Completed state calibration: Actual measurement `@openai/codex-sdk` 0.135.0 The ordinary answer is not sent `item.updated(agent_message/reasoning)`, changed to `turn.started` to record the time consumption, `item.completed` text estimation, `turn.completed` Use authoritative usage to send short calibration tick, and then fall back to 60s `token_usage` window. |
| [CHANGELOG_218.md](CHANGELOG_218.md) | Deepseek (Claude Code) session profile + asset page switching anti-flicker: add `deepseek-claude-code` adapter, independent `~/.agent_deck/.deepseek/settings.json` save Deepseek URL/token/model env, other agents/skills/CLAUDE.md reuse Claude side; create new session/issue solution/CLI/MCP schema Access; `spawn_session(agentName)` goes to Claude bundled agent root; the asset library "Application Convention" Claude/Codex editor is resident and mounted to prevent switching jumps. |
| [CHANGELOG_219.md](CHANGELOG_219.md) | Resources Configuration description entry returned: Added `resources/README.md` to assume the dual-end resource overview and SSOT boundary of claude-config / codex-config, deleted `resources/claude-config/README.md` that was mistakenly placed in the Claude subdirectory, and corrected the packaging path and dual-end maintenance boundary description. |
| [CHANGELOG_220.md](CHANGELOG_220.md) | `spawn_session` is changed to inherit caller permissions/sandbox only with the same adapter, and cross-adaptor uses target by default; Codex→Claude defaults to `bypassPermissions` and no longer falls back to asking every time. |
| [CHANGELOG_221.md](CHANGELOG_221.md) | Sandbox/permission cold restart uses the internal recovery prompt and injects DB history in the normal resume path to avoid model misunderstanding and context loss. |
| [CHANGELOG_222.md](CHANGELOG_222.md) | reviewer Long-term copywriter removes the specific model version and uses Claude/Codex adapter and provider series description instead. |
| [CHANGELOG_223.md](CHANGELOG_223.md) | Withdraw 221 Inject DB history into the restart resume of jsonl - when jsonl is present, CLI/resumeThread has resumed the complete context from jsonl, directly transparently transmitting the internal recovery command and no longer overlaying the DB summary; delete `buildRestartResumePrompt`, jsonl is missing fallback and still continues the history. The sandbox only wraps a single command and does not swallow jsonl. It has been tested to rule out "the sandbox loses jsonl". |
| [CHANGELOG_224.md](CHANGELOG_224.md) | Phantom fork self-healing (closed-loop 223 troubleshooting log): The init frame under CLI `--resume` spits out the running id that never drops to the disk, and the stream-processor no longer writes the running id into `cli_session_id`; when the history is contaminated, precheck back probes `applicationSid.jsonl`, and only mtime is new enough to switch back to applicationSid resume; old SDK stream cleanup and identity guard are added to avoid deleting new internals after cold restart, resulting in unresponsive sending. |
| [CHANGELOG_225.md](CHANGELOG_225.md) | The CLI wrapper uses base64 payload delivery for `new` argv to avoid the rearrangement of `--flag value` by macOS Electron `second-instance`; fixes the issue where the `--permission-mode bypassPermissions` automatically added by the wrapper is lost again and the session returns to every query. |
| [CHANGELOG_226.md](CHANGELOG_226.md) | New project engineering foundation supplement Codex entry template: Add `project-agents.template.md` (AGENTS.md thin pointer → CLAUDE.md), Claude / Codex baseline are written as "CLAUDE.md (shared SSOT) / AGENTS.md (Codex entry difference) in pairs", and clean up the dangling `resources/...` path in the template. |
| [CHANGELOG_227.md](CHANGELOG_227.md) | Fixed `spawn_session` with the adapter inheritance sandbox extra write root missing item: `extraAllowWrite` Now press caller explicit > same-adapter lead inheritance > cross-adapter is not passed; cross-adapter still uses the target adapter default value. Supplement tools.test to override same-adapter inheritance, cross-adapter non-inheritance and explicit override. |
| [CHANGELOG_228.md](CHANGELOG_228.md) | Remove the weakly related general engineering foundation and flow/architecture diagram prompt words. The built-in resources of Agent Deck remain in a self-closed loop and do not reference external skills. |
| [CHANGELOG_229.md](CHANGELOG_229.md) | Corrected the remaining Claude Bash tool name of Codex deep-review skill and unified it into `shell:`. |
| [CHANGELOG_230.md](CHANGELOG_230.md) | Periodic summary and UI Hand-off briefing support Deepseek provider; upgrade Codex SDK to 0.137.0, Claude Agent SDK to 0.3.168; supplement provider flow chart and prompt word asset copywriting to tighten. |
| [CHANGELOG_231.md](CHANGELOG_231.md) | Codex recovery/cold restart no longer passes the `codex-default` statistics occupancy to the SDK, and fixes the flow-level error where the ChatGPT account does not support the model name. |
| [CHANGELOG_232.md](CHANGELOG_232.md) | Project organization cleanup: add `dist/` ignore, clear `.claude/` residue |
| [CHANGELOG_233.md](CHANGELOG_233.md) | MCP handoff/worktree changed to plan-free primitives, removed public archive/cleanup tools |
| [CHANGELOG_234.md](CHANGELOG_234.md) | `hand_off_session` closes the spawn successor when the resource transfer fails, and supplements the primary path diagnostic log |
| [CHANGELOG_235.md](CHANGELOG_235.md) | Upgrade Claude/Codex SDK and included native binary: Claude Agent SDK 0.3.169, Anthropic SDK 0.102.0, Codex SDK/native 0.138.0 |
| [CHANGELOG_236.md](CHANGELOG_236.md) | Codex live path moved to app-server JSON-RPC, added active turn mid-turn steering + IPC/UI/prompt/PlantUML |
| [CHANGELOG_237.md](CHANGELOG_237.md) | Codex mid-turn steering converges to sendMessage routing and main input box |
| [CHANGELOG_238.md](CHANGELOG_238.md) | Codex creates a new session and writes `codex_sandbox` and then reissues `session-upserted`, fixes that the details page still displays "working directory is writable" after explicitly selecting "fully open" |
| [CHANGELOG_239.md](CHANGELOG_239.md) | Codex runtime is fully switched to app-server: oneshot reuses app-server, tok/s only eats the authoritative usage delta, deletes the old SDK translator/dependency |
| [CHANGELOG_240.md](CHANGELOG_240.md) | Business log printing enhancements: Clear the remaining `console.*` on the main end, Issue/Message GC, Issue "new session resolution", session JSON configuration parsing, event payload dirty data unified use of scoped logger, and add issueId/sid/sessionId/retention/limit/catch-up and other troubleshooting contexts. |
| [CHANGELOG_241.md](CHANGELOG_241.md) | UI event description and display copy enhancement: TeamDetail / ActivityFeed / SessionCard displays tool input parameters and waiting reasons, and tightens the exposure of technical words such as task/prompt/diff/raw status. |
| [CHANGELOG_242.md](CHANGELOG_242.md) | Upgrade Claude/Codex CLI and SDK: Codex 0.139.0, Claude Agent SDK 0.3.170, Anthropic SDK 0.104.1. |
| [CHANGELOG_243.md](CHANGELOG_243.md) | reviewer-codex no longer enforces `codexSandbox=workspace-write`, instead follows explicit parameters / same adapter inheritance / Codex default chain; synchronizes MCP tool description, Codex application convention and built-in reviewer copywriting. |
| [CHANGELOG_244.md](CHANGELOG_244.md) | Project-level review Fixed 4 corner defects: spawn setSpawnLink failed orphan session, codex turn interrupted RPC failed to hang, late turn/completed closed the new turn queue in advance, event single-line dirty payload poisoned batch reading (including json_extract SQL level guard). |
| [CHANGELOG_245.md](CHANGELOG_245.md) | Claude Fable alias supports closing: model normalization recognition `claude-fable-5` / `fable`, add `fable` in the asset editor drop-down, Deepseek profile defaults to `ANTHROPIC_DEFAULT_FABLE_MODEL`. |
| [CHANGELOG_246.md](CHANGELOG_246.md) | Added new MCP `request_plan_review`: Codex/third-party models can request users to review markdown plans, reuse existing pending plan UI, and optimize long plan markdown rendering. |
| [CHANGELOG_247.md](CHANGELOG_247.md) | Foundation template alignment: CLAUDE.md adds directory structure/record number/review expiration/500 line guardrail section, `.gitignore` adds `.refs/`, and implements expiry script. |
| [CHANGELOG_248.md](CHANGELOG_248.md) | Entry asset deduplication: CLAUDE.md corrects the IPC expiration path (ipc.ts → src/main/ipc/), and README deletes the three-written packaging agent rules. |
| [CHANGELOG_249.md](CHANGELOG_249.md) | Settings panel MCP introduction is completed, request_plan_review, hand_off_session is included in session orchestration; Claude Agent SDK is upgraded to 0.3.175. |
| [CHANGELOG_250.md](CHANGELOG_250.md) | Claude/Codex native agents and Codex session-level injection: Claude uses SDK `agent`/`agents`, Codex custom-agent TOML is mapped to app-server thread/config; Codex AGENTS and built-in skills no longer write user-level directories, and the asset library supports Codex agents. |
| [CHANGELOG_251.md](CHANGELOG_251.md) | reviewer bilateral thinking has been raised to xhigh: reviewer-codex adds model_reasoning_effort, Claude agent loader adds frontmatter effort support and transparently transmits SDK effort. |
| [CHANGELOG_252.md](CHANGELOG_252.md) | The change page displays metadata.diff, and you can view the current Git final diff of the selected file. |
| [CHANGELOG_253.md](CHANGELOG_253.md) | The basic directory structure supplements the `scripts/` rule and aligns with foundation v0.0.5. |
| [CHANGELOG_254.md](CHANGELOG_254.md) | Codex unified diff reuses Monaco display style with final diff. |
| [CHANGELOG_255.md](CHANGELOG_255.md) | The final diff uses session record file snapshots, and single changes to Codex are rendered with snapshots first. |
| [CHANGELOG_256.md](CHANGELOG_256.md) | The `spawn_session` MCP description indicates that the `permissionMode` / sandbox field is an explicit override and should be omitted when not explicitly requested by the user. |
| [CHANGELOG_257.md](CHANGELOG_257.md) | reviewer-claude added information on the MCP tool whitelist and runtime defense line; issue resolved the pop-up window to remember the last adapter. |
| [CHANGELOG_258.md](CHANGELOG_258.md) | SessionDetail The activity flow tool row has a new "View Input Parameters" folding area, and the Codex/Claude shared tool display can view the complete toolInput. |
| [CHANGELOG_259.md](CHANGELOG_259.md) | Data tab adds Claude/Codex current window, weekly usage and reset time; Deepseek API channel display is not supported. |
| [CHANGELOG_260.md](CHANGELOG_260.md) | Asset library Agents adds Codex switch; Claude bundled Skills/Agents is split into independent switches and old plugin values ​​are migrated. |
| [CHANGELOG_261.md](CHANGELOG_261.md) | The data tab starts prefetching; the quota window is cached/regularly refreshed, Claude can check it in the background, and unsubscribed items are clearly displayed. |
| [CHANGELOG_262.md](CHANGELOG_262.md) | The data tab still automatically reads the quota window; Claude/Codex no longer starts the hidden provider subprocess for the data tab. |
| [CHANGELOG_263.md](CHANGELOG_263.md) | The SessionDetail tool line is changed to click start to expand the input parameters, click end to expand the output parameters, and remove the "View input parameters" button. |
| [CHANGELOG_264.md](CHANGELOG_264.md) | Claude Agent SDK has been upgraded to 0.3.177; Codex 0.139.0 and Anthropic SDK 0.104.1 are the latest stable versions. |
| [CHANGELOG_265.md](CHANGELOG_265.md) | The AskUserQuestion card adds a new comment input for each question and returns it to Claude along with the options/other answers. |
| [CHANGELOG_266.md](CHANGELOG_266.md) | Settings/Asset Library/Data tab Customization of copywriting; Codex requests and summarizes reasoning summary by default. |
| [CHANGELOG_267.md](CHANGELOG_267.md) | Claude Agent SDK upgraded to 0.3.178, Anthropic SDK upgraded to 0.104.2, Codex upgraded to 0.140.0. |
| [CHANGELOG_268.md](CHANGELOG_268.md) | Periodic summary and Hand-off briefing by source prompts the default model when the model name is left blank. |
| [CHANGELOG_269.md](CHANGELOG_269.md) | Unify the dark style of the drop-down box in the application, and fix the horizontal scrolling of session details. |
| [CHANGELOG_270.md](CHANGELOG_270.md) | The in-app drop-down is changed to a self-drawn pop-up list, and the number box is set to hide the native plus and minus buttons. |
| [CHANGELOG_271.md](CHANGELOG_271.md) | Fixed the issue where the drop-down list at the bottom of the conversation page floats to the middle of the application when expanded upward. |
| [CHANGELOG_272.md](CHANGELOG_272.md) | Remove the self-drawn drop-down interval and fix the snapping problem when session details are expanded upward. |
| [CHANGELOG_273.md](CHANGELOG_273.md) | Claude tok/s Preserve mid-generation estimates and calibrate with authoritative output tokens / cumulative decode delta window at the end of the turn. |
| [CHANGELOG_274.md](CHANGELOG_274.md) | Data tab When there is no live session, use a silent probe to read the Claude/Codex quota without sending a message or turning. |
| [CHANGELOG_275.md](CHANGELOG_275.md) | Fixed the quota probe triggering macOS Downloads permission pop-up window and cwd=/ ghost history session. |
| [CHANGELOG_276.md](CHANGELOG_276.md) | Preheat the provider quota cache when the application starts, and reuse the main-side TTL cache for data tabs. |
| [CHANGELOG_277.md](CHANGELOG_277.md) | Fix original-session messages disappearing when the user continues chatting immediately after `hand_off_session`; SDK user messages now clear the short tail-event blacklist. |
| [CHANGELOG_278.md](CHANGELOG_278.md) | `hand_off_session` no longer puts the caller in the short tail-event blacklist, so the original turn's post-handoff output remains visible while the caller stays closed. |
| [CHANGELOG_279.md](CHANGELOG_279.md) | Standalone `spawn_session` now injects first-reply anchors and refreshes spawn tree state immediately. |
| [CHANGELOG_280.md](CHANGELOG_280.md) | Agent Deck MCP plan review timeout now follows the permission approval timeout. |
| [CHANGELOG_281.md](CHANGELOG_281.md) | Team scheduler waits before archiving empty new teams. |
| [CHANGELOG_282.md](CHANGELOG_282.md) | Handoff skips archived teams during resource transfer. |
| [CHANGELOG_283.md](CHANGELOG_283.md) | Foundation prompt assets drop stale wording and align support files. |
| [CHANGELOG_284.md](CHANGELOG_284.md) | Split row-active and operational active-team membership queries for handoff and task paths. |
| [CHANGELOG_285.md](CHANGELOG_285.md) | Filter incomplete and no-op Codex file changes from SessionDetail. |
| [CHANGELOG_286.md](CHANGELOG_286.md) | SessionDetail permissions tab now switches by adapter: Claude keeps `.claude/settings*.json`, Codex shows sandbox, fixed approval policy, MCP status, and `~/.codex/config.toml`. |
| [CHANGELOG_287.md](CHANGELOG_287.md) | AskUserQuestion and Plan rows keep actions in the top-right area only; plan feedback now opens below the top-right buttons. |
| [CHANGELOG_288.md](CHANGELOG_288.md) | Plan feedback opens as a single-line row without changing the button layout. |
| [CHANGELOG_289.md](CHANGELOG_289.md) | Codex sandbox restart resumes the app-server thread without a synthetic prompt when jsonl exists, while Claude permission mode changes persist successfully when the DB session exists but no live SDK query is running. |
| [CHANGELOG_290.md](CHANGELOG_290.md) | Codex sandbox switching now applies to the next turn without restarting or clearing queued messages. |
| [CHANGELOG_291.md](CHANGELOG_291.md) | Claude compaction events render, and thinking copy is adapter-aware. |
| [CHANGELOG_292.md](CHANGELOG_292.md) | Match Electron split-args webFrameMain noise and lower Monaco race logs to debug. |
| [CHANGELOG_293.md](CHANGELOG_293.md) | Session model/thinking chips, Git branch display, and diff enlarge/navigation. |
| [CHANGELOG_294.md](CHANGELOG_294.md) | Session metadata chips show default model as 默认 and refresh Git branch. |
| [CHANGELOG_295.md](CHANGELOG_295.md) | Enlarged diff view uses a portal overlay above the app navigation. |
| [CHANGELOG_296.md](CHANGELOG_296.md) | Codex external hooks install/manage terminal sessions; clean Codex config. |
| [CHANGELOG_297.md](CHANGELOG_297.md) | Issues record branch snapshots; spawn prompt routing test updated. |
| [CHANGELOG_298.md](CHANGELOG_298.md) | Codex internal hooks are SDK-origin filtered; no inferred close without SessionEnd. |
| [CHANGELOG_299.md](CHANGELOG_299.md) | Reuse the Codex quota background app-server across refreshes. |
| [CHANGELOG_300.md](CHANGELOG_300.md) | Data tab quota percentages render as integers. |
| [CHANGELOG_301.md](CHANGELOG_301.md) | Data tab quota refresh is less aggressive and adds a hard refresh button. |
| [CHANGELOG_302.md](CHANGELOG_302.md) | Align live and precise token-rate model buckets. |
| [CHANGELOG_303.md](CHANGELOG_303.md) | `list_sessions` defaults to related sessions and SessionDetail shows tasks. |

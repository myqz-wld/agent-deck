# 项目约定候选（待观察）

> 此文件由 Claude Code 自动维护。**不要手工删条目**。
> 当某条 count ≥ 3 时，会**新建** `<X>-<topic>.md`（X 递增整数）+ 同步 [INDEX.md](INDEX.md) 加行 + 从本文件删除该条。**不再**写到项目 [CLAUDE.md](../CLAUDE.md)「项目特定约定」节，让 CLAUDE.md 保持静态。

## 用法（给 Claude Code 的内部约定）

两类候选，**同一文件分 section**：

| 类型 | 触发条件 |
|---|---|
| **用户反馈** (`# 用户反馈候选`) | 用户给「纠正性 / 偏好性」反馈：「不要…」「应该…」「我已经说过…」「以后…」「记住…」「每次…」 |
| **Agent 踩坑** (`# Agent 踩坑候选`) | Coding Agent 在 review / 修 bug / 排查时**自己**发现踩了同类坑，或 review 报告里反复出现同类问题（典型：try/finally 漏 cleanup、TOCTOU、N+1 查询、async listener 不被 await） |

**操作流程**（每次接到符合条件的反馈 / 自己发现踩坑时）：

1. **判断范围**：
   - 用户反馈：必须是**项目内的工程偏好 / 设计取舍 / 工作流偏好**，一次性请求（"帮我改这个 bug"）不算
   - Agent 踩坑：必须是**模式化问题**（一类问题反复出现），不是单点 bug
2. **读本文件**，找语义相近的已有条目：
   - 找到 → `count` +1，更新 `last_at` 为今天日期
   - 没找到 → 新增条目（`count: 1`），写在对应 section
3. **count 达到 3** → 这是「约定升级」决策，按通用 CLAUDE.md「决策对抗」节走**双对抗三态裁决**：
   - 起两个独立异构 Agent，各自评审升级提案：措辞是否准确 / 边界是否清晰 / 与已有约定有无冲突 / 升级到哪一节最合适
   - 三态结果汇总后告诉用户「这条 [反馈/踩坑] 累计 3 次，对抗审视结论 ✅/❌/⚠️ 如下，要升级吗？」
   - 用户确认后**新建 `conventions/<X>-<topic>.md`**（X 递增整数，与 changelog/CHANGELOG_X / reviews/REVIEW_X 编号空间独立）+ 同步 [INDEX.md](INDEX.md) 加行 + 从本文件删除该条目
4. **count < 3** → 静默更新本文件，不打扰用户

> 30 天未更新且 count < 3 → 下次扫描可主动清理。

---

# 用户反馈候选

按 `count` 倒序。

| ID | 描述 | count | first_at | last_at | 触发样例 |
|----|------|-------|----------|---------|----------|
| U1 | 用户在 UI 上做的选择默认要持久化（DB / 设置文件），不要只放内存让重启丢 | 1 | 2026-04-21 | 2026-04-21 | "权限的记忆也不能放内存吧" |
| U2 | UI 文案术语必须**全局统一**（设置面板用的术语 = 状态徽章 / 列表 / 通知里的术语），且**按平台分流**只显示当前平台相关描述（renderer 用 `IS_DARWIN/IS_WIN/IS_LINUX` 条件渲染，不混 mac/win 文案）；技术细节（如自带 plugin 的 agents/skills 名 / mcp__tasks__* 工具列表）不放在设置面板 toggle 描述里塞，应单独入口集中展示。**约定要素**：标准中文术语 + 必要时括号注英文（如「Claude Code Hook（系统钩子）」）；专有名词保留英文不翻；内部字段 / API / 类型保留英文（active/dormant/permissionMode）；UI 层 title/aria-label 必须用 UI 标准中文（StatusBadge.tsx 的 `closed` 是 bug） | 1 | 2026-05-06 | 2026-05-06 | "看看能否区分环境，mac 环境就只加载 mac 相关的描述，win 环境就只加载 win 相关的描述，不要混在一起 ... 中英对照这个除了配置面板之外，其他地方也要保持统一，不然用户会疑惑设置对应的是什么地方的什么功能"（CHANGELOG_57 已落地术语统一表 B4.2 + 平台分流 4 处 + 资产库独立入口；下次同主题反馈再 +1，count ≥ 3 升级到 CLAUDE.md「项目特定约定 → 术语统一表」小节） |
| U3 | detail 视图（SessionDetail / TeamDetail / 类似容器）的「关闭 / 返回」按钮统一放 header 右上角；**不要左上角**；与右侧 actions 槽（关闭 N 个 teammate / 归档 / 等高 cost actions）共组在右侧，标题居左占 flex-1。**约定要素**：关闭按钮 className 用 `flex h-5 w-5 items-center justify-center rounded text-[11px] text-deck-muted hover:bg-white/10` emoji-only 方按钮（不要文字按钮如「← 返回」）。原因：用户在多个 detail 视图间切换时按钮位置不一致 → 点击错位 + 视觉断裂；emoji `←` 已传达返回语义文字反占空间 | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 反馈 2：「然后团队页面的退出按钮也放到右上角，保持统一」 — TeamDetail/Header.tsx 把「← 返回」从左上角挪到右上角与 actions 共组，与 SessionDetail header 「←」按钮 className 镜像 |
| U4 | 高 cost / 不可逆 / 调外部 API（典型：LLM oneshot / 起新 SDK session / 重启子进程 / Bash 起子进程跑数 GB build）的操作必须经过显式「触发按钮」**而非 mount / dialog open 自动启动**。**约定要素**：(a) modal mount useEffect 仅重置 state 不触发昂贵 IPC；(b) 显示 CTA 按钮（`bg-status-working/30` 主色暗示「主操作」）+ 说明文案明示 cost（如「单次会调用 sonnet API，按 token 计费」）；(c) idle 态结果区不渲染。原因：用户可能 hover 上去看 tooltip 不小心点 / 想看 modal 内说明 / 切回归档会话刷新页面 等无意触发，自动启动直接烧 token / 起进程，浪费 + 用户体感失控 | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 反馈 1：「hand off 的按钮确认后再开始总结，不然点一下就浪费 LLM 资源」 — HandOffPreviewDialog mount 不再自动调 sonnet，改成「✨ 开始总结」CTA 按钮显式触发；hasSummarized state 区分 idle / loading / done / error 四态 |
| U5 | 跨进程渲染分发 / 业务分支判断**不要双轨各自维护描述函数**（典型：SessionCard `summariseToolInput` 加 case 时 ActivityFeed `describeToolInput` 没同步加，新工具调用在 ActivityFeed 详情区只显纯名无摘要）。**约定要素**：(a) 加新工具 case 时一次同步两处 + 测试覆盖；或 (b) 抽 helper（注：本次未抽，因 SessionCard 用作 SessionList 单行 / ActivityFeed 用作详情 + SimpleRow 两处形态接近但 SessionCard 拆分边界附近——抽 helper 反而要 refactor SessionCard，YAGNI）；(c) 任何长尾 mcp__* 工具加常见字段通用 fallback（取 `team_name / cwd-basename / agent_name` 等高识别度字段），避免「多出新 mcp 工具但应用没适配 → 纯名无摘要 → 用户截图反馈」循环 | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 反馈 4：「某些"工具"调用的信息还是没有，感觉是有必要的，特别是 TodoWrite、WebSearch 之类的」截图显示连续 8 个 `mcp__agent-deck__spawn_session` 调用纯名无摘要 — describeToolInput 缺 TodoWrite / WebSearch / WebFetch case；CHANGELOG_94 §B1 加在 SessionCard 的 5 case 不与 ActivityFeed 共用 |
| U6 | lead 在长跑 SDK orchestration（典型：deep-code-review SKILL 跑 30min / 多轮 review × fix）期间**必须保持对 user 的响应能力**——不要用 `wait_reply` 等阻塞工具卡死 lead turn，必须用 `check_reply` 非阻塞 poll + `ScheduleWakeup` 自我调度循环 poll 节奏。**约定要素**：(a) `check_reply` poll 节奏走 60-180s `delaySeconds`（≤ 5min 内 prompt cache 不失效避免每次 wake 重读全 context）；(b) lead 在 poll 间歇能正常 receive user input → 处理 → 继续 schedule 下次 wake；(c) 调长跑 mcp tool 时优先看是否有 non-blocking poll 版本，用 polling 比 blocking wait 更对 user 友好。原因：long-running blocking call 期间 user 任何追加反馈都被 SDK input queue 排队到 wait 超时才处理 → 体感「打字了但 agent 没反应」 | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 反馈 5：「以及 deep code review skill 还是用阻塞的 wait reply」 + 用户澄清「我是想让他用 check_reply」 — deep-code-review SKILL Step 2/4/5 把 `wait_reply` 改成 `check_reply` + `ScheduleWakeup` poll 循环 |

---

# Agent 踩坑候选

按 `count` 倒序。

| ID | 描述 | count | first_at | last_at | 触发样例 |
|----|------|-------|----------|---------|----------|
| P1 | 注册资源 / 标记后没在 try/finally 释放——失败路径漏清，状态卡 N 秒 / 误吞同类事件 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #2: sdk-bridge.ts releasePending 只在成功路径调，失败时 60s ttl 内同 cwd hook 被误吞 |
| P2 | 路径白名单 TOCTOU——校验用原始 path、读取走 realpath，symlink 改指向后越权 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #3: ipc.ts loadImageBlob symlink TOCTOU |
| P3 | 查表 / 校验时用全表扫 + 限 N 条 + JS 侧 .find/.filter，长会话或大数据后旧记录永久查不到 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #6: ImageRead 路径靠 listForSession 500 限制兜底 |
| P4 | Map / Set / cache 写入有分支条件，但 delete 只在某一分支末尾——其他分支线性泄漏 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #4: toolUseNames 只有图片工具分支 delete |
| P5 | Electron `before-quit` / 类似事件 listener 用 `async () => { await ... }`——回调返回的 Promise 不会被 await，清理只能碰运气跑完 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #7: index.ts before-quit |
| P6 | 全局 fuzzy 兜底匹配（"池子里只剩一个就一定是它"）——并发场景下会跨实体误命中 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #1: pendingSdkCwds size===1 模糊匹配误吞外部 CLI |
| P7 | catch 只 console.warn 吞错——上层 / UI 拿不到失败原因，用户看到「神秘 session-end」 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #5: SDK query loop catch |
| P8 | 依赖 npm/dev-only env（`process.env.npm_package_version` 之类）——打包后 undefined，硬编码默认值悄悄上线 | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #8: AppGetVersion 永远显示 0.1.0 |
| P9 | 测试断言 / 命令输出含**几千个连续重复字符**（`'x'.repeat(N)` 类）进对话 context——后续轮次触发 Anthropic AUP classifier 误判为异常负载，整轮回复被拦截。预防：测试用更短输入验证截断（8KB+1 而非 8KB+5KB）；或改用 `.toMatch(/regex/)` 让失败信息少 dump 原文；任何 Bash 输出 ≥ 5K 单字符重复时主动 `wc -l` / `head` 而非完整 read 进 context | 1 | 2026-04-24 | 2026-04-24 | Phase 2.1 vitest 跑 payload-truncate 截断测试，失败 assertion 把 13KB `'xxx...'` 完整 dump 到 stderr → 输出进对话 context → 后续 commit 流程被 AUP 拦截整轮回复 |
| P10 | 单测对生成的 SQL fragment 只做 regex / `toContain` 字符串匹配，没把生成的 SQL 灌进真 sqlite3 跑——broken SQL（语法错 / 列名错 / FTS 别名 MATCH 等）能从单测全过混上线，生产一调就抛。预防：涉及 SQL fragment 构造的纯函数测试要配独立「真 SQL 集成校验脚本」（`scripts/verify-fts5.sh` 即此类），跑在 system sqlite3 CLI 而不是 vitest（绕过 better-sqlite3 binding 不可用），并把 broken 形态作为 regression guard 验证它**仍然 fail** | 1 | 2026-04-24 | 2026-04-24 | Phase 4 N5 落地 search-predicate.test.ts 13 项全过，但 `fts MATCH @kw_fts` 别名形态在 SQLite parse fail（"no such column: fts"），整条 FTS 路径一搜就抛——Opus 4.7 现场用 sqlite3 CLI 跑出来的 |
| P11 | 「字节预算」类阈值用 `string.length` / `s.slice(0, max)` 比较截断——String.length 是 UTF-16 code units，不是 UTF-8 字节，emoji / 中文实际写入字节最高可达声明上限 3 倍；`slice(0, max)` 切到 surrogate pair 中间会切出孤儿 high surrogate（下游 JSON.parse 不报错但 UI 渲染替换字符 / 断字）。预防：任何与「字节」相关的阈值（SQLite 单行上限 / 网络协议 length-prefix / 文件名上限 / DB blob）一律 `Buffer.byteLength(s, 'utf8')` 算字节 + utf-8 安全切（找到 ≤ max 的最后一个 code point 边界），永不混用 `s.length` | 1 | 2026-04-24 | 2026-04-24 | REVIEW_4 H3: payload-truncate.ts 阈值 256K/8K 全用 string.length，emoji/中文实际可写 ~3× + truncateString 切 surrogate pair；测试只用 ASCII 'x' 验证骗不出该 bug |
| P12 | 依赖上游 SDK / CLI / 协议「文档默认行为」前必先最小复现脚本实测一遍——文档与实际行为不符是常态而非例外。涉及 fork / resume / streaming / 协议幂等性等关键假设的代码改动，没做实测就基于文档断言写「当前路径完全收敛」/「默认不会发生 X」类判断的，迟早被生产打脸。预防：写代码前先 5-10 分钟跑个最小脚本验证，把实测输出贴进 CHANGELOG / REVIEW 当证据 | 1 | 2026-04-24 | 2026-04-24 | CHANGELOG_24 备注「SDK 默认 resume: sessionId（不 fork）路径完全收敛」基于 sdk.d.ts:1255-1258 文档断言，但实测 CLI streaming + resume + 新 prompt 一定 fork → CHANGELOG_26 B 方案落地后用户场景仍然崩，REVIEW_6 才用最小复现脚本铁证根因 |
| P13 | 用 Map / Set / 数组的 `entries()` / `for...of` 反查 + `break` 推断「最新创建 / 最近匹配」的项——Map 迭代是**插入顺序**，break 取 first 不等于 latest；同 key 多次 set 不重排，新 entry 在末尾；同条件多 entry 时 first 是历史最早那条。预防：上游函数有返回值的话直接用返回值，不要事后反查；必须反查时显式按 ts / id / cwd+ts 排序后取 last，不要赌 iteration order | 1 | 2026-04-25 | 2026-04-25 | REVIEW_7 H1: sdk-bridge.ts recoverAndSend post-fallback 用 `for...entries() if cwd===rec.cwd break` 取 first 推断 newRealId，注释说「最新」但同 cwd 已有别的 SDK 会话时取错 → events/file_changes 子表错迁到不相关会话 |
| P14 | 跨进程 emit 顺序依赖未文档化的 IPC 队列序——main 端 `eventBus.emit(A) → emit(B)` 经 `webContents.send` 到 renderer 的顺序虽然在 Electron 同 webContents 内是串行的，但跨 channel / 经过 contextBridge 后语义未文档化稳定。renderer 端 store action 应对乱序鲁棒（任意顺序到达不破坏 by-session 数据），不依赖发端顺序。预防：renderer 端 `renameSession / removeSession / upsertSession` 等改 by-session 状态的 action，对「目标 key 已有数据」加 defensive `if (!next.has(toId))`，不覆盖较新数据 | 1 | 2026-04-25 | 2026-04-25 | REVIEW_7 M4: manager.ts renameSdkSession emit `session-renamed → session-upserted`，renderer 若 upsert 先到 → store.sessions.set(toId,newRec) 后 renameSession 用 fromRec 覆盖 → newer record 数据丢失（虽然 fork 路径下 NEW_ID 是新 id 几乎不会触发，但加 defensive 让乱序场景鲁棒） |
| P15 | React `setState((prev) => { ...; setView(x); select(y); return ...; })` updater callback 内调副作用——React StrictMode 开发态会双调 updater 验证 pure，setView / select 各执行 2 次（虽然第二次 noop 但反模式）；生产模式下也可能受并发渲染影响。预防：updater callback 必须 pure（仅返回新 state），副作用挪到外层（用 ref 持当前值在 listener 顶层比较 + 直接调副作用），或挪到 `useEffect` 监听依赖项变化 | 1 | 2026-04-25 | 2026-04-25 | REVIEW_7 L1: App.tsx onSessionRenamed listener 内 `setHistorySession((prev) => { if (prev?.id === from) { setView('live'); select(to); return null; } return prev; })` 在 updater 调 setView/select |
| P16 | Electron / Node main 进程未给 `process.stdout` / `process.stderr` 装 `'error'` listener——packaged GUI 模式下 launchd 把 stdout 接到一段 pipe，对端早期关闭后任何 console.log/error 立即抛 EPIPE → 因 stream 'error' 没 listener Node 升级为 uncaughtException → main 进程整个 die，HookServer / Helper / SDK 跟着退。预防：main 入口 imports 之后第一时间装 `process.stdout.on('error', () => {}); process.stderr.on('error', () => {});`，**不**接管全局 uncaughtException（语义副作用大）；调试输出长期方向是换 logger 而不是裸 console.log，但加 listener 是源头一次解决所有调用点的最小修 | 1 | 2026-04-29 | 2026-04-29 | REVIEW_10: 打包验证 wrapper 拉起后 main 立刻 EPIPE crash，stack 落在 window.ts:85 showOnce 的 console.log；grep src/main/ 全无 uncaughtException / stdout.on / stderr.on 兜底 |
| P17 | 「双通道防护」陷阱——给 SDK / 上游错误加 `expectedClose` 之类的 gate 时，必须同时 gate **catch 通道**（throw / abort / cleanup）和 **frame 通道**（SDK 自己合法推 is_error / error result message 然后正常关流），且**同分支内三个 sink 一起 gate**（红字 message / finished UI 状态 / mac 系统通知 routeEventToNotification 都靠同一 frame 触发）。只 gate 一边等于防护漏一半，只 gate 一个 sink 等于通道漏 2/3。SDK 把「应用主动 abort」翻译成 result 帧而非异常是常态（CLI 内部状态机检查不通过即合法 result frame，不抛错），通道差异通过红字文本前缀差异即可识别（catch 通道带「SDK 流中断：」前缀，frame 通道直翻 `⚠ ${detail}`）。预防：任何「区分应用主动 close 与意外抛错」的 flag 检查，先 grep 出所有 emit 红字 / error / finished / 系统通知的 sink 位置，整段 frame 在 flag 命中时**前置 return 整体静默**比逐个 sink 加 `&& !flag` 更稳 | 2 | 2026-04-29 | 2026-04-29 | REVIEW_11 Bug 1: D' 修法只 gate sdk-bridge.ts:1437 catch 块漏 line 1590 result frame；REVIEW_13 Bug 6: result frame 加 gate 但只 gate 红字 emit 漏 finished emit → routeEventToNotification 推 mac 系统通知「Agent 出错」 |
| P18 | 「allow = 同意退出」语义陷阱——SDK 工具协议中 `behavior:'allow'` 字面意义是「批准这次工具调用」，但某些工具的语义就是「请求退出当前模式」（典型 ExitPlanMode），allow 即**语义性退出**。如果业务想「批准内容但保持模式」必须改用 `behavior:'deny' + message` 形式让工具调用失败，通过 message 告诉 Claude「认可但保持模式」，**不能依赖事后 setPermissionMode 兜底**——post-allow 时序里 SDK 内部状态机已翻档，setPermissionMode 命中 race（CHANGELOG_47 同病灶 + REVIEW_11 Bug 3 双重证据：bypass 与 plan 都被 SDK 在 post-allow 窗口静默吞档）。预防：写 ExitPlanMode 类「mode-toggle 工具」的 resolver 时，识别哪些 targetMode 与「退出」语义冲突（plan→plan 即冲突），单独走 deny+message 分支并守卫外层不调 setPermissionMode；message 文案要明确「不要立刻再次调用同名工具」防 deny→retry 死循环 | 1 | 2026-04-29 | 2026-04-29 | REVIEW_11 Bug 3: ExitPlanMode resolver 对所有 approve 分支都 allow，CLI 把 approve+plan 视为退出 plan 立刻翻 default，外层 setPermissionMode('plan') race 失效 → UI 标签写「保持 Plan」实际行为是 default，下次 edit 走普通 PreToolUse 询问框 |
| P19 | 「同源黑名单只挡精确 ID」陷阱——sessionId 黑名单 / cwd claim 这类「按值匹配」的跨进程去重 / 隔离机制，对「上游进程内部 fork 出新 ID」/「上游 fallback 到不同 cwd 兜底值」的迟到事件天然失效。CLI 子进程被 SIGTERM 后内部 cleanup race 路径可能生成新 sessionId（与应用层 closeSession 时记录的 OLD_ID 完全无关）、可能用 HOME 兜底 cwd（而不是会话原始 cwd）—— 实测 approve-bypass 冷切场景两者**同时**发生 → recentlyDeleted 黑名单 + consumePendingSdkClaim 双兜底全失效，孤儿 source='cli' record 被创建。预防：跨进程边界的去重 / 隔离机制不能完全依赖「上游推上来的值」（sessionId / cwd / pid 都可能错乱），应在**应用 spawn 子进程时主动注入身份标记**（env / args / ipc handshake），下游事件靠该标记识别归属，不依赖业务字段。典型实现：env 注入 → hook 命令转发为 HTTP header → ingest 入口按 header skip。**误伤防线**：身份标记只对「应用主动 spawn 的子进程」生效，外部独立进程（用户终端跑 `claude`）天然没有标记，按「未知 / 默认外部」处理 → 零误伤 | 1 | 2026-04-29 | 2026-04-29 | REVIEW_12 Bug 5: REVIEW_9 1B (recentlyDeleted) + REVIEW_5 H1 (cwd claim) 双兜底防 approve-bypass 孤儿，但 OLD CLI fork 出新 sessionId Y + cwd=`/Users/apple` 兜底 → 两层全失效；改用 AGENT_DECK_ORIGIN env + X-Agent-Deck-Origin header origin tag 协议从源头解决 |
| P20 | 「调研 SDK / 第三方库能力时只看仓库当前调用面推断能力边界」陷阱——SDK 暴露的 API 远超应用当前用法是常态，仅靠 grep / 读 `sdk-bridge.ts` 当前 query options 等于把「没用过的能力」当「不支持」。Sub-agent 只看代码不查 `.d.ts` 时极易做出「SDK 不支持 X」类错判。与 P12 互补——P12 防「文档说有但实际没」（→ 实测），P20 防「代码没用就以为没」（→ 查类型定义）。预防：调研 SDK / 库能力时强制读 `.d.ts` 类型定义（grep 字段名 + Read 关键 section）+ 必要时实测；不能仅凭代码当前调用面下结论；prompt 给 subagent 时显式写「请查 d.ts 看可用但未用的能力，不要只看应用当前用法」 | 1 | 2026-04-30 | 2026-04-30 | REVIEW_14: Agent A Explore subagent 只读 sdk-bridge.ts:476-525 query options，错判「SDK 不支持 PreToolUse/PostToolUse hook，仅 canUseTool」+「Claude Code 无 OS 级沙盒」；主 agent 现场读 sdk.d.ts:1279 (hooks) / 1562 (sandbox) / 4656-4732 (sandbox 完整结构) / 1602 (managedSettings) 拿铁证反驳，三大能力 SDK 0.2.118 早就有，Agent Deck 100% 没用 |
| P21 | 「调研 SDK / 复杂系统**行为机制**时凭直觉 / 局部观察推断 → 误判」陷阱——sandbox / proxy / 双层防护这种「实际跑起来涉及多层并行 + 工具回路 + 环境变量注入」的机制，纯靠「现象观察 + 类比推理」很容易把表象误当根因。典型坏路径：「用户没看到弹框 → 推断分支没触发 → 标死代码」「curl 拿到 proxy 403 → 推断没用工具回路 → 改配置消除 network」。**两者真相都是有 log 一秒就揭穿但没 log 时怎么猜都是错**（双层并行不是二选一 / 自动 deny 路径用户看不到但 log 有铁证）。与 P20 互补——P20 是「能力存不存在」类（→ 查 d.ts），P21 是「行为怎么工作」类（→ 加 log + 实测）。预防：碰到 sandbox / proxy / 跨进程 / SDK 内置工具这类「行为机制」问题，**先在关键决策点加临时 log + 跑实证**（log 完成调查后清理 / 保留 1 行高价值的）；尤其「分支没触发」「字段不生效」这种「负断言」必须靠 log 证伪不能靠「用户视角没看到」 | 1 | 2026-04-30 | 2026-04-30 | REVIEW_15: 实施过程连续 4 次假设错误（managedSettings.sandbox 装载整套 / 不传 network 走工具回路代替 HTTP_PROXY / canUseTool 分支是死代码 / 是某字段触发 SDK 切模式）全被实测证伪；加 [canusetool] log 一秒揭穿真相（双层并行 + canUseTool 分支稳定生效），用户给 X-Proxy-Error: blocked-by-allowlist 头补完真相 |
| P22 | 手写 YAML / frontmatter writer **不转义** value 直接 join `${k}: ${v}`——多行字符串、含 `:` `#` `"` `\` 的 value、起首末尾空白都会让回读 parser 走偏。典型 case：`description: line1\nline2` 写盘 → parser `(.*)$` 单行 regex 静默丢 line2；`description: 'x\n---\nname: hijacked'` 串成嵌套 frontmatter 块；description 含 `#` 在 YAML 标准下应被当注释（与 app 自定义 parser 行为分歧）。预防：要么用真 YAML lib（`yaml` 包），要么走「IPC 入参禁单行字段含 `\n` `\r` `---` + writer 一律加双引号 wrap value + escape `\\` `\"`」三件套；不要赌「中文 description 不会碰到这些字符」 | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R1·F2+F3：双方独立 node 实测复现 user-assets stringifyFrontmatter 多行 + `---` 注入双 bug；修法走 ipc/assets.ts parseSingleLineString 禁三类字符 + utils/frontmatter.ts 双引号 wrap |
| P23 | 新增「跨 dialog / 跨 view 跳转按钮」必须经过当前 dialog 的 dirty 拦截链路——只在主关闭按钮（X / 点外部）走 guardedClose 不够，任何让父级 setOpen(false) 的入口都会让子组件 unmount → cleanup `onDirtyChange?.(false)` 重置 dirty ref → 用户未保存草稿无确认静默丢失。预防：所有「关 dialog A → 开 dialog B / 跳别处」的按钮都要 wrap 进 guarded 函数（同一锁 + 同一 dirty 检查 + 同一 confirmDialog 弹「丢弃 / 继续编辑」）；新增「跳出去」按钮 review 必检 dirty 路径覆盖 | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R1·F1（HIGH，反驳轮 codex 同意）：CHANGELOG_57 设置面板新加「在资产库中查看 ↗」按钮 onClick 直接调父级 onOpenAssetsLibrary 不走 guardedClose，ClaudeMdEditor unmount 静默清 dirty ref；修法 wrap guardedOpenAssetsLibrary 复用 closeInFlightRef 锁 |
| P24 | renderer 与 main 双端校验 regex / 长度限制 / 字段约束**必须共享同一份常量**——分别硬编码迟早漂移：renderer `^[a-z0-9-]+$`（接受 `-foo`）vs main `^[a-z0-9][a-z0-9-]*$`（拒绝），UI 校验通过 → IPC 拒收，错误信息里的 regex 还和 main 实际用的不一致。预防：所有跨进程边界的字段约束（regex / 长度 / 枚举值）抽到 `src/shared/types/<domain>.ts` 或 `src/shared/constants/` 共享常量，renderer + main + 文档错误信息全引同一份；类似 SANDBOX_MODE_VALUES（CHANGELOG_54）/ READ_ONLY_TOOLS（CHANGELOG_56）已有的同模式参照 | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R1·F8+F9：CHANGELOG_57 user-assets ASSET_NAME_REGEX renderer 与 main 三处独立硬编码漂移 + 4 处错误消息 / 注释 regex 与代码不一致；修法抽 ASSET_NAME_REGEX + ASSET_LIMITS 到 shared/types/assets.ts |
| P25 | 多 escape 字符串解码必须按 quote 反序处理（或 char-by-char 走状态机）——多次 sequential `String.prototype.replace` 会因「中间态再匹配」错位。典型坏代码 `text.replace(/\\n/g, '\n').replace(/\\\\/g, '\\')`：用户写字面 backslash + n（如 Windows 路径 `C:\new` / regex `\d` / 文档 `\n`），quoteValue 写文件后 step 1 `\\n → \n` 直接把字面字符吃成换行。预防：写 string unescape 时一律 char-by-char while + switch 单趟扫描（每个 `\` 只能消耗紧跟的一个字符）；或反序 sentinel 替换法（先 `\\\\` → 临时字符再撤其余 escape 最后还原）；写完必跑 round-trip vitest 含「字面 backslash + 转义字符」case 守门 | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R2·R2-F1：reviewer-claude + reviewer-codex 双方独立 node 实测复现 utils/frontmatter.ts unquoteValue 5 次 .replace() 顺序 bug（`'foo\nbar'` 8 字符 round-trip 后 char[4]=10 真换行）；修法 char-by-char state machine + 15 vitest case 守门（`src/main/utils/__tests__/frontmatter.test.ts`） |
| P26 | main 进程**同一模块在 ≥ 2 处用 dynamic import** 引用，且**没有任何静态 import 路径**——vite SSR + rollup 默认 chunking 会把模块代码 inline 进主 `index.js`，单独 chunk 文件只剩 `require(deps)` 空壳没有 `module.exports` / `Object.defineProperty(exports,...)`，运行时 dynamic import 拿到空对象 → destructure 到 undefined → `TypeError: X is not a function`。dev 模式 ESM 直 import 拿原模块测不出来，**只在打包后炸**。对照组：被 ≥ 1 处静态 + ≥ 1 处动态混用的模块，vite 会触发 warning「dynamic import will not move into another chunk」自动当 static 处理（事实上避坑）；唯一一处 dynamic import 的模块（如 `transport-http`）chunk 内容正常带 export。预防：(a) 禁止「main 进程同模块多处 dynamic import 但零静态 import」组合，要么全 static（推荐，简单且无运行时差），要么至少一处加静态 import 让 vite 自动转 static；(b) 写新 dynamic import 前先 `grep -rn "<module-name>"` 确认引用次数；(c) 打包后**必检** `out/main/*.js` 各 chunk 大小，若出现 < 0.5 kB 的「.installer / .writer / .helper」类小 chunk，cat 一眼是不是空壳 | 1 | 2026-05-12 | 2026-05-12 | REVIEW_25：打包验证 wrapper ping 暴露 `[bootstrap] syncAgentDeckSection / syncSkills 失败 TypeError: ... is not a function`；现场对比 `out/main/agents-md-installer-cYcOGELy.js`（0.17 kB 空壳）vs `transport-http-Ctvu5mtC.js`（2.16 kB 完整 export）锁定模式；修法 5 处 dynamic import → static import（index.ts × 2 + settings.ts × 3，含同模式但 ping 触发不到的 toml-writer），3 个空壳 chunk 文件全消失主 index.js +8 kB |
| P27 | bootstrap 启动 fastify / express / koa 等 HTTP server 时，**所有 route 必须在 listen 前注册完**——fastify 5.x 在 `app.listen()` resolve 后调 `app.route()` 直接抛 `FST_ERR_INSTANCE_ALREADY_LISTENING`（`lib/route.js:208 throwIfAlreadyStarted`），框架不支持「热挂 / 热卸路由」(by design)。新增的「按设置项条件挂载」类功能模块（如 plugin 自动启停 / 双开关 gated transport）极易被错误地放在 `listen()` 之后（写代码顺序就近），造成路由完全不挂但应用其他部分继续跑 → 只在打包后用户实际打开开关时才暴露。dev 默认值通常 OFF 不暴露 = 长期沉睡 bug。预防：(a) 用「PRE_LISTEN / POST_LISTEN 分水岭」的概念组织 bootstrap，所有 route 注册必须在 listen 之前；(b) 在 `HookServer.registerRoute` 这类应用层包装入口加 invariant `if (this.started) throw new Error(...)` 把契约固定在 HookServer 边界，错误文案直接指向修法（fastify 通用「Cannot add route」距离应用语义远 + 错误位置在框架内层）；(c) 写「按 settings 条件挂 transport / route」类功能时，必须 grep 确认放在 `hookServer.start()` 之前，不要赌「就近放在原 step 后面」 | 1 | 2026-05-12 | 2026-05-12 | REVIEW_27：打包安装后 wrapper ping 暴露 `[agent-deck-mcp] failed to mount HTTP transport FastifyError: Fastify instance is already listening. Cannot add route!`；引入 commit `cdb01ae feat(mcp): B'4 + R1.A5 + R1.D7 codex 自动挂 agent-deck MCP via HTTP transport` 把 6.5 段错放在 6 步 `await hookServer.start()` 之后（写代码就近放在 6 步注释下面，没意识到 fastify listen 后 addRoute 被禁）；修法把 6.5 段挪到 5.5 PRE_LISTEN + HookServer.registerRoute 加应用层 invariant；dev 默认 `enableAgentDeckMcp=false` 一直沉睡，本次新装 .app 用户数据持久化触发 |
| P28 | 依赖 `AgentEvent` / 跨进程消息字段做渲染分发 / 业务分支判断时，**必须先确认该字段是否在持久化层（DB schema / file / cache）也被一起带过来**——内存中的实时事件流字段齐全，但 listForSession / IPC 重新拉历史 / 重启加载等路径只走 DB schema 投影，不在 schema 里的字段一律 `undefined`。基于「事件长这样」的直觉写守卫（如 `if (event.source === 'sdk')`）对实时事件铁证生效，但对刷新 / 切回历史会话场景**全失效**——bug 只在「等价语义但路径不同」时暴露，复现要靠用户截图。预防：(a) 写任何 `if (event.X === 'Y')` 类渲染分发前，**必须 grep `event-repo.ts` insert / rowToEvent**（或对应 store 的 schema），确认 X 字段被持久化；不在 schema 里就不能依赖；(b) 字段不在 schema 时三选一：① 升级 schema 加列 + migration（重，全用户 DB），② 找等价的 session-level / 上层 props 替代（轻，参考 manager.ts:270 把 event.source 提升到 sessions 表 `source` 列），③ 每次 emit 时把字段 inline 进 payload_json（中等，需迁移老 payload）；(c) 改完必跑「重启应用 / 切别的会话再切回来」回归路径，不能只 trust 实时事件流测试 | 1 | 2026-05-12 | 2026-05-12 | REVIEW_26 Follow-up：REVIEW_26 主修法在 ActivityRow 加 `if (event.source === 'sdk') hide AskUserQuestion/ExitPlanMode tool-use-end`，对实时事件铁证生效但用户刷新窗口后红框「AskUserQuestion 失败」又出现；现场读 `src/main/store/event-repo.ts:5-12 / 28-31` 锁定 events 表 schema 只有 `(session_id, kind, payload_json, ts)` 四列、没有 `source` 列、`rowToEvent` 也不带 source → listEvents 拉历史 `event.source === undefined` → 守卫失效；修法把 `event.source === 'sdk'` 替换为 session-level `isSdk` props（sessions 表 `source` 列 manager.ts:270 持久化、`SessionManager.dedupOrClaim` 保证两者等价），不动 schema 不写 migration |
| P29 | 第三方包升级到 v5 / 引入 ESM `package.json` `exports` 字段 + 是传递依赖（pnpm strict isolation 不暴露 root `node_modules`）联合时，**dev / prod / TS 三套 resolver 行为分歧** → 打包前完全测不出。具体三连：(a) v5 改 exports 路径（如 `refractor/lang/<lang>` → `refractor/<lang>` 由 `"./*": "./lang/*.js"` 重映射），dev 模式 vite deps optimizer 走 `.pnpm/<pkg>@<v>/node_modules/<pkg>/lang/*` 老路径仍能跑、写错也不报；(b) prod rollup 严格按 v5 exports 字段解析失败抛 `Failed to resolve import`；(c) 修完 import 路径后 TS `moduleResolution: "node"` 仍不识别 conditional exports，typecheck TS2307 全报需要 ambient shim 兜（或升 `bundler` 模式）；(d) pnpm strict isolation 让 root `node_modules` 没该包，需要把传递依赖提为直接依赖。预防：(a) 引入 / 升级第三方 ESM 包前先 `cat node_modules/.pnpm/<pkg>@<ver>/node_modules/<pkg>/package.json` 看 `exports` 字段；(b) 用包的子路径前 grep `package.json` 顶层 `dependencies` 是不是直接依赖，否则提一下；(c) 改完 import 必跑 `pnpm dist` 而不只是 `pnpm typecheck` / `pnpm dev`，因为 dev rollup 不严格 / dev vite optimizer 兜底；(d) 单点小修推荐 ambient shim 兜 TS resolver（最小侵入），多个 v5 ESM 包同问题再升级 `moduleResolution: "bundler"` 一次性收口 | 1 | 2026-05-13 | 2026-05-13 | REVIEW_29 HIGH-1：`fe1effe` MarkdownText 引入 `import bash from 'refractor/lang/bash'`（v3 风格） × 15 行，dev 模式 vite optimizer 兜底跑通、`pnpm typecheck` 走 `.pnpm/refractor@5.0.0/.../lang/bash.d.ts` 跑通；`pnpm dist` 一跑 rollup 报 `Rollup failed to resolve import "refractor/lang/bash"` from MarkdownText.tsx；改路径后 TS 又 TS2307 全报需 shim；refractor 是 react-syntax-highlighter@16.1.1 传递依赖不在 root 还要提为直接依赖。三件事必须一气呵成才能跑通 |
| P30 | 写 / review SQLite migration 时如要 `ALTER TABLE ... DROP COLUMN`，**必须先 grep 全 codebase 找所有引用该 column 的 index / view / trigger** 再判断「DROP 安全」；SQLite 3.35+ DROP COLUMN 拒绝 column 被任何 index 引用（含 partial index `WHERE` 表达式 / 普通 index 列引用 / 表达式 index）→ ALTER TABLE 抛错 → migration runner `db.transaction()` 整事务回滚 → user_version 不 bump → 下次启动重跑同样挂 → 应用 bootstrap fatal 永远起不来。注释里凭旧记忆 / 老 PR description / 当时 commit message 判断「列没建 index，DROP 安全」是高频踩坑形态——column 引入 commit 与 index 创建可能在同 migration 也可能跨 migration，时间长后人脑记不全。预防：(a) 写 DROP COLUMN migration 前**强制** `grep -rn "<index_name_pattern>\|<column_name>" src/main/store/migrations/ --include="*.sql"` 全扫，列出所有引用；(b) 任何引用该 column 的 index / trigger / view 必须在同一 migration 内 `DROP INDEX IF EXISTS ... ;` 在 ALTER TABLE 之前；(c) migration 注释 + commit message 必须把「实际清掉的 schema artifact」列清楚（不只是「drop column」），否则下次 review 同主题缺乏现成记录；(d) 严重情况下加 vitest 跑全 migration 链（用临时 DB 文件 / `:memory:`），catch 任何 SQLite 抛错——dev 跑通是因为本地 DB 状态可能跳过某 step，CI 跑全链才能 catch | 1 | 2026-05-13 | 2026-05-13 | REVIEW_29 HIGH-2：`bb13b32 plan team-cohesion-fix Phase F` 落地的 v014 注释明确写「v006 只 ADD COLUMN，DROP 安全」，但 v006 实际同时建过 `idx_sessions_team_name ON sessions(team_name) WHERE team_name IS NOT NULL` 部分索引；v014 直接 `ALTER TABLE sessions DROP COLUMN team_name` 在 SQLite 3.35+ 必挂 `error in index idx_sessions_team_name after drop column: no such column: team_name`；migration 事务回滚 → bootstrap fatal；新装 + 升级路径都中招。修法 1 行 `DROP INDEX IF EXISTS idx_sessions_team_name;` 加在 DROP COLUMN 之前 + 注释完整修正 v006 实际状态 |
| P31 | 调 plugin SKILL / 注册过的 MCP 工具时，**绝不要事先做 SSOT discovery**（`find / -path '*<plugin>*'` / `grep <skill-keyword> ~/.claude/plugins/` / `ls /Applications/<app>.app/Contents/Resources/`）—— Skill 工具与 mcp__* 工具按名直接 invoke 即可，**不存在会报错**，根本不需要前置存在性确认。这种 SSOT discovery 是 reasoning 浪费的高频形态：单次跑 4-6 个 Bash 命令、每个命令 latency 数百 ms 到数秒不等、占大量 context token，最后还是直接调工具（discovery 输出对调用本身完全无影响）。预防：(a) 看到 system-reminder 列出的 SKILL 名 / MCP 工具名直接 invoke，名字打错有 fallback error 提示再 search；(b) 任何「这个 skill 装了吗 / 路径在哪 / 文件存在吗」类问题应该是「调一次试试」而不是「先 find 一遍」；(c) 真要看 SKILL 源码（debug / 开发 SKILL 本身）用 `Glob` 或单条精确 `find <path>` 不要全盘扫 | 1 | 2026-05-13 | 2026-05-13 | REVIEW_32 旁观察：本 plan deep-review-and-split-20260513 H1 lead Claude 与之前会话另一个 Claude 都犯同款 — 判断要不要走 deep-code-review SKILL 时跑 `find / -path '*/agent-deck*' -name 'SKILL.md'` / `grep -i 'deep.code.review'` / `ls /Applications/Agent Deck.app/Contents/Resources/` 等多个 Bash discovery 命令（grep 模式拼写错还吃返回 `(Bash completed with no output)`），最后还是 `Skill(skill: 'agent-deck:deep-code-review', args: '...')` 直接调成功；用户主动指出这个反模式 |
| P32 | useEffect cleanup `disposedRef.current = true` + 新 effect body 立刻置回 false 的「ref guard 自动失效」反模式 —— 看似比 closure 局部 `let disposed = false` 更优雅（共享多次异步调用），实则丢了「per-effect 隔离」语义；任何 effect 重跑（含 props 变化触发：`[open, sessionId]` deps 内 sessionId 切换会先 cleanup 设 true 再立刻新 effect 重置 false）都让旧 in-flight IPC 又能通过 guard 污染新 props 状态。预防：异步 IPC 取消用 sequence counter（`++req` + 闭包捕获 cur + resolve 时校验 `cur !== requestSeqRef.current`）或闭包局部 `let disposed = false`（每次 effect 一份）；**不要**用单 boolean ref；如必须用 ref 且 effect 会重跑，至少加「校验 props 关键 id 仍是触发 IPC 时锁定的值」第二道护栏（`capturedSid !== sessionId`）。与 P14（renderer store action 对乱序鲁棒）同主题（异步状态依赖时序保证）但触发条件正交 | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 review 修复 HIGH-1：reviewer-claude + reviewer-codex 双方独立指出 HandOffPreviewDialog disposedRef 模式 + node -e 实测复现；修法 requestSeqRef + capturedSid 双护栏 |
| P33 | DB FK 约束 `ON DELETE RESTRICT` + 应用层「leave / archive」操作仅 `UPDATE left_at = ?` / 写状态字段而**不物理删 row** 的反模式 —— 看似「保留历史 row」语义清晰，实则 RESTRICT 不在乎 left_at 是否非空，任何 caller `DELETE FROM 父表` 都撞 FK constraint failed；ADR 注释里写「sessionManager.delete 入口加 pre-check 兜底」**实际无效**（pre-check 走的就是 leaveTeam 写 left_at），bug 隐藏在「只有用户极少触发的 hard-delete + active membership」组合下，长期未触发未暴露，直到用户在 active membership 的 session 上做 fork rename / UI 删除才一次性炸出。预防：(a) 设计 FK 时**先想物理删谁、谁不删**：「保留历史 row」语义 → FK 改 `ON DELETE CASCADE`（接受 row 跟随父表一起被清，归属感本就在父表一侧）；「严格阻塞父删」语义 → FK 用 `RESTRICT` + 应用层 leave 必须 hard-delete 子表 row；**不要选 RESTRICT 然后 leave 只 UPDATE 状态字段**——两个目标互斥；(b) 任何「pre-check 兜底」类注释必须有真测覆盖（直接 `DELETE FROM 父表` 抛 FK 的反向断言 + 走应用层 delete 路径成功的正向断言），靠注释 trust 没有真测一旦设计变更立刻烂；(c) 任何 SQL 路径执行 `DELETE FROM 父表` 之前 grep 全 codebase 找 `REFERENCES <父表>(<id>) ON DELETE RESTRICT`，列清楚每个子表的迁移 / 清理策略；(d) 与 P30（DROP COLUMN 没 grep 全 index）+ P10（SQL fragment 没真 SQLite 测）同主题（DB schema 与代码状态认知错位） | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_96 / plan linked-swimming-platypus：用户报 SDK 流中断「⚠ FOREIGN KEY constraint failed」screenshot；DB 数据 + 代码追源完全验证根因（lead 在 4 个 team active 时 fork rename 触发 sessionRepo.rename 内 DELETE OLD 撞 RESTRICT FK 整事务回滚 + 附带发现 sessionManager.delete 同条 FK 链路也撞）；实测铁证 `sqlite3 ... DELETE FROM sessions WHERE id='a6260807-...' → FOREIGN KEY constraint failed (19)`；修法 v017 schema RESTRICT → CASCADE + rename 显式迁 3 类引用 |
| P34 | follow-up plan stub 创建惯例尚未模板化（process gap）—— lead 建 stub 时各凭手感写,常缺关键字段(如本次建 archive-plan-tool-ux-followup-20260515 时,worktree_path frontmatter 必填 + 已知踩坑 inline 父 plan 教训 + 下一会话第一步具体到「先 cat plan + EnterWorktree(path:) + 自检 HEAD」+ 设计决策给 hint level 推荐方向不强 sign-off 等)。预防:把 stub plan 创建模板化(本 stub archive-plan-tool-ux-followup-20260515.md 自身可作 reference),最小要求 = (a) frontmatter 6 字段全(plan_id / created_at / worktree_path / status / base_commit / base_branch / parent_plan_id)+ (b) 总目标 & 不变量 ≥ 3 句明示 scope + 不动哪些既有契约 + (c) 设计决策给 hint level 不强 sign-off + (d) 步骤 checklist 先粗后细(Phase 1-4 框架 + Step N 不要超 5 个) + (e) 下一会话第一步**绝对路径 + 具体动作**(Bash cat / EnterWorktree path / git log -3 / 起对抗 SKILL 等不写「Read」防 cross-session cache)+ (f) 已知踩坑 inline 父 plan 教训 + 本 stub 创建时同步发现的 + (g) 会话风格授权 inline 用户最近一次 hand-off 授权(后续会话默认继承)。原因:每次 hand-off 都重新摸索 stub 写法浪费 mental cycle + 关键字段漏写让新会话 cold-start 撞坑(如 worktree_path 漏 → cold-start 不知去哪 EnterWorktree)| 1 | 2026-05-15 | 2026-05-15 | plan archive-plan-content-overwritten-fix-20260515 收口 hand-off followup 三个 stub plan 创建过程中 lead 反思,REVIEW_44 §follow-up (f) 触发 |

<!-- 历史升级范例（已升到 CLAUDE.md 的可在此处留 1-2 行注解，便于追溯）：
- P1 + P2 + P5 同主题已半升级到「资源清理 & TOCTOU 防线」小节作为预防（CHANGELOG_16），但表里仍保留 count=1 等下次再撞同主题时计数推进
- U2 在 2026-04-24 count=3 走双对抗（Opus 4.7 + codex gpt-5.4 xhigh）三态裁决（修改后落地），新建「会话恢复 / 断连 UX（resume 优先）」小节升入 CLAUDE.md（CHANGELOG_25）
-->

# Convention Candidates

> Agents maintain this file through the repository convention process. Do not delete entries manually.
> When `count >= 3`, run the configured review process, create `ref/conventions/<X>-<topic>.md`, update [INDEX.md](INDEX.md), and remove the promoted item from this tally. Do not copy promoted conventions back into project [CLAUDE.md](../../CLAUDE.md).

## Usage

There are two candidate classes, split into sections in the same file:

| Type | Trigger |
|---|---|
| **User feedback** (`# User Feedback Candidates`) | The user gives corrective or preference feedback: "Do not...", "Should...", "I already said...", "From now on...", "Remember...", "Every time..." |
| **Agent pitfalls** (`# Agent Pitfall Candidates`) | A Coding Agent independently finds the same class of pitfall during review / bug fixing / investigation, or the same class of issue repeatedly appears in review reports (typical examples: missing cleanup in try/finally, TOCTOU, N+1 queries, async listeners not awaited). |

**Operating workflow** (whenever qualifying feedback arrives or a pitfall is found):

1. **Determine scope**:
   - User feedback: must be an in-project engineering preference / design tradeoff / workflow preference; one-off requests ("help me fix this bug") do not count.
   - Agent pitfalls: must be a patterned issue (a class of problem recurring), not a single isolated bug.
2. **Read this file** and look for an existing semantically similar entry:
   - Found -> increment `count` by 1 and update `last_at` to today's date.
   - Not found -> add a new entry (`count: 1`) in the corresponding section.
3. **When count reaches 3** -> treat it as a convention-promotion decision and run this repository's configured review process for a **three-state verdict**:
   - Start two independent heterogeneous Agents to review the promotion proposal: wording accuracy, boundary clarity, conflicts with existing conventions, and the best target section.
   - Summarize the three-state result and tell the user: "This [feedback/pitfall] has accumulated 3 occurrences. The adversarial review verdict is adopt / reject / keep observing as follows. Promote it?"
   - After user confirmation, **create `ref/conventions/<X>-<topic>.md`** from the repository root (X is an increasing integer, independent from changelog/CHANGELOG_X and reviews/REVIEW_X numbering), add a row to [INDEX.md](INDEX.md), and remove the item from this file.
4. **When count < 3** -> silently update this file without disturbing the user.

> If an item has not been updated for 30 days and count < 3, the next scan may proactively prune it.

---

# User Feedback Candidates

Sorted by descending `count`.

| ID | Description | count | first_at | last_at | Trigger example |
|----|------|-------|----------|---------|----------|
| U1 | User choices made in the UI should persist by default (DB / settings file), not live only in memory and disappear after restart. | 1 | 2026-04-21 | 2026-04-21 | "Permission memory cannot just live in memory, right?" |
| U2 | UI copy terminology must be **globally consistent** (terms in the settings panel = terms in status badges / lists / notifications), and must be **platform-split** so only descriptions relevant to the current platform are shown (renderer uses `IS_DARWIN/IS_WIN/IS_LINUX` conditional rendering; do not mix mac/win copy). Technical details, such as bundled plugin agents/skills names or the `mcp__tasks__*` tool list, should not be stuffed into settings-panel toggle descriptions; expose them through a separate centralized entry. **Convention elements**: standard Chinese terms + English in parentheses when needed (for example, "Claude Code Hook (system hook)"); keep proper nouns in English; keep internal fields / APIs / types in English (`active/dormant/permissionMode`); UI-layer title/aria-label values must use the UI-standard Chinese terms (`closed` in StatusBadge.tsx is a bug). | 3 | 2026-05-06 | 2026-06-18 | "Can we distinguish the environment, so mac only loads mac-related descriptions and win only loads win-related descriptions instead of mixing them together ... Apart from the config panel, the Chinese/English comparison should also stay consistent elsewhere, or users will be confused about which setting maps to which feature." Latest trigger: token statistics model labels should use one normalized bucket-key style such as `opus-4.8` and `gpt-5.5`, not a mix of `Opus 4.8` and `gpt-5.5`. |
| U3 | In detail views (SessionDetail / TeamDetail / similar containers), place the "close / back" button consistently at the top-right of the header; **do not put it top-left**. Group it on the right with the actions slot (close N teammates / archive / similarly high-cost actions), while the title stays left and takes `flex-1`. **Convention elements**: close button className uses `flex h-5 w-5 items-center justify-center rounded text-[11px] text-deck-muted hover:bg-white/10`; use an icon-only square button, not a text button such as "← Back". Reason: inconsistent button placement across detail views causes misclicks and visual discontinuity; the `←` icon already conveys back semantics, while text consumes extra space. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 feedback 2: "Then also put the team page exit button in the top-right to keep it consistent." TeamDetail/Header.tsx moved "← Back" from top-left to top-right grouped with actions, mirroring the SessionDetail header `←` button className. |
| U4 | High-cost / irreversible / external-API operations (typical examples: LLM oneshot, starting a new SDK session, restarting a subprocess, using Bash to start a subprocess for a several-GB build) must require an explicit "trigger button" rather than auto-starting on mount / dialog open. **Convention elements**: (a) modal mount useEffect only resets state and does not trigger expensive IPC; (b) show a CTA button (`bg-status-working/30` primary color to indicate "primary action") plus explanatory copy that states the cost (for example, "Each run calls the sonnet API and is billed by token"); (c) do not render the result area in the idle state. Reason: users may accidentally click while hovering for a tooltip, open the modal only to read its explanation, or refresh after switching back to an archived session; auto-starting directly burns tokens / starts processes, wasting resources and making the user feel out of control. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 feedback 1: "Start the summary only after the hand off button is confirmed, otherwise one click wastes LLM resources." HandOffPreviewDialog no longer auto-calls sonnet on mount and instead uses an explicit "Start summary" CTA button; `hasSummarized` distinguishes idle / loading / done / error. |
| U5 | Cross-process rendering dispatch / business-branch decisions should **not maintain separate duplicated description functions** (typical case: SessionCard `summariseToolInput` adds cases but ActivityFeed `describeToolInput` is not updated, so new tool calls show only a raw name with no summary in ActivityFeed details). **Convention elements**: (a) when adding a new tool case, update both places at once and cover with tests; or (b) extract a helper (note: not extracted this time because SessionCard is used for SessionList single-line display while ActivityFeed is used for details + SimpleRow; the shapes are close, but SessionCard sits near a split boundary, so extracting would have forced a SessionCard refactor and was YAGNI); (c) every long-tail `mcp__*` tool should get a common-field fallback using high-recognition fields such as `team_name / cwd-basename / agent_name`, avoiding the loop "new mcp tool appears but app is not adapted -> raw name only -> user screenshot feedback". | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 feedback 4: "Some 'tool' call information is still missing; it feels necessary, especially TodoWrite and WebSearch." The screenshot showed 8 consecutive `mcp__agent-deck__spawn_session` calls with raw names only. `describeToolInput` lacked TodoWrite / WebSearch / WebFetch cases; CHANGELOG_94 §B1 had added 5 cases only in SessionCard and did not share them with ActivityFeed. |
| U6 | During long-running SDK orchestration (typical examples: deep-code-review SKILL runs for 30 minutes, or multi-round review x fix), the lead **must remain responsive to the user**. Do not block the lead turn with tools such as `wait_reply`; use non-blocking `check_reply` polling plus `ScheduleWakeup` self-scheduled polling loops. **Convention elements**: (a) `check_reply` polling cadence should use 60-180s `delaySeconds` (within 5 minutes so prompt cache does not expire and force each wake to reread the full context); (b) between polls, the lead can normally receive user input -> handle it -> schedule the next wake; (c) when calling a long-running mcp tool, first check whether it has a non-blocking polling variant, and prefer polling over blocking wait for better user responsiveness. Reason: while a long-running blocking call is active, any additional user feedback is queued in the SDK input queue until wait timeout, creating the experience that "I typed but the agent is not responding." | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 feedback 5: "Also, the deep code review skill is still using blocking wait reply" plus the user's clarification "I meant I want it to use check_reply." deep-code-review SKILL Step 2/4/5 changed `wait_reply` to `check_reply` + `ScheduleWakeup` polling loops. |
| U7 | mcp tool input / output / enum naming split across three tracks: early app schemas used snake_case for inputs, camelCase for outputs, and kebab-case enum values (`'claude-code'` / `'workspace-write'`), inconsistent with the JS / TS ecosystem norm of camelCase inputs and outputs, creating a split caller experience. **Convention elements**: (a) mcp tool input fields should use camelCase, aligned with outputs and mainstream JS; (b) enum values remain kebab-case because caller-side string literals have established expectations; (c) plan workflow frontmatter fields remain snake_case (`archive_plan` / `hand_off_session` hardcode parsing by snake_case keys; changing frontmatter is outside this convention); (d) SQLite columns / mcp tool function names (wire-level identifiers such as `archive_plan` / `task_create`) remain snake_case. **Reason**: split input field naming across tools increases cognitive load, such as passing `team_name` to spawn_session but reading `teamId` from send_message output. **count = 1 starting point**: one feedback instance plus full plan implementation (see [CHANGELOG_177](../changelogs/CHANGELOG_177.md)); future same-theme feedback increments by 1. count >= 3 promotes to ref/conventions/<X>-<topic>.md. | 1 | 2026-05-29 | 2026-05-29 | During plan deep-project-review-comprehensive-20260528 Phase 5.3 closeout, user feedback said "the coexistence of three naming tracks is inconsistent" -> plan mcp-tool-camelcase-migration-20260529 comprehensively migrated 32 unique fields from snake_case to camelCase (see CHANGELOG_177 + full plan). |
| U8 | Human-gated MCP tools that intentionally wait for UI/user decisions must align the client transport timeout with the app's existing human-approval timeout setting when one exists. If the handler's pending UI card and the MCP client use different timeout sources, the UI can remain pending while the model receives a tool failure. **Convention elements**: (a) confirm whether the timeout lives in service code, MCP SDK request defaults, adapter-specific MCP config, or an existing app setting before changing behavior; (b) prefer reusing the app's permission approval timeout over adding a second human-wait timeout knob; (c) preserve `0 = no timeout` semantics across handler timers and adapter MCP config; (d) keep explicit per-call `timeoutMs` able to shorten the wait, not silently extend beyond the app-level human approval timeout. | 1 | 2026-06-18 | 2026-06-18 | User screenshot/report: "超时时间有问题" after `request_plan_review` left the pending approval card visible but the Codex MCP tool row failed before the user approved/revised. CHANGELOG_280 changed both Codex `mcp_servers.agent-deck.tool_timeout_sec` and the `request_plan_review` handler default to follow `permissionTimeoutMs`. |
| U9 | Lifecycle cleanup jobs must distinguish fully abandoned records from records in a creation/initialization window. A scheduler that sees a newly created object in an intermediate state should apply an age/grace threshold before destructive state changes such as archive/delete, because multi-step creation often creates the parent row before child/session/member rows exist. **Convention elements**: (a) when cleanup keys off "empty/no members/no children", compare `now - createdAt` against a grace window; (b) add a regression test for fresh transitional emptiness and a separate test for old ghost cleanup; (c) verify runtime timestamps when the user reports a cleanup race instead of assuming the sender/receiver ACL is wrong. | 1 | 2026-06-18 | 2026-06-18 | User screenshot showed reviewer `send_message` failures after a spawn review team was archived by scheduler before members were added. DB evidence: team `29eb...` created at 01:34:10.490, archived at 01:34:10.715, members added at 01:34:11.968. CHANGELOG_281 added grace handling for empty teams. |
| U10 | Pending plan approval rows should not change the existing top-right action button layout when optional feedback is revealed. Show feedback as a separate single-line input row instead of embedding a larger textarea into the action group or changing the visible continue button label. | 1 | 2026-06-18 | 2026-06-18 | User screenshot/report: "点击继续讨论时，不要改变原先的按钮布局，加一行输入框就行." CHANGELOG_288 moved `ExitPlanRow` feedback to a one-line row below the header while preserving the button group. |
| U11 | Session-discovery defaults should avoid showing unrelated sessions. Tools or views that are scoped by a current session should default to the current session's related context, while explicit broad search / recovery filters can remain opt-in. | 1 | 2026-06-19 | 2026-06-19 | User feedback: "现在list session还是能看到无关的会话，默认应该只看到有关的会话." |
| U12 | Diff presentation UIs must preserve core red/green change cues even when adding explanatory cards or walkthrough annotations. Extra review guidance should not replace the primary visual diff signal. | 1 | 2026-06-30 | 2026-06-30 | User feedback: "有卡片也需要有红绿diff." CHANGELOG_335 restored added/deleted row tones for annotated `present_diff` PR cards. |

---

# Agent Pitfall Candidates

Sorted by descending `count`.

| ID | Description | count | first_at | last_at | Trigger example |
|----|------|-------|----------|---------|----------|
| P1 | Registered resources / markers were not released in try/finally: failure paths missed cleanup, leaving state stuck for N seconds or incorrectly swallowing same-class events. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #2: sdk-bridge.ts called releasePending only on the success path; on failure, same-cwd hooks were incorrectly swallowed for the 60s ttl window. |
| P2 | Path allowlist TOCTOU: validation used the original path, but reads used realpath, so a symlink retarget could cross authorization boundaries. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #3: ipc.ts loadImageBlob symlink TOCTOU. |
| P3 | Query / validation used full-table scans + N-row limits + JS-side .find/.filter, so old records become permanently undiscoverable after long sessions or large data. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #6: ImageRead path relied on the listForSession 500 limit as a fallback. |
| P4 | Map / Set / cache writes have branch conditions, but delete happens only at the end of one branch, causing linear leaks in other branches. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #4: toolUseNames deleted only in the image-tool branch. |
| P5 | Electron `before-quit` / similar event listeners used `async () => { await ... }`; the returned Promise is not awaited, so cleanup only finishes by luck. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #7: index.ts before-quit. |
| P6 | Global fuzzy fallback matching ("if only one item remains in the pool, it must be the one") can hit the wrong entity under concurrency. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #1: `pendingSdkCwds size===1` fuzzy match incorrectly swallowed an external CLI. |
| P7 | catch only does console.warn and swallows the error, so upstream / UI cannot receive the failure reason and the user sees a "mysterious session-end". | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #5: SDK query loop catch. |
| P8 | Depending on npm/dev-only env such as `process.env.npm_package_version` yields undefined after packaging, and hard-coded defaults silently ship. | 1 | 2026-04-23 | 2026-04-23 | REVIEW_1 #8: AppGetVersion always showed 0.1.0. |
| P9 | Test assertions / command output containing **thousands of repeated consecutive characters** (such as `'x'.repeat(N)`) entered conversation context, triggering Anthropic AUP classifiers in later turns as abnormal load and blocking the entire reply. Prevention: test truncation with shorter inputs (8KB+1 rather than 8KB+5KB); or use `.toMatch(/regex/)` so failure output dumps less raw text; for any Bash output >= 5K repeated single characters, proactively use `wc -l` / `head` instead of reading it fully into context. | 1 | 2026-04-24 | 2026-04-24 | Phase 2.1 vitest ran payload-truncate truncation tests; a failing assertion dumped the full 13KB `'xxx...'` to stderr -> output entered conversation context -> later commit workflow was blocked by AUP. |
| P10 | Unit tests for generated SQL fragments only used regex / `toContain` string checks and never fed the generated SQL into a real sqlite3 run. Broken SQL (syntax errors / wrong column names / FTS alias MATCH issues) can pass all unit tests and then throw in production. Prevention: pure-function tests involving SQL fragment construction need an independent "real SQL integration verification script" (such as `scripts/verify-fts5.sh`), run through the system sqlite3 CLI rather than vitest (to bypass unavailable better-sqlite3 binding), and the broken shape should be kept as a regression guard verifying it **still fails**. | 1 | 2026-04-24 | 2026-04-24 | Phase 4 N5 landed 13 passing search-predicate.test.ts cases, but the `fts MATCH @kw_fts` alias form failed SQLite parsing ("no such column: fts"), so the whole FTS path threw on search. Opus 4.7 found it on-site with the sqlite3 CLI. |
| P11 | "Byte budget" thresholds used `string.length` / `s.slice(0, max)` for comparison and truncation. String.length is UTF-16 code units, not UTF-8 bytes; emoji / Chinese text can write up to about 3x the declared byte limit, and `slice(0, max)` can cut through a surrogate pair, producing a lone high surrogate (downstream JSON.parse does not fail, but UI renders a replacement character / broken word). Prevention: any threshold related to "bytes" (SQLite row limits / network protocol length-prefixes / filename limits / DB blob) must use `Buffer.byteLength(s, 'utf8')` for bytes plus UTF-8-safe cutting (find the last code point boundary <= max), and must never mix in `s.length`. | 1 | 2026-04-24 | 2026-04-24 | REVIEW_4 H3: payload-truncate.ts thresholds 256K/8K all used string.length; emoji/Chinese could actually write about 3x, and truncateString could cut a surrogate pair. Tests only used ASCII 'x', so they could not expose the bug. |
| P12 | Before relying on upstream SDK / CLI / protocol "documented default behavior", first test it with a minimal reproduction script. Documentation differing from real behavior is common, not exceptional. For code changes involving critical assumptions such as fork / resume / streaming / protocol idempotency, writing judgments such as "this path fully converges" or "X cannot happen by default" from docs alone, without measurement, will eventually be disproven in production. Prevention: before coding, spend 5-10 minutes running a minimal script and paste the measured output into CHANGELOG / REVIEW as evidence. | 1 | 2026-04-24 | 2026-04-24 | CHANGELOG_24 noted "SDK default resume: sessionId (no fork) path fully converges" based on sdk.d.ts:1255-1258 documentation, but measured CLI streaming + resume + new prompt always forked. After CHANGELOG_26 option B landed, the user scenario still broke; REVIEW_6 finally proved the root cause with a minimal reproduction script. |
| P13 | Reverse lookup with Map / Set / array `entries()` / `for...of` plus `break` to infer the "newest created / most recent match" item is wrong. Map iteration is **in insertion order**; `break` takes the first, not the latest. Multiple `set` calls for the same key do not reorder it; when multiple entries satisfy the same condition, first means oldest. Prevention: if the upstream function has a return value, use it directly instead of reverse-looking it up later; when reverse lookup is unavoidable, explicitly sort by ts / id / cwd+ts and take the last, rather than betting on iteration order. | 1 | 2026-04-25 | 2026-04-25 | REVIEW_7 H1: sdk-bridge.ts recoverAndSend post-fallback used `for...entries() if cwd===rec.cwd break` to take the first match as newRealId. The comment said "latest", but with another SDK session in the same cwd it selected the wrong one, migrating events/file_changes child tables to an unrelated session. |
| P14 | Cross-process emit ordering relied on undocumented IPC queue order. In main, `eventBus.emit(A) -> emit(B)` is likely serial through `webContents.send` within the same Electron webContents, but cross-channel / contextBridge ordering is not a documented stable semantic. Renderer store actions must be robust to out-of-order arrival (any order should not corrupt by-session data) and must not depend on sender-side order. Prevention: renderer actions that mutate by-session state, such as `renameSession / removeSession / upsertSession`, should defensively check `if (!next.has(toId))` when the target key already has data, so they do not overwrite newer data. | 1 | 2026-04-25 | 2026-04-25 | REVIEW_7 M4: manager.ts renameSdkSession emitted `session-renamed -> session-upserted`. If renderer received upsert first, it did store.sessions.set(toId,newRec), then renameSession overwrote with fromRec, losing newer record data. Although NEW_ID is a new id on fork paths and this almost never triggers, the defensive check makes out-of-order scenarios robust. |
| P15 | React `setState((prev) => { ...; setView(x); select(y); return ...; })` ran side effects inside an updater callback. React StrictMode in dev calls updater callbacks twice to verify purity, so setView / select each run twice (even if the second is a noop, it is still an antipattern); production can also be affected by concurrent rendering. Prevention: updater callbacks must be pure (return only new state). Move side effects outside (keep current value in a ref and compare at the listener top level before calling side effects directly), or move them into a `useEffect` watching dependency changes. | 1 | 2026-04-25 | 2026-04-25 | REVIEW_7 L1: App.tsx onSessionRenamed listener called `setHistorySession((prev) => { if (prev?.id === from) { setView('live'); select(to); return null; } return prev; })`, performing setView/select inside the updater. |
| P16 | Electron / Node main process did not install `'error'` listeners on `process.stdout` / `process.stderr`. In packaged GUI mode, launchd connects stdout to a pipe; when the peer closes early, any console.log/error immediately throws EPIPE. With no stream 'error' listener, Node upgrades it to uncaughtException and the whole main process dies, taking HookServer / Helper / SDK down with it. Prevention: immediately after main-entry imports, install `process.stdout.on('error', () => {}); process.stderr.on('error', () => {});`; do **not** take over global uncaughtException because that has broad semantic side effects. The long-term direction for debug output is a logger instead of raw console.log, but the listeners are the minimal source fix covering all call sites. | 1 | 2026-04-29 | 2026-04-29 | REVIEW_10: packaged wrapper validation launched main and immediately hit an EPIPE crash; stack landed on console.log in window.ts:85 showOnce. `grep src/main/` showed no uncaughtException / stdout.on / stderr.on fallback. |
| P17 | "Dual-channel guard" trap: when adding an `expectedClose`-style gate around SDK / upstream errors, gate both the **catch channel** (throw / abort / cleanup) and the **frame channel** (SDK legitimately emits an is_error / error result message and then closes the stream normally), and gate all three sinks in the same branch together (red error message / finished UI state / mac system notification routeEventToNotification all depend on the same frame). Gating only one channel leaves half the protection missing; gating only one sink leaves 2/3 of that channel open. It is normal for the SDK to translate "app actively aborted" into a result frame rather than an exception (the CLI internal state machine can legally emit a result frame instead of throwing when a check fails). The channels can be recognized by red-message prefix differences: catch channel has "SDK stream interrupted:" while frame channel directly renders `⚠ ${detail}`. Prevention: for any flag distinguishing app-initiated close from unexpected errors, first grep all sinks that emit red messages / error / finished / system notifications, and prefer an early return that silences the whole frame when the flag is hit over sprinkling `&& !flag` on individual sinks. | 2 | 2026-04-29 | 2026-04-29 | REVIEW_11 Bug 1: D' fix gated only the sdk-bridge.ts:1437 catch block and missed the line 1590 result frame; REVIEW_13 Bug 6: result frame was gated, but only the red-message emit was gated and the finished emit was missed, so routeEventToNotification sent a mac system notification "Agent error". |
| P18 | "`allow` = agree to exit" semantic trap: in the SDK tool protocol, `behavior:'allow'` literally means "approve this tool call", but some tools semantically mean "request to exit the current mode" (typical example: ExitPlanMode), so allow is a **semantic exit**. If business logic wants to approve the content while staying in the mode, it must use `behavior:'deny' + message` so the tool call fails and the message tells Claude "approved, but stay in the mode"; it **cannot rely on setPermissionMode afterward**. In the post-allow timing window, the SDK internal state machine has already flipped state, and setPermissionMode hits a race (CHANGELOG_47 same disease + REVIEW_11 Bug 3 dual evidence: bypass and plan were both silently swallowed by the SDK in the post-allow window). Prevention: when writing resolvers for ExitPlanMode-like "mode-toggle tools", identify which targetMode conflicts with the "exit" semantics (plan->plan is a conflict), route that case through a deny+message branch, and guard the outer layer from calling setPermissionMode. The message copy must explicitly say "do not immediately call the same tool again" to avoid a deny->retry loop. | 1 | 2026-04-29 | 2026-04-29 | REVIEW_11 Bug 3: ExitPlanMode resolver allowed every approve branch; CLI treated approve+plan as exiting plan and immediately flipped to default. The outer setPermissionMode('plan') race failed, so the UI label said "Keep Plan" but actual behavior was default, and the next edit used the normal PreToolUse prompt. |
| P19 | "Same-origin blacklist blocks exact ID only" trap: cross-process dedupe / isolation mechanisms that match by value, such as sessionId blacklists or cwd claims, naturally fail for late events where an upstream process internally forks a new ID or falls back to a different cwd value. After a CLI subprocess receives SIGTERM, its internal cleanup race can generate a new sessionId (unrelated to OLD_ID recorded by app-layer closeSession) and can fall back to HOME as cwd instead of the session's original cwd. Measured approve-bypass cold switch produced **both** at once, so recentlyDeleted blacklist + consumePendingSdkClaim both failed and an orphan `source='cli'` record was created. Prevention: dedupe / isolation across process boundaries cannot rely entirely on values pushed by upstream (`sessionId / cwd / pid` can all be wrong); when the app spawns a subprocess, proactively inject an identity marker (env / args / ipc handshake), and classify downstream events by that marker rather than business fields. Typical implementation: env injection -> hook command forwards as HTTP header -> ingest entry skips by header. **Misclassification guard**: identity markers apply only to subprocesses spawned by the app; external independent processes (user runs `claude` in terminal) naturally have no marker and are handled as "unknown / external by default", causing no false positives. | 1 | 2026-04-29 | 2026-04-29 | REVIEW_12 Bug 5: REVIEW_9 1B (recentlyDeleted) + REVIEW_5 H1 (cwd claim) were dual fallbacks against approve-bypass orphans, but the OLD CLI forked into new sessionId Y and cwd=`/Users/apple` fallback, so both layers failed. AGENT_DECK_ORIGIN env + X-Agent-Deck-Origin header origin-tag protocol fixed it at the source. |
| P20 | "Inferring SDK / third-party library capability boundaries from only current repository call sites" trap: SDK APIs are often much wider than the app's current usage. Only grepping / reading current query options in `sdk-bridge.ts` turns "unused capability" into a mistaken "unsupported". Sub-agents that read only code and not `.d.ts` are prone to false claims like "the SDK does not support X". This complements P12: P12 guards "docs say it exists but reality differs" (measure it), while P20 guards "code does not use it so we think it does not exist" (read type definitions). Prevention: when researching SDK / library capabilities, require reading `.d.ts` type definitions (grep the field name + read the key section) plus measurement when needed; never conclude from current code call surfaces alone. When prompting a subagent, explicitly say "check d.ts for available but unused capabilities; do not only inspect current app usage." | 1 | 2026-04-30 | 2026-04-30 | REVIEW_14: Agent A Explore subagent only read sdk-bridge.ts:476-525 query options and wrongly concluded "SDK does not support PreToolUse/PostToolUse hooks, only canUseTool" and "Claude Code has no OS-level sandbox". The main agent read sdk.d.ts:1279 (hooks) / 1562 (sandbox) / 4656-4732 (full sandbox structure) / 1602 (managedSettings) and refuted with evidence; all three capabilities existed in SDK 0.2.118 and Agent Deck simply did not use them. |
| P21 | "Researching SDK / complex-system **behavior mechanisms** by intuition / partial observation -> misjudgment" trap: mechanisms such as sandbox / proxy / dual-layer protection involve multiple parallel layers, tool loops, and env injection at runtime. Pure "phenomenon observation + analogy" easily mistakes symptoms for causes. Typical bad paths: "user did not see a dialog -> branch did not run -> mark as dead code"; "curl got proxy 403 -> tool loop was not used -> change config to remove network". In both cases, one second of logs revealed the truth, but without logs every guess was wrong (dual-layer parallelism is not either/or; automatic deny paths are invisible to the user but clear in logs). Complements P20: P20 is "does capability exist" (check d.ts); P21 is "how does behavior work" (add logs + measure). Prevention: for sandbox / proxy / cross-process / SDK built-in tool behavior issues, **first add temporary logs at key decision points and run empirical checks** (remove logs after investigation or keep only one high-value line). Negative claims such as "branch did not run" or "field has no effect" must be disproved by logs, not inferred from "user did not see it." | 1 | 2026-04-30 | 2026-04-30 | REVIEW_15: implementation made four consecutive wrong assumptions (managedSettings.sandbox loads the whole set / not passing network means the tool loop replaces HTTP_PROXY / canUseTool branch is dead code / a certain field triggers SDK mode switching), all disproved by measurement. Adding a [canusetool] log revealed the truth in one second (dual-layer parallelism + canUseTool branch consistently active); the user supplied the X-Proxy-Error: blocked-by-allowlist header to complete the picture. |
| P22 | Handwritten YAML / frontmatter writers that directly join unescaped values as `${k}: ${v}` break parser round-trips. Multiline strings, values containing `:` `#` `"` `\`, and leading/trailing whitespace can all misread. Typical cases: `description: line1\nline2` written to disk -> parser `(.*)$` single-line regex silently drops line2; `description: 'x\n---\nname: hijacked'` creates a nested frontmatter block; `#` in description should be a YAML comment under the YAML standard, diverging from the app's custom parser behavior. Prevention: either use a real YAML library (`yaml` package), or use a three-part fallback: IPC inputs reject single-line fields containing `\n` `\r` `---`; writer always double-quote-wraps values; writer escapes `\\` and `\"`. Do not bet that "Chinese descriptions will never contain these characters." | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R1-F2+F3: both sides independently reproduced user-assets stringifyFrontmatter multiline + `---` injection bugs with node. Fix used ipc/assets.ts parseSingleLineString to reject the three character classes plus utils/frontmatter.ts double-quote wrapping. |
| P23 | Newly added "jump across dialog / view" buttons must go through the current dialog's dirty guard chain. It is not enough for only the primary close button (X / outside click) to call guardedClose; any entry that makes the parent call setOpen(false) will unmount the child -> cleanup `onDirtyChange?.(false)` resets the dirty ref -> the user's unsaved draft is silently lost without confirmation. Prevention: every button that "closes dialog A -> opens dialog B / navigates elsewhere" must be wrapped in a guarded function (same lock + same dirty check + same confirmDialog offering "discard / continue editing"); reviews of new "jump out" buttons must inspect dirty-path coverage. | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R1-F1 (HIGH, codex agreed in rebuttal round): CHANGELOG_57 added "View in asset library ↗" in the settings panel; onClick directly called parent onOpenAssetsLibrary without guardedClose, so ClaudeMdEditor unmounted and silently cleared the dirty ref. Fix wrapped guardedOpenAssetsLibrary and reused closeInFlightRef lock. |
| P24 | Renderer and main-side validation regexes / length limits / field constraints **must share the same constants**. Separately hardcoding them will drift: renderer `^[a-z0-9-]+$` accepted `-foo`, while main `^[a-z0-9][a-z0-9-]*$` rejected it; UI validation passed -> IPC rejected, and the error message regex still disagreed with the real main regex. Prevention: all cross-process field constraints (regex / length / enum values) should be extracted to shared constants in `src/shared/types/<domain>.ts` or `src/shared/constants/`, and renderer + main + documentation error text should import the same source. Use the same existing pattern as SANDBOX_MODE_VALUES (CHANGELOG_54) / READ_ONLY_TOOLS (CHANGELOG_56). | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R1-F8+F9: CHANGELOG_57 user-assets ASSET_NAME_REGEX drifted across renderer and main with three independent hardcodings, plus 4 error messages / comments whose regex differed from code. Fix extracted ASSET_NAME_REGEX + ASSET_LIMITS into shared/types/assets.ts. |
| P25 | Multi-escape string decoding must process escapes in quote reverse order (or use a char-by-char state machine). Multiple sequential `String.prototype.replace` calls can re-match intermediate states and corrupt data. Typical bad code: `text.replace(/\\n/g, '\n').replace(/\\\\/g, '\\')`. When a user writes literal backslash + n, such as Windows path `C:\new` / regex `\d` / documentation `\n`, quoteValue writes the file, then step 1 `\\n -> \n` eats the literal characters into a newline. Prevention: implement string unescape as a single-pass char-by-char while + switch (each `\` consumes only the next character), or use reverse-order sentinel replacement (first `\\\\` -> temporary char, then process other escapes, then restore). After writing it, gate with round-trip vitest cases containing "literal backslash + escaped character". | 1 | 2026-05-06 | 2026-05-06 | REVIEW_22 R2-R2-F1: reviewer-claude and reviewer-codex independently reproduced utils/frontmatter.ts unquoteValue 5x .replace() ordering bug with node (`'foo\nbar'` 8-character round-trip produced char[4]=10 real newline). Fix used char-by-char state machine + 15 vitest cases (`src/main/utils/__tests__/frontmatter.test.ts`). |
| P26 | In the main process, the **same module imported dynamically from >= 2 locations** with **no static import path** triggers a vite SSR + rollup default chunking trap: module code gets inlined into main `index.js`, while the separate chunk file contains only an empty `require(deps)` shell with no `module.exports` / `Object.defineProperty(exports,...)`. Runtime dynamic import returns an empty object -> destructuring yields undefined -> `TypeError: X is not a function`. Dev mode ESM direct imports do not expose it; it **only fails after packaging**. Control groups: a module used by >= 1 static import and >= 1 dynamic import triggers vite's warning "dynamic import will not move into another chunk" and is effectively treated as static (which avoids the trap); a module with only one dynamic import (such as `transport-http`) gets a normal chunk with exports. Prevention: (a) forbid "main process same module with multiple dynamic imports and zero static imports"; use all static imports (recommended, simple, no runtime difference), or at least add one static import so vite turns it static; (b) before writing a new dynamic import, `grep -rn "<module-name>"` to confirm reference count; (c) after packaging, **inspect** `build/main/*.js` chunk sizes, and if a tiny < 0.5 kB ".installer / .writer / .helper" chunk appears, cat it to see whether it is an empty shell. | 1 | 2026-05-12 | 2026-05-12 | REVIEW_25: packaged wrapper ping exposed `[bootstrap] syncAgentDeckSection / syncSkills failed TypeError: ... is not a function`; comparing `build/main/agents-md-installer-cYcOGELy.js` (0.17 kB empty shell) against `transport-http-Ctvu5mtC.js` (2.16 kB complete exports) pinned the pattern. Fix changed 5 dynamic imports to static imports (index.ts x 2 + settings.ts x 3, including same pattern in toml-writer that ping did not hit), and all 3 empty-shell chunks disappeared while main index.js grew by 8 kB. |
| P27 | When bootstrapping fastify / express / koa or similar HTTP servers, **all routes must be registered before listen**. fastify 5.x throws `FST_ERR_INSTANCE_ALREADY_LISTENING` (`lib/route.js:208 throwIfAlreadyStarted`) if `app.route()` is called after `app.listen()` resolves; the framework does not support "hot mount / hot unmount routes" by design. New "mount conditionally by setting" feature modules (such as plugin auto-enable/disable or dual-switch gated transport) are easy to place after `listen()` because that is nearby in code order, causing routes not to mount while the rest of the app continues; it only appears after packaging when users actually enable the switch. Dev defaults are often OFF, so the bug sleeps for a long time. Prevention: (a) organize bootstrap around a PRE_LISTEN / POST_LISTEN boundary; every route registration belongs before listen; (b) add an invariant at app-layer wrappers such as `HookServer.registerRoute`: `if (this.started) throw new Error(...)`, making the contract explicit at the HookServer boundary with an error message that points to the fix (fastify's generic "Cannot add route" is too far from app semantics and originates inside the framework); (c) when writing "mount transport / route based on settings" features, grep to confirm they are placed before `hookServer.start()`, not just "near the original step below it". | 1 | 2026-05-12 | 2026-05-12 | REVIEW_27: after packaged install, wrapper ping exposed `[agent-deck-mcp] failed to mount HTTP transport FastifyError: Fastify instance is already listening. Cannot add route!`; commit `cdb01ae feat(mcp): B'4 + R1.A5 + R1.D7 codex auto-mount agent-deck MCP via HTTP transport` placed section 6.5 after step 6 `await hookServer.start()` (near the step 6 comment, without realizing fastify forbids addRoute after listen). Fix moved section 6.5 to 5.5 PRE_LISTEN + added HookServer.registerRoute app-layer invariant; dev default `enableAgentDeckMcp=false` had kept it dormant until new .app user data enabled it. |
| P28 | When using `AgentEvent` / cross-process message fields for rendering dispatch or business branching, **first confirm that the field is also carried by the persistence layer (DB schema / file / cache)**. In-memory real-time event streams have complete fields, but listForSession / IPC history reload / restart-load paths only project through the DB schema; fields absent from the schema are always `undefined`. Guards based on the intuition "events look like this", such as `if (event.source === 'sdk')`, work with real-time event evidence but **fully fail** after refresh / switching back to a historical session. The bug appears only when the semantic meaning is equivalent but the data path differs, and reproduction relies on user screenshots. Prevention: (a) before writing any rendering dispatch like `if (event.X === 'Y')`, **grep `event-repo.ts` insert / rowToEvent** (or the corresponding store schema) to confirm X is persisted; if not in schema, do not depend on it; (b) when a field is not in schema, choose one of three options: 1. upgrade schema with a column + migration (heavy, all user DBs), 2. use an equivalent session-level / parent-prop substitute (light, as in manager.ts:270 where event.source was lifted to the sessions table `source` column), 3. inline the field into payload_json on each emit (medium, requires old payload migration); (c) after changing it, run the regression path "restart app / switch to another session and back"; do not only trust real-time event-stream tests. | 1 | 2026-05-12 | 2026-05-12 | REVIEW_26 Follow-up: REVIEW_26 main fix added `if (event.source === 'sdk') hide AskUserQuestion/ExitPlanMode tool-use-end` in ActivityRow; it worked with real-time event evidence, but after the user refreshed the window the red-box "AskUserQuestion failed" appeared again. Reading `src/main/store/event-repo.ts:5-12 / 28-31` showed the events table schema has only four columns `(session_id, kind, payload_json, ts)`, no `source` column, and `rowToEvent` does not carry source -> listEvents history reload had `event.source === undefined` -> guard failed. Fix replaced `event.source === 'sdk'` with session-level `isSdk` props (sessions table `source` column persisted by manager.ts:270, and `SessionManager.dedupOrClaim` guarantees equivalence), with no schema change or migration. |
| P29 | When upgrading a third-party package to v5 / introducing ESM `package.json` `exports` fields while the package is also a transitive dependency (pnpm strict isolation does not expose root `node_modules`), **dev / prod / TS resolvers diverge** and the problem is invisible before packaging. Concrete chain: (a) v5 changes exports paths, such as `refractor/lang/<lang>` -> `refractor/<lang>` through `"./*": "./lang/*.js"` remapping; dev-mode vite deps optimizer still resolves the old `.pnpm/<pkg>@<v>/node_modules/<pkg>/lang/*` path and does not complain; (b) prod rollup strictly follows v5 exports and throws `Failed to resolve import`; (c) after fixing import paths, TS `moduleResolution: "node"` still does not understand conditional exports, so typecheck reports TS2307 and needs an ambient shim (or an upgrade to `bundler` mode); (d) pnpm strict isolation means root `node_modules` lacks the package, so the transitive dependency must be promoted to a direct dependency. Prevention: (a) before adding / upgrading a third-party ESM package, `cat node_modules/.pnpm/<pkg>@<ver>/node_modules/<pkg>/package.json` and inspect `exports`; (b) before using a package subpath, grep top-level `package.json` dependencies to confirm it is a direct dependency, otherwise promote it; (c) after changing imports, run `pnpm dist`, not only `pnpm typecheck` / `pnpm dev`, because dev rollup is less strict and dev vite optimizer masks issues; (d) for one-off small fixes, prefer an ambient shim to satisfy TS resolver with minimal intrusion; if multiple v5 ESM packages hit the same issue, then upgrade `moduleResolution: "bundler"` to converge once. | 1 | 2026-05-13 | 2026-05-13 | REVIEW_29 HIGH-1: `fe1effe` MarkdownText introduced `import bash from 'refractor/lang/bash'` (v3 style) x 15 lines. Dev-mode vite optimizer masked it, and `pnpm typecheck` passed through `.pnpm/refractor@5.0.0/.../lang/bash.d.ts`; `pnpm dist` then failed rollup with `Rollup failed to resolve import "refractor/lang/bash"` from MarkdownText.tsx. After fixing the path, TS reported TS2307 and needed a shim; refractor was only a transitive dependency of react-syntax-highlighter@16.1.1 and also needed promotion to a direct dependency. All three had to be handled together. |
| P30 | When writing / reviewing a SQLite migration that uses `ALTER TABLE ... DROP COLUMN`, **first grep the whole codebase for every index / view / trigger that references that column** before deciding "DROP is safe". SQLite 3.35+ rejects DROP COLUMN when any index references the column (including partial-index `WHERE` expressions / normal index column references / expression indexes) -> ALTER TABLE throws -> migration runner `db.transaction()` rolls back the whole transaction -> user_version does not bump -> next launch reruns the same failing migration -> app bootstrap is permanently fatal. Judging "the column has no index, DROP is safe" from old memory / an old PR description / the commit message at the time is a high-frequency pitfall: the column-introducing commit and index creation may be in the same migration or different migrations, and human memory decays. Prevention: (a) before writing a DROP COLUMN migration, **force** a full scan such as `grep -rn "<index_name_pattern>\|<column_name>" src/main/store/migrations/ --include="*.sql"` and list every reference; (b) any index / trigger / view referencing the column must be dropped in the same migration with `DROP INDEX IF EXISTS ... ;` before ALTER TABLE; (c) migration comments + commit messages must list the actual schema artifacts cleaned up, not just "drop column", so later reviews have ready evidence; (d) in severe cases add vitest coverage for the full migration chain (temporary DB file / `:memory:`) to catch any SQLite throw. Dev can pass because local DB state may skip a step, while CI running the full chain can catch it. | 1 | 2026-05-13 | 2026-05-13 | REVIEW_29 HIGH-2: v014 from `bb13b32 plan team-cohesion-fix Phase F` explicitly commented "v006 only ADD COLUMN, DROP safe", but v006 actually also created partial index `idx_sessions_team_name ON sessions(team_name) WHERE team_name IS NOT NULL`. v014 directly ran `ALTER TABLE sessions DROP COLUMN team_name`, which must fail in SQLite 3.35+ with `error in index idx_sessions_team_name after drop column: no such column: team_name`; migration transaction rolled back -> bootstrap fatal; both new install and upgrade paths were affected. Fix added one line `DROP INDEX IF EXISTS idx_sessions_team_name;` before DROP COLUMN and fully corrected the v006 actual-state comment. |
| P31 | When invoking a plugin SKILL / registered MCP tool, **never do SSOT discovery first** (`find / -path '*<plugin>*'` / `grep <skill-keyword> ~/.claude/plugins/` / `ls /Applications/<app>.app/Contents/Resources/`). Skill tools and `mcp__*` tools can be directly invoked by name; **if they do not exist, the invocation errors**, so pre-checking existence is unnecessary. This SSOT discovery is a high-frequency reasoning waste pattern: one instance runs 4-6 Bash commands, each with hundreds of ms to seconds of latency, consumes a lot of context tokens, and still ends by directly calling the tool (discovery output has no effect on invocation). Prevention: (a) when a system-reminder lists a SKILL name / MCP tool name, invoke it directly; if the name is wrong, use the fallback error prompt to search; (b) questions like "is this skill installed / where is it / does the file exist" should be answered by "try invoking once" rather than "find first"; (c) when genuinely reading SKILL source for debugging / SKILL development, use `Glob` or one exact `find <path>`, not a full-disk scan. | 1 | 2026-05-13 | 2026-05-13 | REVIEW_32 side observation: in plan deep-review-and-split-20260513 H1, lead Claude and another Claude from an earlier session both made the same mistake. To decide whether to run deep-code-review SKILL, they ran multiple Bash discovery commands such as `find / -path '*/agent-deck*' -name 'SKILL.md'` / `grep -i 'deep.code.review'` / `ls /Applications/Agent Deck.app/Contents/Resources/` (one grep pattern was misspelled and returned `(Bash completed with no output)`), then still called `Skill(skill: 'agent-deck:deep-code-review', args: '...')` directly and it succeeded. The user explicitly called out this antipattern. |
| P32 | useEffect cleanup `disposedRef.current = true` plus immediately resetting it to false in the new effect body is a "ref guard auto-invalidates" antipattern. It looks cleaner than a closure-local `let disposed = false` because it can be shared across multiple async calls, but it loses per-effect isolation. Any effect rerun, including prop changes in deps such as `[open, sessionId]` where sessionId changes, first runs cleanup setting true and then immediately resets false in the new effect, allowing old in-flight IPC to pass the guard and pollute new-props state. Prevention: cancel async IPC using a sequence counter (`++req` + closure-captured cur + resolve-time check `cur !== requestSeqRef.current`) or a closure-local `let disposed = false` (one copy per effect); **do not** use a single boolean ref. If a ref must be used and the effect can rerun, at least add a second guard verifying the key prop id is still the captured value from the IPC start (`capturedSid !== sessionId`). Same theme as P14 (renderer store actions robust to out-of-order events: async state cannot rely on timing), but triggered by an orthogonal condition. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_95 review fix HIGH-1: reviewer-claude and reviewer-codex independently identified the HandOffPreviewDialog disposedRef pattern, and node -e reproduced it. Fix used requestSeqRef + capturedSid dual guards. |
| P33 | DB FK constraint `ON DELETE RESTRICT` plus app-layer "leave / archive" operations that only `UPDATE left_at = ?` / write a status field and **do not physically delete the row** is an antipattern. It appears to preserve historical row semantics, but RESTRICT does not care whether left_at is non-null; any caller running `DELETE FROM parent_table` hits FK constraint failed. ADR comments saying "sessionManager.delete entry has a pre-check fallback" are **actually ineffective** when the pre-check itself calls leaveTeam, which only writes left_at. The bug hides in the rarely triggered combination "hard-delete + active membership" and can remain invisible for a long time, until the user performs fork rename / UI delete on a session with active membership. Prevention: (a) when designing FK, **first decide which row is physically deleted and which is not**. "Preserve historical row" semantics -> use `ON DELETE CASCADE` (accept the row being cleared with its parent; ownership already lives on the parent side). "Strictly block parent deletion" semantics -> use `RESTRICT` + app-layer leave must hard-delete child rows. **Do not choose RESTRICT while leave only UPDATEs status fields**; the two goals conflict. (b) Any "pre-check fallback" comment must have real test coverage (negative assertion that direct `DELETE FROM parent_table` throws FK + positive assertion that the app-layer delete path succeeds). Trusting comments without real tests breaks as soon as design changes. (c) Before any SQL path executes `DELETE FROM parent_table`, grep the whole codebase for `REFERENCES <parent_table>(<id>) ON DELETE RESTRICT` and list every child table's migration / cleanup strategy. (d) Same theme as P30 (DROP COLUMN without grepping all indexes) + P10 (SQL fragment without real SQLite test): DB schema and code-state mental models drift apart. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_96 / plan linked-swimming-platypus: user reported SDK stream interruption "⚠ FOREIGN KEY constraint failed" screenshot; DB data + code tracing fully verified the root cause. While the lead was active in 4 teams, fork rename triggered DELETE OLD inside sessionRepo.rename and hit RESTRICT FK, rolling back the transaction; sessionManager.delete was also found to hit the same FK chain. Measured evidence: `sqlite3 ... DELETE FROM sessions WHERE id='a6260807-...' -> FOREIGN KEY constraint failed (19)`. Fix changed v017 schema RESTRICT -> CASCADE + rename explicitly migrated 3 classes of references. |
| P34 | Follow-up plan stub creation is not yet templated (process gap): when leads create stubs by feel, key fields are often missing. In this instance, archive-plan-tool-ux-followup-20260515 needed required `worktree_path` frontmatter, known pitfalls inline from the parent plan, a next-session first step as concrete as "first cat the plan + EnterWorktree(path:) + self-check HEAD", and design decisions framed as hint-level recommendations rather than strong sign-off. Prevention: template stub plan creation (this stub archive-plan-tool-ux-followup-20260515.md can itself serve as a reference). Minimum requirements = (a) all 6 frontmatter fields (plan_id / created_at / worktree_path / status / base_commit / base_branch / parent_plan_id) + (b) goal & invariants with at least 3 sentences explicitly stating scope and which existing contracts not to touch + (c) design decisions as hint-level, not strong sign-off + (d) checklist from coarse to detailed (Phase 1-4 frame + no more than 5 Step N items) + (e) next-session first step with **absolute path + concrete action** (Bash cat / EnterWorktree path / git log -3 / start adversarial SKILL, etc.; do not write "Read", to avoid cross-session cache ambiguity) + (f) known pitfalls inline from the parent plan and any discovered while creating this stub + (g) session-style authorization inline from the user's most recent hand-off authorization, inherited by later sessions by default. Reason: every hand-off otherwise rediscovers stub style, wasting mental cycles; missing key fields make cold-start sessions fail (for example, missing worktree_path leaves the cold-start unsure where to EnterWorktree). | 1 | 2026-05-15 | 2026-05-15 | During closeout of plan archive-plan-content-overwritten-fix-20260515, the lead reflected while creating three hand-off follow-up stub plans; REVIEW_44 §follow-up (f) triggered it. |
| P35 | Two typical claude / codex dual-end prompt asset mirror drifts are hard to catch by eye: (1) one side is changed and the other side is forgotten, causing SSOT drift; (2) one side's simplified wording conflicts with a precise description later in the **same file**. Typical example: simplifying code semantics like "when a session row is physically deleted, CASCADE deletes tasks" into "when the session dies, tasks follow it"; the earlier wording becomes vague while later wording is precise, leaving readers unsure. **Prevention**: (a) after fixing dual-end mirrors, always run `grep -n "<key phrase>" <claude-asset> <codex-asset>` for literal paired verification (each bullet should have at least one unique keyword anchor); (b) before writing / fixing simplified wording, grep later in the same file for an existing precise description to avoid contradictions; when both exist, **use the precise version** or add an inline exception clause earlier so both sides are consistent; (c) intentional one-sided asymmetry (for example, reviewer-claude.md says "CLI implicit fork soft fork also does not count as fresh" but reviewer-codex.md does not, because codex SDK uses a fresh thread and has no CLI implicit-fork concept) must be explicitly marked under the item as "non-mirror by intent: reason X" to avoid the next review misclassifying it as drift. | 1 | 2026-05-29 | 2026-05-29 | plan deep-project-review-comprehensive-20260528 Phase 2 Step 2.7 R1 reviewer-codex LOW-1: grep found CLAUDE.md:29 + CODEX_AGENTS.md:32 simplified task deletion timing as "when the owning session dies, tasks are automatically deleted with it (ON DELETE CASCADE)", while later lines 718-719 / 222-223 in the same files precisely stated "historyRetentionDays GC triggers sessionRepo.delete, and CASCADE deletes tasks"; the contradiction was fixed by adding inline exception clause "(note closed/archived_at only marks lifecycle and does not delete the row, so it does not trigger CASCADE)" so both sides' boundaries are coherent. |
| P36 | In plan files, line-level references inside the "Design Decisions" / "Current Progress" / "Next Session First Step" sections **must be backed by measured evidence from grep / wc / ls**, not written from memory or partial memory. Typical failures: (1) overly strong universal quantifiers ("every SKILL.md line 6 has X comment" but 1 of N files is an exception); (2) LOC / count drift ("baseline 1664 LOC -> current 1672 LOC, +8 LOC" but measured step-level deltas add up to +3); (3) internal inconsistency between aggregate stats and per-step deltas. **Prevention**: (a) before writing line-level references, run measured checks such as `grep -n / wc -l / ls / git show <commit>:<path> \| wc -l`; (b) cross-check numbers / counts against step-level and file-level delta sums; (c) universal quantifiers such as "every" / "all" / "all files" require ls + cat full enumeration to confirm no exceptions; if exceptions exist, either change wording to "M of N files" or fix the files so the universal statement is true; (d) plan files themselves are untracked (`.claude/plans/` ignored), but as SSOTs their wrong data is permanently committed when archive_plan moves them into `ref/plans/`; do not relax measurement rigor just because "the file is not tracked." | 1 | 2026-05-29 | 2026-05-29 | plan deep-project-review-comprehensive-20260528 Phase 2 Step 2.7: two reviewers independently found this. R1 reviewer-claude MED-1 flagged "Current Progress LOC +8 measured +3", and R2 reviewer-codex LOW-1 flagged §D9 line 110 "every SKILL.md line 6" as too strong (hello-from-deck was an exception). Both findings required grep + wc + ls + git show measurements. |
| P37 | **macOS .app launchd starts the main process with an extremely sparse PATH** (cross-OS-common Electron subprocess spawning pitfall). When double-clicking a .app, launchd gives the main process only four minimal PATH entries: `/usr/bin:/bin:/usr/sbin:/sbin`; brew/nvm/cargo/go/bun/pnpm and any user terminal tools are absent. Spreading `process.env` 100% into SDK subprocesses makes every spawn hit `command not found`. **Prevention**: (a) early in Electron main-process bootstrap (after applyClaudeSettingsEnv, before initDb), use `execFileSync($SHELL || /bin/zsh, ['-ilc', sentinel-printf-cmd], {timeout:3000, stdio:['ignore','pipe','pipe']})` to measure the user's real terminal PATH, cache it, and union it into process.env.PATH; (b) execFileSync must **not** concatenate a command string, to avoid command injection when `$SHELL` contains quotes (use argv API); (c) wrap PATH output with **sentinel markers** (`__APP_PATH_BEGIN__/END__`) and do not rely on the last line, protecting against zsh `.zlogout` or other shell-exit-hook output polluting the final line; (d) use **sentinel two-state memoization** (`captured: boolean` + `cached: string | null`), and cache failure too to avoid reruns + repeated warnings; (e) failure fallback uses `originalPath ?? ''` and bootstrap guards with `&& newPath !== ''` to avoid undefined -> '' degradation; (f) limit compatible shells for `-ilc` to zsh/bash/sh/dash/ksh. **tcsh/csh/fish/nu** are explicit non-goals (`-ilc` incompatible, measured `Unknown option: '-lc'` / `-i: unknown flag`), so those shell users fall back to launchd minimal PATH and remain a follow-up plan; (g) mutate the main process rather than changing the SDK adapter spread layer, because getSdkRuntimeOptions / snapshotProcessEnv are spawn-time functions whose spread automatically inherits the new PATH (not module-level caches); (h) add local-state directories such as `.claude/worktrees/`, `.deep-review-cache/`, and `node_modules/` to `.gitignore`. | 1 | 2026-05-29 | 2026-05-29 | plan sdk-spawn-shell-path-20260529 (helper user-shell-path.ts + bootstrap-infra.ts Phase 0.5 + 18 unit tests): RFC 5 rounds + spike1 measurements + Step 1.5 plan deep-review 3 rounds 16 finding fixes + Step 3.6 code deep-review 2 rounds 6 finding fixes. reviewer-codex produced three on-site measured proofs: (1) tcsh/csh `-ilc` `Unknown option: '-lc'` disproved spike-stage assertion that "`/etc/shells` default list all supports -ilc"; (2) Node default spawnSync without shell returns `error.code='ENOENT'` and no stderr, while only `shell:true` returns `status=127` + stderr containing `command not found`; (3) zsh temporary HOME + .zlogout measurement proved sentinel markers prevent last-line pollution. |
| P38 | **Tree grouping algorithm "strict guard over-strictly punts valid nodes to root" antipattern**: when a grouping algorithm adds a "strict condition" to fix one stale counterexample (such as stale spawn-link after archive_caller=false adopt, with typical code `if (!ownerLeadsSomeTeamOfS) continue`), that condition can easily become over-strict and eject another valid case from the main path. The two counterexample shapes may be orthogonal (for example, a = lead-only mid-tier node; b = mixed-child stale owner overlaps only with the child-lead side), so strictness must target each shape separately rather than with one blanket rule. **Prevention**: (a) when writing a two-layer strict / fallback grouping algorithm, first enumerate the two / N counterexample shapes and draw one decision-table row per shape (condition / action / fallback path), rather than assuming one condition covers all; (b) strict conditions must pair with the fallback path that can handle the counterexample shape (in this case, a strict-cut child must be limited to children containing roles Phase 2 fallback can handle); (c) any strict-condition change needs **N+1 corner tests**: the counterexample cut by strictness plus the valid-case counterexample accidentally harmed by strictness (this bug was missed for 2 years because corner 6/7/8 coverage was absent); (d) jsdoc section titles should distinguish "intent of strictness" from "strictness regression", and add review-number anchors so maintainers can recover context later. | 1 | 2026-05-29 | 2026-05-29 | REVIEW_65 SessionList 3+ layer spawn-link tree rendering bug: Phase 1 strict condition `ownerLeadsSomeTeamOfS`, intended to fix HIGH-A counterexample (stale caller adopts teammate child), over-strictly cut two valid cases: lead-only mid-tier (L1 spawns L2, which opens T3 as lead) + mixed-child (B teammate T1 + lead T2 + B.spawnedBy=stale A leads T2). codex found v1 fix still had a mixed-child HIGH-A escape counterexample -> v2 fix `ownerLeadsSomeTeammateTeamOfS` (only look at child teammate teams) + corner 6/7/8 three-round coverage. |
| P39 | **vitest 2.1.9 mock behavior has four patterned integration-test pitfalls**. When introducing an npm package such as electron-log v5 that couples main+renderer dual processes, CJS transitive dependencies, and vite env injection, tests commonly hit four traps: **(1) CJS package top-level `require('electron')` cannot be intercepted by vitest-hoisted `vi.mock('electron')`**: vitest hoisting works for ESM `import`, but not for internal CJS `require`, so the entire CJS package (such as electron-store@8.2.0) must be separately mocked with `vi.mock('electron-store', () => ({ default: MockStore class }))` to intercept the import chain. **(2) Runtime sequence `vi.unmock('X') + vi.doMock('X', stub)` cannot undo a setupFiles global `vi.mock('X')` lock**: the original plan spec used vi.unmock + vi.doMock so tests could import the real module, but vitest 2.1.9 measurement showed all of it failed (still used the global mock). The compromise is mock-mediated tests that verify business modules call methods on the mock, sacrificing real API drift verification and leaving dist e2e as fallback. **(3) `import.meta.env.MODE` replacement is abnormal when vitest runs .ts under renderer subdirectories**: probes showed the test file had MODE='test', but logger.ts inside the same module graph (`src/renderer/utils/`) saw MODE as undefined (possibly due to electron-vite multi-config splitting + flattened vitest config conflict). Adding `define: { 'import.meta.env.MODE': '"test"' }` to vitest.config.ts also did not work. **(4) rg `-t ts -t tsx` reports "unrecognized file type: tsx"**: ripgrep built-in type aliases do not recognize `tsx`; `.tsx` must be explicitly registered with `--type-add 'tsx:*.tsx' -t ts -t tsx`. Also, test spies on console.warn must fail after source migrates from console to logger; the common fix pattern is to spy `log.scope('xxx').warn` (vitest-setup.ts mock returns a cached vi.fn() object with the same name, so it is the same object). **Prevention**: (a) before adding a main+renderer dual-process + CJS-dependent npm package, first `cat node_modules/.pnpm/<pkg>@<v>/node_modules/<pkg>/index.js` head and inspect top-level `require` shape; any CJS top-level `require('electron')` requires mocking the whole package separately; (b) before using advanced vitest mocks (`vi.unmock`/`vi.doMock`/`vi.importActual`), write a minimal repro against the current vitest version; if it fails, accept mock-mediated compromise + dist e2e fallback; (c) for renderer logger.ts-style top-level side effects such as `Object.assign(console, log.functions)`, vitest-setup.ts must mock the whole electron-log/renderer package so functions are no-op vi.fn(), preventing vitest reporter stdout runaway; (d) Bash grep CI scripts using ripgrep must explicitly register .tsx with `--type-add`; (e) after bulk replacing console.X -> logger.X with sed, run vitest for regressions; broken tests should consistently move to spy log.scope().warn pattern (vitest-setup.ts already mocks cached vi.fn()). | 1 | 2026-05-30 | 2026-05-30 | plan runtime-logging-electron-log-20260529 landed 19 commits with accumulated pitfalls: Step 3.0.2.5 electron-store CJS mock not intercepted, driving D15 §mock-scope expansion; Step 3.5.1 vi.unmock+vi.doMock sequence measured ineffective, driving mock-mediated compromise; Step 3.5.1.5 vitest reporter stdout stuck for 5+ min, driving global electron-log/renderer mock; Step 3.5.2.5 rg -t tsx error, driving `--type-add 'tsx:*.tsx'` fix; Step 3.3.1-3.3.5 fixed 6 broken test files using the spy console.X -> spy log.scope().X pattern. |
| P40 | **A `position: fixed` full-screen overlay / modal can be trapped by an ancestor `backdrop-filter` (or `transform` / `filter` / `perspective` / `contain:paint` / `will-change:transform`) so its containing block degrades from the viewport to that ancestor, then an intermediate `overflow` container clips it**. CSS spec: these properties make descendant `position:fixed` position relative to the ancestor instead of the viewport. In the frosted-glass app (`.frosted-frame` global `backdrop-filter`), any deeply nested `fixed inset-0` modal is affected: it looks full-screen in code but is actually clipped into a strip by an `overflow-y-auto` / narrow-width container (this case: log modal inside the settings panel card with `w-[340px] max-h-[85%] overflow-y-auto`). **Hard to notice in dev**: the same kind of modal mounted shallowly (for example at IssueDetail top level, with no intermediate overflow clipping) happens to be full-screen; only deeply nested mounting exposes it, and changing z-index / adding `fixed` cannot fix it because the root cause is containing block, not stacking level. **Prevention**: (a) any "full-screen overlay / centered modal / toast / dropdown layer" should render via `createPortal(jsx, document.body)` and escape the backdrop-filter/transform ancestor chain at the DOM level; (b) before writing `fixed inset-0`, check whether its DOM ancestor chain has `backdrop-filter`/`transform`; in a frosted-glass app it does by default, so default to portal; (c) audit existing `fixed` modals with `grep -rn 'fixed inset-0' src/renderer` and confirm each is portaled, prioritizing ones buried inside `overflow` containers; (d) comments must not say "`fixed` escapes overflow", which misstates the root cause (the old comment did); they should state that the "portal escapes the backdrop-filter ancestor chain." | 1 | 2026-06-02 | 2026-06-02 | CHANGELOG_196: user screenshot showed Settings -> Logs -> View Logs modal "Daily Logs" clipped into a strip by the settings panel. Root cause: SettingsDialog `backdrop-blur-sm` + FloatingFrame `.frosted-frame` backdrop-filter degraded LogViewerModal's `fixed inset-0` containing block, then `w-[340px] overflow-y-auto` card clipped it. Fix used createPortal to document.body. |

<!-- Historical promotion examples (items already promoted to CLAUDE.md may leave 1-2 note lines here for traceability):
- P1 + P2 + P5 same theme has already been partially promoted to the "Resource cleanup & TOCTOU defenses" subsection as prevention (CHANGELOG_16), but the table still keeps count=1 so the next same-theme hit can advance the count.
- U2 reached count=3 on 2026-04-24 and went through dual adversarial review (Opus 4.7 + codex gpt-5.4 xhigh) with a three-state verdict (landed after edits); it created the "Session resume / disconnect UX (resume first)" subsection in CLAUDE.md (CHANGELOG_25).
-->

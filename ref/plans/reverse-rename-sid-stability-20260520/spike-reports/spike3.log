[spike3.1] wire prefix builder (universal-message-watcher/index.ts):
  L112: return `[from ${safeDisplayName} @ ${safeAdapterId}][msg ${message.id}][sid ${message.fromSessionId}]\n${message.body}`;

[spike3.2] spawn handler wire prefix (spawn.ts):
  L235: `[from ${leadFromName} @ ${leadAdapterSanitized}][msg ${placeholderId}][sid ${caller.callerSessionId}]\n` +

[spike3.3] send_message handler fromSessionId 来源:
  L106: fromSessionId: caller.callerSessionId,

[spike3.4] send_message shared team check (走 sessions.id):
  L53: const sharedTeams = agentDeckTeamRepo.findSharedActiveTeams(

[spike3.5] HandlerContext.caller.callerSessionId 来源 (helpers.ts):
  L44: callerSessionId: callerSid,

[spike3.6] HTTP transport callerSessionId 提取链:
  L49: import { EXTERNAL_CALLER_SENTINEL, type McpAuthInfo } from './types';
  L87: * - `authInfo.fallbackToGlobal === true` → return EXTERNAL_CALLER_SENTINEL（防 spoofing）
  L89: * - 缺 authInfo / resolvedSid 兜底 → return EXTERNAL_CALLER_SENTINEL
  L98: return EXTERNAL_CALLER_SENTINEL;
  L102: return authInfo?.resolvedSid ?? EXTERNAL_CALLER_SENTINEL;

[spike3.7] mcp-session-token-map 写的 sid 含义:
  L2: * Per-session MCP Bearer token ↔ sessionId 双向 map（plan codex-handoff-team-alignment-20260518
  L11: * sid → extra.authInfo.resolvedSid 注入 → tool handler 内 callerSessionIdOverride 命中。
  L31: * sid → token（一对一）。检查「该 sid 是否已有 token」用此 map；release 时同步清。
  L43: * **v4 M2 修法 — 防 re-allocate 同 sid 时旧 entry 残留**：
  L44: * 同一 sid 可能因为 createSession failure 重试、ensureCodex 重新 new Codex 等路径再次
  L46: * （指向已废弃的 sid），导致旧 token 仍能被反查到 sid → 安全风险 + 内存泄漏。所以先把

[spike3.8] 反向 rename 后 wire prefix sid 稳定推论:
  caller.callerSessionId 来自 mcp transport callerSessionIdOverride()
  → in-process: 注入 spawn 时透传的 sessionId (sessions.id)
  → http: authInfo.resolvedSid (mcp-session-token-map.get 反查 sessions.id)
  → stdio: EXTERNAL_CALLER_SENTINEL (固定字面量)
  ✅ 三种 transport 都不读 cli_session_id,反向 rename 后 wire prefix sid 100% 稳定

[spike3] 完成。结论 inline 进 spike3-wire-prefix-sid.md

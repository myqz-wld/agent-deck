[spike2.1] claude fork detect (stream-processor.ts):
  L305: if (resumeId && resumeId !== realId) {
  ✅ fork 判定逻辑命中

[spike2.2] claude renameSdkSession 调用点:
  src/main/adapters/claude-code/sdk-bridge/recoverer.ts:466 (recoverer (jsonl-missing fallback))
  src/main/adapters/claude-code/sdk-bridge/stream-processor.ts:279 (stream-processor (fork detect))
  src/main/adapters/claude-code/sdk-bridge/stream-processor.ts:313 (stream-processor (fork detect))
  src/main/adapters/claude-code/sdk-bridge/restart-controller.ts:189 (restart-controller (close-restart / open-restart))
  src/main/adapters/claude-code/sdk-bridge/restart-controller.ts:341 (restart-controller (close-restart / open-restart))
  共 5 处

[spike2.3] codex renameSdkSession 调用点:
  src/main/adapters/codex-cli/sdk-bridge/recoverer.ts:339 (recoverer (jsonl-missing fallback))
  src/main/adapters/codex-cli/sdk-bridge/thread-loop.ts:154 (thread-loop case 3 (post-resume fork future-proof))
  src/main/adapters/codex-cli/sdk-bridge/thread-loop.ts:263 (thread-loop case 3 (post-resume fork future-proof))
  src/main/adapters/codex-cli/sdk-bridge/restart-controller.ts:134 (restart-controller)
  共 4 处

[spike2.4] hook event sid 来源 (translate.ts):
  L31: sessionId: p.session_id,
  L43: sessionId: p.session_id,
  L214: sessionId: p.session_id,
  L233: sessionId: p.session_id,
  L252: sessionId: p.session_id,
  ✅ hook payload.session_id 即 CLI 子进程当前 thread sid

[spike2.5] codex 不支持隐式 fork 注释:
  L30: *   `summariseCodexSessionForHandOff` 走 codex SDK 自身（codex 不支持 systemPrompt + 4 节模板
  L34: * - codex 不支持 implicit fork：spike-A2 实测 codex CLI resume 永远返回同 thread_id（详
  L153: * 行为零变化(codex 不 implicit fork,详 L34 节注释)。失败路径 reject 仍透传(catch 静默
  thread-loop case 3 future-proof:
  L250: // case 3: 恢复路径但 SDK 返回不同 id（罕见 + future-proof）

[spike2.6] 已有 fork test 覆盖 (claude consume-fork.test):
  L103: it('first realId === opts.resume → 不触发 fork 分支（不调 renameSdkSession）', async () => {

[spike2] 完成。结论 inline 进 spike2-fork-detect-trigger.md

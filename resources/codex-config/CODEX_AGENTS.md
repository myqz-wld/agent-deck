# CODEX_AGENTS.md (codex 视角协议层)

P3 阶段 placeholder。内容由 P4 Step 4.1 写入（codex SDK 视角的 Agent Deck 应用环境约定 / wire format / 团队协作 / hand-off / archive_plan / EnterWorktree 协议规约）。

注入路径：codex-config/agents-md-installer.ts 把本文件内容同步到 `~/.codex/AGENTS.md`（详 plan §D5 fallback 策略 — 本文件不存在时 throw 显式 error，**禁** silent fallback 到 claude-config/CLAUDE.md）。

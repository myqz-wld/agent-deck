node_modules
out
release
dist
.DS_Store
*.log
!ref/plans/**/spike-reports/*.log
# ↑ spike artifacts exception:plan-driven spike reports 内的 .log 是 spike 实测命令输出 trace
# (case-A.log / case-B.log 等),与 spike1-<topic>.md 结论同等重要(实测 evidence 历史可追溯),
# 必须入 git 归档(详 user CLAUDE.md §Step 0.5 spike 节 + 本仓库 plan archive 流程)。
.vite
*.tsbuildinfo
.deep-code-review/
# ↑ 历史目录（已迁移到 ~/.claude/state/deep-code-review/<cwd-slug>/，user 级隔离）。
# 防御性留 entry：万一 skill regression / 用户手工建临时目录，不会被误 commit。
.deep-review-cache/
# ↑ plan codex-handoff-team-alignment-20260518 P6.5 reviewer-claude HIGH-E 修法:deep-review SKILL
# §Sandbox 处理 auto-cp cache 目录(每次 SKILL invocation 落 invocation-id 子目录 + manifest)。
# worktree 内 cache 目录不入 git;ff-merge 回 base_branch 时也不污染。
resources/codex-config/agent-deck-plugin/skills/
# ↑ plan reviewer-codex-cross-adapter-20260519 §Phase 3 Step 3.3:codex-config 端 SKILL 镜像由
# build-time `scripts/sync-codex-skills.mjs` 从 SSOT (`resources/claude-config/agent-deck-plugin/skills/`)
# cp 生成,不入 git (SSOT 仍单源)。dev / build / dist 时 npm `predev` / `prebuild` hook 自动同步。
.claude/worktrees/
.claude/scheduled_tasks*
.claude/plans/
.claude/settings.local*
# ↑ Claude Code 工具运行时产物：worktree 临时目录 / cron 任务持久化与锁文件 / plan local 工作目录 / per-user permissions allow list。
# 不应入 git（per-user / per-machine state — settings.local.json 含 user 路径 hardcode + 命令授权白名单仅本机本用户有效）。
# 注：plan completed 后归档到顶级 `ref/plans/<plan-id>.md` 入 git（详 ~/.claude/CLAUDE.md「§Step 2 Plan 文件 hand off」节）。

# ↓ Appended by plan personal-projects-cleanup-20260520 (2026-05-20) — D2 小众系统垃圾兜底
Thumbs.db
.AppleDouble
._*
*~
.*.swp
.*.swo
__MACOSX/

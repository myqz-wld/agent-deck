# Changelog 索引

> **范围**：功能变更（新功能 / 行为修改 / API / 依赖升级）。
> Debug / 性能 / 安全 review 见 [`reviews/`](../reviews/INDEX.md)（CHANGELOG_16 起划分）。

| 文件 | 概要（≤80 字） |
|------|------|
| [CHANGELOG_1.md](CHANGELOG_1.md) | 项目初始化 M1-M9（Electron+React+TS+Tailwind 半透明窗 / Adapter+Diff 双插件 / SDK+Hook 双通道 / DB v3）+ 应用图标 |
| [CHANGELOG_2.md](CHANGELOG_2.md) | 工具适配 AskUserQuestion + ExitPlanMode：独立类型 + 独立 IPC + 专用 UI（option 按钮 / 批准计划 + markdown 反馈） |
| [CHANGELOG_3.md](CHANGELOG_3.md) | 对话气泡 + Markdown 渲染（user message + MD/TXT 切换从全局级联翻为单条独立）+ AskRow 实色提交 + 毛玻璃底色加深 + header 计数对齐 |
| [CHANGELOG_4.md](CHANGELOG_4.md) | 通知 / 提示音整套：去窗口闪屏 + sound.ts 异步回退 bug + SoundPicker 自定义 + Windows PowerShell MediaPlayer + 防叠播 + 真实 appName |
| [CHANGELOG_5.md](CHANGELOG_5.md) | README 三轮同步：首条消息必填 / settings env / Permission 内嵌 / resume / 466→264 重构 |
| [CHANGELOG_6.md](CHANGELOG_6.md) | 权限交互演进：permissionMode 写库 + bypassPermissions 配套 + 死锁四层兜底（pending 重建 / 警告 / 超时 abort / chip）+ slash 拦截 + cancelled 不再误报 |
| [CHANGELOG_7.md](CHANGELOG_7.md) | 命令行 `agent-deck new`：复用 single-instance + macOS shell wrapper + 默认 prompt"你好" |
| [CHANGELOG_8.md](CHANGELOG_8.md) | SessionList/Detail 行为修复：stickySelected 不闪 + archivedAt 过滤 + cwd 缺省 home + PendingTab 集中（抽 pending-rows + 批量按钮） |
| [CHANGELOG_9.md](CHANGELOG_9.md) | 打包 / 安装 5 步流程：mac.icon + extraResources bin + ad-hoc codesign + pkill 旧进程（chunk hash 错配 → monaco 源码露出） |
| [CHANGELOG_10.md](CHANGELOG_10.md) | Monaco / Diff 红屏修复：window.onerror 同步 + unhandledrejection async 双白名单 + ActivityFeed select-text 可复制 |
| [CHANGELOG_11.md](CHANGELOG_11.md) | 主进程稳定性：safeSend 守 isDestroyed + pin 残影根因升级（`::before mix-blend-mode` 治根 + invalidate 100ms + setBackgroundThrottling(false)） |
| [CHANGELOG_12.md](CHANGELOG_12.md) | 权限 tab 三层 → 四层（user / user-local / project / local）+ HistoryPanel 整行可点 + 已响应/已被 SDK 取消拆开显示 |
| [CHANGELOG_13.md](CHANGELOG_13.md) | 综合优化批次（haiku 模型 + 12 条 + 10 条）：Bearer 鉴权 / Summarizer 超时 / 写放大 / env 白名单 / 历史超期清理 / 死代码清理 |
| [CHANGELOG_14.md](CHANGELOG_14.md) | Adapter 扩展：自带 CLAUDE.md+skill 注入 / Codex CLI adapter / MCP 图片工具 / vision 文生图图生图 / thinking 单独识别 / pin one-shot 强刷 / codex spawn ENOTDIR 修 |
| [CHANGELOG_15.md](CHANGELOG_15.md) | SDK runtime 主线：0.1.x PATH 找不到 node 用 process.execPath 兜 / 升级 0.2.118 修 Task 工具会话死 / 0.2.x native binary spawn ENOTDIR + summarizer 兜底三 bug |
| [CHANGELOG_16.md](CHANGELOG_16.md) | 引入 reviews/ 双轨机制 + CLAUDE.md 简化对齐 + 反馈升级加 Agent 踩坑双对抗三态裁决 + REVIEW_1 八处修复落地 |
| [CHANGELOG_17.md](CHANGELOG_17.md) | 通用 CLAUDE.md 骨架优化（节序重排 / 合并冗余 / 模板瘦身）+ 新增 CHANGELOG/REVIEW 模板（按本项目实际范式抽取） |
| [CHANGELOG_18.md](CHANGELOG_18.md) | REVIEW_2 二十处修复 + 用户报 2 BUG 修 + 已审文件过期机制 + REVIEW_X 模板加 frontmatter + CLAUDE.md 注入 toggle + codex 拆批跑约定 |
| [CHANGELOG_19.md](CHANGELOG_19.md) | ActivityFeed 695 行拆 8 文件 + 顺手补初始化 race + 6 候选对抗评估（仅候选 4 落地，5 个不做附触发条件） |
| [CHANGELOG_20.md](CHANGELOG_20.md) | 双对抗架构评审 Phase 0：N1 payload 截断（256KB + shrink 大字段）+ N2 删会话同步关 SDK live Query（adapter capabilities 加 canCloseSession） |
| [CHANGELOG_21.md](CHANGELOG_21.md) | 双对抗架构评审 Phase 1-3：SettingsDialog 720→338 行渐进拆分 + 引入 vitest（14 单测）+ ingest 拆 5 段 + SettingsSet 拆 8 函数 + N6 事务保护 + N8 token 分支 + notify event-router + summarizer 错误诊断聚合到设置面板 |
| [CHANGELOG_22.md](CHANGELOG_22.md) | 双对抗架构评审 Phase 4：N4 migrations 单轨化（vite ?raw inline）+ N5 FTS5 历史搜索（trigram + external content + 触发器同步）+ Opus 实跑 SQL 抓出 alias MATCH 致命 broken + EXISTS→IN+DISTINCT 200×加速 + UPDATE WHEN 防御 + case_sensitive=1 维持原行为 + verify-fts5.sh 12 项集成校验 |
| [CHANGELOG_23.md](CHANGELOG_23.md) | REVIEW_4 修复落地（4 HIGH + 17 MED + 2 LOW，跨 19 文件）：H1 删除会话竞态（intentionallyClosed + 黑名单双保险）+ H2 SettingsSet 运行时 rollback + H3 字节预算改 utf-8 安全切 + KNOWN_LARGE 递归 + H4 RECENT_LIMIT 与 listEvents 对齐 + IPC 边界校验 helper + 双 finished 屏蔽 + event-router try/catch + ClaudeMd save 返回真实写盘内容 + NumberInput integer + ActivityRow memo + thinking/message 折叠 |
| [CHANGELOG_24.md](CHANGELOG_24.md) | REVIEW_5 修复落地：resume 路径两条 active 重复会话根治（H4 sdk-bridge 入口预占 claim + fallback 复用 OLD_ID）+ H1 manager dedupOrClaim 双保险（cwd 命中即便 existing 也 claim）+ payload-truncate 单字段 8KB→64KB（修「展开后仍带 [truncated] marker」） |
| [CHANGELOG_25.md](CHANGELOG_25.md) | 应用内会话「断连自动续」：ComposerSdk 删恢复按钮，sendMessage 抛 not found 直接走 SDK resume；App.tsx history detail 跟 sessions Map 取最新（修 closed→active 复活看不到）+ U2 候选 count=3 走双对抗升级，新建「会话恢复 / 断连 UX（resume 优先）」节进 CLAUDE.md |
| [CHANGELOG_26.md](CHANGELOG_26.md) | 断连自愈下沉到 sdk-bridge.recoverAndSend（B 方案，双对抗推荐）：单飞锁 + 30s UX 占位 message + 从 sessionRepo 补回 cwd/permissionMode + sdk-bridge.test.ts 4 case 覆盖；renderer 删字符串匹配，约定改主语下沉到 owner 层 |
| [CHANGELOG_27.md](CHANGELOG_27.md) | CLI streaming + resume 隐式 fork 兜底：sdk-bridge.consume 内 first realId ≠ opts.resume → renameSdkSession(OLD_ID, NEW_ID) 把 DB record + 子表整体迁；App.tsx historySession 跟随 session-renamed 切 ID；REVIEW_6 双对抗 + 最小复现脚本铁证根因在 CLI native binary，文档与实测不符；新增 sdk-bridge.test.ts 2 case (44/44) |
| [CHANGELOG_28.md](CHANGELOG_28.md) | 第二种 fork 边界兜底（jsonl 不在）：recoverAndSend 预检 ~/.claude/projects/<encoded-cwd>/<sid>.jsonl 是否存在 → 不在则走不带 resume 的 createSession + 事后手工 renameSdkSession(OLD_ID, newRealId)；CLI 历史失但应用层 events DB 保留 + sessionId 切换链路一致；vitest 加 1 case (45/45)；CLAUDE.md fork 边界扩成两类 |
| [CHANGELOG_29.md](CHANGELOG_29.md) | HistoryPanel 周边微调（事后补）：rename 后自动切 live view + 内/外标签 + 监听 rename/upsert 自动 reload + recoverAndSend archived 自动 unarchive |
| [CHANGELOG_30.md](CHANGELOG_30.md) | REVIEW_7 落地修复（1 HIGH + 4 MED + 4 LOW + 1 新 vitest case）：sdk-bridge post-fallback 用 createSession 返回值 + renameSdkSession 内聚 claim 转移 + HistoryPanel listener stale closure + renderer renameSession defensive + App.tsx updater 副作用挑出 + 标签 tooltip + ipc apply 列表统一来源 + 注释精确化（46/46） |
| [CHANGELOG_31.md](CHANGELOG_31.md) | 设置面板加「置顶时透明」开关：AppSettings.transparentWhenPinned（默认 true 维持原行为），关掉则 pin 时仍保留 macOS under-window 实玻璃；即改即生效 |
| [CHANGELOG_32.md](CHANGELOG_32.md) | 修设置面板 hookStatus 红框（user scope 不需 cwd，main 校验 scope-aware）+ 关透明玻璃后 pin 与非 pin 颜色不一致（CSS 改为 data-transparent，App 算 pinned && transparentWhenPinned） |
| [CHANGELOG_33.md](CHANGELOG_33.md) | ExitPlanMode 4 档目标权限 + bypass 冷切：respondExitPlanMode 接 `{decision:'approve',targetMode}` 走热切 / `{decision:'approve-bypass'}` 走冷切；新 `restartWithPermissionMode` 销毁旧 SDK 子进程 + 用 `allowDangerouslySkipPermissions:true` 重启（复用 recoverAndSend 全套护栏）；SessionDetail 下拉切 bypass 同步走冷切；PendingTab section 头 shared selector + ExitPlanRow 4 档；顺手修 closeSession 不 emit cancel 的 zombie row 隐患 |
| [CHANGELOG_34.md](CHANGELOG_34.md) | ExitPlanMode 批准 bypass 不再弹红字 + 不再多出 cli 孤儿会话：InternalSession 加 expectedClose flag（approve-bypass resolver 之前 + closeSession interrupt 之前双保险），catch 块识别应用主动关闭的 SDK 副产品错误（典型 [ede_diagnostic]）降为 warn 不弹红字；renameSdkSession 把 fromId 加进 recentlyDeleted 60s 黑名单，OLD CLI 子进程 SIGTERM 后异步飞的迟到 hook event 在 ingest 入口被丢弃
| [CHANGELOG_35.md](CHANGELOG_35.md) | Agent Teams 接入 M1（基础设施 + 创建入口）：v006 加 sessions.team_name + AdapterCapabilities.canJoinTeam + AppSettings.agentTeamsEnabled（默认 OFF）+ sdk-bridge per-query env 注入 CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1（summarizer 天然隔离）+ NewSessionDialog/SessionCard/SettingsDialog 三处 UI 收口；resume 路径双道防线 throw；M2/M3 后续
| [CHANGELOG_36.md](CHANGELOG_36.md) | agent-deck-plugin 第一次注入 agents/（0 应用代码改动，SDK 协议原生支持）：抽出 reviewer-claude (Opus 4.7) + reviewer-codex (sonnet wrapper, 含完整 codex CLI 模板 + 大 scope 拆批跑) 两个独立 subagent，主 agent 同 message 起 2 个 Task call 做对抗；新增反驳轮（仅单方独有 HIGH 触发，spawn 对方 reviewer 专注单点反驳）；deep-code-review skill Step 2 委托新 agent + 加 Step 2.5 反驳轮；两份 CLAUDE.md 决策对抗节同步重写并解耦（~/.claude/CLAUDE.md 用裸名不绑定 agent-deck，resources 用 agent-deck:* 全名）
| [CHANGELOG_37.md](CHANGELOG_37.md) | M1+：NewSessionDialog 填 teamName 后 prompt 输入框自动回填可编辑模板（含 3 个 teammate 占位符 + shared task list 协调收尾）；用 lastInjectedTemplateRef 记录上次回填、用户改过就尊重不覆盖；teamName 清空 / 切 adapter / 关 toggle 都不回退 prompt（一次性引导非双向绑定）
| [CHANGELOG_38.md](CHANGELOG_38.md) | ActivityFeed Task 工具专门渲染：describe.ts 加 Task case 一行摘要；tool-row.tsx 加 Task 分支显示 🤖 + subagent_type 紫色 chip + 折叠/展开 prompt 全文按钮（仿 diff 折叠模式）；deep-code-review / Agent Teams 场景下大量 Task 调用从「🔧 Task」一图标升级为「🤖 Task → agent-deck:reviewer-claude [展开 prompt]」高信息密度
| [CHANGELOG_39.md](CHANGELOG_39.md) | Agent Teams M2：team-fs.ts 只读访问 ~/.claude/teams + tasks（realpath 防越权）+ team-watcher.ts chokidar 引用计数 + 60s grace；4 个 IPC channel + TeamDataChanged event；App.tsx 加 view='teams' tab + TeamHub.tsx（列表 + 5s polling）+ TeamDetail.tsx（4 个 section：概要 / 成员 / 应用内会话 / shared task list markdown）+ subscribeTeam fs watch 实时刷新；in-process cleanup 上游 bug 给 UI 提示
| [CHANGELOG_40.md](CHANGELOG_40.md) | Agent Teams M3：接 v2.1.32+ 三个新 hook event（TaskCreated / TaskCompleted / TeammateIdle）—— hook-installer + hook-routes + translate 宽容字段提取（schema 演进兜底 + raw 备查）+ SessionManager ingest 加 team-* 早返避免被 sdkOwned dedup 吞掉 + event-repo.findTeamEvents JOIN 查询；TeamDetail 加 hook 事件流 section（图标 + 时间线）+ 「给 teammate 下指令」入口（应用包装 `Tell teammate <name>: <text>` 塞 lead.sendMessage）+ force-cleanup 按钮（兜底 Claude in-process cleanup 上游 bug，rm -rf 两个目录）；不做 CLI 版本 gating（老版本 CLI 静默忽略新 hook，零冲突）
| [CHANGELOG_41.md](CHANGELOG_41.md) | Sandbox 三档接入（'off'/'workspace-write'/'strict'，默认 off）+ canUseTool 顶部 SandboxNetworkAccess auto-deny + 结构化 message → model 100% fallback dangerouslyDisableSandbox 仅 1 弹框 UX 收口；删 spike env gate 改 settings 驱动；SettingsDialog 11 section 折叠化 + localStorage 持久化 + 沙盒下拉框 + amber 色「仅下次新建会话生效」hint；新增 sandbox-config.ts + 22 cases vitest；resources/claude-config/CLAUDE.md 加沙盒模式段；与 codex 对齐主入口 OS 隔离
| [CHANGELOG_42.md](CHANGELOG_42.md) | SDK Task Manager 地基（5 个 in-process MCP 工具 task_create/list/get/update/delete + tasks 表 + zod schema）：v007 migration 加 tasks 表（含 team_name TEXT 与 sessions.team_name 同语义，team 删不级联删 task）；task-repo.ts 仿 summary-repo 同步 SQL + cascade BFS 防自循环 + safeJsonArray 容错；tools.ts 走 loadSdk 异步加载（避 Vite ESM 转译陷阱）+ snake_case args ↔ camelCase repo 映射 + isError 不中断 agent loop；server.ts getTaskMcpServer() 单例懒构造；23 cases vitest（Node 20.18 binding 兼容，22+ 自动 skip 守门，照搬 search-predicate.test.ts 惯例）；本期不挂 sdk-bridge / 不自动注入 team_name / 不做推送，列 follow-up
| [CHANGELOG_43.md](CHANGELOG_43.md) | SDK Task Manager 接入运行时（缺口 1+2+3）：sdk-bridge.ts query() options 挂 mcpServers + allowedTools（受 settings.enableTaskManager 控制，默认 OFF）+ getTasksMcpServerForSession(teamName) per-session 实例（删 CHANGELOG_42 单例）+ buildTaskTools(repo, teamName) closure 注入（task_create/update/delete 写锁 task.teamName === closure，task_list/get 只读允许跨 team）+ tools.ts handler 写完 emit task-changed → eventBus → IPC IpcEvent.TaskChanged 推 renderer（Layer A+B，Layer C UI 不做）+ preload onTaskChanged + SettingsDialog 实验功能 section 加 toggle + amber 「下次新建会话生效」提示；新增 tools.test.ts 24 cases 纯 mock 验证 closure + emit + 写权限锁；README 设置节同步
| [CHANGELOG_44.md](CHANGELOG_44.md) | deep-code-review skill 改 teammate 主流程 + subagent fallback：SKILL.md 重写 §Step 2 (lead 自然语言 spawn 两 teammate 而非 Task subagent) + §Step 2.5 (sendMessage 对方 teammate 反驳，复用 R_N context) + §Step 5 (sendMessage 复用同一对 teammate，不重新 spawn — teammate 模式 token 省 + 反驳精准的核心 gain 落地点) + §Step 6 (cleanup teammate + force-cleanup 上游 bug 兜底) + 新加 §Fallback subagent (CLI<v2.1.32/agentTeamsEnabled OFF/非 SDK 会话退回原 Task subagent，零成本兼容)；reviewer-{claude,codex}.md body 改 dual-mode (frontmatter 保留给 subagent 注册用，body 加 §使用形态识别 A/B + teammate 模式 multi-round context 复用提示)；reviewer-codex 加 wrapper-level skip 拼接 (外部 codex 进程 stateless，wrapper teammate session 记得上轮 codex 输出，新一轮把上轮 finding 摘要追加到新 codex prompt skip 字段，让 stateless codex 间接享受 teammate context 持久化好处)；走双对抗 (Explore Opus 4.7 + 外部 codex gpt-5.5 xhigh 并发) 初判 ❌ → 用户挑战 F2 误读 (canJoinTeam 是 codex adapter capability 不是 reviewer-codex wrapper 限制) + 漏掉多轮反驳轮 context 持久化 gain → 现场 grep 验证 → 翻 ✅；resume limitation 是 Anthropic 官方实验特性约束不是 agent-deck 自加；后续追加 reviewer-codex 强调 codex CLI **必须 `run_in_background: true`**（同步阻塞会锁死并发对抗）+ 三文件精简 -22% (777→602 行，去重「使用形态识别 A/B 合并表」「reviewer-codex 一句话自检节删掉」「SKILL.md 反模式表合并」「示例触发 4→2」，行为约束完全保留)；resources/claude-config/CLAUDE.md「决策对抗」节顶部加「单次 vs 多轮」分流指引 + 三级兜底链 (teammate skill → subagent plugin agents → 手动并发)；resources/claude-config/CLAUDE.md 决策对抗节去掉 `agent-deck` 字眼，命名走「裸名 + 环境替换」pattern (与 ~/.claude/CLAUDE.md 措辞统一)；同步加分流段到 ~/.claude/CLAUDE.md 决策对抗节顶部
| [CHANGELOG_45.md](CHANGELOG_45.md) | Inbox Watcher (Part A) — teammate 权限审批通路打通：in-process backend 把 teammate 的 permission_request 文本塞进 lead inbox 文件等审批，应用之前没监听 → teammate 调 Bash 死等。新增 inbox-protocol.ts (slugify/lock/permission_response schema 与 SDK CLI 二进制实证对齐) + inbox-watcher.ts (chokidar + 60s grace + per-team requestId 去重) + main bootstrap 按 active team 自动订阅 + IPC TeamRespondPermission 写 permission_response 到 teammate inbox + TeamPermissionRow UI 复用 PendingTab 第 4 类 section + 跨 renderer TeamPermissionResolved 同步事件 + 28 cases vitest；Part D (teammate 看不到 `mcp__tasks__*`) 仍待 5 分钟手工 spike 确认 in-process teammate 是否会从 .mcp.json 加载 MCP 后再投资实施
| [CHANGELOG_46.md](CHANGELOG_46.md) | Team 名一致性纯工程方案：fs SSOT，应用 DB 反向同步。新增 team-coordinator.ts (sync 单一收口 + chokidar root watcher 监听 ~/.claude/teams/*/config.json) + hook-routes PreToolUse 拦 CLI builtin 工具 TeamCreate/TeamDelete/Teammate/SendMessage 抽 team_name (最早通道) + 三个 team hook (TaskCreated/TaskCompleted/TeammateIdle) payload 反向同步 (补强) + fs add (补强)；删 NewSessionDialog teamName 输入框 (team 由 lead 自由建)；task-mcp closure 改 lazy 工厂；放开 sdk-bridge resume + teamName throw 改 warn；撤回所有 prompt-engineering 路线 (system prompt 注入 / SKILL.md 先 ls 检查) — 不再让 LLM 听话；31 cases vitest；HOOK_EVENTS 28 个全列实证无 create 类 hook，PreToolUse 才是真正最早通道
| [CHANGELOG_47.md](CHANGELOG_47.md) | 活动流工具行展示增强 + toolName 双通道漏传 bug 修复：新建 tool-icons.ts 集中映射 emoji（实证 jq 7 天频次驱动白名单 + 避撞 ✅/📝/📋/🤖）；describe.ts 加 Skill case 提取 `{skill, args}`；tool-row.tsx ToolStartRow / ToolEndRow / ExitPlanMode / Task 都接 toolIcon，ToolEndRow 加图标对称；SessionCard 同步改 toolIcon + summariseToolInput 加 Skill case；sdk-bridge.ts emit tool-use-end 从 toolUseNames map 反查补 toolName，codex-cli/translate.ts 三处 emit (Bash/mcp_tool_call/WebSearch) 直接拼 toolName 与 start 对齐；plan 阶段走双异构对抗（Explore subagent + codex CLI gpt-5.5 xhigh）+ 三态裁决，对抗发现 Codex 通道同 bug + SessionCard 同款硬编码 🔧 双双纳入。补丁 1：补 Agent/TeamCreate/SendMessage/TaskOutput/TaskStop describe case + Agent 并入 Task 特化分支；ActivityFeed build toolUseId→startEvent map 透传 ToolEndRow，让 Skill 完成补回 skill 名 / Bash 完成补回 cmd 摘要 / 老 events 缺 toolName 也兜回（前提同窗口）
| [CHANGELOG_48.md](CHANGELOG_48.md) | fs watcher symlink path mismatch fix（chokidar 在 macOS fsevents 下回 realpath，与 raw symlink path 严格 `!==` 比较永远 true → inbox-watcher / team-coordinator / team-watcher 三处 fs 通道全静默失效；用户 ~/.claude → .claude-default symlink 触发；修法 = subscribe 入口 realpathSync 缓存比较基线 ~30 行，详见 REVIEW_16）+ Agent Teams 权限审批 UX 三件套：① systemPrompt 注入 ## Agent Teams 节硬约束 lead 不 SendMessage 劝 teammate 放弃 / 不回 permission_response（events 43839-43843 双重实证生效）② TeamPermissionCancelled 通路（idle_notification 检测 → emit team-permission-cancelled → store 删 pending + activity-feed 标灰 ⊘ 已被 teammate 取消）③ lead session detail activity-feed 也渲染 TeamPermissionRow + PendingTab 该 row 整体可点击跳转 lead session detail（参考 session header onOpenSession 同款，按钮 stopPropagation）；events 表自动验证全链路通（注入 fake permission_request 出 row 43837 + 注入 idle_notification 出 row 43846 reason teammate-idle）
| [CHANGELOG_49.md](CHANGELOG_49.md) | deep-code-review SKILL.md 加 §Milestone tracking 子节（5 milestone 调 mcp__tasks__task_create / task_update：Round 起 / Round 收 / 反驳轮起 / 反驳轮裁决 / 收口校验）+ Step 2/3/5/6 加 task 节奏引用 + 关键约束 8 + 反模式 2 条；不破坏 SendMessage 主路径，enableTaskManager OFF 整段跳过；TeamDetail hook 事件流时间线从「只剩 TeammateIdle」补齐 TaskCreated/Completed；已知风险：teammate 也拿得到 task 工具理论上可能乱建，本期不预防（reviewer agents/*.md 没禁 task_*），运行后看实际再修
| [CHANGELOG_50.md](CHANGELOG_50.md) | 大文件拆分 S1+S2+S3 三 atomic commit：shared/types.ts (777→13 barrel + 8 domain module) / ipc.ts (997→ipc/ 10 子文件 per-domain register) / sdk-bridge.ts 抽 formatAskAnswers 纯函数（1995→1972 class 字节零变更）；显式保护 sdk-bridge class / session/manager / codex sdk-bridge / 4 个 UI 组件不动；零行为变更 typecheck+build+54 vitest 全过
| [CHANGELOG_51.md](CHANGELOG_51.md) | 第二轮大文件拆分 P1+P2+P3+P4 四 atomic commit：pending-rows/index.tsx (728→22 barrel + 4 row + helper) / SessionDetail.tsx (618→322 目录化 + 5 sub-component) / TeamDetail.tsx (587→257 目录化 + 6 sub-component) / session/manager.ts (548→486 抽 4 个 pure helpers normalizeCwd/nextActivityState/extractCwd/deriveTitle，class 字节零变更)；显式保护 sdk-bridge class / session/manager class / codex sdk-bridge / store / translate 不动；4 个 import 站点零变更，typecheck+build+54 vitest 全过
| [CHANGELOG_52.md](CHANGELOG_52.md) | 第三轮大文件拆分 13 atomic commit：claude sdk-bridge (1972→839 facade + 7 sub-module / 5 sub-class) / codex sdk-bridge (559→253 facade + 4 sub-module + ThreadLoop) / session-store (534→473 抽 8 type-guards) / manager.test (561 拆 3 文件 + 共享 setup) + audio 资源 (waiting/done.m4a 内置 + extraResources 路径修法) + CLAUDE.md 新增「单文件 ≤ 500 行试拆」约定；激进拆 class 落地（Plan agent 5 finding 全部修复，吸收 F1 thunk + F2 SHARED Map 关键设计）；REVIEW_20 双对抗 0 HIGH/MED/LOW + 1 INFO 注释 stale，typecheck+build+76 vitest 全过
| [CHANGELOG_53.md](CHANGELOG_53.md) | deep-code-review skill 引导修复：原 SKILL.md §Step 2 用「自然语言指令」+ 模板示意 spawn teammate，没出现 `TeamCreate` 工具名 / 没说必须先建 team / 没列 `Agent(subagent_type, name, team_name, prompt)` 四字段不可缺 → lead 退化套用 §Fallback 模板 = 纯 subagent 通路 → reviewer-codex Bash 在 SDK 上游被默认 settings.json 权限策略 deny → PendingTab 永远不触发；db events 双会话工具调用对比硬证根因。修法仅改文档（plugin 注入路径已开）：SKILL.md §Step 2 拆「TeamCreate + Agent×2 四字段齐全」两步走 + 红字标注 + 自检 + §Fallback 顶部红字警告；reviewer-codex/-claude.md 加「Bash 权限通路差异」段（A/B 两形态走向 + 第一次失败的应对）；不动应用代码（通路没坏）/不动 CLAUDE.md（用户 injectAgentDeckClaudeMd OFF 改了不生效）/不动 settings.json allow list（subagent 才需要、且 zsh:* 大放权收益<风险）
| [CHANGELOG_54.md](CHANGELOG_54.md) | 沙盒机制小步加固 A-1+A-2+B-4：A-1 SANDBOX_EXCLUDED_COMMANDS 扩 docker/watchman/orb/lima/colima/make/xcodebuild（保守版，不加 node/npx/brew —— 直接 spawn JS 等于通用 backdoor / brew 写 /usr/local 太宽）+ A-2 buildSensitiveDenyReadPaths 扩 ~/.zsh_history / ~/.bash_history / ~/Library/Keychains / ~/Library/Cookies；B-4 Codex 一路接 sandbox 三档（settings.codexSandbox: workspace-write/read-only/danger-full-access，默认 workspace-write 零行为变更，复用 codex SDK 原生 union 不做映射）补齐 REVIEW_14「双 backend 沙盒对称」目标的另一半：AppSettings 加字段 + ipc/_helpers 加 parseCodexSandboxMode + ipc/settings 加 applyCodexSandboxMode 进 APPLY_FNS + AgentAdapter 接口加 setCodexSandboxMode? + CodexCliAdapter init 钩子 + setter 转发 + CodexSdkBridge 加 currentSandboxMode 字段（startThread 时透传）+ SettingsDialog 实验功能 section 加三档下拉框；切档仅下次新建会话生效（spawn-time 锁定，与 claudeCodeSandbox 同模式）；A-3（默认 'off' → 'workspace-write'）用户决策暂不切，仅扩名单；22 sandbox-config case 仍全绿（不引新单测文件）
| [CHANGELOG_55.md](CHANGELOG_55.md) | 跨平台兼容性 Windows 支持基础设施（REVIEW_21 落地）：新增 src/main/platform.ts 抽象 IS_DARWIN/IS_WIN/IS_LINUX + encodeClaudeProjectDir；5 处硬编码 '/' 改 path.sep（recoverer.ts jsonl 路径 / team-watcher chokidar dispatch / team-coordinator unlink+sync / manager-helpers deriveTitle / electron.vite.config alias）；新增 resources/bin/agent-deck.cmd Win wrapper（三段 .exe fallback + 自动补 new --cwd）；package.json 加 win/nsis/linux 块 + dist:mac/dist:win/dist:linux 拆分 + scripts/gen-icon-ico.mjs 用 png-to-ico 生成 icon.ico（279 KB 提交进 git）；notify/sound.ts Win 走 PowerShell [console]::beep 替代 stdout BEL + visual.ts dock.bounce 注释 by-design no-op；新增 manager-helpers.test.ts 15 case；180 vitest 全过 mac 端零回退；Win 端真机 E2E 留 CI runner
| [CHANGELOG_56.md](CHANGELOG_56.md) | TeamDetail 结构化 tasks UI（mcp__tasks__* 写入实时显示，新 IPC TaskListByTeam + onTaskChanged 订阅 + McpTasksSection 5 状态 chip）+ A3 mcp tools.ts 同 ingest team-task-created/completed AgentEvent 让事件流也显示 mcp 操作（与 CLI hook 同 kind 同视图，不动 SQL/类型/TeamEventRow）+ Teammate 权限 auto-approve 三档（off/read-only/follow-lead，默认 read-only）：抽 READ_ONLY_TOOLS+EDIT_TOOLS 到 shared/constants 让 lead canUseTool 与 inbox-watcher 共享白名单 + 新建 auto-approve.ts 决策（read-only 命中放行 / follow-lead 跟 lead permissionMode：bypass→全放，acceptEdits→加 EDIT_TOOLS）+ lookupLeadPermissionMode 三级回退（fs SSOT config.json.leadSessionId 优先 → DB SDK 候选 → null 降级）+ inbox-watcher.processInboxFile 接入嵌套 try/catch（append fail → 回滚 dedup + emit UI 兜底防死锁；append ok 后 emit fail 不回滚防双 response；reviewer 双对抗 4 处 HIGH/MED 修法）+ SettingsDialog 加 select 行 + 运行时即时生效（不像 sandbox 要等下次新建会话）；37 + 9 task-tools case 全过；189 vitest 全过
| [CHANGELOG_57.md](CHANGELOG_57.md) | 设置面板文案统一与平台分流（preload `process.platform` 静态字段 + IS_DARWIN/IS_WIN/IS_LINUX renderer util；4 处 Mac/Win 混合描述按平台只显示对应版本：透明窗口 vibrancy / Claude 沙盒 Seatbelt / 敏感目录路径例 / 快捷键格式；Section 标题与字段 label 统一中文，必要时括号注英文术语；StatusBadge `closed`→`关闭`/`休眠中`→`休眠` + SummaryView `Summarizer`→`间歇总结` 全局术语收口）+ Header 新增「📚 资产库」按钮独立 Dialog（三 Tab：Skills / Agents / 应用约定，集中展示 agent-deck plugin 内置 + 用户 ~/.claude/{agents,skills}/ 自定义两类资产，含查看完整内容只读 + 用户自定义新建/编辑/删除/Finder reveal，新增 6 个 Assets* IPC channel + main bundled-assets.ts 启动缓存 + user-assets.ts 原子写）+ 设置面板 PluginAssetsSection / ClaudeMdSection 描述瘦身（删大段技术罗列改一句话「详情见资产库」）+ SettingsDialog 532→200 行拆 9 个 sections 子文件（settings/sections/{Hook,Notify,Lifecycle,Summary,Window,HookServer,ExternalTools,ClaudeMd,PluginAssets,Experimental}Section.tsx）；术语统一表写入 conventions-tally count=1；typecheck+build 全过
| [CHANGELOG_58.md](CHANGELOG_58.md) | CLAUDE.md 编辑器整体迁到 AssetsLibraryDialog「应用约定」tab：设置面板 ClaudeMdSection 只剩注入 toggle + 跳资产库链接（toggle = 设置语义 / 编辑器 = 内容语义，分开干净）+ 资产库三 Tab 「应用约定」从预览 + 「在设置中编辑」回环改为直接落 ClaudeMdEditor（保存/撤销/恢复默认/dirty 拦截整套迁过来，加 closeInFlightRef + guardedClose + guardedSwitchTab）+ ClaudeMd / PluginAssets section 跳资产库按钮文案统一为「在资产库中查看 ↗」（原 PluginAssets「查看内置资产 ↗」与 ClaudeMd「在资产库中查看 ↗」并存让用户疑惑）+ 删 AssetsLibraryDialog.onOpenSettings 回环；SettingsDialog 254→186 行；typecheck 全过
| [CHANGELOG_59.md](CHANGELOG_59.md) | NewSessionDialog 删「模型」下拉 + CLI `--model` flag + IPC `raw.model` + 各 adapter / sdk-bridge 入参 + SDK `query({ model })` 透传五层一次性全删（让 Claude / Codex CLI 子进程自己读 ~/.claude/settings.json / ~/.codex/config.toml 默认 model）。Codex 加「权限模式 (sandbox)」per-session 下拉（三档直接映射 codex SDK 原生 SandboxMode：workspace-write/read-only/danger-full-access，覆盖 settings.codexSandbox 全局值，仅新建路径透传）。CreateSessionOptions 删 model 加 codexSandbox、IPC adapters 走 parseCodexSandboxMode 复用现成白名单、codex adapter+sdk-bridge 透传链补齐；不暴露 approvalPolicy（codex 无 canUseTool 等价回调）；typecheck 全过
| [CHANGELOG_60.md](CHANGELOG_60.md) | ComposerSdk + NewSessionDialog 输入框「粘贴/拖放/上传按钮」三件套发图。Claude SDK 走 base64 image content block；codex SDK 只接 local_image path，主进程 base64 落盘到 `<userData>/image-uploads/<uuid>.<ext>` 喂下游。新增 attachment 共享类型 + paths.ts + image-uploads 模块（write/load 严格 realpath+sep+TOCTOU / reapStaleUploads 14 天 mtime 兜底）+ IPC `UploadedImageLoad` channel + AdapterSendMessage 三参 envelope + AdapterCreateSession opts.attachments + 抽 _image-constants.ts。Adapter 层：claude `pendingUserMessages` thunk 化 lazy materialize 不常驻 30MB×N base64（HIGH-2）+ recoverer thunk 三参签名透传 attachments 第二条等待者不丢图（HIGH-1）+ codex `pendingMessages: Input[]` 走 SDK 原生 + closeSession 残留 attachments fire-and-forget unlink。Renderer：useImageAttachments hook（fullBase64 走 useRef 不进 React state，缩略图 200px canvas-resize）+ useImageBlob 抽出 cache+abort 状态机 + UploadedImageThumb 组件失败灰底兜底 + MessageBubble 渲染附图。mime 收紧 4 种（png/jpeg/gif/webp）与 Claude SDK Base64ImageSource 限制对齐；单图 20MB / 总图 30MB / 100KB text 上限不动；typecheck+build 全过
| [CHANGELOG_61.md](CHANGELOG_61.md) | R1.A 阶段 codex adapter 能力对齐 Claude（plan v3 落地，6 commit + 5 spike 验证）：A1 codex item.updated 增量打开 + UI 按 toolUseId in-place upsert + 跨 adapter tool-use-end status 红色显示；A2a sandbox 持久化（DB v008 + sessionRepo.codexSandbox + resumeThread 透传）+ A2b 冷切（canRestartWithCodexSandbox capability + bridge.restartWithCodexSandbox + IPC channel）；A3 Summarizer 跨 adapter dispatch + codex SDK oneshot runner（read-only sandbox + low effort + cachedCodex 懒创建）；A4a ~/.codex/config.toml mcp_servers 段 marker 包裹 + atomic write + 13 tests（不引 TOML parser 依赖手序列化）；A9 agent-deck CLI `--adapter codex/claude` alias + `--codex-sandbox` flag。spike 全 ✅：sandbox 透传真生效 / 5 codex 并发资源温和 / AGENTS.md 32KiB 上限不存在 / mcp-sdk HTTP transport 集成 ~80 LOC / tool handler caller 必须强制 input schema 注入。剩余 A4b UI / A5 (dep B'4) / A8 单测扩展 / A2c UI 下拉留 follow-up
| [CHANGELOG_62.md](CHANGELOG_62.md) | R1.D 阶段 codex 配置生态对齐 Claude（plan v3 落地，4 commit）：D1 (+ D5 决策) AGENTS.md 注入 Agent Deck 段（HTML 注释 marker 包裹 + atomic write + 自愈 + 用户段保留）；D2 skills 同步到 ~/.codex/skills/agent-deck/（mtime 对比避免每次写 + 删除规则保持镜像 + agent-deck/ 命名空间避免撞名）；D4 Settings UI「Codex 注入」section 双 toggle（与 ClaudeMd / PluginAssets 平行）；D6 packaging 检查无需改动（extraResources resources/claude-config 已含路径分流就绪）；D5 决策合并到 D1 JSDoc 不单独出文档。bootstrap step 7.0 app ready 后双同步。剩余 D3 (reviewer-* codex skill 内容创作) / D7 (codex-runtime config dep B'4) 留 follow-up
| [CHANGELOG_63.md](CHANGELOG_63.md) | R2 阶段 Agent Deck MCP server（plan v3 落地，8 commit + reviewer 双对抗 ADR）：自跑 in-process MCP 让 claude / codex / 第三方 MCP client 通过 5 个 tool（spawn / send / wait_reply / list / shutdown）跨 adapter 编排其他 coding agent session。三 transport 并存（in-process / HTTP / stdio）+ 防递归 4 条规则（depth / spawn-rate / fan-out / cwd cycle 整链回溯 + per-caller race protection）+ 47 单测。合并 R1.A5（codex 自动注入 mcp_servers.agent-deck via SDK config）+ R1.D7（不写盘 ~/.codex/config.toml，env var bearer_token_env_var 引用）。新增 v009_mcp_spawn_chain.sql + sessionRepo upsert/rename 扩 spawned_by/spawn_depth + 顺手补 v008 codex_sandbox 漏列 latent bug + sessionManager.close（lifecycle=closed 而非 hard-delete 防 ON DELETE CASCADE 丢 events）。Settings UI 8 个新字段 + token 自动生成。reviewer-claude + reviewer-codex 双对抗 4 HIGH 全修：spawn schema 加 team_name / wait_reply baseline_ts + backfill / sessionRepo rename 扩列 / shutdown 改 lifecycle=closed
| [CHANGELOG_64.md](CHANGELOG_64.md) | deep-code-review skill 流程重构 + 全局/应用级 CLAUDE.md 加「复杂 plan：worktree 隔离 + 跨会话 hand off」约定（reviewer-claude + reviewer-codex 双对抗 review 收口：1 HIGH 反驳 1 / 6 MED / 2 LOW / 5 SUGGEST-SIMPLIFY 全 fix）。SKILL.md 拆 §Step 2「建 team（前置基础设施，与 Round 概念正交）」+ §Step 3「初轮 spawn」、description 1073→698 bytes、§Step 2 自检并入红字段、task_create 时机统一、反模式表合并 cross-ref。CLAUDE.md 新增 hand off 节明示触发（预计跨 ≥ 2 会话 / 破坏性希望整片回退两核心信号）+ 4 步流程（EnterWorktree / plan 文件落 main repo 绝对路径 / 接力姿势 / cleanup 走 ExitWorktree(keep) + Bash git worktree remove，绕过 ExitWorktree(remove) 跨会话 errorCode 4）+ 与 changelog/tasks/ExitPlanMode/决策对抗 关系澄清（"plan" 一词两种语义区分）
| [CHANGELOG_65.md](CHANGELOG_65.md) | R3 Universal Team Backend 硬切（plan v3 PR-A 5 commit + PR-B 同 PR ~3000 LOC churn）：team 抽象脱钩 Claude Code Agent Teams in-process backend，cross-adapter（claude/codex/aider/generic-pty）session 通过 DB envelope + universal-message-watcher 投递跨 adapter 消息。新建 v010 三表（agent_deck_teams + members + messages）+ v011 tasks.team_id + repo + watcher（hybrid event+poll + retry/退避 + per-team rate limit + per-target backpressure + crash recovery）。adapter 接口扩 canCollaborate + receiveTeammateMessage + notifyTeammateEvent；MCP send_message 改走 messageRepo + team_id selector；spawn_session 改走 ensureByName + addMember；task-manager teamNameProvider → teamIdProvider；sessionManager.delete 加 leaveTeam pre-check + 0-lead 自动 archive；wait-reply-coordinator 加 session-upserted lifecycle=closed 监听。**老 backend 全删**：inbox-watcher (452) + team-coordinator (313) + inbox-protocol (306) + auto-approve (117) + team-watcher (152) + 老 tests (570) + team-fs 296 LOC + hook-routes 3 team route + sdk-bridge env 注入 + translate 5 老 fn + canJoinTeam capability + TeamPermission* 类型 + agentTeamsEnabled / autoApproveTeammateMode settings 字段（REMOVED_KEYS 清历史）+ 10 老 IPC channel + 4 老 event-bus event + 50 处 renderer 消费点。UI 重写：TeamHub / TeamDetail / NewSessionDialog / PendingTab / session-store / activity-feed / event-type-guards / session-selectors / App.tsx / ExperimentalSection。CLI 加 --team / --member 跨 adapter spawn。SKILL + reviewer-claude + reviewer-codex agent body 完全重写为 mcp__agent_deck__* 范式（since_ts race buffer 推荐 + 失败兜底矩阵）。E12 老 team data 一次性 export + 启动 dialog。typecheck + 233 vitest 全过
| [CHANGELOG_66.md](CHANGELOG_66.md) | R4 Generic-PTY adapter 实装 + aider PTY 接入（plan v3 §F 5 任务 + F-bonus，6 atomic commit）：用 node-pty 包装任意 stdin/stdout-only CLI（aider/continue/自管 wrapper），双层 spawn-helper 权限兜底；F1 shared types + zod + 2 preset；F2 GenericPtyBridge per-session lifecycle (spawn/sendMessage/interrupt/close/shutdownAll) + v012 migration generic_pty_config + sessionRepo upsert/rename 17 列对齐 + IPC zod parse + asarUnpack；F3 inline stripAnsi (抄 ansi-regex@6 MIT 不引依赖) + PtyOutputBuffer 环形 + IdleDetector emit waiting-for-user (promptSuffixRegex 二次校验); F4 chokidar PtyFileWatcher emit file-changed (fs-event kind, 不读 content) + DEFAULT_IGNORED 14 条 + skipHomedirWatch + close 必 await; F-bonus aider/generic-pty 都开 canCollaborate=true + receiveTeammateMessage = bridge.sendMessage 让 universal team backend 真支持跨 adapter 投递 (aider 共享 GenericPtyBridge class 各 own instance + 注入 'aider' preset 作为 fallback); F5 NewSessionDialog 加 GenericPtyConfigForm 子组件 (preset 下拉 + 字段编辑 + 实时 zod parse 防脏 + 命令预览); 全量 typecheck + build + 319 vitest 全过
| [CHANGELOG_67.md](CHANGELOG_67.md) | REVIEW_24 落地修复（11 ✅ finding，3 atomic commit）：HIGH-1 PtyOutputBuffer 单 chunk ≥ capacity 整 buffer 归零（aider --no-stream 5-15KB chunk + `> ` prompt 必中）+ HIGH-2 bridge cap (byteLength 100_000) ↔ messageRepo cap (length 102_400) 不一致 CJK 投递失败（修 R4 部分 / R3 老 adapter 留 follow-up）+ MED Codex 1+2 closeSession 顺序倒置 SIGTERM 不可达 + killTimer leak（先 SIGTERM → check sessions.has() 才设 killTimer → fileWatcher.close fire-and-forget）+ MED Claude4 closeSession 后 sendMessage 仍写 PTY 浪费 watcher retry quota（intentionallyClosed check throw "is closing"）+ MED Claude3 spawn-helper asar 路径替换裸 String.replace 改 regex 锚定路径段（与 sdk-runtime.ts:87 同款）+ MED Claude5 chmod boolean 单飞 → promise 单飞消除 race window + MED Codex 3 promptSuffixRegex ReDoS 长度上限 200 + MED Codex 6 parseGenericPtyConfigJson 弱 cast → genericPtyConfigSchema.safeParse 二次校验 + MED Claude6 + Codex 7 GenericPtyConfigForm key={agentId} 强制 remount + LOW Codex 5 fix-pty-permissions access TOCTOU 删 access 直接 chmod 捕 ENOENT + LOW Codex 9 args parse 注释「引号包裹」改「不支持引号」field label 同步。reviewer-codex wrapper 三连 503 / reconnect / 静默 timeout 失败后绕过 wrapper 直 Bash 拆 2 批 background 跑通（异构保留 — codex CLI 仍 gpt-5.5）；6 regression case 守门；typecheck + 325 vitest 全过
| [CHANGELOG_68.md](CHANGELOG_68.md) | R3.E12 一次性「即将硬切」弹窗 + LegacyTeamExportSection 整体下线（PR-B 已上线 + R4 已实施，备份窗口期早过）。**只删不加**：3 个整文件（LegacyTeamExportSection / legacy-teams ipc / 顺手发现的 team-fs 孤儿文件）+ 7 处局部（App.tsx 启动 useEffect / SettingsDialog import & 渲染 / preload 两 facade / ipc-channels 两 channel / ipc bootstrap import & 调用 / settings.ts 字段定义 & 默认值）+ REMOVED_KEYS 加 `r3LegacyExportNoticeAcked` 自清历史持久化值（升级后首启 log `[settings] removed legacy field "r3LegacyExportNoticeAcked"` 实测出现）。typecheck + dist + wrapper ping 全绿；同期会话 REVIEW_25 修打包 chunk 空壳 bootstrap 报错的 follow-up 清理 |
| [CHANGELOG_69.md](CHANGELOG_69.md) | 设置面板信息架构重组：13 section 平铺 → 4 主题分组（会话 / 提醒与外观 / 集成与运行环境 / 跨工具协作 MCP）；5 个资产注入 toggle 从 SettingsDialog 三 section 整体迁到 AssetsLibraryDialog 三 tab 顶部（实现「资产编辑 + 注入开关」单一真源）；删 3 老 section 文件（ClaudeMd/Plugin/CodexInjection）+ 移除设置↔资产库跨 dialog 跳转 prop；新建 InjectionToggleBar + 抽 ContentViewerModal 控制 AssetsLibraryDialog 单文件 ≤500；TeamHub「暂无团队」空态删 CHANGELOG_68 已下线的「老 Claude Code Agent Teams 数据」兼容文案；LifecycleSection 接 HookSection defaultOpen 高优先位；README「设置」节按 4 组重写。0 main/preload/shared 改动，5 settings key 全保留 |
| [CHANGELOG_70.md](CHANGELOG_70.md) | 修打包后 bootstrap mount MCP HTTP transport 失败（FST_ERR_INSTANCE_ALREADY_LISTENING）：cdb01ae R2/B'4 引入 6.5 段错放在 hookServer.start() 之后，fastify listen 后 addRoute 拒。修法把 6.5 段挪到 5.5 PRE_LISTEN（hookServer.start() 之前）+ HookServer.registerRoute 加应用层 invariant 把契约固定在边界。详见 REVIEW_27 |
| [CHANGELOG_71.md](CHANGELOG_71.md) | REVIEW_28 落地：移除 spawn-guards.ts §6.2 cwd realpath 整链回溯（拦截了 deep-code-review SKILL 合法用例）+ rate token 扣除时机重排到 fan-out 之后防饥饿（reviewer-codex MED-1）+ list_sessions 加 spawned_by_filter + 新增 get_session tool（5→6 tool；EXTERNAL_CALLER_ALLOWED Record / 跨 src/docs/resources/README ≥15 处文档同步）+ ADR §6.2 / §3.4 / §3.5 / §10 V11/V20 obsolete + V22-V24 新加 + SKILL Step 0 删 list_sessions 自检 + 失败兜底改用 get_session/spawned_by_filter |
| [CHANGELOG_72.md](CHANGELOG_72.md) | 决策对抗节去 subagent 统一双 Bash CLI（~/.claude + resources + SKILL.md 同步，删 §Fallback 节）+ 大图自动压缩 client-side（base64 超 4.8MB 走 JPEG 重编码 + downscale 序列）+ Bug 3 bypass 短路修复（InternalSession.permissionMode in-memory cache + canUseTool 默认路径前短路；6 单测） |
| [CHANGELOG_73.md](CHANGELOG_73.md) | 清理 experiments/ 过期归档（R1 hand off + plan v3 spike 报告，结论早已落地 R1-R4 实施）+ docs/agent-deck-mcp-protocol.md:7 删「关联」字段 SPIKE_REPORT.md 引用（避免 ADR 顶部断链）；docs/ 整体保留（src 注释 8 处 + README/5 changelog 引用为 active 协议规范） |
| [CHANGELOG_74.md](CHANGELOG_74.md) | claudeCodeSandbox per-session 覆盖 + 运行时切档（与 codex 对称三件套）：DB v013 + IPC + adapter capability 字面镜像 + NewSessionDialog 4 档下拉「跟随设置 / off / workspace-write / strict」+ ComposerSdk 运行时冷切（切到 'off' 弹 confirm，与 codex `danger-full-access` 反向同性质）；顺带抽 `RestartController` sub-module 含 restartWithPermissionMode + restartWithClaudeCodeSandbox 两个语义同构方法（sdk-bridge/index.ts 866 → 816 行净 -50 朝 ≤500 护栏走，剩余拆分写入「不动文件保护清单」） |
| [CHANGELOG_75.md](CHANGELOG_75.md) | Cmd+Alt+T 透明化全局快捷键（plan deep-review-flow-fix Phase T1）：toggle「设置 → 窗口 → 置顶时透明」开关，pin 状态下立即切 CSS frosted-frame + macOS vibrancy；与 `Cmd+Alt+P`（pin）镜像 pattern（main globalShortcut + IpcEvent.TransparentToggled + preload onTransparentToggled + renderer App.tsx 监听 setSettings 持久化）。选 `Cmd+Alt+T` 而非 plan D9 默认 `Cmd+Shift+T`（后者被浏览器「重开关闭标签页」抢，OS 级 globalShortcut 会真冲突），与 `Cmd+Alt+P` 命名一致；README 新增「键盘快捷键」节 |
| [CHANGELOG_76.md](CHANGELOG_76.md) | spawn_session schema 加 agent_name 自动注入 plugin agent body（D1）+ projectSession 反查 universal team backend 修「lead teamName: null 不对称」（D3）（plan deep-review-flow-fix Phase B）。tools.ts SPAWN_SESSION_SCHEMA 加 `agent_name: regex([a-zA-Z0-9._-]+).optional()` + handler 调 `getBundledAssetContent('agent', name)` 拼 body 到 prompt 头部、未找到立即 err（防静默 fallback 落空）；projectSession 改成「先调 findActiveMembershipsBySession + agentDeckTeamRepo.get 反查 → fallback 到 sessions.team_name」。SKILL.md 同步「Step 1 prompt 注入两种形态」与「Step 0.6 选项 B」描述。tools.test.ts 新增 5 测试（D1 三 + D3 二），54 vitest 全过。D2 inherit_caller_permissions / D4 wait_reply 区分 finished/waiting-for-user 留 follow-up（详 CHANGELOG 备注） |
| [CHANGELOG_77.md](CHANGELOG_77.md) | SessionList 按 spawnedBy 树形折叠 + lead/teammate badge（plan deep-review-flow-fix Phase C）：实时面板 active/dormant 各组内 root + children 缩进显示（左侧细蓝边 indicator）；SessionCard 加 teamRole prop（lead 蓝色边框 / 👑 lead 浅蓝 chip；teammate ↳ teammate 更淡蓝 chip），与已有 🛡 紫色 teamName chip 风格区分（紫=team 名 / 蓝=team 内角色）。孤儿 teammate（owner 已 closed/归档/跨 group）按 D8 不绑死，平铺为 root 无 badge。点击行为不变（onSelect → store.selectSession，进 detail / ComposerSdk 流程沿用）；改 renderer 走 HMR，无需重启 dev |
| [CHANGELOG_78.md](CHANGELOG_78.md) | 团队凝聚力修复 6 Phase 全（plan team-cohesion-fix-20260513）：A v014 drop sessions.team_name 走 universal team backend + SessionRecord.teams[] / SessionCard +N badge；B send/reply/wait_reply 三 tool（messages.reply_to_message_id 对话链 + B5 spawn placeholder + B6 删 wait-reply-coordinator）；B7 wire format `[from X][msg <id>]` 注入让 teammate 能 reply_message + 4 doc 同步（SKILL.md / reviewer-*.md / 应用 CLAUDE.md）；C TeamDetail 重写 6 sections「团队工作面板」（拆 8 文件 ≤200 行 + 新 IPC AgentDeckTeamGetFull）；D PendingTab teammate chip + role badge（store 自动 reactive 不需新 IPC）；F D6 close 自动 leaveTeam + D7 TeamLifecycleScheduler 5min/30min grace + IPC ShutdownAllTeammates + Header 按钮（关闭 N 个 teammate / 归档）|
| [CHANGELOG_79.md](CHANGELOG_79.md) | deep-code-review SKILL + 两份 CLAUDE.md 大规模文档减肥（REVIEW_30 落地）：5 核心 doc 1910→728 行（瘦 62%，SKILL.md 282→92 / reviewer-*.md 微缩 / 应用 CLAUDE.md 679→56 SSOT 拆分 / user CLAUDE.md 624→272）+ 外迁 11 文件到 ~/.claude/{templates,SOPs}/（6 markdown 模板 + 2 reviewer bash 模板 + 3 SOP 脚本/笔记）；废弃应用 CLAUDE.md ↔ user CLAUDE.md 人工同步约定（机器无法执行已漂移），改 user SSOT + app Δ；conventions-tally 从 .claude/ 隔离到顶级 conventions/ 独立目录（tally.md + INDEX.md，与 changelog/ reviews/ plans/ 同级），count=3 升级**新建** `conventions/<X>-<topic>.md` 而非写项目 CLAUDE.md（让 CLAUDE.md 保持静态）|
| [CHANGELOG_80.md](CHANGELOG_80.md) | bug 修：lead 归档→team auto-archive 联动 + REVIEW_32 R1 fix 9 条（plan deep-review-and-split-20260513 Phase 1）：原 bug 链 countActiveLeads SQL JOIN sessions + manager.archive/unarchive 改 async 加 _archiveTeamsIfOrphaned/_unarchiveTeamsForRevivedLead 两 helper（lazy import 模式）；REVIEW_32 R1 异构对抗双方共挖 16 finding，本会话挑 8+1 修：HIGH 1 markDelivered SQL 接纳 'pending'（spawn placeholder 不再被 watcher 重投递）/ HIGH 2 findSharedActiveTeams JOIN archived_at（archived team 不再误判 common active）/ HIGH 3 wait_reply nudge 自循环过滤（reply 方向校验）/ HIGH 4 spawn_session return 加 agentName+displayName（用户加）/ HIGH 5 spawn 默认继承 lead permission/sandbox + claude_code_sandbox schema（用户加 + reviewer-codex 旁证）/ HIGH 6 setRole otherLeads JOIN sessions（与 countActiveLeads 一致）/ MED 7 v016 archive_reason 列 + 5 union 来源 + unarchive 联动只复活 'last-lead-archived' / HIGH 9 caller_session_id 改 optional（用户加，7 schema + deriveCaller helper） |
| [CHANGELOG_81.md](CHANGELOG_81.md) | 拆 tools.ts 1060 → tools/ 11 文件（plan deep-review-and-split-20260513 Phase 2 Step 2.1）：facade index.ts (139) + schemas.ts (236) + helpers.ts (154) + handlers/{spawn,send,reply,wait,list,get,shutdown}.ts（39-281）。外部 caller import 路径不变（`from './tools'` 自动 resolve 到 tools/index.ts）。顺手修 REVIEW_32 §Follow-up 2 条 MED：MED-1 fan-out race（setSpawnLink 提到 try 内闭合 race window）/ MED-3 send teamId 跨污染（reply_to_message_id 反查 original.teamId 与 resolved teamId 一致性校验）。剩余 MED 留 H3 拆 scheduler/watcher 时就近修。typecheck 双端通过。 |
| [CHANGELOG_82.md](CHANGELOG_82.md) | 拆 agent-deck-team-repo.ts 658 → agent-deck-team-repo/ 5 文件（plan deep-review-and-split-20260513 Phase 2 Step 2.2）：facade index.ts (130) + types.ts (139) + team-crud.ts (211) + member-crud.ts (181) + member-query.ts (166)。Dependency DAG：member-query → team-crud → member-crud。facade re-export 错误类 + input/option 类型保持外部 caller import 路径不变（`from '@main/store/agent-deck-team-repo'` 自动 resolve）。仅物理拆分零业务行为变更（19 method 签名 / SQL / API surface 100% 一致）。typecheck 双端通过。 |
| [CHANGELOG_83.md](CHANGELOG_83.md) | 拆 session-repo.ts 590 → session-repo/ 7 文件（plan deep-review-and-split-20260513 Phase 2 Step 2.3 / Tier 1 收口）：facade index.ts (38) + types.ts (92) + core-crud.ts (198) + archive.ts (21) + lifecycle.ts (131) + rename.ts (122) + spawn-chain.ts (91)。archive.ts 单独抽出呼应「lifecycle 与 archived_at 正交」原则。`delete` reserved-word workaround 走 `_delete` → `delete` spread 重命名。仅物理拆分零业务行为变更（27 method / SQL / API surface 100% 一致）。Tier 1 三大文件（tools/team-repo/session-repo）全拆完，max sub-file LOC 281。typecheck 双端通过。 |
| [CHANGELOG_84.md](CHANGELOG_84.md) | 拆 pty-bridge.ts 506 → pty-bridge/ 5 文件（plan deep-review-and-split-20260513 Phase 3 Step 3.1 / Tier 2 起步）：facade index.ts (274 含 createSession 主体) + pty-session-state.ts (91 type/常量) + spawn-helper.ts (49 chmod 函数 + promise 单飞 state 留 class) + lifecycle.ts (104 closeSession + shutdownAll) + message-io.ts (150 sendMessage + interrupt + onData/onExit listener factory)。外部 caller import 路径不变（3 处 `from './pty-bridge'`/`'../generic-pty/pty-bridge'` 自动 resolve）。仅物理拆分零业务行为变更（9 method 签名 / SIGTERM-grace-SIGKILL 时序 / fileWatcher.close fire-and-forget 100% 一致）。typecheck 双端通过 + 31 pty-bridge + 7 adapter shared 单测全过。 |
| [CHANGELOG_85.md](CHANGELOG_85.md) | 瘦身 sdk-bridge/index.ts 816 → 495 ≤ 500（plan deep-review-and-split-20260513 Phase 3 Step 3.2 / Tier 2 收口）：抽 6 新 sub-module（pending-cancellation 125 / mcp-server-init 85 / query-options-builder 151 / send-validation 65 / session-finalize 68 / sandbox-resolve 35）+ types.ts 加 makeInternalSession factory + 13 处 stale import 清理 + class field/ctor/thin wrapper jsdoc 瘦身（~110 行 jsdoc 减肥）。3 高密度抽（mcp 拼装 / query options / closeSession cleanup）+ 3 低密度抽（sandbox resolve / internal factory / finalize 链）+ sendMessage validation 抽。仅物理拆分零业务行为变更（method 签名 / emit 顺序 / SIGTERM 时序 / sub-module ctx pattern 100% 一致）。typecheck 双端通过 + 28 单测全过。 |
| [CHANGELOG_86.md](CHANGELOG_86.md) | 拆 manager.ts 734 → 5 sibling 文件（plan deep-review-and-split-20260513 Phase 4 Step 4.3 / Tier 3 收口）：3 atomic commit — Step 4.3.1 enrich (55) / Step 4.3.2 team-coordinator (158, 含 dup 消除 leaveTeamsAndAutoArchive 合并 _leaveAllActiveTeams + delete 段 1 用 satisfies map 区分 reason) / Step 4.3.3 ingest-pipeline (224, IngestContext facade 取代原 implements 设计 — Object.freeze 5 closures 让 pipeline cast `(ctx as any).sdkOwned` 不可达 + 5 个 helper method 保持 private)。manager.ts 439 LOC（margin 12%）；走 deep-code-review SKILL R1 异构对抗（reviewer-claude + reviewer-codex 22 finding，HIGH-B1 facade 反向裁决采纳）；29 session it + 全量 324 vitest 全过；4 条 H5 follow-up（markDormant dead code / mock 缺 .get .unarchive / characterization test / #sdkOwned 真私有升级）。 |
| [CHANGELOG_87.md](CHANGELOG_87.md) | J bug fix（lead detail 重复显示 reply）+ check_reply mcp tool（plan mcp-bug-and-feature-batch-20260513 Phase 1）：2 atomic commit。J 修 — `universal-message-watcher.deliver` 对 reply (replyToMessageId != null) 短路跳过 `adapter.receiveTeammateMessage` 防 lead detail 双显（wait_reply tool_result + SDK echo user message 双重）+ 防 lead Claude 把 reply 当 user input 跑空 agent loop。B check_reply — wait_reply 的非阻塞配对版（lead 调即时返回 `{ reply \| null, timedOut: false }` 不挂 listener / 不起 nudge / 不阻塞），与 J 协同（reply 不再 inject 给 sender SDK 后，sender 走 wait_reply 阻塞 / check_reply poll 主动拿）。helper isLegitReply / replyProj 抽 helpers.ts 让 wait.ts / check.ts 共用。47 tests 全过（3 watcher J + 44 tools 含 3 新 check_reply）。 |
| [CHANGELOG_88.md](CHANGELOG_88.md) | main 端 backlog cleanup C/E/G/H 4 项（plan mcp-bug-and-feature-batch-20260513 Phase 2）：4 atomic commit。C MED-D7 — TeamEventDispatcher.start() 预填 lastArchivedAt cache 防首次 archive transition 被吞（lead archive 联动 + emit team-updated 第一次见到该 team 时 prev=undefined 直 return → active member 收不到 team-archived event）。E LOW — team-lifecycle-scheduler.scan() while-loop pagination 防 long-running 实例 team > 200 漏扫。G MED-A7 — makeAgentDeckTeamRepoMock 补全 18 method（用 AgentDeckTeamRepo 强类型兜底，未来真 repo 加 method 编译期强制提示）。H HIGH-B2 — 新建 manager-team-coordinator.test.ts 5 it 覆盖 leaveTeamsAndAutoArchive closed/deleted/0-lead/多 membership/orphan 边界（H4 dup 消除当时只靠代码 diff）。typecheck 双端通过 + 全 34 session tests + 5 watcher + 5 coordinator tests 全过。D/F backlog 已确认非真 bug 移除。 |
| [CHANGELOG_89.md](CHANGELOG_89.md) | SessionManager `#sdkOwned` 升级 ECMAScript 真私有 + 公开 hasSdkClaim API（plan mcp-bug-and-feature-batch-20260513 Phase 3 / CHANGELOG_86 §H5 follow-up #4 收口）：1 atomic commit。`private sdkOwned: Set<string>` → `#sdkOwned`（runtime 强制不可访问，cast `(sessionManager as any).sdkOwned === undefined`）+ 新公开 method `hasSdkClaim(sessionId: string): boolean`（与 IngestContext.hasSdkClaim 同源）；ingestCtx facade 内 hasSdkClaim 改 delegate 公开 API；删 manager.ts/manager-ingest-pipeline.ts 过时「不能升级真私有」jsdoc 警告；测试反射 `as { sdkOwned }` 改 sessionManager.hasSdkClaim() API（manager-public-api.test.ts:134-136）。typecheck 双端通过 + 全 vitest 23 文件 344 it 通过（含 sdk-bridge.test.ts 7 it 验证 SDK fallback / fork detection 路径无回归）。决策由 plan §决策 6 用户拍板「直接改」走全 vitest 验证，未走对抗。 |
| [CHANGELOG_90.md](CHANGELOG_90.md) | N bug fix — 用户「续聊」归档会话自动 unarchive（plan mcp-bug-and-feature-batch-20260513 Phase 1.5 / 用户 H2 新报告 critical UX bug）：1 atomic commit。根因 IPC `AdapterSendMessage` handler 不检查 `archivedAt`，归档会话被用户从 SessionDetail 历史里「续聊」后 archived_at 一直保持。修法严格区分「用户主动 sendMessage = 显式信号 → unarchive」与「事件流被动到达 → 不动 archived（保持 manager.ts:152-156 正交约定）」两条路径。新公开 API `sessionManager.unarchiveOnUserSend(sid)` 封装 archived guard + 复用 `unarchive()` 已有 archive_at = null + emit + team unarchive 联动；IPC handler 调一行 `await sessionManager.unarchiveOnUserSend(sidParsed)`。3 新单测覆盖 dormant + archived / 未 archived noop / not-found noop。typecheck + 全 vitest 23 文件 347 it 通过。 |
| [CHANGELOG_91.md](CHANGELOG_91.md) | archive_plan mcp tool 实现 + 文档同步（plan mcp-bug-and-feature-batch-20260513 Phase 4a / K1 hand-off 自动化）：2 atomic commit。**Step 1-5**（commit `81a15d8`）archive_plan tool 实现 — types.ts/schemas.ts/tools/index.ts 加 9 tool 注册；archive-plan-impl.ts ~330 LOC DEFAULT_DEPS inject 模式 14 步流程（rev-parse main repo / 预检 worktree clean + cwd 不在 worktree 内 + plan status ≠ completed + 非 detached HEAD / ff merge / frontmatter 更新 + mv plan 到 `<main>/plans/<id>.md` / INDEX append (不存在创建 / plan_id 已在则跳) / git add + commit / worktree remove + branch -D）；archive-plan.ts 薄 wrapper handler；11 it 单测 cover happy path + 5 预检失败分支 + 3 路径 fallback。**Step 6-9**（本 CHANGELOG）文档同步 — `~/.claude/CLAUDE.md §Step 4 cleanup` 拆 3 sub-section（推荐 mcp tool 自动化 + fallback 手动 Bash + 中止特例不走 tool）；`resources/claude-config/CLAUDE.md` 7 tool → 9 tool 列表 + 新加 §check_reply 非阻塞 poll + §plan hand-off 自动化（archive_plan 完整 ts 模板 + 14 步流程概述）。typecheck 双端 + 全 vitest 24 文件 358 it 全过（base 347 + 11 新 archive-plan it）。decision：plan §决策 3+7 用户拍板单 mcp tool 路径，未走对抗。 |
| [CHANGELOG_92.md](CHANGELOG_92.md) | start_next_session mcp tool 实现 + 文档同步（plan mcp-bug-and-feature-batch-20260513 Phase 4b / K2 hand-off 自动化）：1 atomic commit。**实现** — types.ts 9→10 tool 加 `startNextSession` + EXTERNAL_CALLER_ALLOWED false（同 spawn / archive_plan 防 SDK fork bomb）；schemas.ts START_NEXT_SESSION_SCHEMA 8 字段（plan_id 必填 + phase_label / cwd / adapter / team_name / permission_mode / plan_file_path / caller_session_id 可选）；start-next-session-impl.ts ~190 LOC deps inject 5 步业务（plan 文件路径解析 caller cwd 反查 main-repo > fallback ~/.claude / parseFrontmatter / 校验 worktree_path / 校验 status === 'in_progress' completed/abandoned 各自 hint / 构造 cold start prompt 含 phase_label 后缀）；start-next-session.ts ~95 LOC handler 入口（deny external + 调 impl + 组装 SpawnSessionArgs cwd=worktree_path/team_name=plan_id/adapter=claude-code 默认 + 调 spawnSessionHandler 透传同一 ctx + JSON.parse spawn 字段 + 包 K2 metadata + 透传 spawn 字段，spawn isError 直接透传不嵌套包装）；tools/index.ts 9→10 tool 注册。22 it 单测（impl happy path 6 + 失败分支 8 + base_branch 透传 2 / handler deny external 1 + happy 4）。**文档同步** — `~/.claude/CLAUDE.md §Step 3 接力姿势` 拆 §选项 A 用户手动 cold start prompt + §选项 B K2 mcp tool 自动起新会话双姿势；`resources/claude-config/CLAUDE.md` 9 tool → 10 tool + 加 §plan hand-off 自动化：start_next_session 节（完整 ts 调用模板 + 业务流程 + 「新 session system prompt 必须含 user CLAUDE.md」校警）。typecheck 双端 + 全 vitest 25 文件 380 it 全过。 |
| [CHANGELOG_93.md](CHANGELOG_93.md) | K3 hand-off UI 按钮 + LLM 历史总结 + modal preview（plan mcp-bug-and-feature-batch-20260513 Phase 4c）：3 atomic commit（main 实现 / renderer 实现 / 本 CHANGELOG）。**main 端** — summarizer.ts 加 `summariseSessionForHandOff(cwd, events)` ~115 LOC 复制 summariseViaLlm SDK query 模板（loadSdk + permissionMode:'plan' + settingSources:[]）改 model `process.env.ANTHROPIC_DEFAULT_SONNET_MODEL || ANTHROPIC_MODEL || 'sonnet'` 三级 fallback + K3 专用 4 节结构化 prompt（【目标】【已做】【下一步】【相关文件】）+ 60s timeout + 4000 字 result max；formatEventsForPrompt 加 export 共用；ipc-channels.ts 加 `SessionHandOffSummarize: 'session:hand-off-summarize'` + `SessionHandOffSpawn: 'session:hand-off-spawn'` 双 channel；ipc/sessions.ts 加 2 个 handler（双阶段拆分让 modal preview / 编辑后再起 session：Stage 1 拉 200 条 events + LLM 总结返回，Stage 2 用 finalPrompt adapter.createSession 起新 session 沿用原 cwd / agent / permissionMode + 自动 sessionManager.archive 原 session warn-only 不阻塞 + emit session-focus-request 让 App 自动切 detail）；hand-off.test.ts 5 it（happy path / empty events / SDK empty result / timeout 占位 / ANTHROPIC_DEFAULT_SONNET_MODEL env override）。**renderer 端** — preload 加 handOffSummarize / handOffSpawn 2 facade method；HandOffPreviewDialog.tsx 新建 ~140 LOC modal（summarizing / spawning / error 三态 + 重试总结按钮 + busy disabled 防中断 + disposed flag 防 unmount setState）；SessionDetail header 加 button group：仅 isSdk 时显示 📤 hand-off 按钮（CLI 会话起新 session 无 prompt 注入意义不大避免歧义）。**用户开放问题决策**：Q1 自动归档 / Q2 200 条 events / Q3 sonnet 4.6 走本地 settings.json / Q4 modal preview + 编辑形态。**已知踩坑** — modal busy disabled close 防中断 + archive warn-only 不阻塞 newSid + emit focus-request 复用 cli.ts `agent-deck new` 同款自动切焦点通道。dev smoke 推迟 Phase 6 H6 与其他 phase 一起 smoke。typecheck 双端 + 全 vitest 26 文件 385 it 全过。 |
| [CHANGELOG_95.md](CHANGELOG_95.md) | UX 微调六项 + review 修复 9 条 — hand-off 显式触发 + detail header 风格统一 + 接力按钮挪到 Composer 右下 + ActivityFeed 工具描述补齐 + deep-code-review SKILL 改 user-driven check_reply poll + nudge 同时 poll 两个 messageId 解决 wire prefix 协议冲突（用户连发六条反馈一次性落地，再走单次决策对抗双 Bash review × Opus 4.7 + gpt-5.5 异构对抗 → 9 条 ✅ 真问题全修）：HandOffPreviewDialog mount 不再自动调 sonnet 改 CTA 显式触发 + **review 修法 requestSeqRef + capturedSid 双护栏替代 disposedRef**（防 sessionId 切换 in-flight IPC 污染新 session）+ idle 派生加 `&& !spawning` + textarea cond 加 `|| spawning`（防 submit 期间 CTA 闪现 + textarea 消失）；TeamDetail Header「← 返回」从左侧挪到右侧 emoji-only 方按钮（与 SessionDetail 镜像）；SessionDetail header `📤` 整段移除 + ComposerSdk 加 `onHandOff?` 可选 prop 底部 button group 插入「📤 接力」；activity-feed/describe.ts `describeToolInput` 补 TodoWrite/WebSearch/WebFetch 三 case + **review 修法 TodoWrite null 元素 type guard 守门**；deep-code-review SKILL Step 2/4/5 把 wait_reply 改成 user-driven check_reply poll（**review 修法去掉 ScheduleWakeup（限 /loop 作用域）改 user-driven**：lead 每次 turn 开头 check_reply 一遍由 user message 触发，UI 实时活动让 user 自判进度）+ **review 修法 nudge 必须同时 poll 两个 messageId**（watcher 自动给 nudge 注入 `[msg <nudgeId>]` wire prefix → teammate 按纪律 reply nudgeId 而非 originalId → lead 单 poll original 永远拿不到，codex 实测铁证）+ 7→10 tool 数字修正。typecheck 双端通过。tally U3/U4/U5/U6 + P32（disposedRef 反模式）待累计。 |
| [CHANGELOG_94.md](CHANGELOG_94.md) | Phase 5 — A cross-session UI 渲染区分 + L SessionCard 增强 + M 透明 / 置顶解耦（plan mcp-bug-and-feature-batch-20260513 Phase 5）：3 atomic commit（A `cdb2f87` + L `cdb46d1` + M 本 commit）。**A**（Step 5.1+5.2）— `src/shared/wire-prefix.ts` 新 parser + 9 it 单测 与 main 端 `buildWireBody` 形式对称（regex `[^\]]+` 防贪婪）；`message-row.tsx` 仅 user role parse → cyan chip「↩ X」hover title 完整 adapter+msgId + body-only render；`agent-deck-message-repo.ts` 加 `listBySession(sid)` SQL `from OR to` ORDER BY DESC + 1 it；新 IPC `AgentDeckMessageListBySession` + handler + preload facade；新建 `SessionDetail/MessagesPanel.tsx` ~120 LOC（参考 TeamDetail/MessagesSection 风格 + 区分 sender/receiver 加 → / ↩ 标记 + reply chain chip + onAgentDeckMessageChanged 200ms 节流重拉）；SessionDetail Tab type 加 `'messages'`「跨会话」按钮。**L**（Step 5.3+5.4）— summariseToolInput 5 个 case 增强（TodoWrite `[N/M done] · activeForm` 进度 / WebSearch query / WebFetch url / Task+Agent subagent_type · description）；describeLiveActivity 返回 string[] 最多 3 行 + 去重连续同行 + cycle recent.slice(0, 12)；SessionCard render 多行 truncate（i=0 主色 i≥1 副色视觉分层）+ useMemo 缓存防滚动重算。**M**（Step 5.6）— settings 字段 `transparentWhenPinned` → `windowTransparent` 重命名 + REMOVED_KEYS 一次性 migration（旧值 → 新字段不丢偏好）；window.ts setVibrancy 不再依赖 alwaysOnTopCurrent（删 dead field）+ `setTransparentWhenPinned` → `setWindowTransparent`；ipc/settings.ts applyTransparentWhenPinned → applyWindowTransparent；main/index.ts bootstrap + Cmd+Alt+T shortcut handler 同步换名；App.tsx state + FloatingFrame transparent prop 不再 `&& pinned` 解耦 alwaysOnTop；WindowSection.tsx label「窗口透明」+ 文案明示解耦。typecheck 双端 + 全 vitest 27 文件 394 it + 56 skipped 全过。**已知踩坑** — listBySession 不走部分索引（rows 几千 OK 未来 100k+ 再加双索引 + UNION ALL）/ migration 必须在 REMOVED_KEYS delete 之前 / wire prefix chip 仅 user role / Step 5.5 dev smoke 推迟 Phase 6 H6。 |
| [CHANGELOG_96.md](CHANGELOG_96.md) | session rename / delete 撞 FK constraint 收口（v017 schema CASCADE + rename 显式迁 team_members/messages/spawned_by，plan linked-swimming-platypus）：用户报 SDK 流中断「⚠ FOREIGN KEY constraint failed」 截图 + DB 数据完全验证根因（lead 在 4 个 team active 时 fork rename 触发 sessionRepo.rename 内 DELETE OLD 撞 RESTRICT FK 整事务回滚 → consume catch + finally emit 红字 + finalizeSessionStart emit session-start/user message 全到 NEW 名下成 cwd=空孤儿 row）+ 附带发现 sessionManager.delete 同条 FK 链路也撞（leaveTeam 写 left_at 不删 row → DELETE FROM sessions 仍撞 RESTRICT，UI「删除」按钮永远删不掉带 active team_members 的 session）。**v017 migration** recreate `agent_deck_team_members` 表 session_id FK RESTRICT → ON DELETE CASCADE（SQLite 12-step：CREATE _new + INSERT FROM old + DROP old + RENAME _new + 重建 3 索引）+ 注册 migrations/index.ts。**rename.ts** 事务内紧邻已有 events/file_changes/summaries UPDATE 之后增 3 段 UPDATE：(a) `agent_deck_team_members.session_id` OLD→NEW 让 NEW 续接 OLD lead/teammate 角色（CASCADE 自动删会让 NEW 失去 membership team auto-archive 违反 rename 语义；显式迁 + 防御性 PK 冲突 DELETE-then-UPDATE）；(b) `agent_deck_messages.from/to_session_id`（FK 不强制但 watcher 反查 sessionRepo.get(toSessionId) 投递否则 markFailed 假阴性）；(c) `sessions.spawned_by` 自引用（v009 ON DELETE SET NULL 不撞 FK 但保 spawn chain 完整性）。加 `renameWithDb(db, fromId, toId)` test seam（生产 `rename(fromId, toId)` 改薄 wrapper，sessionRepo facade `spread { rename }` 不变）。**测试** — agent-deck-repos.test.ts import v012-v017 + makeMemoryDb 数组同步 + import renameWithDb；改写 RESTRICT 测试为 CASCADE（delete sessions 不抛 + COUNT=0 验证级联）；加 3 个 rename 迁移 it（team_members 续接 lead 角色 / messages 双字段 / spawned_by 自引用）。**注释更新** — v010 加 marker「v017 update：CASCADE 修正 v010 设计冲突」；rename.ts 顶部 jsdoc 整段重写删过期 contract「caller 自行 leaveTeam(OLD)+addMember(NEW)」（实际 6 处 caller 均无实现）+ 加双轨说明；manager.ts delete + manager-team-coordinator.ts leaveTeamsAndAutoArchive jsdoc 删 `@warning FK RESTRICT` 段 + 改 `@note await UX 正确性而非 FK 绕行`（先 leaveTeam 写 left_at + emit member-changed + 0-lead auto-archive，然后 sessionRepo.delete 走 CASCADE 物理清，颠倒顺序会让 CASCADE 已删 row 后 leaveTeam 找不到 active row → 不 emit → UI 不刷新 + archive 联动跑空）。typecheck 双端 + dev 实测 v17 migration 跑通（`[db] migrated to v17 (agent_deck_team_members_cascade)` + sqlite3 验证 schema CASCADE + PRAGMA user_version=17）。decision：用户 Q1 拍板 schema CASCADE 不走对抗（设计单点 + 实测铁证 + 2 Explore agent 调研）。Q2 用户自己手写 SQL cleanup 历史孤儿数据（plan §Step 5）。tally P33 候选 +1：「DB FK ON DELETE RESTRICT + 应用层 leave 仅 UPDATE left_at 不删 row 反模式（pre-check 形式实质失效）」。 |
| [CHANGELOG_97.md](CHANGELOG_97.md) | K2 接力 baton 语义改造（default 不加 team + default 归档 caller）：用户实测 K2 plan 接力撞「原会话被打 lead 标签 / 新会话被打 teammate 标签」UX 噪音 + DB 实证铁证（plan team `desk-assistant-20260512` 从创建以来仅 1 条 K2 自动 placeholder message，lead/teammate 之间从未真正对话；对照组真 reviewer team `deep-review-32` / `phase4-manager-split-review` 各 4-8 条 message）→ 设计语义错配（plan 接力本质是 baton 单向交接，不是 dispatch 派人干活）。**A. start-next-session.ts:96** spawn args `team_name: args.team_name ?? args.plan_id` → 条件展开 `...(args.team_name !== undefined ? { team_name } : {})`，default 不传给 spawn → spawn handler 短路 ensureByName/addMember/placeholder 全套，caller/新 session 都不被打 lead/teammate 标签。**B. start-next-session.ts:126** spawn 成功后加 `archiveSession(caller.callerSessionId)` 块（baton 语义 = 原会话退出，新会话独立接手），失败 warn-only 不阻塞 K2 ok return（同 K3 hand-off 模式），EXTERNAL_CALLER_SENTINEL 双保险防御。`StartNextSessionHandlerDeps` 加 `archiveSession?` test seam（与 `spawnSession` seam 同款 inject 模式）。spawn 失败 / impl 失败时**不**调 archive（baton 还没出手）。**schemas.ts** team_name `.describe()` 改写明示「Default: not set (CHANGELOG_97 baton semantic) ... pass explicitly only if you specifically want lead/teammate communication」。**index.ts** K2 tool annotation 整段重写加「Baton semantic」段 + 返回字段标注 default null（teamId/teamName/spawnPromptMessageId）。**两份 CLAUDE.md** 同步：user CLAUDE.md L188-194 + 应用 CLAUDE.md L88-102 删「加入 plan-id team」+ 加「不加任何 team」+「自动归档 caller session」段，return 字段注释加默认 null 标注，适用场景段去掉「lead 监 teammate 反馈」（baton 语义下 caller 已被 archive 不再监督）+ 加「caller 在 archive 后仍可在历史面板查看接力前对话」。**测试** — happy path 改 mockSpawn return 全 null + 加 `mockArchive` test seam + `archiveCalls` 记录调用 sid + 断言 `spawnArgs.team_name).toBeUndefined()` + `mockArchive).toHaveBeenCalledTimes(1)`；新 it「archive 失败 warn-only 不阻塞 K2 ok return」用 warnSpy 验证；spawn 失败 / impl 失败 it 加 `mockArchive).not.toHaveBeenCalled()`；显式 team_name override it 加 mockArchive 验证显式 team_name 时仍归档 caller（baton 语义与是否启用 team 通信关系正交）。decision：A+B 合并不走对抗（用户已选 C 方案 + 实证铁证 + 设计单点）。**已知踩坑** — caller archive 链 A→B→C→D 逐级归档是预期 baton 链；K3 hand-off 已是 archive+不加 team 模式本 PR 不动；`team_name` 显式 override 时仍归档 caller（设计正交）；caller 仍可历史面板查看接力前对话；EXTERNAL_CALLER_SENTINEL 防御冗余但保留（防 future external caller 路径放开误删）；不删历史 plan team 数据由用户自己决定。 |
| [CHANGELOG_98.md](CHANGELOG_98.md) | mcp tool R1 deep review 25 finding + R2 反馈 9 项 + R3 收口 + B14 dormant 反直觉踩坑沉淀（plan `mcp-handoff-fix-and-skill-timer-20260514`）：4 atomic commit。**A** (`438a613`) src 修复 7 项：H5 caller cwd 反查 / H2 wait_reply nudge 死锁修双查 nudgeMessageIds / H1 mcp__agent-deck__ 连字符 41 处替换 + server pattern / M7 abandoned 拒绝 / A5 archived 三态字段 / M1 shutdown_session 数据保留 + 90 mcp test 全过。**B** (`0cc4f79`) 文档大改 13 项：H3 docs/agent-deck-mcp-protocol.md 729→30 行 stub / H4 跨会话救火 shared-team 前置 callout / H6+D4 user CLAUDE.md §Step 2.5 hand off 触发节 / M4+D5 SKILL.md timer fallback (CronCreate 非阻塞兜底) + 严禁阻塞型 timer 边界澄清 / M3 Opus 4.7 default thinking / M5 wire format id invariant 锚定 / M6 NO MSG ANCHOR 完整化 (send_message + list_sessions 反查 + SDK assistant output 三层兜底) / M8 reviewer 模板 mktemp $TMPDIR / B12 shutdown 不删数据列表补 team_member soft-exit + spawn_link / 8 LOW INFO 顺手。**B14** (`58a5db9`) dormant ≠ 丢 mental model 反直觉踩坑沉淀到 3 处 md：spawn R2 reviewer 时误推理 dormant=fresh → user 提醒 SDK resume → 验证 recoverer.ts:103-220 dormant + send_message → createSession({resume}) 复原对话历史 → mental model 通过 conversation history 隐式保留 ✅。补 app CLAUDE.md / SKILL.md / reviewer-{claude,codex}.md FRESH SESSION 自检条款。**E + F** (`4d48ef0`) R2 反馈 1 HIGH + 4 MED + 3 LOW 收口：E1 HIGH K2 baton spawn_depth 误判修（spawn-guards + spawn.ts 加 opts.batonMode 双改 + start-next-session 显式传 batonMode:true + setSpawnLink 写 lateral parentDepth + 4 case test 覆盖 batonMode 跳 depth / fan-out 仍 enforce / rate 仍 enforce / batonMode=false 默认 - reviewer-codex 警告「双改必须同步」否则 depth 4/5/... 累积污染普通 spawn）/ F1 MED A5 archived caller row missing 探针修 + 1 新 case / F2 MED 补 A5 三态 + A2 nudge 双查 4 case 含 reply 给 nudgeId 核心反向场景 / F3 MED ~/.claude/CLAUDE.md §Step 2.5 触发信号 vs 前置条件分组 / F4 LOW 4 改 README + wait.ts comment + tools/index nudgeMessageIds + schemas.ts:296。**R3 收口** reviewer-claude ✅「全部 R2 finding fix 对症 + 0 引新 bug + 可合 (R3 收口)」（含实测 8-phase K2 baton 链 + fallback chain 各路径 + 6 focus 全过）。typecheck + build + 98 mcp test 全过 (R2→R3 +8 case)。tally P34「mcp baton spawn_depth 误判」+ U7「dormant ≠ 丢 mental model」候选 +1。 |
| [CHANGELOG_99.md](CHANGELOG_99.md) | cwd 失效根治:K2 default mainRepo + 双模式 + archive caller + recoverer 启发式 fallback + tool rename hand_off_session(plan `cwd-resilience-fix-20260514`):8 atomic commit。**根治用户报「会话发消息撞 Path does not exist 弯绕错误链」**(K2 baton 起 session cwd=worktree, worktree 被 archive_plan 删后 sessionRepo.cwd 失效)。**A** (`bdbaace`) Phase A K2 default cwd = mainRepo + tool rename `start_next_session` → `hand_off_session`(用户反馈与现有 hand-off 术语对齐):3 文件 git mv + 所有 export name 同步 + EXTERNAL_CALLER_ALLOWED key + impl 加 mainRepo 计算 + handler 双层 fallback + schemas 描述 + 28 tests pass。**A′** (`1b21f6c`) Phase A′ 双模式改造(用户反馈"hand-off mcp 应当更通用"):plan_id 变 optional + 加 prompt 字段 + impl 双模式分流(plan-driven / generic) + handler default cwd 按模式不同 + ok return 加 mode/ignoredFields + 8 新 generic test → 36 tests pass。**B** (`aa6575a`) Phase B archive_plan default 归档 caller(K2 baton 同款语义):4 新 archive caller test → 17 tests pass。**C** (`aadd3d2`) Phase C recoverer cwd 启发式 fallback(兜底老 K2 session + 边角):cwd precheck + findFallbackCwd 启发式(`/.claude/worktrees/` 模式 + parent walk 不超过 home) + cwdFellBack 强制走 jsonl missing 下游 + 4 新 fallback case + cwdExistsOverride test seam → 11 tests pass。**D** (`db7c893`) Phase D 4 处文档同步(app CLAUDE.md / SKILL.md / docs/protocol.md / user CLAUDE.md):rename + 双模式 + cwd resilience + archive caller。**E3** (`b213d61`) R1 双异构对抗 reviewer 5 MED + 4 LOW 全修(reviewer-claude 0H/1M/2L/2I + reviewer-codex 1H/4M/2L/1I,共识 + 单方独有,三态裁决):MED-1 sessionRepo.get 不对称双 handler try/catch + MED-2 archived auto-unarchive 顺序前置 + MED-3 findFallbackCwd regex 允许子目录 + MED-4 generic callerCwd existsSync precheck + MED-5 archive caller 重新反查 ground truth + LOW-2 home 边界 isAncestorOrEqual + LOW-7 plan 注释错位 + LOW-8 cwd fallback test rename 断言 + LOW-9 spawn-guards stale comment → 254 tests pass。**E4** R2 双 reviewer **0 finding 可合(R3 收口)**:9 项 fix 全部对症 + 0 引新 bug。**G2** Phase G2 3 处文档加 CHANGELOG_99 过渡期警告(发现 J fix 一刀切拦截 reply_message dispatch 设计缺陷,新 plan `mcp-tool-simplify-20260514` 完全删 reply_message + wait_reply + check_reply + J fix 统一 send_message + adapter dispatch 心智模型,10→7 tool)。typecheck + build + 259 test 全过。 |
| [CHANGELOG_100.md](CHANGELOG_100.md) | mcp 协议大简化 10→7 tool + wire format 双锚点 + 文档去冗余（plan `mcp-tool-simplify-20260514`）：删 `reply_message` / `wait_reply` / `check_reply` 三 tool + universal-message-watcher J fix（reply 短路）+ wait-reply nudge 机制。统一 `send_message` 为唯一发消息入口，所有 reply 改用 `send_message + reply_to_message_id` 与普通 send_message 同款 dispatch 自动注入 receiver SDK conversation flow（**心智模型从「lead 主动 wait_reply / check_reply 拉 reply」变成「lead 把 reply 当作普通 user message 收到」**）。Wire format 升级为三段双锚点 `[from <name> @ <adapter>][msg <id>][sid <senderSessionId>]`，spawn handler 注入 lead context block（Lead session_id / Team id / displayName + send_message 用法），双层冗余防协议漂移。Renderer 端 message-row.tsx 加 hand-off context disclosure（默认收起）+ chip 显示 senderSessionId 8-char hash。R2 双异构对抗 reviewer 共挖 6 MED / 5 LOW / 3 INFO finding，三态裁决修 3 条 must-fix：codex MED-1 displayName 未转义 `]` 破坏 wire prefix（新增 `sanitizeWireFieldName` helper + spawn/buildWireBody 双处 sanitize）/ codex MED-2 spawn ensureByName 提前后 createSession 失败遗留 active 空 team（teamCreatedNow flag + 再次 verify 后 hardDelete cleanup）/ 双方共识 tools.test.ts 补 cross-team `reply_to_message_id` reject 测试覆盖（3 case）。附加文档大幅去冗余按用户反馈全砍 5 类（CHANGELOG/Phase/K2/start_next_session 历史代号 + 老协议对比叙事 + 废弃模式声明 + 未来假设 + 重复内容），4 份 agent-deck 文档 + user CLAUDE.md 共减 29 行。typecheck 全绿 + 103/107 test pass（4 fail 是 Electron binding 已知问题与本 phase 无关）。 |
| [CHANGELOG_101.md](CHANGELOG_101.md) | REVIEW_33 9 条 HIGH 修复（plan `review-33-high-fix-20260513`）：8 atomic commit + merge main。**H1** archive_plan ff-merge 前 verify+checkout base_branch（旧实现在 caller 当前 HEAD 上 ff 合错分支）/ **H2** reject abandoned + unknown status（与 main `438a613` Phase A4 R1 deep review 同时修，merge 时收敛 main 措辞）/ **H4** team-lifecycle-scheduler.scan() 两阶段先收集后批量 archive（修前边迭代边 archive 让 archived_at 改非 NULL → list active 缩 → offset 跳错 → 实测 500 条全 ghost 漏扫 200）/ **H6** sessions.ts handOffSpawn 透传 codexSandbox / claudeCodeSandbox（修前用户切沙盒后 hand-off 隐性 downgrade）/ **H7** HandOffPreviewDialog ref guard + main dedupHandOff inflight Map 双闸（修前双击起多次 sonnet IPC + 多 SDK 子进程）/ **H8** 删 settings.ts:209-212 + _helpers.ts:147 parseAutoApproveTeammateMode 孤儿 IPC validation（CHANGELOG_56 后字段已搬到 settingsStore 直读，IPC 路径残留 33 行死代码）/ **H9** archive-plan-impl post-ff-merge 9 个 try/catch 加 `[post-ff-merge:<phase>]` 前缀（修前 ff 后失败 caller 拿 generic error 不知道 main HEAD 已动 → 简单 retry 撞「branch already merged」）/ **H10** archive_plan + hand-off-session 加 worktreePath exists 预检（修前 worktree 已删后 cwd 失效 spawn 起 SDK ENOENT 一片）。**H3 作废**：main commit `7639b23` 删 wait_reply/check_reply/reply_message 三 tool，目标文件 wait.ts 不存在。merge commit `ef11087` 解决 archive-plan-impl.ts + archive-plan.test.ts 双方都改的冲突（H2 措辞收敛到 main，H1 verify+checkout 与 main 改动正交自动保留）。typecheck 双端通过 + 全套 vitest 465/465 通过（含 23 个 H1+H2+H9+H10 archive-plan case + 6 H4 scheduler case + 14 H6+H7 sessions case + 2 H10 hand-off-session case）。**已知**：worktree 跑 vitest 需先 link 主仓库 electron binding（path.txt + dist symlink）；hand-off-session.test.ts makeDeps fallback 改造让 `.claude/worktrees/` 路径默认 exists，`state.missingWorktree=true` 显式控制。 |
| [CHANGELOG_102.md](CHANGELOG_102.md) | REVIEW_35 deep-review-and-refactor-20260514 plan：12 文件热点综合 3 轮异构对抗（4 batch × 3 文件，8 reviewer 并发，跨 R1/Wave 2/R2 三轮）共挖 49+ finding，3 commit 落地 36 真问题修复（9 HIGH + 20 MED + 7 LOW）。Wave 1 (cd1af8c) 修 backpressure 死锁 + dispatcher cache 漏 + summarizer codex timeout + N+1 lookup + 多处 emit 漏 + 死代码删 等 15 真问题；Wave 2 (d2c9e68) 修 useImageAttachments 30MB race + ComposerSdk attachments capability gating + IdleDetector dispose flag + busy 双 send race + main bootstrap 隔离 等 12 真问题；R2 (4a85f68) 又挖 5 个新 HIGH（含 2 fix-to-fix 衍生 race + 1 fix 不彻底）：dispatcher cache 半解（spawn_session/CLI 仍漏 emit → 改 dispatcher 防御方案）/ summarizer in-flight rename FK race / fullBase64Ref 孤儿 leak（React 18 batching 闭包陷阱）/ IPC capability last-line defense / bootstrap fatal dialog。typecheck 0 错 + 467/526 vitest 全过。Follow-up 18 MED + 25+ LOW/INFO 覆盖架构改动（starvation guard 公平 / pty listener 时序 / chokidar 复用 / 多文件 500 LOC 拆分 / 测试基础设施补全）。 |
| [CHANGELOG_103.md](CHANGELOG_103.md) | REVIEW_35 follow-up 优先级 1+2 落地 (plan `review-35-followup-p1-p2-20260514`)：1 commit 9 文件 +390/-36 = 5 真问题修复 + 4 测试盲区补全。**P1 真问题**：C-M5 pty sendMessage emit/write 顺序 + try/catch 兜底 / C-M1 pty-bridge listener 紧贴 spawn 后注册 + earlyDataBuffer/earlyExit 缓存（解 codex 实测 misses=6/20 秒退丢 exit）/ rF R2-2 SIGTERM/SIGKILL kill() throw 兜底（清 fileWatcher + emit session-end + sessions.delete）/ rH R2-M3 useImageAttachments mountedRef + generationRef 防 unmount/clear race / rH R2-M4 HookServer EADDRINUSE fail-loud（dialog + app.exit(1) 不让半启动）。**P2 测试补全**：A1 R2 backpressure stateful test 5 case 替代 regex grep（升级技术债）/ A2 R2 findActiveMembershipIn 4 case + listActiveMembers JOIN 1 case / rE R2 #2 IdleDetector dispose flag 2 case / rE R2 #3 TaskOutput READ_ONLY 2 case。typecheck 0 错 + 475/539 vitest 全过 (+8 新加)。**Follow-up 留独立 plan**：A3 starvation guard 公平排队（架构改动）+ 优先级 3 拆分护栏（4 文件 ≤500 LOC，1 文件 1 plan）+ 优先级 4 cosmetic（GIF dataUrl / 双倍读 / COMPRESS 卡 UI 等）。 |
| [CHANGELOG_104.md](CHANGELOG_104.md) | summarizer.ts 625 LOC 拆 3 子文件（plan `summarizer-split-20260514`，CHANGELOG_103 优先级 3 拆分护栏第 2 项落地）：**纯物理拆分**零业务行为变更。`summarizer/{event-formatter.ts:110, llm-runners.ts:243, index.ts:287}` 总 640 LOC，最大 287 ≤ 500 ✅。档位 1（抽 module-level 纯函数）—— event-formatter 含 formatEventsForPrompt + summariseToolInput + truncate + localStatsFallback / llm-runners 含 summariseViaLlm + summariseSessionForHandOff 两个独立 oneshot helper / index 含 Summarizer class 主体 + summarizer 单例 + re-export facade（外部 4 处 caller `from '@main/session/summarizer'` 零改动，TS module resolution 自动 fallback 到 index.ts）。所有 REVIEW_35 注释（HIGH-B1 / MED-B1 / MED-B2 / R2 HIGH-B1 / LOW-B3 共 5 处加固）一字不改迁移。typecheck 双端 0 错 + hand-off.test.ts 5/5 全过。 |
| [CHANGELOG_105.md](CHANGELOG_105.md) | 全仓库 ≤ 500 LOC 护栏批量收口（plan `universal-message-watcher-split-20260514`，CHANGELOG_103 follow-up 优先级 3 拆分护栏全部完成）：扫全仓 11 个超标文件，11 个独立 commit 收口 = **8 个文件物理拆分**（3 src + 5 测试）+ **1 个顺便修 bug**（TeamHub memberCount=0 pre-existing：listAgentDeckTeams IPC handler 不返回 members 字段，handler 加 N+1 listActiveMembers + 类型签名升级，TeamHub 现有逻辑零改动直接 work + lastEventAt 也算对）+ **2 个加入「不动文件保护清单」**（sdk-bridge/index.ts 511 LOC 仅超 11 行 + CHANGELOG_52+85 已 4 轮深度拆分；mcp tools.test.ts 1227 LOC 但 vitest vi.mock 文件级 hoisting 限制 + ~425 LOC setup 不可跨文件共享 → 拆完任意 sub-test ≥ 500）。8 个真拆分单文件 LOC 全部 ≤ 500 ✅，最大文件：universal-message-watcher/index.ts 346（拆 4）/ preload/api/misc.ts 154（拆 5）/ ComposerSdk.tsx 422（抽 ErrorBanner+SelectRow generic）/ test 单文件最大 hand-off-session.handler-cwd-generic.test.ts 475。测试拆分两种模式：deps inject（archive-plan / hand-off-session）抽 _setup.ts 共享，vi.mock 模式（sdk-bridge / pty-bridge / agent-deck-repos / task-manager-tools）每 sub-test 独立 hoisted vi.mock + 可共享纯 helper（class / factory / mutable state）抽 _setup.ts。typecheck 双端 0 错 + 198/198 vitest 验证通过（agent-deck-repos 38 probe-skipped binding ABI 不匹配与拆分无关）。**已知踩坑**：worktree 缺 electron binary 跑 vitest 报错（CHANGELOG_101/104 同款）；preload composite project 仅加 api/**/*.ts 不加 index.ts（d.ts type-only import 延迟解析）；vitest vi.mock 文件级 hoisting 是 mcp tools.test.ts 保护清单根因；_setup.ts 下划线开头自动跳过 test glob。 |
| [CHANGELOG_106.md](CHANGELOG_106.md) | baton 收口 + cwd 失效 UX 双修（同会话两件事打包）：**A. baton teammate shutdown**（archive_plan / hand_off_session 默认 shutdown caller=lead 的 team 内其他 active teammate 避免孤儿，新增 keep_teammates schema 字段允许跳过 + ok return.teammatesShutdown 三态字段；新 helper `shutdown-teammates-on-baton.ts` 106 LOC 三 deps inject seam 串行 close + 失败容错；**时序**先 helper 后 archive caller 不可颠倒，否则 archiveTeamsIfOrphaned 先把 team auto-archive、helper listActiveMembers JOIN sessions archived_at IS NULL 看不到 caller）。**B. jsonl missing emit 修复**（用户报「会话 resume 后丢了历史上下文」「你是不是没有历史会话信息了，这里是」根因 — recoverer.ts jsonl missing fallback 路径只 console.warn 不 emit UI 提示，与对称 cwdFellBack=true 路径实现不一致；补一条同款 emit info message 告诉用户「CLI 内部对话历史已丢失，Claude 这条新启动的 CLI 不知前情，如要继续之前话题请在下条消息把背景再告诉它一次」，文案区分「**已**丢失」vs cwdFellBack 路径「**将**丢失」差一个字让 test filter 精确锚定）。新增 17 case + 6 处原 case 注入 noopShutdown 防 stderr 撞 DB 未 init 噪音；typecheck 0 错 + 148/148 vitest 全过。**已知踩坑**：caller 显式传 team_name 让新 session 接管 lead 时应同时传 keep_teammates: true 否则原 reviewer 被关掉新 session 没人对话；helper 时序颠倒会让语义混乱；test seam 必须主动注入否则集成后默认走真 helper 撞 DB。**手段 2「fallback 自动 LLM 摘要注入」另起独立 plan**（用户拍板）。 |
| [CHANGELOG_107.md](CHANGELOG_107.md) | CHANGELOG_106 「手段 2」落地（独立 plan `llm-handoff-summary-fallback-20260514`）：让 jsonl missing / cwdFellBack=true 两条 fallback 路径起 fresh CLI 之前自动调 LLM (sonnet) 摘要应用 DB events 历史 → prepend 到首条 prompt 让 Claude 知前情，让用户体感「能续聊」（替代 CHANGELOG_106 兜底「请补背景」手动补）。**9 step / 6 commit** 顺序 1→2→5→3→4→6+7（5 提到 3 之前避免占位 true 后改）：Step 1 (`c2c564d`) recoverer 加 SummariseFnThunk type + ctor 第 6 参数 + facade `protected summariseForHandOff` 双层 wrapper / Step 2 (`7962a11`) 抽 helper `recoverer-helpers.ts` ~190 LOC、5 种 failReason (settings-off / no-events / summary-empty / over-length / thunk-throw)、永不抛错、五等号块拼接、不直接 emit 让 caller 解耦 / Step 5 (`bd9863b`) `settings.autoSummariseOnFallback: boolean` 字段全栈（schema + DEFAULT + UI Toggle 在 ExperimentalSection 含 sonnet 计费警告）— 不动 ipc/settings.ts 同 enableSound 等纯 boolean / Step 3 (`c8e123d`) jsonl missing 路径 createThunk 之前调 helper、`result.used=true` emit「LLM 摘要已注入,Claude 应能续上」/ `false` emit 原 CHANGELOG_106 文案 + vi.mock event-repo + settings-store（同时完成 Step 7 noopSummarise 注入）/ Step 4 (`3f4c8a3`) cwdFellBack outer emit 删「将丢失」字眼留 cwd 切换 fact + inner 加 cwdFellBack 子分支按 result.used 文案分支 / Step 6+7 (`00adbd0`) 加 4 新集成 case（jsonl 摘要成功 / settings off / thunk throw / cwdFellBack 摘要成功）+ TestBridge summariseThrow field。**核心不变量**：摘要失败 / DB 没历史 / 摘要超长 / settings 关 → 退回 CHANGELOG_106 emit 路径不阻塞主路径 / 不持久化每次 fallback 重算 / 仅 fallback 路径触发正常 resume 路径不调摘要。typecheck 0 错 + sdk-bridge 整目录 37/37 全过。**已知踩坑**：mockImplementation 撞 settingsStore.get 泛型签名需 `as never` cast / cwd 入参 cwdFellBack=true 时传 rec.cwd 而非 effectiveCwd 让摘要保留「原本是哪个 worktree」语义 / outer 「将丢失」字眼必须移到 inner 否则前后矛盾误导。 |
| [CHANGELOG_108.md](CHANGELOG_108.md) | REVIEW_36 R1+R2 sandbox + resume + hand-off 真实生效性 + 优化空间深度 review × fix 收口（用户主动「sandbox 配置 resume / hand-off 后是否真生效」）：异构对抗（1 reviewer-claude 全 scope + 2 reviewer-codex 拆批，codex 第一次撞 sandbox EPERM 失败 → 拆批 + prompt 硬约束重试）共挖 R1 6 真问题 + R2 5 真问题（含 1 R2 HIGH-A **fix-to-fix bug** hand_off_session(team_name=x) baton 关掉新 spawn session）。**R1 fix**: 3 HIGH 全部双方/多方独立提出（HIGH-1 recoverer fallback claudeCodeSandbox **静默降级安全 critical** / HIGH-2 hand_off_session schema 缺 sandbox 字段 / HIGH-3 外置 worktree allowWrite 不覆盖）+ 3 LOW（spawn 单次反查 / 注释更新 / 删死代码）。**R2 fix**: R2 HIGH-A baton excludeSessionIds + R2 HIGH-B cwd fallback 写权限 emit warn 透明化 + R2 MED-A restart 透传 permissionMode + R2 MED-B restart 单飞标记提前 set + R2 MED-C 外置 worktree mainRepo plan 文件 sandbox extraAllowWrite 透传链架构性重构（buildSandboxOptions / createSession / spawn / hand-off / schema 五层透传 + plan-driven 外置 worktree 自动加 mainRepo）。**用户中段反馈**：user CLAUDE.md §Step 4 ff-merge 目标分支应该是 base_branch 而非直接 main → 同步修文档 + archive_plan tool schema 默认值（caller > frontmatter > main 优先级）。**INFO follow-up**：HIGH-3 注释 + schema cwd describe + codex restartWithCodexSandbox rename defense（防 SDK 升级 silently fail）。共修 13 文件 + 8 新 regression case + 全套 503 tests / typecheck 双端全过。 |
| [CHANGELOG_109.md](CHANGELOG_109.md) | Plan `model-wiring-and-handoff-20260514`：闭环 model 链路 3 处不闭环点。**A**: spawn_session 解 agent body frontmatter `model` 字段透传给 SDK + 持久化 sessionRepo（v018 migration），让 reviewer-claude.md `model: opus` 等真正生效（修前死字段）；codex SDK 不接受 per-thread model override，仅持久化让 UI 显示 + console.warn 提示用户改 toml。**B**: settings 加 `summaryModel` / `handOffModel` 两字段 + SummarySection UI 加输入框（draft / focus / blur 模式），优先级 `settings 显式值 > ANTHROPIC_DEFAULT_*_MODEL env > ANTHROPIC_MODEL > alias 兜底`，空 = 沿用现有 env 链路（零迁移成本）。**C**: 新增 `adapters/codex-cli/handoff-runner.ts` `summariseCodexSessionForHandOff`（4 节模板 + reasoningEffort='medium' + 60s timeout），`ipc/sessions.ts:70` 按 session.agentId 派发 — codex session 终于由 codex 自己出 hand-off 简报（修前借用 claude SDK + sonnet 不一致）。共改 14 文件 + 新建 3 文件 + 1 新 regression case + 全套 504 tests / typecheck 双端全过。详 [`plans/model-wiring-and-handoff-20260514.md`](../plans/model-wiring-and-handoff-20260514.md)。 |
| [CHANGELOG_110.md](CHANGELOG_110.md) | REVIEW_37 R1+R2+R3 宏观重构机会三轮异构对抗 × P1+P2+P3 三档落地（plan deep-review-and-refactor-r37-20260515）。reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 xhigh teammate 三轮全 scope main 进程重构机会扫描共挖 8 ✅ HIGH（R1 7 + R2 1）+ 10 ✅ MED（R1 8 + R2 2）+ 6 LOW/INFO 真问题 + 3 R2 ❌ pre-existing 不归 R37（codex/claude 架构对称留专门 plan）+ 3 calibration 跳过（plan 决策不实施 + R3 双方独立验证理由站得住）。**Phase 1 P1 trivial 高 ROI**（4 commit）：D withMcpGuard + preload subscribe / G codex-instance-pool / B recoverer message builder / Phase 2 trivial（omitUndefined / 注释碎片 / 占位文案）。**Phase 2 P2 中等改造**（6 commit）：F shared mock factory（5 类） / H 4 LLM oneshot runner 抽 helper（race/clean/prompt/SDK 1 SSOT） / I summariseEvents dispatch 下放 adapter / E codex sdk-bridge 拆 input-pack/session-finalize/restart-controller(543→409 过 500 护栏) / M runBatonCleanup helper（archive_plan / hand_off_session 共享 ~80 LOC） / N message-delivery-state SSOT + SQL backoff fragment 派生。**R2 fix**（2 commit）：R2 HIGH-1 spawn handler batonRole opts + hand-off-session 透传 'lead'（修 hand_off_session(team_name=X) baton 0-lead auto-archive） + R2 MED-1 codex handoff slice 不限长度 + R2 MED-2 codex timeout race scope 包整 SDK init。**Phase 4 P3 散落收口**（4 commit）：C cwd resolver / C recoverer.emitFallbackMessage / L 7 result type satisfies（拦下 3 处真实类型漂移）/ J shared/ jsdoc contract vs policy 边界。**3 calibration 跳过**：Step 4.2 single-flight helper（真单飞只 2 处 + sub-class state ownership 改造高风险）/ Step 4.4 preload misc 拆（misc.ts 154 LOC 未触 500 LOC 护栏 + invoke<T>() Step 1.1 已否决）/ Step 4.6 IPC errorMode wrapper（throw vs result 两类已合理分 + handler return type 自表达 + 改造大土无 bug）。**R3 收口**：reviewer-claude (sid `cb9e3722`) 0 HIGH/0 MED + 2 INFO 非阻塞 / reviewer-codex (sid `f366fbde`) 0 finding + 命令验证全过 → 双方一致 ✅ 可合。共 16 commit / 行为零变化 / typecheck 双端 + vitest 524/524 全过。详 [REVIEW_37.md](../reviews/REVIEW_37.md) + [`plans/deep-review-and-refactor-r37-20260515.md`](../plans/deep-review-and-refactor-r37-20260515.md)。 |
| [CHANGELOG_111.md](CHANGELOG_111.md) | Plan `worktree-stale-base-bug-20260515`：Claude Code CLI v2.1.112 EnterWorktree builtin **stale base bug** R1 异构对抗 × **document-only** 收口（详 [REVIEW_38.md](../reviews/REVIEW_38.md)）。R37 archive_plan 收口后立即 hand-off 准备阶段实测撞 bug：调 `EnterWorktree(name: "X")` 创新 worktree HEAD = `origin/<default-branch>` 而非本地 HEAD（实测 worktree HEAD = f253794 落后本地 main 91c4568 二十个 commit）。Lead 独立 grep + minified bundle 反编译 100% 实证定位 root cause（cli.js byte ≈ 11794070 默认走 `git worktree add --no-track -B <branch> <path> origin/<default-branch>`；CLI tool description 明文承诺 `based on HEAD` 与实现严重不符 = contract vs implementation 矛盾 bug）后 spawn R1 异构对抗 review（reviewer-claude Opus 4.7 focus 代码 + minified bundle 验证 + reviewer-codex gpt-5.5 xhigh focus git plumbing + man page + reflog 实证）双方一致 ✅ 6 HIGH（含 reflog `branch: Created from origin/main` 加强证据）+ 3 ✅ MED/LOW + 1 ❌（fromHead 隐藏参数不可靠）+ 1 ❓（bare/submodule 未验证）。**Lead 三态裁决 document-only**（plan §设计决策 5 阈值「fix 复杂留独立 plan」；hand_off_session / archive_plan 都不在 EnterWorktree 调用链；application 无 hook 点 wrap CLI builtin；非 evidence 冲突未走反驳轮）。**Workaround**：(b) Bash `git worktree add -b ... <path>` 隐式 HEAD 主路径已被本 plan worktree 自身验证铁证 + (a) 自检 + reset 兜底；user CLAUDE.md §Step 1 typed warning 同步落地。**Upstream**：应按 bug 报 anthropics/claude-code GitHub issue（reproduction recipe + suggested fix + 模板见 REVIEW_38）。**长远 fix 提案**：新增 `enter_worktree_safe(plan_id)` mcp tool 留独立 plan 锚点。**无代码改动**（typecheck / vitest 不需跑）。 |
| [CHANGELOG_112.md](CHANGELOG_112.md) | hand_off_session 不写 baton spawn-link + UI ↳ teammate badge bug 方案 1 修（plan `hand-off-mcp-teammate-bug-20260515`）。用户实测「『hand off mcp』还是会挂成 teammate」+ 补「**没有 team 标志,但是在实时会话页面上有层级关系**」。异构对抗 R1+R1.5+R2 双 reviewer 同对跨轮复用：**root cause** spawn.ts:262 setSpawnLink 在 batonMode=true 路径仍写 spawnedBy=callerSid + SessionList Phase C(CHANGELOG_77)按 spawnedBy 树形分组渲染 ↳ teammate badge,**与 team membership 完全无关**。**HIGH 时间窗 race**: 真实窗口来自 spawn 后续 child upsert(typical recordCreatedPermissionMode → manager notifyTeamMembershipChanged emit session-upserted)早于 runBatonCleanup archive caller 完成 — 即使 archive 100% 成功也触发。**R1+R1.5 反驳轮双方互换立场**(claude 推 1→改 5 / codex 推 5→改 1)互不可调和,用户决策走方案 1。**fix**：spawn.ts:260-263 加 `&& !opts?.batonMode` 跳 setSpawnLink 分流 + ok return spawnDepth fallback 同步 + 2 新守门 test case(batonMode=true 跳 + 缺省守门)+ R37 R2 HIGH-1 现有 test 加 1 行断言(LOW-1/INFO-1 双方独立 ✅ 守门「batonMode + 显式 team_name」组合)+ hand-off-session.ts:306 stale 注释清理(INFO-4 codex 单方)。**HIGH 独立 follow-up**: R37 caller `024289d4` 实测仍 active 是独立 root cause(archive 失败 warn-only / 旧 archive_plan / unarchiveOnUserSend),与本 fix 正交,用户拍板单独建 plan `archive-failure-ux-upthrow-20260515` 跟进。typecheck 双端 + tools.test.ts 46/46 + spawn-guards.test.ts 12/12 全过。详 [REVIEW_39.md](../reviews/REVIEW_39.md)。 |
| [CHANGELOG_113.md](CHANGELOG_113.md) | REVIEW_40 R1+R2+R3 codex/claude adapter 架构对称性深度 review × Phase 2+3 fix 落地（plan codex-claude-adapter-symmetry-20260515,REVIEW_37 R2 ❌ pre-existing 触发本 plan）。reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 xhigh teammate 三轮 focused single-topic review 13 文件 + 6 sub-focus(sandbox 字段命名 / restart method signature / resume defense / event emit 时序 / ensureXxx pool 模式 / SDK lifecycle 边界)。**关键反转**: R37 R2 ❌ pre-existing HIGH-2/HIGH-3 「命名/signature 不一致」claim **不成立**(reviewer-claude grep 36/38 + 32 处实证规则已统一,enum value 不一致是 SDK 内禀差异);仅 MED-3「codex resume defense 与 claude 不对称」是真问题。R1 共挖 9 真问题(2 HIGH + 6 MED + 1 LOW),含 1 反驳轮 reviewer-codex 单方 HIGH 被 reviewer-claude 部分支持降为 MED。**Phase 2 P2 R1 fix**（6 commit / 按风险升序）：MED-C jsdoc / MED-B 删 codex currentSandboxMode 三层镜像(R37 P1 G 同款先例) / HIGH-A single-flight + MED-A emit upserted(字面镜像 claude REVIEW_36 R2 MED-B) / HIGH-B 新建 codex SessionRecoverer ~280 LOC + MED-E jsonl pre-check + LOW-A cwdExists / MED-D thread-loop case 3 + resume await thread.started(future-proof) / MED-F extraAllowWrite jsdoc reflect reality(fictional claim 删除)。**Phase 3 R2 fix**（1 commit）：R2-1 sessions cleanup(reviewer-codex HIGH 单方+lead 实证 c9c94d7 引入) + R2-2 cwdFellBack 不强制 fallback + emit text 改正(reviewer-claude MED-G 单方,fix codex jsonl 独立于 cwd 自相矛盾) + R2-3 30s timeout 补 emit info(reviewer-claude LOW-B)。**Phase 4 顺手 polish 跳过**(R37 R3 INFO #1/#2 触发条件未满足)。**Phase 5 R3 fix**（1 commit）：R3-1 late earlyErr cleanup(reviewer-codex 单方 R2-1 漏洞,30s timeout 后短路 cleanup)。**3 follow-up 留独立 plan**：cross-adapter parity(recoverer waiter Promise<string>)/ double rename owner cleanup(idempotent 不阻塞)/ codex sdk-bridge unit tests(横跨 HIGH-A/B/MED-D/E)。共 8 commit / 行为零变化 / typecheck 双端 + vitest 524/524 全过 / 全程同会话内完成无 hand-off 触发(R37 教训规避成功)。详 [REVIEW_40.md](../reviews/REVIEW_40.md) + [`plans/codex-claude-adapter-symmetry-20260515.md`](../plans/codex-claude-adapter-symmetry-20260515.md)。 |
| [CHANGELOG_114.md](CHANGELOG_114.md) | `hand_off_session` mcp tool 加 `archive_caller` 字段让 caller 可选 opt-out 归档(plan `hand-off-mcp-archive-opt-20260515`)。修前 hand-off 强制 archive caller(baton 单向交接默认语义),不灵活;新增 `archive_caller: false` 让 caller 起新 session 并行做事自己仍 active —— 典型用例 lead 起多个 follow-up hand-off 自己继续协调进度 / debug 工具实测某 plan 但 caller 仍要观察。schema 默认 `true` 保持向后兼容,与 `keep_teammates` 字段对称命名(baton 默认动作可显式 opt-out)。**改动 4 文件**: schemas.ts(HAND_OFF_SESSION_SCHEMA 加字段 + HandOffSessionResult.archived jsdoc 扩 'skipped' 多来源)/ hand-off-session.ts(透传 args.archive_caller !== false 给 runBatonCleanup)/ baton-cleanup.ts(RunBatonCleanupInput 加 archiveCaller?: boolean optional 保 archive_plan 调用方零改动 + phase 2 入口短路标 archived='skipped' 零副作用)/ test 5 case(baton-cleanup 加 case 11+12 / hand-off-session.handler-deny-happy 新建 archive_caller opt-out describe 加 3 case 含正交两字段同时启用)。trivial schema 加 + handler 透传 + helper 分支,不走多轮对抗 review。typecheck 双端 + vitest 全套 39 文件 / 531 测 0 regression。 |
| [CHANGELOG_115.md](CHANGELOG_115.md) | Plan `codex-sdk-bridge-tests-20260515`：codex sdk-bridge 单测套件 + LOW double rename owner cleanup（REVIEW_40 R2 reviewer-claude INFO-T 横向技术债 + reviewer-codex LOW 顺手清）。**P1**: 抽 codex `__tests__/sdk-bridge/_setup.ts`（镜像 claude TestBridge extend pattern，3 protected method override：createSession / cwdExists / codexResumeJsonlExists）+ `sdk-bridge.recovery.test.ts` 15 case 守门 HIGH-B (recoverAndSend) + MED-E (jsonl pre-check) + LOW-A (cwd 启发式 fallback) + R2-2 (cwdFellBack 保留对话历史) + 单飞 / placeholder 5s dedup / MAX_LENGTH / archived → unarchive / codexSandbox+model 透传(HIGH-1 等价)。**P2**: `sdk-bridge.consume-fork.test.ts` 9 case 守门 ThreadLoop case 1/2/**3 (MED-D 核心 fix)** / intentionallyClosed catch 静默退出 / RestartController HIGH-A 单飞 / MED-A emit `session-upserted` 前置+回滚双路径 / 边界。**P3**: 删 `restart-controller.ts:113-128` post-rename 防御 block + unused sessionManager import（thread-loop case 3 已 owner rename 三件套，restart-controller 这里再调是 idempotent no-op 但 console.warn 多打一次误导日志读者，删除让 SSOT 集中）。共 3 commit / 行为零变化 / typecheck 双端 + codex/__tests__/ 48/48 全过（24 translate + 15 recovery + 9 consume-fork）。**未覆盖留 follow-up**：R2-1 sessions cleanup（resume earlyErrCb path）+ R3-1 late earlyErr cleanup（30s timeout 后）— 需真 createSession + fake codex SDK + 控制 thread.runStreamed 抛错，测试 infra 工作量较大。**电源切断**：vi.mock 6 个入口模块（codex-binary / image-uploads / paths / settings-store / mcp-injector / instance-pool）绕过 vitest node 环境下 electron 模块的 'failed to install'。详 [`plans/codex-sdk-bridge-tests-20260515.md`](../plans/codex-sdk-bridge-tests-20260515.md)。 |
| [CHANGELOG_116.md](CHANGELOG_116.md) | CHANGELOG_115 §Phase 2 末尾 R2-1 + R3-1 follow-up 守门补齐 — `sdk-bridge/index.ts:337-388` createSession resume path earlyErrCb wrapper 两条修复点（commit `6e0eb37` R2-1 + commit `726af8d` R3-1）从「无 unit test 守门 / 仅生产实测发现 bug」升级到「3 case unit test 端到端守门」。**测试 infra 关键差异**：earlyErrCb wrapper 在真 createSession closure 内部，必须真跑 createSession（不能用 _setup.ts TestCodexBridge override createSession 的捷径），所以本文件直接 `new CodexSdkBridge` 裸用 + `vi.mock('@main/adapters/codex-cli/sdk-loader', () => ({ loadCodexSdk: vi.fn() }))` 注入 fake `Codex` 类，其 `resumeThread / startThread` 返 `ControlledThread`（test-only `vi.fn` 返 pending Promise，外部通过 `rejectStreamed / resolveStreamed` 控制 fulfill 时机）。**3 case**：(1) R2-1 path 1 — runStreamed 立即抛错 → cleanup + reject + finished:error 一次（同时验证 30s warn 不误触 + R3-1 late err 不误触）/ (2) R3-1 path 2 — `vi.useFakeTimers()` 推 30s 触发 fallback resolve → 然后 reject runStreamed 模拟 late earlyErr → cleanup + emit late err msg + finished:error 一次 + createPromise 不抛 / (3) R2-1 联合 recoverer — cleanup 后 sendMessage 走 `recoverer.recoverAndSend` 自愈（sessions miss + sdkClaim release → emit placeholder + sessionRepo.get）端到端验证。**fake codex SDK 不抽 _setup.ts**（仅本 follow-up 用，避免污染 P1/P2）+ **bare loadCodexSdk mock 不复用 `makeBareSdkLoaderMock()`**（factory 返 `loadSdk` 是 claude 端 export 名，codex 端 `loadCodexSdk` inline 写避免 mismatch）。**不走对抗 review**：纯 test infrastructure 加 + fix 代码本身已 REVIEW_40 三态裁决过，行为正确性靠 vitest 实践验证（51/51 通过）。typecheck 双端 0 错 + codex/__tests__/ 51/51 全过（24 translate + 15 recovery + 9 consume-fork + **3 early-err-cleanup**）。 |
| [CHANGELOG_117.md](CHANGELOG_117.md) | cross-adapter-parity-20260515 plan 收口（REVIEW_40 R1 reviewer-codex MED-F + R2 MED parity 限制双 follow-up 独立 plan）—— `sessions.extra_allow_write` 持久化全链路 + recoverer waiter Promise<string> signature。Phase A 6 commit:migration v019 + session-repo Row/crud/rename 列扩 17→19(顺手补 v018 model 在 rename.ts 漏列 latent bug)+ SessionRecord 字段 + claude session-finalize/createSession/recoverer 全链路持久化 spawn → recover + codex 端同款(parity 对称写库 / SDK runtime 不消费 / 与 model 同款语义)+ adapters/types.ts jsdoc 反映现实 + 3 regression case。Phase B 1 commit:claude/codex recoverer.recoverAndSend signature 改 Promise<string>(返 final session id)+ 1 waiter regression case 锁 fallback rename 后等待者拿 newRealId 不撞 not found。**Phase C 异构对抗 review fix**(详 REVIEW_41):reviewer-claude sandbox 锁 cwd 失败(6 条 *未验证* 全 ❌ 不计入)+ reviewer-codex gpt-5.5 xhigh 4 finding 全部 lead grep 现场实证 → 3 ✅ MED 真问题(MED-1 codex CodexCliAdapterImpl.createSession 完全没接 extraAllowWrite 字段断档 / MED-2 claude/codex recoverer resume path implicit fork case 等待者仍撞 not found / MED-3 claude restart-controller 冷重启路径不带 extraAllowWrite)+ 1 ❓ LOW(信任边界内留 follow-up)。MED-1/2/3 fix 全部 1 commit 落地 + 1 forkOnResumeOverride seam regression case。共 7 commit / 16 文件 / +693/-54 LOC / typecheck 双端 + vitest 531/595 全过(64 skipped 是 better-sqlite3 ABI 环境问题与本 plan 无关)。**编号注**:本 plan 收口期间 main 已被 hand-off-mcp-archive-opt(114) + codex-sdk-bridge-tests(115) + sdk-bridge-tests follow-up(116) 抢先,rebase 后 rename 114→117。详 [REVIEW_41.md](../reviews/REVIEW_41.md) + [`plans/cross-adapter-parity-20260515.md`](../plans/cross-adapter-parity-20260515.md)。 |
| [CHANGELOG_118.md](CHANGELOG_118.md) | archive-failure-ux-upthrow-20260515 plan 收口（REVIEW_39 R1 HIGH-3 R37 caller 仍 active 独立正交 root cause follow-up）—— caller archive 失败 UX 上抛通道,从 mcp baton-cleanup(archive_plan / hand_off_session) + K3 SessionHandOffSpawn 三处触发 emit `caller-archive-failed` event → main bootstrap listener 桥 macOS 系统通知 + IPC 上抛 renderer。**修前**: archive='failed' 字段透传到 mcp tool ok return string 字段无消费方(grep 验证 renderer / IPC handler / event-bus 全 0 处),用户感知不到。**Phase 2 实施 commit 8b9a96e**: event-bus.ts EventMap 加 'caller-archive-failed' event(sessionId / toolName / reason / reasonKind)+ ipc-channels.ts IpcEvent.CallerArchiveFailed + baton-cleanup.ts 两路 emit(row-missing 探针短路 / archive-throw catch)+ sessions-hand-off-helper.ts 抽 archiveSourceSessionWithEmit 纯函数 helper(避免 sessions.test.ts 拉 Electron import 链)+ sessions.ts K3 调 helper + EventMap satisfies 编译期守门 + main/index.ts bootstrap listener notifyUser + safeSend + 守门 test 8 case。**R2 异构对抗 fix commit ccd6d93**: HIGH-1 双方共识 listener throw 撞穿 emit caller(node:events 实测铁证)→ 顶部 try/catch + 改错误注释 / MED-1 codex K3 row-missing 不可能结论错误(setArchived no-op 实证)→ archiveSourceSessionWithEmit 加 getSession 探针 + reasonKind union / MED-1 claude SessionHandOffSpawn IPC 内部名暴露 → TOOL_DISPLAY_NAME 映射表 + INFO 顺手修 case 9/10 emit not 守门。**R3 fix commit 67365e7**: reviewer-codex MED-1 listener notifyUser/safeSend 同 try 块串行(notifyUser 抛错吞 safeSend)→ 拆双通道独立 try/catch。**留 follow-up plan**: K3 helper probe-then-archive TOCTOU race(R3 双方共识非 R2 引入,与 mcp baton-cleanup 同款,scope 超本 plan 留 archive-toctou-fix-<date>) / LOW probe-throw reasonKind / 4 INFO polish + P2 enhancement(renderer toast UI + 重试按钮)。共 3 commit / 8 文件 / +432/-60 LOC / typecheck 双端 + vitest 531/595 全过 / `heterogeneous_dual_completed: true`。详 [REVIEW_42.md](../reviews/REVIEW_42.md) + [`plans/archive-failure-ux-upthrow-20260515.md`](../plans/archive-failure-ux-upthrow-20260515.md)。 |
| [CHANGELOG_119.md](CHANGELOG_119.md) | archive-toctou-fix-20260515 plan 收口（REVIEW_42 §已知 follow-up MED race window + LOW probe-throw + INFO TOOL_DISPLAY_NAME union 三条数据层耦合一次性收口）—— K3/baton archive helper TOCTOU race window fix(setArchived `.changes === 1` throw `SessionRowMissingError` SSOT 修法 A,R1 双方共识)+ reasonKind 'probe-throw' 加值推全链路(LOW probe-throw bug fix,DB 异常可重试与 row 真不存在区分)+ EventMap toolName + TOOL_DISPLAY_NAME `Record<CallerArchiveFailedToolName, string>` union narrow 强制完整覆盖(加新 emit 触发点忘加映射 tsc 编译期 fail)+ IPC SessionArchive/SessionUnarchive 幂等静默 return true(用户主动操作 row 已不在等价无害不打扰)+ adapters.ts AdapterSendMessage unarchiveOnUserSend 进 try/catch 块共享 attachments cleanup(修法 A 后 unarchive throw 必须走 cleanup 兜底避免 attachments leak)。**Phase 2 实施 commit 3c27b98**: archive.ts 新建 SessionRowMissingError class + setArchived throw / event-bus.ts EventMap union narrow / baton-cleanup.ts + sessions-hand-off-helper.ts probe try/catch 拆 + archive instanceof 区分(R1 reviewer-codex MED-1 修法 — 修前 catch-all 把 setter no-op 误归 'archive-throw' 误导 UI 重试) / ipc/sessions.ts SessionArchive/Unarchive 幂等静默 + ipc/adapters.ts cleanup 共享 / main/index.ts TOOL_DISPLAY_NAME narrow + listener 三档文案 / 守门 test 11 文件 (8 prod + 3 test 含 archive.test.ts 7 case + baton-cleanup.test.ts case 7 改 probe-throw + case 8b 加 SessionRowMissingError + sessions.test.ts 3 case)。**R2 异构对抗 fix commit ddea608**: MED-1 reviewer-claude 死代码清理(main/index.ts isRetryable + void workaround YAGNI 违反 → 删两段) / LOW-1 reviewer-codex shared/ipc-channels.ts 注释 union 同步 'probe-throw' / LOW-2 reviewer-codex acceptable defer(mock setArchived throw 撞 vi.mock hoisting 陷阱无法干净修留 P2 plan 重组)。**留 follow-up**: LOW mock 行为对齐(P2 测试基建升级)+ LOW IPC handler integration test(双方 defer)+ LOW renderer 裸 await + recoverer error.message UX(P2 toast plan)+ INFO instanceof 双判别保留(工程美学)。共 2 commit / 13 文件 / +467/-66 LOC / typecheck 双端 + vitest 577/641 全过(64 skipped 是 better-sqlite3 ABI 环境问题)/ `heterogeneous_dual_completed: true`。详 [REVIEW_43.md](../reviews/REVIEW_43.md) + [`plans/archive-toctou-fix-20260515.md`](../plans/archive-toctou-fix-20260515.md)。 |
| [CHANGELOG_120.md](CHANGELOG_120.md) | adapter-architecture-design-20260515 plan 收口（REVIEW_40 P2 留 architectural plan 3 个 design question：P4 BaseAdapter / 跨 adapter sandbox 继承 / scheduler 命名）—— **仅产 design 决策不动 src/ 代码**，输出 `docs/adapter-architecture-rfc-20260515.md` + 三章 follow-up 决策记录（不建独立 stub plan，未来真触发时新建 implementation plan 引用 RFC chapter）。R1 双外部 CLI（reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 xhigh）异构对抗 9 finding（3 HIGH + 6 MED），R1.5 反驳轮把 Chapter 2 codex HIGH「Option D 默认放宽」反驳成立 → 重写 Option D 为 string enum + Chapter 1 D 升 D2 typed registry binding 双方独立提 + Chapter 3 Scheduler 范围定义（排除 Summarizer/Watcher）+ 双类周期 settings 约定。R2 修订 +322/-98 行后用户三章全 ✅ accept。**Chapter 1 sign-off**: Option D2（拆判别联合 + typed registry overload + buildCreateSessionOptions builder helper + exhaustive switch），触发条件 = parity-plan Phase A+B 收口 + 协调 extraAllowWrite 字段位置。**Chapter 2 sign-off**: Option D 重写（`inherit_sandbox: 'restrictions-only'` 仅安全方向映射 / `allow_unrestricted_mapping: true` escape hatch）+ Option E 重写（`warnings: string[]` 字段 + emit message 双发，不止 console.warn 不可见）+ 加 hand-off-session.ts:281-303 字面镜像逻辑，触发条件 = 用户报跨 adapter spawn sandbox 配错 bug。**Chapter 3 sign-off**: Scheduler 范围定义 + 双类周期 settings 约定（lifecycle 热更新阈值 / 周期任务统一 setIntervalMs setter），不需独立 plan。**R1.5 关键反转**: reviewer-claude R1 finding F3 事实错误（reviewer-* 引用是应用 CLAUDE.md 非 user CLAUDE.md，同 system prompt 内 cross-reference 不构成 hard dependency），lead 在 R1 把 supplementary citation 与 hard dependency 一刀切错被 R1.5 双方独立反驳纠正。共 4 commit / 1 文件 docs / +389/-111 LOC / typecheck 不需跑（无代码改动）/ `heterogeneous_dual_completed: true`。详 [`docs/adapter-architecture-rfc-20260515.md`](../docs/adapter-architecture-rfc-20260515.md) + [`plans/adapter-architecture-design-20260515.md`](../plans/adapter-architecture-design-20260515.md)。 |
| [CHANGELOG_121.md](CHANGELOG_121.md) | `p4-baseadapter-d2-implement-20260515` plan 收口（RFC §1 Chapter 1 sign-off Option D2 实施，CHANGELOG_120 architectural design plan 三章触发条件之一首兑现）—— `CreateSessionOptions` 拆 4 union arm + 4 interface（`ClaudeCreateOpts/CodexCreateOpts/PtyCreateOpts × 2 共享`）+ `agentId` 字段判别联合 + `buildCreateSessionOptions` builder helper exhaustive switch + 4 adapter typed export。caller 端 TS 编译期阻止字段误传（codexSandbox 给 claude / permissionMode 给 codex 等）+ 直接 import typed instance 暴露 adapter-专属方法（claudeCodeAdapter.restartWithClaudeCodeSandbox 等）。**3 轮异构对抗 review × fix 收口**：R1 reviewer-claude Y / reviewer-codex N(MED 守门只单侧)→ R1 fix `8a15a6f` 修 F2 cosmetic + F3 4 处 SSOT 守门(AssertSameKeys 双向 trick)→ R2 reviewer-claude Y + 1 LOW 注释孤儿 / reviewer-codex N + 1 MED isAgentId 手写白名单未 SSOT → R2 fix `ad221de` 修 F4 抽 AGENT_IDS as const SSOT 驱动 isAgentId + 加守门 (5) + F5 修注释表 → R3 双方 0 真 finding 收口 Y。**5 处 TS 编译期强守门**(types union ↔ builder switch ↔ ByAdapter entry ↔ AdapterIdMap entry ↔ AGENT_IDS list)+ 3 处 runtime 边界流程检查(register / MCP zod / cli enum)。**6 处 caller migration**(RFC §1.5 漏列 sessions-hand-off-helper.ts 第 6 处 helper 一并补)：spawn / cli×2 / ipc/adapters / sessions-hand-off-helper。**Phase 3 跳过**(Option C delegateOrThrow RFC 自评收益小 + adapter lifecycle 行为不一致需独立 plan)。共 4 commit(Phase 1+2 ebefdfb 14 文件 + R1 fix 8a15a6f 3 文件 + R2 fix ad221de 1 文件 + Phase 5 收尾)/ ~+880/-254 LOC(略高于 RFC §1.5 估 +165/-140,主要在新单测 + builder 注释 + 守门补强)/ typecheck 双端 0 错 + vitest 596 全过 + 18 type-narrow tests(6 @ts-expect-error 全 trigger)+ 反向漏改实测(临时漏 AdapterIdMap.aider / ByAdapter.aider / AGENT_IDS.aider 任一 → TS 报错)/ `heterogeneous_dual_completed: true`。详 [`plans/p4-baseadapter-d2-implement-20260515.md`](../plans/p4-baseadapter-d2-implement-20260515.md) + [`docs/adapter-architecture-rfc-20260515.md`](../docs/adapter-architecture-rfc-20260515.md) Chapter 1。 |
| [CHANGELOG_122.md](CHANGELOG_122.md) | `archive-plan-content-overwritten-fix-20260515` plan 收口（`p4-baseadapter-d2-implement-20260515` plan 收口 archive 实测撞 archive_plan body overwrite bug + commit `30467f6` manual fix 后立 plan 系统性根治）—— archive_plan tool step 9-10 写归档文件用 step 6（ff-merge 前）read 的 stub planContent.body 覆盖 ff-merge 进来的 caller 收尾 commit（[x] step checklist / 跳过理由 / 已知踩坑修正等）回滚 bug 修。修法 A 拆两次 read：step 6 预检 → step 8 ff-merge → step 8b 重新 read 拿 freshFm + freshContent → step 9-10-11 全部用 fresh 数据写归档 + step 8c 加 freshFm.status === 'in_progress' re-check（防 caller worktree branch 漂移 status 静默归档 abandoned 为 completed,违反 user CLAUDE.md §Step 4 中止契约 + 回归 REVIEW_33 H2 abandoned 防线）+ INDEX.md summary 切 freshFm.description（同根因 step 11 漏修）+ cleanup hint 完整决策树（reset --hard ORIG_HEAD 推荐 + revert 限定 abandoned 路径专用,continue 必须 reset 不能用 revert 防 git 拓扑分叉）。**4 轮异构对抗 review × fix 收口**：R1 双方独立 1 HIGH（INDEX summary stale）+ 反驳轮 codex 单方独有 1 HIGH（status carry-forward 缺）✅ 必修 → R1 fix `39e7b14` + 2 case → R2 codex 1 MED（cleanup hint range 多 commit）+ claude 2 INFO doc nit → R2 fix `f377a11` → R3 codex 1 MED（选项 (2) 闭合防误编辑 main 撞 dirty working tree）+ codex 1 LOW（line 19 docstring abandoned 与实现矛盾）+ claude 2 INFO doc drift → R3 fix `f974d2c` → R4 双方 ack ✅ 可合 + codex 1 LOW polish（revert+continue git 拓扑分叉 — revert 限 abandoned）→ R4 polish `fa0f0ec`。**4 轮 cluster 收敛**：fm/freshFm SSOT 切割契约（pre-ff-merge 用 fm preflight + ff-merge target；post-ff-merge 用 freshFm 写入）grep 实测 0 stale 残留,docstring + inline 注释 + 实现代码三层完全对齐 + invariant 提醒未来贡献者。共 7 commit / 2 文件 / +328/-23 LOC / archive-plan tests **42/42 pass**（impl-core 12 + impl-r33 15 + handler 9 + ff-merge-body **6** = 老 4 + 新 2）+ 全套 vitest **602/602 pass + 64 skip + 0 fail** + typecheck 双端 clean / `heterogeneous_dual_completed: true`。详 [REVIEW_44.md](../reviews/REVIEW_44.md) + [`plans/archive-plan-content-overwritten-fix-20260515.md`](../plans/archive-plan-content-overwritten-fix-20260515.md)。 |
| [CHANGELOG_123.md](CHANGELOG_123.md) | `archive-plan-tool-ux-followup-20260515` plan 收口(REVIEW_44 留 4 项同主题 followup 一并收口,全部属 archive_plan tool UX 完善范畴)—— **(a)** fallback 链加 `<main-repo>/plans/<id>.md` 中间档(贴本项目 stub 实际惯例);**(b)** schema 加 optional `changelog_id`(string + csv,容空格)+ INDEX smart update(行级匹配锚定行首 + canonical rewrite 4 列 + α/β fallback 不强制清空已有)+ return shape 升级 `plansIndexAppended: boolean → plansIndexAction: 'created'\|'appended'\|'updated'\|'unchanged'` + `warnings: string[]` 字段;**(c)** INDEX 4 列 canonical header `\| 文件 \| 状态 \| 关联 changelog \| 概要 \|` + escapeTableCell escape `\\\|` + 换行 + formatChangelogCell csv → markdown link 拼接;**(d)** 7 phase 专用 phaseHint 替代旧通用 GENERIC + 给具体 manual recovery 决策树(rev-parse-HEAD / mkdir-plans-dir / write-archived-plan / sync-plans-INDEX / unlink-original-plan / git-add / git-commit)。**3 轮异构对抗 review × fix 收口**:R1 双方共识 0 HIGH + 5 真 MED + 1 polish + 1 反驳(claude MED-1 'unchanged' git-commit 死锁 — `status: in_progress → completed` 必产生 archivedPath frontmatter diff,死锁不可达,反驳成立 ✅);R1 fix 6 项含 `upgradeIndexHeader` helper 自动平滑迁移老 2 列 INDEX header → 4 列(idempotent + 保守 detect 防误改)+ `POST_FF_MERGE_RETRY_INVARIANT_PREFIX` 让 phaseHint override 路径不丢「Cannot retry archive_plan as a whole」invariant + HIGH-1 plan_file_path stem 守门(防 step 12 silent unlink caller 文件)+ HIGH-2 silent override warn(plans/<id>.md 双存覆盖警告);R2 双方 ack 0 HIGH/MED + 共识 LOW polish 2 项合进(escape-aware invariant 守门 + 多 table INDEX 单 table invariant 注释)。共 8 文件改 / +410/-61 LOC / archive-plan tests **92/92 pass**(原 42 + 新 50 case 守门 11 项 fix + R1/R2)+ 全套 vitest **652/652 pass + 64 skip + 0 fail** + typecheck 双端 clean / `heterogeneous_dual_completed: true`。详 [`plans/archive-plan-tool-ux-followup-20260515.md`](../plans/archive-plan-tool-ux-followup-20260515.md)。 |
| [CHANGELOG_124.md](CHANGELOG_124.md) | 全局快捷键加「一键最大 / 一键回默认」窗口尺寸切换 + 4 轮异构对抗 review × fix 收口 —— 与 `Cmd+Alt+P`(置顶)/`Cmd+Alt+T`(透明)系列连贯,新增 `Cmd+Alt+=` 一键到屏幕 workArea 最大(减 40px 边距)/ `Cmd+Alt+-` 一键回默认 520×680;两键各自 toggle,再按一次恢复上次「自定义」尺寸(共享 `preferredSize` 记忆字段)。`FloatingWindow.toggleMaximize/toggleDefault` 新方法 + `applyTargetSize/rememberIfCustom/isNear/centerInDisplay/clampPositionInDisplay` 5 helper + `preferredSize`/`lastToggleAt`/`emitCompactChanged` 3 字段 + 5 常量。**用户改完后主动触发 deep-code-review SKILL,异构对抗跑 4 轮 × fix 收口**(reviewer-claude Opus 4.7 + reviewer-codex gpt-5.5 high 跨 R1-R4 复用同对): R1 HIGH 1(setSize 不改位置离屏 codex 现场算术验证 default x=900 + maxW=1400 右边界 2300 > 1440 离屏 860px,修法 setBounds + centerInDisplay)+ 4 MED(compact UI 不同步 / preferredSize clamp 死锁 / R1 LOW-1 minHeight=COMPACT_HEIGHT regression / preferredSize 跨屏污染)+ 4 LOW + 5 INFO 全 → 7 修 + 2 defer + 1 follow-up plan。**三层短路设计** rememberIfCustom: (1) curSize === lastNormalSize 跨屏不污染 + (2) Date.now() - lastToggleAt < 300ms macOS animate 中间帧不污染 + (3) !atMax && !atDefault 自身跳变不污染。**fallback 撞顶双层保护** 朴素 isNear + clamp 后再 isNear 走 alt fallback。BrowserWindow constructor 加 minWidth/minHeight 底线 + toggleCompact 双向 setMinimumSize + create() 末尾 4 字段 reset 防 Cmd+W 关窗 dock activate 重建携带旧状态。新增 IpcEvent.CompactToggled 端到端通道让 toggle 退 compact 时同步 renderer 避免按钮 label 反转。typecheck 三轮全过 / `heterogeneous_dual_completed: true`。详 [REVIEW_45.md](../reviews/REVIEW_45.md)。 |
| [CHANGELOG_125.md](CHANGELOG_125.md) | `codex-handoff-team-alignment-20260518` plan 收口（5 phase + 22 commit + 71 file / 6809+/167-）—— codex-cli adapter 全面接入 hand_off / archive_plan / team mcp 编排，解 4 个 HIGH 阻塞（HIGH-1 caller_session_id transport 注入 / HIGH-2 cold-start 协议 codex 不识别 / HIGH-4 Wire format 协议 codex 不可见 / HIGH-C archive_plan cwd_release_marker 4 态分流）+ EnterWorktree CLI v2.1.112 stale base bug 解 + bundled assets 多 root 路由。**P1**: sessions.cwd_release_marker 列 + 双 mcp tool（enter_worktree / exit_worktree）+ archive-plan-impl 4 态分流改对（cwd valid/invalid × marker null/match/mismatch）+ rename.ts 20 列扩展 + sessionManager.markClosed/close hook 联动清 marker。**P2**: per-session bearer token map（mcp-session-token-map.ts SSOT）+ HookServer.checkMcpAuth 三态分流 + transport-http extra.authInfo + codex bridge per-session Codex 实例 + sessionManager.renameSdkSession 集成 4-key 原子迁移（sessions Map / sdkOwned / token map / codexBySession Map）。**P3**: getAgentDeckPluginPath 拆 adapter-aware + bundled-assets multi-root scan + AssetMeta.adapter 字段（breaking change 6-caller cascade）+ spawn handler agent_name 按 args.adapter 路由 + agents-md-installer 源切到 codex-config/CODEX_AGENTS.md + options-builder reviewer-* 4 字段 unsafe default spread enforce。**P4**: 4 个 codex 视角内容资产（CODEX_AGENTS.md 178 行 + reviewer-codex.md 132 行 + reviewer-claude.md 190 行 + claude-config/CLAUDE.md 加 §enter/exit_worktree 节）+ spike4 mini-spike claude -p bypassPermissions 嵌套 sandbox PASS + options-builder additionalDirectories 加 /tmp。**P5 收尾 6 路异构对抗 review × fix**：3 batch × claude+codex 各 2 路（含 1 路合规兜底 lead 自跑外部 codex CLI）= 6 reviewer，裁决出 3 ✅ HIGH（H1 archive_plan 4 态分流改对 / H2 codex bridge resume earlyErrCb 漏清 codexBySession + mcpSessionTokenMap 双方独立 / H3 reviewer-claude wrapper `claude -C` 不存在参数）+ 21 MED + 4 降级 MED + 多 LOW/INFO，全 inline fix（4 commit P5 batch A/B/C + settings 默认值调高 mcpSpawnRatePerMinute 10→20 / mcpMaxFanOutPerParent 5→10）。Smoke test 部分通过：spawn 链路 + options-builder default spread + per-session Codex envOverride 全 ✅；codex SDK runtime macOS `failed to initialize in-process app-server client: Operation not permitted` env block 未达 mcp tool 实测，独立环境跑 fresh `pnpm dev` 验证留 follow-up。**不变量 5/6/7**: marker 4 态分流 / options-builder enforce 点 / mcpSessionTokenMap.rename via sessionManager.renameSdkSession。共 26 commit / typecheck + build 0 错 + 730/801 vitest 0 fail / `heterogeneous_dual_completed: true`。详 [`plans/codex-handoff-team-alignment-20260518.md`](../plans/codex-handoff-team-alignment-20260518.md)。 |
| [CHANGELOG_126.md](CHANGELOG_126.md) | Plan `codex-handoff-team-alignment-20260518` P6 流程改进收口（CHANGELOG_125 P5 收口姊妹篇）—— 沉淀本 plan v1→v4.1 写作流程暴露的问题到流程资产。**A** user CLAUDE.md §复杂 plan 节升级 v2 流程图（Step 0 RFC → Step 0.5 spike → Step 1 Plan → Step 1.5 Deep-Review → Step 2 EnterWorktree user confirm → 2.5/3/4 同款）+ §触发条件 v2 拓宽 bullet 3 跨 adapter/schema/进程边界 + bullet 1 子条件加 spike。**B** 应用 CLAUDE.md cross-ref 最小动 + enter_worktree / archive_plan / hand_off_session 调用签名同步实际 schema（hand_off_session 补 archive_caller 字段）。**C** 4 reviewer body 加 §Sandbox 限制说明节（claude-config × 2 + codex-config × 2）覆盖 SDK 实测 sandbox 边界 (READ 宽松 / WRITE 严格 / macOS Seatbelt OS 层) + caller 责任分流（走 SKILL auto cp / 绕开 SKILL 自己处理）。**D** `deep-code-review` SKILL 改造（typed scope kind code/plan/mixed + 3 套 prompt 模板 + auto sandbox cp + manifest + kind='mixed' 成本与失败兜底）+ 物理 git mv 到 `deep-review/` + 老目录重建作 6 个月 deprecation stub。**E** `.gitignore` 加 `.deep-review-cache/` entry 防 cache 入 git。**meta-review 双对抗（reviewer-claude + reviewer-codex）共 5 HIGH + 13 MED + 3 LOW 全 inline fix**（5 HIGH 含 SKILL slash 名 vs 物理 name 不一致 / cache file 并发 race / kind='mixed' 设计自相矛盾 / claude-config sandbox 说明 over-restrict 实测不符 / .gitignore 缺 entry；1 ❌ false positive reviewer-claude 误以为 spike4 文件不存在）。详 [`plans/codex-handoff-team-alignment-20260518.md`](../plans/codex-handoff-team-alignment-20260518.md) §P6 章节。 |
| [CHANGELOG_127.md](CHANGELOG_127.md) | Prompt 资产按 user CLAUDE.md §提示词资产维护 6 条硬约束清理 + `deep-code-review` SKILL deprecation stub 物理删（git rm 整目录）—— 8 资产规范化（user CLAUDE.md + app CLAUDE.md + 4 reviewer body claude-config × 2 + codex-config × 2 + deep-review/SKILL.md + codex-config CODEX_AGENTS.md M1 fix）：删 plan phase 历史标注（P3/P5/P6.x 修法 + spike3/spike4 编号 + spike4 衔接节）+ 删 deprecation 时间窗口预测语（"6 个月 deprecation stub" / "P6.7 改名"）+ 改「建议/推荐/强烈建议/通常」为可执行约束动作 + 删 placeholder 空话节（claude-config CLAUDE.md §应用环境 RFC/spike 差异）+ 合并冗余 Bash 重复块（user CLAUDE.md Step 2 主体 + stale base bug callout (b)）。**单次决策对抗 review**（双 Bash 起异构 CLI 并发：reviewer-claude Opus 4.7 xhigh + reviewer-codex gpt-5.5 xhigh）：0 HIGH + 1 ✅ M1 fix（codex-config CODEX_AGENTS.md:28 老 SKILL 名漂移双方独立提出 + grep 实测命中，stub 物理删后协议层指向不存在 SKILL）+ 1 ❌ M3 反驳（reviewer fresh session / NO MSG ANCHOR template 字符串内嵌「建议 lead」是 advisory message，template content ≠ regulation 合规）+ M2/INFO 多项 follow-up cleanup（顶级 README / docs/ / conventions/ / .gitignore 老名引用 + claude-config CLAUDE.md pre-existing residue）。改动未破坏关键护栏（Sandbox / Wire format / NO MSG ANCHOR / Fresh session / Worktree 路径自检 / send_message reply chain / 异构对偶纪律全保留）。`heterogeneous_dual_completed: true`。详 [REVIEW_46.md](../reviews/REVIEW_46.md)。 |
| [CHANGELOG_128.md](CHANGELOG_128.md) | `deep-review-batch-a1-b-fixes-20260519` plan 收口 — deep-code-review SKILL 6 batch 累积出的 **6 HIGH + 11 MED**（A1 batch claude-code SDK bridge + B batch agent-deck-mcp tool handlers）集中收口,走「复杂 plan 流程 v2」(RFC 两轮 + spike + plan-review 双对抗 + Phase 1/2/3 fix + R3 verify)。**Phase 1 P0 安全/数据丢失 3 HIGH**(commit `91cb584`):B-HIGH-1 (C) 两层 spoofing 守门(helpers.ts stdio invariant assertion + transport-stdio.ts force sentinel + transport-http.ts fallbackToGlobal force sentinel)+ B-HIGH-3 base_branch `refs/heads/<name>` 严格 namespace 校验(拒 SHA/tag/detached)+ B-HIGH-4 mainRepo dirty fail-fast precheck(防 git commit -m 吞 staged 文件混入归档)。**Phase 2 P0 功能 broken 3 HIGH**(commit `f260fd8`):A1-HIGH-1 createSession `realId === tempKey` guard 彻底失败语义(让 catch 走完整 cleanup + rethrow,阻断假 session leak)+ A1-HIGH-2 setTimeout fallback 对称切 sessions Map(delete tempKey + set fallbackId,防 sendMessage(fallbackId) miss 触发 recoverer 起第二个 SDK CLI 子进程)+ B-HIGH-2 hand_off batonMode 条件化(`args.archive_caller !== false`,archive_caller=false 退化 normal spawn 走 depth/fan-out 双护栏)。**Phase 3 11 MED + 测试补全**(commit `c64ba31`):A1-MED ×6(setPermissionMode rollback / REVIEWER_AGENT_NAMES SSOT + isReviewerAgentName guard / recoverer findFallbackCwd caller 链路 NOTE / translatePostToolUse 4 处 file-changed emit toolCallId 透传 / file-changed emit 延迟到 user.tool_result+completed (pendingFileChangeIntents Map + pushFileChangeIntent/consumePendingFileChangeIntent helpers) / restart-controller while(inflight) 循环 re-check 防 multi-waiter race)+ B-MED ×5(cwd 4 态 marker 拆 c-1/c-2/c-3 子档 / exit_worktree partial-success markerCleared 对称 + return error / 新建 plan-path-helpers.ts 抽 resolvePlanFilePath 3 档 fallback helper / archive_plan filesToAdd 加 planRelative 让归档 commit 含 source D+archived A / hand_off plan_file_path stem 校验)+ 5 新测试文件(translate-post-tool-use-toolcallid 5 cases + file-change-intent-delay 6 cases + set-permission-mode-rollback 3 cases + plan-path-helpers 8 cases + plan-path-helpers helper module)+ archive-plan/_setup.ts makeDeps 加透明 mainRepo status mock 修 Phase 1 引入的 mock 队列偏移 regression。**R3 verify**:重 spawn 4 reviewer fresh review(caller session 不在原 R1/R2 team,丢 mental model),0 fix 引入 regression + 出 5 HIGH + 9 MED follow-up(全属未识别同类深层 bug + 测试补全 debt — consume vs setTimeout fallback 30s 并发 mutate race 双方独立 + createSession throw 路径不 interrupt SDK query 生 ghost session + translateSdkMessage 同步 SDK status mode 只写 DB 不写 internal cache 安全边界错位 + transport-http-extra-auth.test.ts inline 合约 stale + plan §Phase 1+2 承诺 7 个 P0 regression test 文件零创建)。**用户 R3 反馈**:hand_off baton 应 shutdown dormant teammate + team 不应跟着旧 lead 流传(default keep_teammates:false 的 shutdownTeammatesOnBaton helper 在 dormant teammate 上没生效)。共 3 commit + 24 file changed + 920+/73- LOC + typecheck/826 测试/build 全过 + `heterogeneous_dual_completed: true`。详 [REVIEW_47.md](../reviews/REVIEW_47.md) + 归档 plan [`plans/deep-review-batch-a1-b-fixes-20260519.md`](../plans/deep-review-batch-a1-b-fixes-20260519.md)。 |
| [CHANGELOG_129.md](CHANGELOG_129.md) | `deep-review-batch-a1-b-followup-r3-20260519` plan 收口 — 上轮 R3 verify 发出的 **5 HIGH + 9 真 MED + 2 用户反馈 (F1/F2)** 全量 follow-up + R3 fresh reviewer 兜底再挖 **4 真 HIGH + 6 真 MED + 2 LOW/INFO**, 22 commit chain 落地 (Phase 1-6 共 15 + R3 fix 7 atomic)。**Phase 1-6 (commit `034efea`-`e24e335`)**: Test debt 工程化补全 (lambda export 真生产代码 + MockSdkQuery stateful 三态机) + Race 修法 (C) 双保险 (fallback fire / createSession throw 双路径 fire-and-forget interrupt + idempotency `interruptFired` flag + consume L221 first-id (B) guard 临时 `incomingId` 不写 realId + sid 三档链) + H3 internal cache 同步 + archive_plan precheck 精确化 (NUL parser + R/C 双 path + repo-relative + commit pathspec) + F1 baton + escape hatch `shutdown_baton_teammates` mcp tool + 杂项收口注释精确化。**Phase R3 fresh reviewer fix (commit `313410f`-`b08359e`)**: 4 reviewer pair 异构对抗 (reviewer-claude 全量 + reviewer-codex Batch A/B/C+D 拆批因 32 文件 xhigh 撞 6m4s budget 限制) 发出 4 真 HIGH 全 land — H1 restart-controller race (claude MED-1 + codex A HIGH-1 合并升级: 两 restart 方法对称 listener + transfer recovering Map entry from OLD → NEW) + H2 archive-plan `runGit.trim()` 破坏 porcelain `-z` NUL 输出 (codex B HIGH-1: runGit 加 `opts.raw` 选项) + H3 base_branch rev suffix 绕过 (codex C+D HIGH-1: 加 `git check-ref-format --branch` 一阶 reject `main~1` / `main^{commit}`) + H4 git status 缺 `--untracked-files=all` (codex C+D 未验证升级: untracked critical 文件展开全路径)。**6 真 MED 全 land** — M3 setPermissionMode chain 串行化替代 Phase 2.7 seq counter (codex A HIGH-2 降级: 防双失败脏 cache) + M2 stdio override lambda export (codex C+D MED-1: test 真 import 绑 production) + M5 exit-worktree markerCleared 透传 (codex B MED-2: err helper 加 optional extras 参数) + M6 dormant SQL invariant in-memory DB case (codex C+D MED-2: defense in depth) + M1 writeTools 矩阵补 `shutdown_baton_teammates` (reviewer-claude LOW 升级) + M4 commit message 加 mainRepo unrelated dirty 注脚 (codex B MED-1: git log 持久化归档时刻状态)。**2 真 LOW/INFO trivial 顺手清** — L1 应用 CLAUDE.md `archive_caller=false` 退化 normal spawn 文档修订 + I1 两处 fire-and-forget interrupt 加 `.catch` 吞错防 unhandled rejection。typecheck / 32 file 416 test pass + 5 skip (binding ABI by-design) / build 全过。详 [REVIEW_48.md](../reviews/REVIEW_48.md) + 归档 plan [`plans/deep-review-batch-a1-b-followup-r3-20260519.md`](../plans/deep-review-batch-a1-b-followup-r3-20260519.md)。 |
| [CHANGELOG_130.md](CHANGELOG_130.md) | `reviewer-codex-cross-adapter-20260519` plan 收口 — deep-review SKILL 异构对偶 reviewer 编排在双向 lead × adapter 矩阵下都用 native(删 wrapper × 2,净 -450 行),codex CLI 用户 `/agent-deck:deep-review` 同款触发。**Phase 0 cross-adapter dispatch BLOCKER 5 commit fix chain**:fix v1 stateless 旧路径(`c67ddde`)→ fix v2 transport-http per-request fresh transport(`835aa7c`,走 mcp-sdk 1.29 `simpleStatelessStreamableHttp.js` 标准 pattern)→ phantom dep `@modelcontextprotocol/sdk` 转 direct dep(`1f70582`,Node ESM 严格 resolve 不走 pnpm hoist fallback)→ 8 tool spec-compliant ToolAnnotations(`eb65878`,readOnly/destructive/idempotent/openWorld 4 fields)→ fix v3 spawn_session/hand_off_session `openWorldHint:true → false`(`923468e`,锁定 ow:true 是 codex CLI mcp_tool_call_approval gate cancel 触发器)。**Step 0.5 双向 cross-adapter pair PASS**:claude lead × codex reviewer-codex 方向 A + codex lead × claude reviewer-claude 方向 B + same-adapter regression × 2(claude × claude in-process / codex × codex HTTP)双双 PASS。**Phase 1** SKILL 跨 adapter spawn 编排 + 失败兜底 native 化(`8cb9ec4`)。**Phase 2** 删 2 份 wrapper agent body + cleanup wrapper code(`da5c0eb`,净 -450 行,删 `resolve-bundled-claude.ts` 整文件 + `options-builder.ts` reviewer-claude wrapper envOverrideExtra 注入分支 + 重写 `teammate-spawn-defaults.test.ts`)。**Phase 3** SKILL build-time auto cp SSOT(`5b727e0`,`scripts/sync-codex-skills.mjs` Node fs cpSync + `predev`/`prebuild` hook + `.gitignore` 加 entry)。**Phase 4** 资产面板 dual-adapter UI dedup(`48141ec`)— `dedupBundledByName` group helper(同 kind+name 跨 adapter 合并为单 group,claude-code 优先 / codex-cli 后排序)+ `AssetCard` signature `asset → assets[]` 加 dual-adapter chip 双角标 + `ContentViewerModal` schema `asset → assets[]+currentAdapter` + `[claude]/[codex]/[user]` tab 切换 + `onTabSwitch` callback 走 seq guard fetch + 抽 `AssetCard.tsx`(132 行)主文件回 420 ≤500 阈值。**Phase 5** 端到端回归(Step 5.1-5.4 spawn pair / dormant 唤醒,Step 5.3 codex CLI interactive 委托 user / Step 5.5 资产面板 UI 视觉委托 user)。**Phase 6** sweep 应用 CLAUDE.md / CODEX_AGENTS.md 2 处遗漏 wrapper 描述清理。typecheck / 71 file 876 test pass + 76 skip 0 fail / build 全过 + `heterogeneous_dual_completed: true`。详 [`plans/reviewer-codex-cross-adapter-20260519.md`](../plans/reviewer-codex-cross-adapter-20260519.md) + spike-reports/(spike1+2/3/4)。 |
| [CHANGELOG_131.md](CHANGELOG_131.md) | `remove-aider-generic-pty-adapters-20260520` plan 收口 — 删除 aider + generic-pty 两 adapter(整目录 + 所有引用)+ ComposerSdk slash 拦截 + node-pty/chokidar native dep + GenericPtyConfig TS type 整片删,剩 2 adapter(`claude-code` + `codex-cli`)。用户实测无任何历史数据,N3 单向不可逆强删,无 graceful fallback。**24 commits chain** P0-P9 串行(base `84a6910` → archive `0dc2fd9`):**P0** ComposerSdk slash 拦截删除 → **P1 (9)** 删 adapter 实现层 + GenericPtyConfig type 整片删(SSOT 链式 enforce 5 处编译期 + 3 处 runtime 边界全闭环)→ **P2 (4)** 删 UI → **P3 (5)** 删 IPC / MCP schema + 注释清扫 → **P4 (7)** 删 shared types + jsdoc 残留清扫 → **P5 (2)** 删测试 / mock(spawn-agent-name-routing 删 TC7+TC7b + tools.test.ts vi.spyOn 局部 mock)→ **P6 (1)** 卸 native dep node-pty + chokidar + scripts/fix-pty-permissions.mjs 整删 + asarUnpack 2 globs 删,顺带解锁 install-app-deps 撞 distutils 的 environmental blocker → **P7** DB schema 0 hit 自检 → **P8 (2)** 文档扫描 + CHANGELOG_131 → **P9 (3)** deep-review 3 round 异构对偶 reviewer pair。**P9.3 ⚑ pnpm test 全跑提前 unblock**:64/64 file passed | 762/838 test passed(76 skipped 全是 better-sqlite3 binding ABI v130 vs v137 守门 skip,符合 CHANGELOG_42 教训)。**P9 R1 fix `a8a4685`**:MED 双方独立 → escalate 整删 `SessionRecord.genericPtyConfig` 字段(plan focus #7 0 alive caller 触发)+ 4 LOW 注释残留;**R2 fix `c66b4e5`**:reviewer-codex 单方 MED + lead spot-check verify → hand-off `SessionHandOffSummarize` Stage 1 加 `if (!adapter?.createSession) throw` early gate(与 Stage 2 line 156 镜像)防老 SQLite row 走 fallback paid Claude oneshot 浪费 LLM quota;**R3** 双方 ✅ 可合 + 0 finding 终态裁决。typecheck PASS / build PASS / test PASS。**Follow-up F1**(LOW): hand_off_session adopt_teammates feature(留独立 plan);**Follow-up F2**(MED): 防递归阈值默认值上调(本 plan 后续 CHANGELOG_132 已实施)。详 [`plans/remove-aider-generic-pty-adapters-20260520.md`](../plans/remove-aider-generic-pty-adapters-20260520.md)。 |
| [CHANGELOG_132.md](CHANGELOG_132.md) | Plan `remove-aider-generic-pty-adapters-20260520` Follow-up F2 实施 — 防递归阈值默认值上调最后一公里。`shared/types/settings.ts` defaults 已是 20/10(CHANGELOG_125 P5 plan codex-handoff-team-alignment-20260518 升过)+ jsdoc 描述也对齐"默认 20/min / 默认 10",但 `spawn-guards.ts:80-81` runtime fallback 仍 `?? 5` / `?? 10`(老值)+ user persisted store 老值 5/10 未 migrate(用户截图设置面板显示 5/10 = persisted 而非 default)。修法:① `spawn-guards.ts:80-81` fallback `?? 5`/`?? 10` → `?? 10`/`?? 20` 与 settings-defaults 对齐;② `settings-store.ts:51` 加一次性 migration(transparentWhenPinned 同模式):persisted 值正好等于老 default(5/10)→ 升级到新 default(10/20);user 显式选非 default 值(7/15 等)→ 保留。Trade-off:罕见 false positive(user 主动选与老 default 同值时被 migrate),但 deep-review 多 batch 场景下老 default 已实测撞顶,migrate 实际是 friendly action 与设置面板 jsdoc"默认 10/20"语义一致。typecheck GREEN / spawn-guards.test 12/12 pass(testfile 显式 set settingsState,fallback 不触发,无 regression)。详 [`plans/remove-aider-generic-pty-adapters-20260520.md`](../plans/remove-aider-generic-pty-adapters-20260520.md) §Follow-up F2 节。 |
| [CHANGELOG_133.md](CHANGELOG_133.md) | Plan `add-claude-cli-path-override-and-bump-sdks-20260520` 收口 — 加 `claudeCliPath` 设置项填 historical asymmetry(codex 早就有 codexCliPath,claude 没对应入口)+ bump 内置 SDK 至最新稳定版。**4 commits chain**(`99a9373`-`d6d72d3`):**Phase 1**(`99a9373`,5 文件 ~32 LOC base)settings.ts +字段 +DEFAULT null / ipc/settings.ts +applyClaudeCliPath no-op 钩子(D6:claude SDK 不持 instance pool 不需 invalidate,与 codexCliPath apply 链对称)/ sdk-bridge:253 + claude-runner:55 inline priority chain `(claudeCliPath && trim) \|\| getPathToClaudeCodeExecutable()`(镜像 codex codex-instance-pool:46 + codex-cli/sdk-bridge:240 inline pattern)/ ExternalToolsSection.tsx +Claude picker;**测试 mock 0 LOC**(D8 priority chain 短路语义自动接住 5 个现有 mock)。**Phase 2**(`b855fe1`)bump @anthropic-ai/claude-agent-sdk 0.2.118 → 0.3.144(major 数字升但 d.ts +390 行全 additive,移除 4 个 unstable_v2_* 实验导出 agent-deck 0 grep 命中)。**Phase 3**(`38483f1`)bump @openai/codex-sdk 0.120.0 → 0.131.0(11 minor 跨越但 d.ts +2 行 additive 仅 `reasoning_output_tokens`,vendored binary 路径 5 platform 完全一致)。**Phase 4.1 build GREEN**(`pnpm build` 187+8+449 modules,4.2-4.3 user 实测 deferred);**Phase 4.4 / 4.5 部分 implicit smoke**(本会话由 hand_off baton 起来 + 跑 deep-review SKILL 双 reviewer pair,完整 5 大 smoke 仍待 user)。**Phase 5 deep-review SKILL kind='code' R1+R2** 双 reviewer ✅ 可合 — 反驳 1 真 MED(codex 单方 / asarUnpack glob 在 pnpm 布局兼容性,历史 .app `/Applications/Agent Deck.app/...node_modules/@anthropic-ai/claude-agent-sdk-darwin-arm64/version: 0.2.118` 实证 electron-builder pnpm-aware hoist 真生效,reviewer-codex R2 接受撤回);修 1 真 MED(claude 单方 / plan §当前进度 stale)+ 1 真 LOW `d6d72d3` controls.tsx tooltip 工具无关化(codex 单方 / ExecutablePicker 复用 codex 设计的 hardcoded 文案);3 LOW 接受作 backlog / Phase 4 兜底。**typecheck GREEN / 762 test pass + 76 skip 0 fail / build GREEN**(每 phase ⚑ checkpoint)。**Follow-up F1**(LOW backlog,plan §D9):`@anthropic-ai/sdk` peer dep `>=0.93.0` warn 留 `pnpm add` follow-up;**F2**(LOW,plan §D7):existsSync 护栏(镜像 codex 现行不做);**F3**(LOW,plan §D8 §1.7):priority chain 单元测试(~80 LOC,codex 等价无专测);**F4**(deferred):Phase 4.1-4.3 user 完整 e2e smoke。详 [`plans/add-claude-cli-path-override-and-bump-sdks-20260520.md`](../plans/add-claude-cli-path-override-and-bump-sdks-20260520.md) + spike-reports/(spike1+2+3)。 |
| [CHANGELOG_134.md](CHANGELOG_134.md) | Plan `add-claude-cli-path-override-and-bump-sdks-20260520` Follow-up F1+F2+F3 实施 — **F1**(`dab3101`):`pnpm add @anthropic-ai/sdk@^0.96.0` 直接 dep 消除 claude-agent-sdk 0.3.144 peer warn(0.81 → 0.96 跨 15 minor stable);**F2+F3**(`d6b7df6`):抽 helper `resolveClaudeBinary()`(`src/main/adapters/claude-code/resolve-claude-binary.ts`)封装 priority chain + existsSync 护栏 + console.warn on missing path + bundled fallback,2 caller(`sdk-bridge/index.ts:253` + `claude-runner.ts:55`)改 1 行 helper 调用;新建 `__tests__/resolve-claude-binary.test.ts` 6 case 覆盖优先级链 6 边界(null/空/全空白/不存在/存在/含前后空白 trim)。**两个 design 决策 deviation**(plan §D5 + §D7,user 显式 opted in):§D5「inline 不抽 helper」→ 抽 helper 让 F3 单测干净;§D7「不加 existsSync」→ 加 existsSync 护栏 + warn 防 user 填错路径撞 ENOENT。结果:claude / codex priority chain 不再字面对齐(claude 单方向 enhance,codex 同款 enhance 留作 future follow-up)。**verify**:typecheck 0 errors / test **768 pass**(+6 F3 new) | 76 skip 0 fail / build GREEN。**残留 F4**:user 完整 e2e smoke `pnpm dist` + install + 4.2-4.3 交互(本 follow-up commit 后 caller 跑 dist)。详 [`plans/add-claude-cli-path-override-and-bump-sdks-20260520.md`](../plans/add-claude-cli-path-override-and-bump-sdks-20260520.md) §设计决策 D5+D7+D8+D9。 |
| [CHANGELOG_135.md](CHANGELOG_135.md) | `hand-off-session-adopt-teammates-20260520` plan 收口 — `hand_off_session` adopt 语义 + spawn-link bug 双修。**bug fix**:Phase 1 root cause 调研排除 P2(K2 mcp 漏守门)+ 实证 P3(REVIEW_39 fix 当天 14 条历史遗留数据);Phase 2 抽 `shouldWriteSpawnLink(opts)` SSOT helper 强化 N2.a 不变量(spawn.ts 三处 inline 改用 helper)。**feature add**:加 `adopt_teammates: boolean` 让新 session 接管 caller 同 team 当 lead,原 teammate 与新 session 共享 active team 可继续 send_message 沟通;配套 N5 ≥1 lead 硬约束 + firstTeam fatal abort + zero dual-lead window 通过 swapLead transaction atomic 实现。**schema 简化**:删 `keep_teammates` 字段(grep == 0 hard gate 1)+ zod strict 双层命名(SHAPE 给 tool 注册 / `*_ARGS_SCHEMA = z.object(SHAPE).strict()` 给 parse 校验)+ N2.c 互斥 invariant(adopt_teammates × team_name 不可同传 zod refine reject + handler 防御性硬约束 defense in depth — Phase 7 reviewer-codex Round 1 HIGH 修法防生产 SHAPE-注册路径不跑 strict 漏守门)。**8 不变量**(N1 zero dual-lead window / N2.a default 不写 spawn-link / N2.b adopt 路径独立 prompt 装配不复用 spawn buildLeadContextBlock + 不写 placeholder / N2.c 互斥 / N3 closed teammate 不静默忽略 / N4 schema 双层 / N5 ≥1 lead + firstTeam fatal abort / N8 event emit 同步)。**plan deep-review Round 1-8 八轮**经双方共识 ✅ 收口(0 真 HIGH 0 真 MED + Round 8 顺手清理 LOW/INFO 进 v9 final)。**实施 9 commit chain**(`825d258`-`b486590`)Phase 1-7 全收口:Phase 4 抽双 helper(`lead-context-block.ts` spawn 路径 SSOT refactor + 新建独立 `adopted-teams-context-block.ts` adopt 路径不含 wire prefix / placeholderId / "回 lead" 指令)+ Phase 5 swapLead transaction `db.transaction(callback)` 包 caller demote + newSid INSERT 同 transaction 三 case 分流 + Phase 6 handler 内 phase 1.5 swapLead loop + firstTeam fatal abort try/catch + lifecycle precheck + N8 emit。**Phase 7 deep-review SKILL kind='code' 3 round**(R1 commit `6a43c19` codex 4 finding 修 / R2 commit `4ca89e5` codex 3 finding 修 / R3 commit `b486590` codex LOW + claude INFO-5 polish 顺手收口)— reviewer-codex 抓 reviewer-claude 漏的 deeper 残留(schema strict 没接生产 mcp tool 路径 deep design hole / archived team/teammate ghost membership filter / teamsTotal 数学一致性 / archived teammate 仍出现 cold-start prompt 第二轮残留),双方 R3 共识 ✅ 收口。**Test coverage**:Phase 6 baseline 803 → Phase 7 final 807 tests passed(+4 守门 case T4.3d / T6.A1 / T6.A2 / T6.A3)+ 83 skipped(SQLite binding ABI by-design)+ T4.1-T4.7 + T5.1-T5.7 + T6.1-T6.X4 + T6.A1-T6.A3 完整覆盖 8 N invariants。**留 follow-up note 不阻合**:claude INFO-3(adopt-local filter 未来可 SQL-化)+ INFO-4(其他 ok-path case 加 emit spy)+ INFO-6(promptGetSessionFn vs phase 1.5 getSessionFn 重复声明)+ INFO-7(archivedTeamIds 含 team-not-found corner case)— 全 future-improvement 不阻合。`heterogeneous_dual_completed: true`。详 [`plans/hand-off-session-adopt-teammates-20260520.md`](../plans/hand-off-session-adopt-teammates-20260520.md) + spike-reports/(spike1+2)。 |
| [CHANGELOG_136.md](CHANGELOG_136.md) | `reverse-rename-sid-stability-20260520` plan 收口 — sessions.id 稳定化反向 rename 设计。**核心思路**:`sessions.id` = applicationSid 永不变(应用层 events / mcp token / wire prefix `[sid]` 稳定 key);新加 `sessions.cli_session_id` 列承载 SDK / CLI thread sid (允许变化)。bridge InternalSession metadata 双阶段化(applicationSid + cliSessionId 两字段);spawn 主路径 ctor=tempKey first realId 后切到 realId 冻结,resume / fallback 路径 ctor=opts.resume 全程不变。**6 处反向 rename 路径修法**:claude jsonl-missing fallback + codex jsonl-missing fallback (resumeMode='fresh-cli-reuse-app' 显式 mode + applicationSid 复用) + claude streaming fork detect + codex thread-loop case 3 fork detect (走 `sessionManager.updateCliSessionId(applicationSid, realId)` 替代旧 `renameSdkSession(OLD, NEW)` cascade rename) + claude restartWithPermissionMode + restartWithClaudeCodeSandbox (RestartCreateOpts 加 resumeCliSid 双轨入参 + 删 fork rename block + 返 currentSid)。**SSOT 接口拆分**:applicationSid (sessions.id 维度,events.session_id / mcp token / wire prefix [sid] / sessions Map key 全用此) + cliSessionId (SDK / CLI thread sid 维度,SDK options.resume + jsonl preflight stat path 用此) + effectiveResumeCliSid (S1 R6 升级 bridge 内部 resolver 三分支 guard) + resumeMode 显式 mode 字段。**InternalSession 字段命名升级**:realSessionId → cliSessionId + 新增 applicationSid 双阶段生命周期。**5 不变量**(N1 sessions.id 永不变 / N2 cli_session_id 列承载 SDK thread sid 允许变化 / N3 wire prefix [sid] 100% 写 sessions.id / N4 mcp token map key=sessions.id 维度永远稳定 / N5 迟到 hook event 黑名单 60s + ingest 4 态分流)。**plan Round 1-8 八轮 deep-review** 双方共识 ✅ 收口(0 真 HIGH 0 真 MED + 3 LOW non-blocking implementation-time tweak)累计 ~40+ 项 fix,详 plan §设计决策 D1-D8。**实施 6 sub-commit chain**(`579f934`-`a33b9b6`):A-1 schema migration v021 + sessionRepo cli_session_id helper / A-2 ingest 4 态分流 + 黑名单双写 / A-3 bridge identity split S1+S2+S3+S4+S4b atomic / A-4 S5+S6+S6.5+S8+S9 atomic (createSession return + fork detect + jsonl-missing fallback 重写 + finalizeSessionStart 双入参) / A-5 测试矩阵收口 + S3 ctor sessions Map key 修正 + stream-processor.ts S3 三分支补全 / Step C restart-controller 反转 (claude × 2 + codex × 1 三方法对称)。**verify**:typecheck 0 errors / **811 test pass** (+11 新增,replacing OLD rename assertions) | 83 skip 0 fail / build GREEN。详 [`plans/reverse-rename-sid-stability-20260520.md`](../plans/reverse-rename-sid-stability-20260520.md) + spike-reports/(spike1+2+3+4)。 |
| [CHANGELOG_137.md](CHANGELOG_137.md) | 资产库「应用约定」tab 加 codex 视角编辑器 — 后端 SSOT 已分 `claude-config/CLAUDE.md` + `codex-config/CODEX_AGENTS.md` 两份(CHANGELOG_125 P5 plan §D5 切独立维护),但前端 UI 之前只暴露 claude 侧用户副本编辑器,codex 侧用户改约定只能改源文件 + 重 build。补齐对偶 UI:`agents-md-installer.ts` 加 4 公开函数 (getActive / getBuiltin / saveUser + 立即同步 ~/.codex/AGENTS.md 段 / resetUser) + 3 IPC handler (CodexAgentsMdGet/Save/Reset) + preload api 3 method + 新建 CodexAgentsMdEditor.tsx(130 行,镜像 ClaudeMdEditor)+ AssetsLibraryDialog 「应用约定」tab 加 adapter switcher(Claude / Codex 二选一,新加 SubTabBtn 组件)+ sub-tab 切换 dirty 二次确认拦截。typecheck 0 errors / 811 test pass | 83 skip 0 fail / build GREEN。 |

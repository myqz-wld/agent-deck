# Changelog 索引

| 文件 | 概要 |
|------|------|
| [CHANGELOG_1.md](CHANGELOG_1.md) | 项目初始化（M1–M9 一次性交付）：Electron + React 19 + TS + Tailwind 4 半透明悬浮窗；Adapter / Diff 双插件架构；Claude Code Hook + SDK 双通道；activity 状态机 + lifecycle 调度；列表/详情/历史/Diff/总结；权限请求面板；本地 OAuth 复用；LLM 总结（三层降级 + 并发上限 + 配置面板）；CSS Acrylic 毛玻璃；archived 与 lifecycle 拆分（DB v3） |
| [CHANGELOG_2.md](CHANGELOG_2.md) | 应用图标：Wakaba_Mutsumi（若叶睦）头像，webp → 1024×1024 png 写入 resources/icon.png；BrowserWindow icon + macOS Dock setIcon |
| [CHANGELOG_3.md](CHANGELOG_3.md) | AskUserQuestion 工具适配：SDK 通道 canUseTool 拦截 `AskUserQuestion`，弹独立 UI（每题选项按钮 + 其他自由输入 + 单选立即提交/多选提交按钮），答案塞进 deny.message 反馈给 Claude；新增 IPC `adapter:respond-ask-user-question`、store 独立 pendingAskQuestionsBySession；summarizer 把 `[Claude 主动询问用户]` 加进 prompt 上下文，避免 LLM 误判主语 |
| [CHANGELOG_4.md](CHANGELOG_4.md) | 内部会话能看完整对话：sendMessage 现在 emit user message 事件（之前完全丢失），assistant message 加 role 标记；ActivityFeed 改为对话气泡渲染（用户绿色右对齐 / Claude 边框灰左对齐 / 错误红框 / 完整文字保留换行），其他事件保留单行简述并加 emoji & 工具入参 detail；listEvents 拉取上限 50 → 100 |
| [CHANGELOG_5.md](CHANGELOG_5.md) | 提醒整顿：去掉窗口整体闪屏（太抢眼），改用 sound.ts 修复异步回退 bug —— 之前 mp3 不存在时 execFile 异步失败被外层 try/catch 接不住、回退根本没触发；现在用 existsSync 同步判断 + execFile 失败回调真正回退；macOS 系统声音从 Sosumi（错误音）改成 Glass（waiting）/ Tink（finished） |
| [CHANGELOG_6.md](CHANGELOG_6.md) | 自定义提示音：设置面板「提醒」section 加 `SoundPicker`（waiting/finished 各一个），可选本地 mp3/wav/aiff/m4a/ogg/flac + 试听按钮 + 重置；新增 settings 字段 `waitingSoundPath`/`finishedSoundPath`、IPC `dialog:choose-sound-file` 与 `app:play-test-sound`；sound.ts 解析顺序：自定义路径 → 内置 resources/sounds → 系统提示音 |
| [CHANGELOG_7.md](CHANGELOG_7.md) | 提示音播放健壮化：Windows 改用 PowerShell + PresentationCore.MediaPlayer 支持 mp3（之前 SoundPlayer 只支持 wav）；新增防叠播（全局只允许 1 个播放器子进程，新触发 SIGTERM 旧的）+ 5 秒时长上限（超长自动截断）；before-quit 调 stopAllSounds() 清理残留子进程 |
| [CHANGELOG_8.md](CHANGELOG_8.md) | README 与代码现状同步：补「首条消息必填」、`applyClaudeSettingsEnv()` 注入 settings.json 的 env、DB v4 migration（permission_mode）、设置面板「测试系统通知」按钮、项目结构补 settings-env.ts 与 migrations/；追加去掉本机私有路径（项目目录 / userData 绝对路径 / 示例 cwd） |
| [CHANGELOG_9.md](CHANGELOG_9.md) | 新建会话「权限模式」修复：AdapterCreateSession 补写 `sessions.permission_mode`（之前永远 NULL → 底部下拉显示对不上）；sdk-bridge 加 `allowDangerouslySkipPermissions`（bypassPermissions 必须配套这个安全 flag 才生效）；追加：PermissionRow 按钮挪到 header 行 + diff 容器加 `overflow-hidden`，修 Monaco 溢出盖住授权按钮；追加：输入框发送键由 `Cmd/Ctrl+Enter` 改成 `Enter`（Shift+Enter 换行 + IME 拼写防护） |
| [CHANGELOG_10.md](CHANGELOG_10.md) | 权限请求死锁治理（四层兜底）：A. sdk-bridge 暴露 `listPending` / `listAllPending`，renderer HMR / 切会话时拉取重建 store（修 store 空 → 按钮不显示 → SDK 死锁）；B. sendMessage 时若有 pending 推一条警告 message；C. 新增 `permissionTimeoutMs`（默认 300s），超时自动 deny+interrupt 触发 lifecycle 进 dormant；D. header 加 `⚠ N 待处理` chip，点击跳首个 pending 会话 |
| [CHANGELOG_11.md](CHANGELOG_11.md) | AskRow 提交按钮显眼化（实色 + header 右侧 + 「已选 N/M」进度 + 底部兜底）+ 毛玻璃默认（无 pin）底色加深（0.55 → 0.78）、brightness 1.12 → 0.92，让文字在浅色桌面下也清楚 |
| [CHANGELOG_12.md](CHANGELOG_12.md) | README 与代码现状再次同步：补 PermissionRow/AskRow 已从顶部 banner 改为活动流内嵌、AskRow 不再「单选立即提交」、新增「会话恢复（resume）」节、SDK vs Hook 通道 session-end 差异化处理、30s fallback / tempKey 整体迁移、cwd 待领取标记；项目结构里把过期的方法名 / 文件描述全部拉齐 |
| [CHANGELOG_13.md](CHANGELOG_13.md) | 命令行新建应用内会话：新增 `agent-deck new --cwd ... --prompt "..." [--agent / --model / --permission-mode / --system-prompt / --resume / --no-focus]`，复用 `requestSingleInstanceLock` 的 second-instance 通路，首启与已运行实例共用同一段 argv 解析；新增 macOS shell wrapper（resources/bin/agent-deck）把相对 cwd 在 shell 端转绝对；新建后默认聚焦窗口 + 选中新会话（新事件 `event:session-focus-request`）；追加：wrapper 简化（裸 `agent-deck` 等价 `new --cwd "$PWD"`，首参为 -- 开头时自动补 new） |
| [CHANGELOG_14.md](CHANGELOG_14.md) | SessionDetail 不再被「刷新跳转」回 SessionList：select(sid) 同步 + session-upserted 异步的 race 让 `sessions.get(selectedId)` 短暂为 null，detailSession 闪一帧 null 跳回 list 再跳回 detail；App.tsx 加 `stickySelected` 缓存最近一次成功 get 到的 record，sessions 暂时丢失时仍渲染缓存，仅在 selectedId 显式置 null 时清；覆盖 onCreated/onSessionFocusRequest/jumpToPending 全部入口 |
| [CHANGELOG_15.md](CHANGELOG_15.md) | 间歇总结优先用 haiku：summariseViaLlm 加 `model: ANTHROPIC_DEFAULT_HAIKU_MODEL ?? ANTHROPIC_MODEL ?? 'haiku'` 三层优先级，取自 settings.json env（applyClaudeSettingsEnv 注入），用户升级 haiku 版本不用改代码；省去高价模型干一句话总结的开销 |
| [CHANGELOG_16.md](CHANGELOG_16.md) | 打包配置修复 + 安装流程文档化：package.json 加 `build.mac.icon` 修 dmg 出不来（icon directory 报错）+ `build.extraResources` 把 `resources/bin → bin` 让 wrapper 真正进 .app；CLAUDE.md 删掉 `~/Repository/personal/...` 个人路径，新增「打包与本地安装」节（dist → 覆盖 cp → xattr → 软链 4 步 + 已踩坑清单），README.md 同步「安装到本机」节；.gitignore 加 `*.tsbuildinfo` |
| [CHANGELOG_17.md](CHANGELOG_17.md) | 修复归档会话仍出现在「实时」面板：SessionList 过滤补 `archivedAt === null`，因为归档与 lifecycle 正交、`session-upserted` 推回的 record 仍带原 lifecycle |
| [CHANGELOG_18.md](CHANGELOG_18.md) | 测试系统通知提示按真实 appName 显示：`AppShowTestNotification` IPC 返回多带 `app.getName()`，renderer 拼成「请到 系统设置 → 通知 → ${appName}」；dev='Electron'、prod='Agent Deck'，避免装好的 .app 让用户去找 Electron 找不到 |
| [CHANGELOG_19.md](CHANGELOG_19.md) | 抑制 Monaco 卸载 race 弹红屏：window.onerror 兜底白名单加 `TextModel got disposed before DiffEditorWidget`，@monaco-editor/react 卸载 DiffEditor 时的内部 race 不再被当成致命错误全屏遮挡，console.warn 留痕即可 |
| [CHANGELOG_20.md](CHANGELOG_20.md) | 修打包后 LLM 总结全降级：SDK 默认 `executable: 'node'`，但 .app 走 launchd 启动 PATH 不含 nvm/homebrew node；新增 sdk-runtime.ts 让 summarizer + sdk-bridge 都用 `process.execPath` + `ELECTRON_RUN_AS_NODE=1` 复用 Electron 内置 Node runtime |
| [CHANGELOG_21.md](CHANGELOG_21.md) | 安装流程加 ad-hoc codesign：dist 后 `codesign -dvv` 看到 Identifier 是 `Electron`（来自 Electron 二进制 linker 阶段签的）与 Info.plist 的 `com.agentdeck.app` 不一致，会让 macOS 通知中心 / Gatekeeper 按 'Electron' 注册；CLAUDE.md / README 安装步骤插入 `codesign --force --deep --sign -` 把签名 Identifier 拉回 com.agentdeck.app；附带说明：测试通知不出横幅大概率是「专注模式」与签名是两件事 |
| [CHANGELOG_22.md](CHANGELOG_22.md) | 修 webContents.send 撞已销毁窗口：index.ts 的 6 条 eventBus listener 闭包持有 bootstrap 时的 win，macOS 关窗后 win 已 destroyed，scheduler/IPC 再触发就抛 `Object has been destroyed`；抽 `safeSend` 通过 `floating.window` 动态取窗 + `isDestroyed` 守卫，pin 快捷键回调同样改造 |
| [CHANGELOG_23.md](CHANGELOG_23.md) | 新建会话 cwd 缺省回落用户主目录：NewSessionDialog 去掉 cwd 必填（placeholder 改「留空使用主目录 (~)」）；ipc.ts AdapterCreateSession + cli.ts parseCliInvocation 同步兜底，cwd 空时统一 `os.homedir()`；wrapper 的 `$PWD` 兜底保留 |
| [CHANGELOG_24.md](CHANGELOG_24.md) | 修 pin 模式下主界面残影：pin 时主进程关掉 vibrancy 后，CSS backdrop-filter 不会跟随下层 app 内容变化重新采样（Chromium 合成器只在窗口自身有事件时才重绘）→ 切下层 app / 桌面动画时残留旧画面，需要拖窗口才刷新；FloatingWindow 增加 invalidateTimer，pin + macOS 期间每 200ms 调一次 `webContents.invalidate()`，setAlwaysOnTop / close 负责启停 |
| [CHANGELOG_25.md](CHANGELOG_25.md) | 修 monaco unhandledrejection 红屏 + 活动流可复制：(1) `diffProviderFactoryService.js:110` 在 model dispose race 后抛 `no diff result available`，走 async 变 promise rejection 绕过 CHANGELOG_19 加在 `window.onerror` 的白名单 → 全屏红屏；抽出 `isMonacoUnmountRaceNoise` 公共判断，error / unhandledrejection 两条 listener 都用，覆盖同步 + 异步两条 race。(2) ActivityFeed 顶层 `<ol>` 加 `select-text` 覆盖全局 `user-select: none`，让消息气泡 / tool 输出 / JSON 入参可选可复制，拖窗仍只靠 header 的 .drag-region |
| [CHANGELOG_26.md](CHANGELOG_26.md) | 安装流程加 pkill 旧进程：实测踩坑——重装后旧 main 进程没退，macOS 复用旧实例，旧 renderer 引用的 chunk hash 跟新 .app 对不上，monaco dynamic import 拉错文件被当 plain text 渲染（窗口里直接显示一坨 Editor 组件源码）；CLAUDE.md「打包与本地安装」补第 0 步 pkill + 「踩坑清单」加条目说明根因 |
| [CHANGELOG_27.md](CHANGELOG_27.md) | 修 header 会话计数（抽 selectLiveSessions 共享 helper，过滤归档 + closed，与 SessionList 对齐）+ MessageBubble 加 MD/TXT 切换按钮（react-markdown + remark-gfm，localStorage 全局默认） |
| [CHANGELOG_28.md](CHANGELOG_28.md) | ExitPlanMode 工具适配：SDK 通道 canUseTool 拦截 `ExitPlanMode`，新增独立类型 / IPC / store map / `ExitPlanRow` 组件（绿色 header「📋 Claude 提议了一个执行计划」+「批准计划，开始执行」实色按钮 + 「继续规划」次按钮带可选反馈输入框）；plan 用 `MarkdownText` 渲染（复用 CHANGELOG_27 引入的依赖，零新依赖）；Hook 通道在 ToolStartRow 识别 ExitPlanMode 也展开 plan markdown（只读）；超时按「继续规划 + 默认反馈」处理避免打断 turn；summarizer 加 `[Claude 提议执行计划]` 前缀 |
| [CHANGELOG_29.md](CHANGELOG_29.md) | 会话详情新增「权限」tab：按当前会话 cwd 解析三层 Claude Code settings.json（user / project / local），顶部展示按 SDK 顺序合并后的生效规则（每条标 `[U]/[P]/[L]` 来源 chip），下面三层卡片各展示完整 settings.json（轻量 JSON 高亮，无 monaco 依赖）+「打开」按钮调 `shell.openPath` 用系统编辑器编辑（main 端白名单校验路径）；纯只读，agent-deck 不写任何配置文件 |
| [CHANGELOG_30.md](CHANGELOG_30.md) | ComposerSdk 拦截 `/` 开头的 slash 命令（SDK streaming mode 不支持），本地用 sendError 红条提示「应用内会话不支持斜杠命令，请回终端运行 claude」，不发 IPC 避免撞 SDK 报错 |
| [CHANGELOG_31.md](CHANGELOG_31.md) | 修「等待你的输入」误报：SDK 通道的 `*-cancelled` 事件复用 `waiting-for-user` kind，原先一律按真请求处理，导致点完按钮 / timeout / session-end 后仍弹系统通知 + 状态卡 waiting；本次让 manager.ts 的 `nextActivityState` 与 index.ts 的通知分发都按 `payload.type` 区分，cancelled 不再切 activity、不再触发 notifyUser |
| [CHANGELOG_32.md](CHANGELOG_32.md) | 权限 tab 补 user-local 层（`~/.claude/settings.local.json`）：CHANGELOG_29 按官方文档实现的三层模型漏了 SDK 实际会读的 user-local，导致用户在 ~/.claude/settings.local.json 写的 allow/deny 不出现在生效合并里、「打开」也找不到入口；本次扩到四层 `user → user-local → project → local`，shared/types + scanner + ipc 白名单 + PermissionsView 全链路对齐 |
| [CHANGELOG_33.md](CHANGELOG_33.md) | 综合优化批次（12 条）：P0 HookServer Bearer token 鉴权（首启自动生成 256-bit hex 持久化）+ Summarizer LLM oneshot 超时（Promise.race + q.interrupt 兜底，修单卡死锁死整个调度）；P1 SettingsSet 补分发 summaryIntervalMs 即改即生效 + ingest 写放大瘦身（仅状态真变化才广播 session-upserted）+ useEventBridge 只在新会话拉 latestSummaries + bypassPermissions 切换弹 confirm；P2 settings-env 注入加白名单（ANTHROPIC_*/CLAUDE_*/proxy）+ HistoryPanel keyword debounce 300ms + 后端短关键词只搜 title + sdk-bridge sendMessage 字节/队列上限；P3 死代码清理（PermissionRequests / AskUserQuestionPanel / AskQuestionForm / QuestionRow / 重复 toolInputToDiff / diffCollector / RouteHelpers）+ ingest/ensure 不再自动 unarchive（与正交约定对齐）+ writeSettings temp+rename 原子写 |
| [CHANGELOG_34.md](CHANGELOG_34.md) | MessageBubble MD/TXT 切换改为单条独立（推翻 CHANGELOG_27「切单条 = 切全局」取舍）：每条 bubble mount 时读一次 localStorage 作为初始默认，切单条只改本地 state，不写 localStorage、不广播；切过的 bubble 卸载/重启后回到默认（不持久化按 message id 的偏好 map）；render-mode.ts 删 useGlobalRenderMode + write + EVENT_NAME，仅留 readInitialRenderMode |
| [CHANGELOG_35.md](CHANGELOG_35.md) | pin 残影根因升级 + render-mode.ts 整体删除：(1) 文字残影真凶是 `.frosted-frame::before` 的 `mix-blend-mode: overlay` + `isolation: isolate` + `backdrop-filter` 三件套把文字层缓存进 offscreen group surface（webContents.invalidate 失效），pin 态 `display:none ::before` 治根；invalidate 频率 200→100ms（10fps 下层桌面感知率），create 时 setBackgroundThrottling(false) 防失焦节流；修 CHANGELOG_24 注释里的认知错误（backdrop-filter 在 transparent 窗口里只对自身 layer 生效，下层 app 像素是 NSWindow 简单合成，根本没经过 blur）。(2) CHANGELOG_34 之后没人写 localStorage，readInitialRenderMode 永远返回 'plaintext'，整个 render-mode.ts 删除，类型 + 默认 inline 到 ActivityFeed |
| [CHANGELOG_36.md](CHANGELOG_36.md) | Agent Deck 自带 CLAUDE.md + skill 注入机制：新增 `resources/claude-config/`（自带 CLAUDE.md 暂时复制 ~/.claude/CLAUDE.md + 占位 plugin/skills/hello-from-deck），sdk-bridge createSession 通过 SDK 的 `systemPrompt: { type: 'preset', preset: 'claude_code', append }` + `plugins: [{ type: 'local', path }]` 注入到所有应用内会话（与 cwd 无关）；同时去掉用户自定义 systemPrompt 功能（避免 isolation mode 冲突），CLI `--system-prompt` flag、NewSessionDialog 输入框、CreateSessionOptions.systemPrompt 字段全部清理；summarizer 保持 isolation 模式不变；package.json extraResources 把 claude-config 打进 .app/Contents/Resources/ |
| [CHANGELOG_37.md](CHANGELOG_37.md) | 优化批次（10 条）+ 历史会话超期自动清理：removeSession 漏清三张 by-session Map → 加上；CLI 与 IPC 入口 createSession 后持久化 permissionMode 抽 helper 共用；HookServer Bearer 改 timingSafeEqual；session-repo upsert 加 permission_mode 字段、历史搜索 LIKE 子查询改 EXISTS LIMIT 1、新增 batchSetLifecycle 单事务批量推进；Lifecycle scheduler 重写 scan 改用批量推进 + 加历史超期清理（默认 30 天，0 = 关）；session:list-history 收编到 IpcInvoke + preload facade；sdk-bridge / summarizer 共用 sdk-loader.ts；Summarizer 挂 session-removed listener 防 lastSummarizedAt 泄漏；token 阈值改 64 与生成长度对齐；追加 App header「⚠ N 待处理」chip 补算 ExitPlanMode（之前 useMemo 只 select PermissionRequest + AskUserQuestion 两个 map，含 ExitPlan 待处理的会话不计入也无法被点击跳转） |
| [CHANGELOG_38.md](CHANGELOG_38.md) | MCP 图片工具支持（ImageRead/Write/Edit/MultiEdit）：识别 `mcp__<server>__Image*` 工具名，解析 `tool_result` JSON 翻译成 file-changed 事件（payload 用 `ImageSource` 不带二进制）；新增 `window.api.loadImageBlob` 由 main 进程做白名单+ext+size 校验按需读盘；占位 ImageDiffRenderer 替换为 side/after-only/slide 三视图，新增 ImageThumb/ImageBlobLoader 通用组件 + SessionContext 注入 sessionId；ImageMultiEdit 拆 N 条 file-changed 但 filePath 用真实路径让 SessionDetail 按文件分组聚合；shared/types 加 ImageSource/ImageToolResult/LoadImageBlobResult，shared/mcp-tools 新建 isImageTool；MCP server 单独仓库按协议契约实现 |
| [CHANGELOG_39.md](CHANGELOG_39.md) | 图片工具语义修正（ImageRead = vision LLM 描述图，ImageWrite = 文生图，ImageEdit = 图生图，ImageMultiEdit = 链式图生图，CHANGELOG_38 全猜错了重写）：`ImageToolResult.image-read` 加 `description: string`；4 kind 加可选 `provider/model`；ToolEndRow 在收 image-read 结果时左缩略图右描述并排展示，header 标 `[provider · model]`；imageResultToFileChanges 把 prompt/provider/model 透传到 metadata；配套独立仓库 `~/Repository/personal/agent-deck-image-mcp` 用 Gemini provider（vision = gemini-2.5-flash，生图/编辑 = gemini-2.5-flash-image-preview）落地，env `IMAGE_READ_PROVIDER` / `IMAGE_GEN_PROVIDER` / `IMAGE_EDIT_PROVIDER` 可独立路由 |
| [CHANGELOG_40.md](CHANGELOG_40.md) | 集中「待处理」Tab：顶部新增第三个 tab，把所有有未响应请求（permission / ask / exit-plan）的会话按 section 平铺一屏，直接响应不必跳到 SessionDetail；header chip 行为从「跳第一个」改为「打开 PendingTab」，tab 名右侧带 badge；每 section 提供「全部允许 / 全部拒绝」批量按钮（仅作用于 permission + exit-plan，AskUser 必须人审）；section header 整行可点跳进 SessionDetail；新增 `selectPendingBuckets` / `sumPendingBuckets` 共享 selector（与 selectLiveSessions 同口径过滤，归档 + closed 不进列表）；为复用渲染把 `PermissionRow` / `AskRow` / `ExitPlanRow` / `toolInputToDiff` 从 ActivityFeed 抽到新文件 `pending-rows/index.tsx`，ActivityFeed 瘦身约 460 行 |
| [CHANGELOG_41.md](CHANGELOG_41.md) | Codex CLI adapter（@openai/codex-sdk 接入）：占位 30 行 → 完整实装。「诚实对等」capabilities：createSession/sendMessage/interrupt/resume = true；canUseTool/AskUserQuestion/ExitPlanMode/setPermissionMode/installHooks = false（双对抗 Agent 源码核实 SDK 物理不支持）。新建 sdk-loader/translate/sdk-bridge 三文件，sdk-bridge 处理 thread.started → realId 同步（30s fallback）+ pendingMessages 串行 turn 队列（codex 同 thread 不能并发）+ AbortController interrupt（SIGTERM 子进程）；translate 完整覆盖 8 种事件 + 8 种 item 映射；index.ts 默认安全策略写死 approvalPolicy='never' + sandboxMode='workspace-write'。AppSettings 加 codexCliPath + 设置面板「外部工具 → Codex 二进制路径」字段（ExecutablePicker 组件）+ 新 IPC channel DialogChooseExecutable；NewSessionDialog 按 capabilities 隐藏 model/permissionMode 字段；SessionDetail ComposerSdk placeholder 按 agentId 切「给 Codex/Claude 发消息」+ 权限模式 select 在 codex 隐藏；SessionList 欢迎文案补 codex 入口。`@openai/codex-sdk@^0.120.0` 强制 dep `@openai/codex` → 当前平台 vendored 二进制（darwin-arm64 ~150MB）跟随打包 |
| [CHANGELOG_42.md](CHANGELOG_42.md) | 应用约定（CLAUDE.md）UI 编辑 + 对抗 Agent 约定升级（Claude + Codex 异构）+ codex CLI 调用模板：注入到 SDK system prompt 末尾的 agent-deck CLAUDE.md 现在通过设置面板新 Section 直接编辑，用户副本写到 `userData/agent-deck-claude.md` 覆盖内置（应用升级不冲掉自定义）；保存 / 重置清主进程注入缓存，下次新建会话生效（已运行 SDK 会话不热改）；新增 sdk-injection.ts 4 个导出函数 + 3 条 IPC channel + preload facade + ClaudeMdEditor 组件（textarea + 等宽字体 + 撤销/保存/恢复默认三按钮，destructive 走 confirmDialog 二次确认）。两份 CLAUDE.md 中「对抗 Agent」那条改为「默认一 Claude（Explore/general-purpose）+ 一 Codex（Bash 直接调 codex CLI），异构对抗降低同模型偏见」，并新增独立 bullet「codex CLI 调用模板」写明工程上靠谱的固定姿势：`zsh -i -l -c "codex exec --sandbox read-only --skip-git-repo-check -C <REPO> -o <OUT> - < <PROMPT>"`，因为 stdout 是 banner+reasoning+final 混合（且 final 重复一次）必须 `-o` 单独抓干净结果 |
